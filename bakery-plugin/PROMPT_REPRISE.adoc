= PROMPT_REPRISE — Bakery Gradle Plugin
:session: 228
:date: 2026-09-17
:etat: Session 227 ✅ — dette `asciidoctorRevealJs` SOLDÉE cross-borough (slider S-072) : cause racine double JRuby, correctif `AsciidoctorJPin` + bump slider 0.0.20. Zéro code bakery modifié. Antérieur S-226 : **EPIC BKY-LANG-22 TERMINÉ 10/10 US**.
:epic_actif: Aucun EPIC bakery actif. Reste : MASS-6 (~675h LLM) + EPIC transverse GBL-006 COPY MULTI-MARCHÉ — **accord pilote requis**.

== Contexte de fin (S-226)

Cross-borough slider (LANG-22-8), capsule (LANG-22-9), MEMPHIS (LANG-22-10) :

- **8** : 3 sources × 12 nouvelles langues = 36 paires via `translateDeck`
  (`gemma4:31b-cloud`, `gpt-oss` épuisé en limite mensuelle). Corpus 63 adoc +
  63 context. Pin `TranslatedDemoDeckTest` levé (catalog-driven). Drift
  `7_rtl_layout` (`fa`) corrigé (préexistant S-217).
- **9** : bug P0 réel — `extractSpeakerNotes` produit `<slug>_<lang>-deck-script.txt`
  mais `CapsuleVideoPlanner` exigeait `<deckName>_<lang>-script.txt` → fail-fast.
  Fix `resolveScript` (tolère le suffixe `-deck`, miroir S-082 côté sortie WebM).
  Dogfooding réel 22/22 vidéos espeak+Playwright (dégradation propre th/ja/ar).
- **10** : ordre DAG réel (gardes MEMPHIS D8 anti-ghost) = capsule d'abord.
  capsule 0.0.1 (infra nmcp créée) + MEMPHIS 0.0.48 + bakery 0.0.13 + slider
  0.0.19. Propagation repo1 HTTP 200 vérifiée.

Gate bakery `test` 1845/0, slider 261 UT + 270 cucumber, capsule 1168/14,
MEMPHIS 111/0.

== Mission Session 227 — à la demande (accord pilote)

. *GBL-006 COPY MULTI-MARCHÉ* : `ContentSeed` + `PlatformFormat` + `CopyPiece` +
   `MarketProfile[22]` (traduire ≠ écrire, 8 US ~31 pts) — cadré
   `CADRAGE_EPIC_GBL_006_COPY_MULTIMARKET.adoc` workspace racine. US-1 modèles
   purs zéro coût LLM ; US-2 revue pilote des profils.
. *MASS-6* : montée en charge 108 articles × 10 langues (~675h LLM).

Dette locale actionnable (zéro coût externe) : *rendu `asciidoctorRevealJs`
Gradle cassé* (gems JRuby `jar-dependencies 0.5.4 vs 0.4.1`, préexistant
S-023/S-034) — bloque tout dogfooding HTML automatisé.

*MISE À JOUR S-227* : dette **SOLDÉE** (slider S-072). Ce n'était pas une
dette bakery mais une dette **slider** (le task `asciidoctorRevealJs` vit
dans slider-gradle). Cause racine : double JRuby sur le worker OUT_OF_PROCESS.
Correctif `slider.extension.AsciidoctorJPin` + `pinAsciidoctorJ` (bump slider
0.0.20). Le dogfooding HTML est désormais automatisable (plus de Ruby système).

== Reprendre à

. `git status` dans bakery-gradle — vérifier qu'aucune session concurrente n'écrit
   (session S-225 a déjà committé `ArticleGenerator` `f6cc35b`).
. `.agents/sessions/227-asciidoctorj-pin-cross-borough.adoc` (rapport S-227)
. `./gradlew test` — non-régression (baseline 1845/0 attendue)
. `publishToMavenLocal` après chaque modification code source (règle INDEX 0)

== Rappel

* Gate métier : `grep -rwE "REAC|AFNOR|SPG|SPD|FPA|RNCP|CDA" src/` = 0.
* *Modèle LLM* : `gpt-oss:120b-cloud` épuisé (limite mensuelle) → utiliser
  `gemma4:31b-cloud` + `OLLAMA_POOL_PORTS=11439,11440,11441,11442`.
* *Publication Central* : ordre DAG réel — publier le consommateur absent AVANT
  de l'ajouter au catalogue MEMPHIS (garde D8 anti-ghost).
* *Règle 0* : `./gradlew -q publishToMavenLocal` OBLIGATOIRE après toute
  modification du code source.
* Commit/push autorisés (décision pilote 2026-09-17). Zéro secret.
* Baby-step DDD/TDD/BDD strict — 1 fichier à la fois, gate `test` complète.
