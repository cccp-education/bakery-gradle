= PROMPT_REPRISE — Session 058 : BKY-LENS-3 Budget + Injection JBake (Reconnect)

== Mission (Session 058)

1. **LENS-3.1** — Créer `LensBudget` data class : maxArticlesPerPage (4), minSimilarity (0.7)
   - Injection dans `jbake.properties` (`relatedArticlesData`)
   - `AugmentedContextDsl` enrichi avec `budget { }` DSL
2. **LENS-3.2** — Remplacer `SiteManager.registerCollectRelatedArticlesTask()` : appeler `AugmentedArticlesService` au lieu de `RelatedArticlesService`
   - Tâche Gradle `collectRelatedArticles` → utilise LENTILLE pipeline
3. **LENS-3.3** — Nettoyage : supprimer `RelatedArticlesService`, `RelatedArticlesGraph`, `RelatedArticlesResolver`, `kgraph/` (4 fichiers)
   - ⚠️ Feature Cucumber 17-19 doivent être réécrites (LENS-4) — ne pas les lancer avant

== État actuel (Session 057 — LENS-2.3 TDD + BDD)

- LENS-1 ✅ ENTIEREMENT IMPLÉMENTÉ ET TESTÉ (30 tests PASS)
- LENS-2.1 ✅ IMPLÉMENTÉ (LensRules, LensRagConfig, AugmentedContextResolver, LensConfig enrichi)
- LENS-2.2 ✅ IMPLÉMENTÉ + TESTÉ (AugmentedArticlesService 25 UT, ScoredNode, scoring hybride)
- LENS-2.3 ✅ TESTÉ (7 boundary AugmentedArticlesService + 4 boundary AugmentedContextResolver + Feature 22 fix 3/3)
- Build compile ✅, publishToMavenLocal ✅
- 54 échecs Cucumber pré-existants (non liés à LENS, à traiter par LENS-4)

== Architecture LENS — Rappel

```
graph.json (203k nœuds) ← ✅ LENS-1
    ↓ SubgraphExtractor → site-subgraph (~200 nœuds)
    ↓ + RAG pgvector + règles métier ← ✅ LENS-2
site-graph (~200 nœuds, ~500 edges)
    ↓ LensBudget (4-6 articles/page, minSimilarity=0.7) ← SESSION 058
jbake.properties (relatedArticlesData)
    ↓
post.thyme (AUGMENTÉ) (LENS-6)
```

== Contrat DAG

- bakery (N2) importe codebase-contracts (N0) ✅
- bakery (N2) importe graphify-plugin (N0) ✅

== Méthodologie

- **Baby steps DDD/TDD/BDD** : chaque étape compile ET fait passer ses tests AVANT de passer à la suivante
- 1 fichier à la fois, validation immédiate après chaque modification

== Attention

- Ne pas lancer `./gradlew test` sans permission explicite
- BKY-BKG (RelatedArticlesService, RelatedArticlesGraph, RelatedArticlesResolver, kgraph/) sera supprimé en LENS-3.3 — sauvegarder avant suppression
- Les 54 échecs Cucumber pré-existants seront traités par LENS-4 (réécriture Features 17-19)
- L'import de `CompositeContext`, `ChannelBudget`, `ContextChannel` (codebase-contracts) se fait dans AugmentedArticlesService (LENS-2.2), pas dans LENS-1