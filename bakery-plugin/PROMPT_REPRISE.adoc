= PROMPT_REPRISE — Bakery Gradle Plugin
:session: 240
:date: 2026-09-25
:etat: Session 239 ✅ — **BKY-LANG-NAV-8 : brique N2 `bakeVariants` livrée** + bloquant magic-stick débloqué (bug template `feed.thyme`).
:epic_actif: **BKY-LANG-NAV** (🟡 NAV-0→7 ✅, NAV-8 🟡 enabler + chaîne de bake complète livrée ; reste la CI réelle + rollout sites restants).
:epic_suivant: **NAV-8 déclenchement CI réel** cccp/magic (accord pilote — publication production) OU absorption du duplicat cheroliv (`VariantBakeAssembler`/`VariantServePlan`/`VariantDeployability`) dans `bakery.i18n.variant` (kill split-brain D5).
:dette: FLAKY-5 (drift fixture cheroliv.com) + ktlint baseline dépôt (rouges à HEAD, préexistants).

== État S-239

La **brique N2 manquante** du rollout i18n est livrée : `bakeVariants` bake
chaque variante `i18n/{lang}` déployable dans `{destDirPath}/{lang}/` avec le
même moteur JBake que la référence.

. **Domaine pur** `bakery.i18n.variant` (`VariantLayout`, `VariantDeployability`,
   `VariantBakePlan`, `VariantBakeAssembler`, `VariantBaker`) — DDD/TDD strict,
   30 UT. Garde S-049 (variante incomplète ignorée), shell content vide pour
   corpus partiel (D8), config-free (découverte corpus/bundles, ne lit jamais
   `site.yml` gitignoré).
. **Tâche** `BakeVariantsTask` + 6 FT + BDD feature 67 `@variant-bake` (6/6).
. **Chaîne de déploiement** câblée : `materializeTemplates` → `injectLangSwitch`
   → `bake` → `bakeVariants` → `pagefind` → `deploySite` (`deploySite` dépend de
   `bakeVariants`, sinon les sous-arbres `{lang}/` ne partent pas).
. **magic-stick débloqué** : `feed.thyme` évaluait `published_posts[0].date` sans
   garde → avortement du rendu sur corpus vide. Correctif ternaire (cccp garde
   déjà) + resync fixture/golden.

Dogfooding `/tmp` : cccp 11 + magic 12 templates, 0 clé non résolue, arbre EN
**baké** (homepage EN littérale, sélecteur page-aware, feed.xml rendu), **zéro LLM**.

== Mission Session 240

*Déclencher la CI réelle* `deploy-cccp-education` / `deploy-magic-stick` — les
workflows appellent désormais `materializeTemplates ... injectLangSwitch
publishSite` ; le tree EN doit être publié en production. **Accord pilote requis**
(publication réelle sur gh-pages).

Alternative : *absorber le split-brain cheroliv* D5 — `office/sites/cheroliv.com`
duplique `VariantBakeAssembler`/`VariantServePlan`/`VariantDeployability` ; la
nouvelle brique bakery est générique et devrait devenir la source unique.

== Reprendre à

. `git status` dans bakery-gradle ET office.
. `.agents/sessions/239-…` (ce rapport) + `.agents/CADRAGE_EPIC_BKY_LANG_NAV.adoc` (§ NAV-8).
. `.agents/sessions/238-…` (rollout partiel) + `.agents/237-…` (enabler bundles gelés).
. `src/main/kotlin/bakery/i18n/variant/` (la brique) + `src/test/features/67_variant_bake.feature`.
. Gate Économie d'Encre : ne relancer `test`/`cucumber` que si le code source change.

== Rappel

* Gate métier : `grep -rwE "REAC|AFNOR|SPG|SPD|FPA|RNCP|CDA" src/` = 0.
* **JAMAIS lire un `site.yml`** (gitignoré, contient un token) — `git check-ignore -v` d'abord.
* *Modèle LLM* : `gemma4:31b-cloud` + `OLLAMA_POOL_PORTS=11439,11440,11441,11442`.
* *Règle 0* : `./gradlew -q publishToMavenLocal` OBLIGATOIRE après toute modif source.
* *Pattern BDD runner dédié* : tout tag non couvert doit avoir son runner (S-082).
* Commit/push autorisés (décision pilote 2026-09-17). Zéro secret.
* Baby-step DDD/TDD/BDD strict — 1 fichier à la fois, gate `test` complète.
