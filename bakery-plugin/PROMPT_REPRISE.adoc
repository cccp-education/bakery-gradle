= PROMPT REPRISE — Bakery
:session: 144
:date: 2026-06-23

== Mission accomplie (session 143)

*BKY-PUB-2 Publication 0.0.2 Maven Central + Gradle Plugin Portal* — ops publication.
- Maven Central : ✅ LIVE — `publishAggregationToCentralPortal` SUCCESS,
  bundle `81764b1c-1bf0-4d2d-a78e-86165b619cbe`, vérifié HTTP 200 sur
  `bakery-plugin-0.0.2.pom` (repo1.maven.org).
- Gradle Plugin Portal : 🟠 soumis — `publishPlugins` a échoué car la version
  0.0.2 existe déjà (créée 2026-06-22 23:33, soumission antérieure session
  138/139). Plugin en attente de validation Gradle (page Portal "not found",
  propagation 24-48h).
- Compte publication : `cccp-education` (credentials GPPK-* dans
  `~/.gradle/gradle.properties`, profil Portal existant HTTP 200). Confirmé
  par utilisateur : cccp-education, pas cheroliv.
- Pré-requis nmcp vérifiés : version 0.0.2 hardcodée, POM conforme (Apache 2.0,
  developer cccp-education + organization/organizationUrl), deps N0 toutes
  releases + sur Central (graphify 0.0.2, codebase 0.0.2, codebase-contracts
  0.0.1, i18n-contracts 0.0.1, workspace-bom 0.0.1), signing GPG OK.
- Aucun changement code source (publication-only).

== Statut

- BKY-PUB-2 : ✅ TERMINÉ (Maven Central live, Portal en propagation)
- BKY-CR-V6 : US-1 ✅ (3 pts), US-2 et US-3 à faire (5 pts restants)

== Prochaine session (144)

Démarrer **BKY-CR-V6 US-2** : `DomainConfigInjectors` data-driven — 9 injecteurs
répétitifs (113 lignes, 3 variantes : gate sur champ trigger, gate sur boolean,
injection systématique).

Baby steps DDD/TDD/BDD :
- DDD : `InjectorSpec` sealed class (3 sous-classes pour les 3 variantes) +
  `InjectorRegistry` (registre data-driven).
- TDD : tests unitaires par injecteur (RED→GREEN) — caractériser les 9
  comportements existants avant refactor.
- Refactor : `configInjectors` Map → `InjectorRegistry.all()`. Vérifier
  comportement identique (tests existants GREEN).
- `publishToMavenLocal` après modif code.

Fichiers cibles :
- `bakery/injection/DomainConfigInjectors.kt` (9 injecteurs, 113 lignes)
- NOUVEAU `bakery/injection/InjectorSpec.kt` (sealed class + registry)
- Tests : `DomainConfigInjectorsTest.kt` existant à compléter

== Vérification à faire (asynchrone)

Gradle Plugin Portal propagation (24-48h après session 143) :
----
curl -sL "https://plugins.gradle.org/plugin/education.cccp.bakery" | grep -i "not found"
# si vide → plugin live sur Portal
----

== Note technique

- `./gradlew test` reste la commande de validation rapide en session.
- Kover threshold 85% maintenu.
- `Option` Arrow dépréciée en 2.x — utiliser nullable Kotlin + `Either` pour
  les erreurs (pattern établi session 142).
- bakery-plugin 0.0.2 désormais consommable depuis Maven Central directement
  (`education.cccp:bakery-plugin:0.0.2` ou plugin Portal
  `education.cccp.bakery` version `0.0.2` une fois propagated).