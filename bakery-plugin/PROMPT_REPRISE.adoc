= PROMPT_REPRISE — Bakery Gradle Plugin
:session: 223
:date: 2026-09-17
:etat: Session 222 ✅ — **FLAKY-4 résolu** : drift fixture `contact.thyme` (site réel cheroliv.com S-031 retrait champ `subject`) resynchronisé + golden master régénéré. Gate `test` **1777/0 FAIL** (baseline S-221 1775/2 → les 2 FAIL historiques disparaissent).
:epic_actif: Aucun EPIC bakery actif. BKY-TEST-FLAKY étendu FLAKY-4 ✅.

== Contexte de fin

Session 222 — nettoyage de dette locale actionnable (zéro coût LLM/publication) :

- **Diagnostic** : les 2 FAIL « préexistants » (S-218→S-221) n'étaient **pas un flake**
  mais un drift réel. Le site `office/sites/cheroliv.com` a retiré le champ `subject`
  du formulaire contact (commit `d96dcec`, 2026-09-14, US-35) ; la fixture bakery
  n'avait pas été resynchronisée.
- **Fix** : resync `i18n-fixtures/cheroliv-com/jbake/templates/contact.thyme` + recâblage
  `contact.6`/`contact.7` → `contact.6` + régénération golden master (procédure
  one-shot `PostMigrationSnapshotGenerator`, churn timestamp réverté).
- **Résultat** : `FixtureAlignmentAuditorTest` 8/0, `RealSiteI18nMigrationIntegrationTest`
  2/2. Diff chirurgical (5 fichiers, +6/-23).

Gate : `test` **1777/0 FAIL**, `publishToMavenLocal` VERT, grep métier src/ = 0.
**Les gates cross-borough MEMPHIS n'ont plus besoin de la mention « baseline 2 FAIL ».**

== Mission Session 223 — à la demande

Les 3 pistes restantes exigent toutes un **accord pilote** :

1. **BKY-LANG-22-8/9** : dogfooding LLM réel 22 langues (slider corpus + capsule vidéo)
   — coût LLM metered.
2. **BKY-LANG-22-10** : publication Central consommateurs (BOM/catalog 0.0.43) —
   `bakery-plugin` ≥ 0.0.13 débloque le wiring talaria en CI.
3. **MASS-6** : montée en charge 108 articles × 10 langues — coût LLM ~675h.

## Reprendre à

. `git status` dans bakery-gradle — vérifier qu'aucune session concurrente n'écrit.
. `.agents/sessions/222-flaky-4-contact-thyme-drift-resolu.adoc` (rapport)
. `./gradlew test` — non-régression (baseline 1777/0 attendue)
. `publishToMavenLocal` après chaque modification code source (règle INDEX 0)

## Rappel

- Gate permanente : `grep -rwn "REAC|AFNOR|SPG|SPD|FPA|RNCP|CDA" src/` = 0
- **Décision pilote (S-054)** : pas de migration `LlmBuildService` (codebase N1) —
  socle LLM bakery **local** (`bakery.llm.PooledOllamaLlmService`), hors périmètre.
- **Config pool LLM consommateur** : `site.yml → ollama:` résolue par `IaConfigResolver`
  (S-221), prioritaire sauf DSL explicitement activé.
- **Collision de sessions** : vérifier `git status` — d'autres boroughs écrivent parfois
  dans le même arbre (graphify S-221/S-028).
- Note environnement : suites cucumber complètes timeout sous charge (load 14+).

(PROMPT_REPRISE — ~50 lines)
