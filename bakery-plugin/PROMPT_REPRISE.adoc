= PROMPT_REPRISE — Session 057 : BKY-LENS-2 AugmentedArticlesService (Scoring Hybride)

== Mission (Session 057)

1. **LENS-2.2** — Créer `AugmentedArticlesService` : scoring hybride RAG+KG+tags+xref (α=0.4/β=0.3/γ=0.15/δ=0.15)
   - `score(nodeId, subgraph, ragResults, lensRules)` : calcule le score composite
   - `scoreAll(subgraph, ragResults, lensRules)` : scoring pour tous les nœuds du sous-graphe
   - `applyRules(scoredNodes, lensRules)` : filtre excludeDrafts, excludeTags, bonus crossRef/community
2. **LENS-2.3** — Tests TDD : `LensRulesTest`, `AugmentedContextResolverTest`, `AugmentedArticlesServiceTest`
   - Baby steps DDD/TDD/BDD : 1 fichier à la fois, validation après chaque modification
3. **LENS-2.3** — Cucumber BDD : Feature 22 `22_augmented_context_lens.feature`

== État actuel (Session 056 — Démarrage LENS-2)

- LENS-1 ENTIEREMENT IMPLÉMENTÉ ET TESTÉ (30 tests PASS)
- LENS-2.1 IMPLÉMENTÉ (pas encore de tests) :
  - `LensRules` data class (excludeDrafts, excludeTags, prioritizeCrossReferences, crossRefBonus, communityAffinity)
  - `LensRagConfig` data class (enabled, similarityThreshold, topK)
  - `LensConfig` enrichi avec `rules` + `rag` + 2 DSL actions
  - `AugmentedContextDsl` enrichi avec `minSimilarity`
  - `AugmentedContextResolver` (resolve composite-context.json, extractChannels)
- Build compile ✅
- 53 échecs Cucumber pré-existants (non liés à LENS)

== Architecture LENS — Rappel

```
graph.json (203k nœuds)
    ↓ SubgraphExtractor (LENS-1 ✅) → site-subgraph (~200 nœuds)
    ↓ + RAG pgvector (top-K=20, sim>0.7) + règles métier (LENS-2) ← SESSION 057
site-graph (~200 nœuds, ~500 edges)
    ↓ LensBudget (4-6 articles/page, minSimilarity=0.7) (LENS-3)
jbake.properties (relatedArticlesData)
    ↓
post.thyme (AUGMENTÉ) (LENS-6)
```

== Scoring Hybride LENS-2

```
score = α × ragSimilarité      // 0.40 — similarité sémantique (pgvector)
      + β × graphProximité     // 0.30 — voisinage structurel (graphify depth)
      + γ × tagRecouvrement   // 0.15 — tags éditoriaux partagés (JBake metadata)
      + δ × crossRefCount     // 0.15 — références croisées AsciiDoc (xref)

  + bonus 0.2 si même communauté (communityAffinity)
  + bonus 0.2 si cross-reference AsciiDoc vérifiée (prioritizeCrossReferences)
```

== Contrat DAG

- bakery (N2) importe codebase-contracts (N0) ✅ (déjà dans build.gradle.kts)
- bakery (N2) importe graphify-plugin (N0) ✅ (déjà en implementation)

== Méthodologie

- **Baby steps DDD/TDD/BDD** : chaque étape compile ET fait passer ses tests AVANT de passer à la suivante
- 1 fichier à la fois, validation immédiate après chaque modification

== Attention

- Ne pas lancer `./gradlew test` sans permission explicite
- BKY-BKG (RelatedArticlesService, RelatedArticlesGraph, RelatedArticlesResolver, kgraph/) sera supprimé en LENS-3.3 — ne pas y toucher maintenant
- Les 53 échecs Cucumber pré-existants sont connus et seront traités par LENS-4 (réécriture Features 17-19)
- L'import de `CompositeContext`, `ChannelBudget`, `ContextChannel` (codebase-contracts) se fait dans AugmentedArticlesService (LENS-2.2), pas dans LENS-1