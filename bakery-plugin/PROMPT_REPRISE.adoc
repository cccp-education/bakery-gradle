= PROMPT_REPRISE — Bakery Gradle Plugin
:session: 221
:date: 2026-09-17
:etat: Session 220 ✅ — **EPIC BKY-I18N-JS TERMINÉ 4/4 US** : JS-4 livrée (propagation `maquette/js/` → `jbake/assets/js/` byte-identique + garde de format parse↔write + garde frontière zéro HTTP). Le harnais node talaria est officiellement remplaçable.
:epic_actif: BKY-I18N-JS ✅ TERMINÉ 4/4 US — débloque talaria `T-I18N-BAKERY` US-2 (wiring build) + US-3 (retrait harnais node).
:lang22: BKY-LANG-22 🟡 7/10 US — reste LANG-22-8/9 (dogfooding LLM 22 langues) + LANG-22-10 (publication Central consommateurs).

== Contexte de fin

Session 220 — **BKY-I18N-JS-4** (clôture EPIC) :
- domaine pur `bakery.i18n.js` : `I18nClientPropagation` (`targetFor`,
  `pairs`, `drift` — miroir `propagate()` du harnais) + `I18nClientFormatGuard`
  (`unbalanced-language-block`, `round-trip-drift`) +
  `I18nJsDictionary.unbalancedLanguageBlocks`.
- `TranslateI18nClientTask` : CLI `--i18nClientPropagate` + intention/DSL
  `propagate` (défaut `true`), `propagateToPublication()` delta-only (aligné =
  no-op), garde de format câblée en production (fichier invalide écarté,
  résultat invalide jamais écrit), dry-run zéro écriture.
- 4 FT Oven nouveaux (9/9) + feature 64 étendue à 10 scénarios (10/10) +
  test d'architecture frontière HTTP (3 UT).

Gate : `test` **1768/2 préexistants** (baseline 1743/2 → +25 exactement),
`functionalTest` 9/9, Cucumber 64 10/10, `publishToMavenLocal` VERT,
grep métier = 0. 3 fichiers créés, 8 modifiés.

== Mission Session 221 — à la demande

1. **BKY-LANG-22-8/9** : dogfooding LLM réel 22 langues (slider corpus 3 decks
   + capsule vidéo) — Loi Économie d'Encre, coût LLM metered, accord pilote.
2. **BKY-LANG-22-10** : publication Central des consommateurs (BOM 0.0.43).
3. **Transfert talaria `T-I18N-BAKERY` US-2/US-3** : maintenant que l'EPIC
   bakery est clos, le wiring build talaria + retrait du harnais node peuvent
   être planifiés côté `office/sites/talaria.school`.
4. *(hors EPIC)* MASS-6 (accord pilote requis — coût LLM ~675h).

## Reprendre à

. `git status` dans bakery-gradle
. Lire `BACKLOG.adoc` (EPIC BKY-I18N-JS ✅ TERMINÉ en tête)
. `src/main/kotlin/bakery/i18n/js/` (6 fichiers prod)
. `src/test/features/64_i18n_client_dictionary.feature` (10 scénarios, pattern BDD)
. Harnais de référence (lecture seule, à retirer côté talaria) :
   `office/sites/talaria.school/tools/translate-i18n.mjs` (`propagate()` L177)
. `publishToMavenLocal` après chaque modification code source (règle INDEX 0)

## Rappel

- Gate permanente : `grep -rwn "REAC|AFNOR|SPG|SPD|FPA|RNCP|CDA" src/` = 0
- **Décision pilote (S-054)** : pas de migration `LlmBuildService` (codebase N1) —
  socle LLM bakery **local** (`bakery.llm.PooledOllamaLlmService`), hors périmètre.
- **Ordre maquette** : `maquette/js/` = source de dev, `jbake/assets/js/` = copie de
  publication byte-identique (`MaquetteParityGuard`).
- **Source dirs** de `translateI18nClient` sont relatifs au `projectDir`
  (pas au content root) — layout talaria `maquette/js` ∥ `jbake/assets/js`.
- **Règle gouvernance** : `office/sites/talaria.school/site.yml` est gitignoré
  (credentials réels). Ne pas le lire/modifier — repo `office` privé.
- Note environnement : suites cucumber complètes timeout sous charge (load 14+).

(End of PROMPT_REPRISE — ~62 lines)
