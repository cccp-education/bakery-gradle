= PROMPT_REPRISE — Session 081 : CS-FIN-2 (JSON escape Jackson) + CS-FIN-3 (nettoyage DRY)

== Mission (Session 081)

**Objectif** : Terminer les 2 derniers code smells de finalisation (CS-FIN-2 + CS-FIN-3, 2.5 pts).

*CS-FIN-2 (P1, 1.5 pts)* :
- LensPipelineService ligne 125-136 : remplacer le `mapOf` manuel par `mapper.writeValue(file, output)` (LensPipelineOutput data class)
- Ajouter test unitaire avec JSON complexe (Unicode, guillemets, `\n` littéral)

*CS-FIN-3 (P2, 1 pt)* :
- CS-14 : Supprimer code mort LensPipelineService.kt:73-75 (condition impossible)
- CS-15 : Documenter `ragResults: Map<String, Double> = emptyMap()` avec commentaire
- CS-17 : Remplacer `println()` par `logger.lifecycle()` dans DeployTaskRegistrar
- CS-19 : Réutiliser `jacksonObjectMapper()` comme champ privé dans LensPipelineService
- CS-20 : Extraire `createLlmServiceIfEnabled(iaConfig, logger)` dans ContentTaskRegistrar

== État bakery (Session 080)

- **EPICs** : 28/28 ✅ (100%)
- **Code smells** : 10/22 résolus (CS-FIN-1 ✅, CS-1→12 ✅)
- **Smells restants** : 8 → CS-FIN-2 (1.5 pts), CS-FIN-3 (1 pt), CS-18 accepté
- **Build** : ✅ Zéro warning
- **Tests** : 663 JUnit5 PASS, 129 Cucumber PASS, ~55 FT PASS, ~17 E2E PASS
- **Coverage** : 77.25% (seuil 75% ✅)

== Méthodologie obligatoire — Baby Steps DDD/TDD/BDD

1. DDD — Identifier le comportement manquant
2. TDD — Test unitaire AVANT implémentation
3. BDD — Cucumber si comportement utilisateur
4. Vérification — `./gradlew test` après chaque fichier
5. Coverage — `./gradlew koverXmlReport koverPrintCoverage` après chaque ajout
