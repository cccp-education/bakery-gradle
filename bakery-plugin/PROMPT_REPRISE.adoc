= PROMPT_REPRISE — Bakery Gradle Plugin
:session: 231
:date: 2026-09-24
:etat: Session 230 ✅ — **BKY-LANG-NAV-2 TERMINÉ** (hôte Thymeleaf page-aware). Antérieur S-229 : NAV-1 (`LangSwitchPath` règle pure + vecteurs).
:epic_actif: **BKY-LANG-NAV** (🟡 NAV-0 ✅, NAV-1 ✅, NAV-2 ✅, NAV-3→8 À FAIRE) — P1, cœur NAV-3→5 (~11 pts) puis rollout sites NAV-6→8 (~13 pts).

== État S-230

`LangSwitchPath.thymeleafHref(currentLang, targetLang, defaultLang)` — le frère
**symbolique** de `resolveSamePage` : émet un `th:href` en `${content.rootpath}` /
`${content.uri}`, évalué **par page au bake**. `LangSwitchUrl.thymeleafHref()` et
`LangSwitchThymeleafRenderer` le consomment (fin de l'`index.html` en dur, D4).
`resolve()` reste la cible de dégradation page-blind (D8).

Preuve anti split-brain D3 : `LangSwitchPathThymeleafHrefTest` rejoue **11/14
vecteurs partagés via le TemplateEngine réel** ; `LangSwitchRendererEvaluationTest`
évalue la **sortie réelle du renderer** et l'égale à `resolveSamePage`.

Tests : 9+5+4 UT nouveaux + renderer réécrit page-aware (13) ; FT 8/8 ;
feature 51 5/5, feature 55 (runner dédié) 0 échec. RED prouvé → GREEN.

== Mission Session 231 — BKY-LANG-NAV-3 (hôte JS + garde de parité)

*NAV-3 — Hôte JS.* Module `lang-switch.js` : résolveur pur `resolveLangPath(...)`
rejouant **les mêmes vecteurs** que NAV-1 (`bakery/langswitch/lang-switch-path-vectors.json`)
+ adaptateur DOM (lit `data-lang`, réécrit `href` au load sur arbre ; relocalise
sur dict client). Testé en **node/jsdom**.

*Garde de parité* (D6 : présence **et** comportement) : le module existe **des
deux côtés** (`maquette/js/` et `jbake/assets/js/`) **et** résout correctement
(`setLang` présent ≠ page préservée).

Tests : node (vecteurs partagés), adaptateur (jsdom), self/no-op ; garde
présence + comportement.

== Reprendre à

. `git status` dans bakery-gradle — vérifier qu'aucune session concurrente n'écrit.
. `.agents/CADRAGE_EPIC_BKY_LANG_NAV.adoc` (cadrage complet, D1→D10).
. `src/main/kotlin/bakery/langswitch/LangSwitchPath.kt` (+ `thymeleafHref`) et
   `src/main/resources/bakery/langswitch/lang-switch-path-vectors.json` (socle).
. `.agents/sessions/230-bky-lang-nav-2-thymeleaf-host.adoc` (rapport S-230).
. Gate Économie d'Encre : ne relancer `test` que si le code source change.

== Dette découverte (FLAKY-5)

`FixtureAlignmentAuditorTest` + `RealSiteI18nMigrationIntegrationTest` (cheroliv-com)
= **2 FAIL préexistants** : drift fixture ↔ site réel (chrome nativement FR,
S-053/055, 5 templates). Stash-prouvé non introduit par NAV-2. Session dédiée :
resync fixture + régénération golden master (procédure `PostMigrationSnapshotGenerator`).

== Rappel

* Gate métier : `grep -rwE "REAC|AFNOR|SPG|SPD|FPA|RNCP|CDA" src/` = 0.
* *Modèle LLM* : `gpt-oss:120b-cloud` épuisé → `gemma4:31b-cloud` +
   `OLLAMA_POOL_PORTS=11439,11440,11441,11442`.
* *Publication Central* : ordre DAG réel (publier le consommateur absent AVANT
   de l'ajouter au catalogue MEMPHIS — garde D8 anti-ghost).
* *Règle 0* : `./gradlew -q publishToMavenLocal` OBLIGATOIRE après toute
   modification du code source.
* Commit/push autorisés (décision pilote 2026-09-17). Zéro secret.
* Baby-step DDD/TDD/BDD strict — 1 fichier à la fois, gate `test` complète.
