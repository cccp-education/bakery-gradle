= PROMPT_REPRISE — Session 044 : BKY-BKG vérification build complet + BKG-0

== Mission (Session 044)

1. Vérifier le build complet : `./gradlew check` — tous les tests unitaires, fonctionnels et Cucumber doivent passer.
2. Si des tests échouent, les corriger (en priorité les tests legacy impactés par la suppression de jsoup/injectRelatedArticles).
3. Si le build est vert, avancer sur BKG-0 (enrichissement graphify pour contenu éditorial) — uniquement si le temps le permet.

== État actuel (Session 043 terminée)

BKY-BKG BKG-1 **Steps 0-6 terminés** :
- Architecture pivot : Extension DSL + jbake.properties + Thymeleaf côté serveur ✅
- `RelatedArticlesConfig` + `RelatedArticlesDsl` (6 UT) ✅
- `injectRelatedArticlesConfigIntoJbakeProperties()` (3 UT) ✅
- Fragment `related-articles.thyme` (5 UT rendering + 2 UT intégration) ✅
- FT Scaffold (2 FT) + Cucumber (3 scénarios) ✅
- Nettoyage : jsoup supprimé, `InjectRelatedArticlesFunctionalTest` supprimé, `registerInjectRelatedArticlesTask` supprimé

== Attention

- `libs.versions.toml` : jsoup version et library supprimés. Si d'autres boroughs référencent jsoup via ce TOML, vérifier.
- Les tests Cucumber et FT n'ont pas été lancés en `./gradlew check` complet — uniquement des runs ciblés.

== Baby Steps DDD/TDD/BDD pour BKG-0 (si le temps le permet)

| Étape | Type | Action | Tests |
|-------|------|--------|-------|
| 1 | DDD | Étendre `ArticleNode` avec `:jbake-tags:`, `:jbake-description:` | 3 UT |
| 2 | TDD | `RelatedArticlesService.buildGraph()` enrichi (tag co-occurrence) | 3 UT |
| 3 | BDD | Cucumber `18_related_articles_kg_enriched.feature` | 3 scénarios |