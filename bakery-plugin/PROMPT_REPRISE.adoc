= PROMPT_REPRISE — Session 054 : Choix prochain EPIC

== État actuel (Session 053 terminée)

- EPIC 17 ✅ TERMINÉ — generateArticle graceful fail (2 Cucumber corrigés, 70/70 PASS)
- 437 UT PASS, 53 FT PASS, 70/70 Cucumber PASS (dont EPIC 17)
- Bug #2 (`IaConfig.enabled = false`) déjà corrigé avant la session
- Bug #1 (step Cucumber) + Bug #3 (assertion message) résolus par ajout step `without IA`

=== 53 Cucumber failures préexistants (liés au scaffolding JB-2→JB-7)

Les 53 échecs Cucumber restants sont des tests de scaffolding (Thymeleaf components,
Google Forms, Firebase, Analytics, Layouts, Theme) qui nécessitent un site configuré
complet. Ils sont liés au chemin `createGradleProject()` qui ne crée PAS les dossiers
site/ et maquette/. CONV-1.x à poursuivre.

== Candidats pour Session 054

1. **CONV-1.9** — Corriger les 53 Cucumber failures restantes (scaffolding JB-2→JB-7)
2. **EPIC 6** — Intégration tache publishProfil dans le plugin
3. **Tests unitaires IaConfig** — Ajouter UT pour couvrir IaConfig.enabled=false par défaut
4. **Nettoyage DuplicateStepDefinitionException** — ConfigResolverSteps vs ThymeleafComponentSteps

== Architecture EPIC 17 — Rappel (RÉSOLU)

```
IaConfig.enabled = false (par défaut) ✅
  ↓
Si enabled = false → generateArticle enregistré SANS LlmService → IllegalStateException("Aucun LlmService") ✅
Si enabled = true + LLM disponible → article créé
Si enabled = true + LLM indisponible → ConnectException
```

== Fichiers modifiés (Session 053)

* `src/test/features/9_generate_article.feature` — 2 scénarios mis à jour
* `src/test/scenarios/bakery/scenarios/GenerateArticleSteps.kt` — ajout step `without IA`

== Attention

- Ne pas lancer `./gradlew test` sans permission explicite
- 53 Cucumber failures préexistants (CONV-1.x)