= PROMPT REPRISE — Bakery — Session 170
:session: 170
:date: 2026-06-29

== Session 169 terminée — Maintenance CI : 3 bugs résolus, build clean SUCCESSFUL

3 bugs CI résolus :
- Bug 1 : `settings.gradle.kts` — `error("ossrhUsername")` → `?: ""` (credentials optionnelles)
- Bug 2 : `CucumberTestRunner.kt` — filtre `not @personal` (scénarios cheroliv.com trop lents exclus)
- Bug 3 : `AccessibilityAuditTask.kt` — `@PathSensitive(RELATIVE)` sur `auditDir` (Gradle 9.5)
- Bug 4 NON résolu : `org.jetbrains:annotations:{strictly 13.0}` dans codebase-plugin POM (hors périmètre)

`./gradlew clean build` BUILD SUCCESSFUL (26/26 tasks, 8m57s). Coverage 93,09%.

== Prochaine mission — EPIC migration i18n réelle des 3 sites (sur copies, TDD/BDD)

Objectif : migrer réellement les 3 sites (magic-stick, cccp.education,
cheroliv.com) vers l'i18n sur des COPIES (jamais sur la production), validée
par la méthodo baby step DDD/TDD/BDD (domaine pur testable sans Gradle → UT
rouge → BDD Cucumber bout en bout).

La tuyauterie existe (MigrateToI18nTask, I18nMigrationService, fixtures
alignées S150) mais aucun site réel n'a été migré (0 messages_*.properties).

== Contexte technique
- Version : 0.0.2 Maven Central + Gradle Portal (LIVE)
- Coverage : 93,09% (Kover, seuil PASS)
- 3 sites réels : magic-stick, cccp.education, cheroliv.com — 0/3 migrés
- Ports Ollama : 11437-11465. Modèles : gpt-oss:120b-cloud, gemma4:31b-cloud
- Anglais imposé : nommage, commentaires, Gherkin, steps
- Bug 4 ouvert : codebase-plugin:0.0.2 POM `strictly 13.0` (website.yml cassé)