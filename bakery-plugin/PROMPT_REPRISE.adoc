= PROMPT_REPRISE — Bakery Gradle Plugin
:session: 241
:date: 2026-09-25
:etat: Session 240 ✅ — **release bakery 0.0.20 (BKY-LANG-NAV) publiée Central + CI réelle cccp.education/magic-stick VERTE (tree EN en production)** + **bump cross-borough Gradle 9.7.1 → 9.8.0**.
:epic_actif: **BKY-LANG-NAV** (🟡 NAV-0→8 ✅, **déployé en production** ; reste absorption split-brain cheroliv D5).
:epic_suivant: **Absorber le split-brain cheroliv** — `office/sites/cheroliv.com` duplique `VariantBakeAssembler`/`VariantServePlan`/`VariantDeployability` ; `bakery.i18n.variant` (S-239) est générique et devrait devenir la source unique.
:dette: FLAKY-5 (drift fixture cheroliv.com chrome FR, 2 FAIL préexistants) + ktlint baseline dépôt.

== État S-240

Le **blocage release** est levé : la CI résolvait un `bakery-plugin:0.0.19`
**périmé** (Central S-233 sans la feature NAV) → échec `materializeTemplates`
(task not found). Chaîne DAG exécutée :

. **MEMPHIS 0.0.58** (bundle `1bef33da`) : BOM/catalog + `bakery-plugin 0.0.20`.
. **bakery 0.0.20** (bundle `409aa5fc`) : repin ws 0.0.58 + BOM 0.0.58, JAR
   porte `MaterializeTemplatesTask`/`BakeVariantsTask`/`MessageBundleResolver`/
   `LangSwitcherContainerMask`.
. **runner** : pin catalogue 0.0.58 → `ws.plugins.bakery 0.0.20`.

**CI réelle VERTE** : cccp (run 36158522284, 3m18s) + magic (36158531602, 3m1s),
`bakeVariants 1 variante(s) bakée(s) : en`. Production vérifiée 200 :
`cccp.education/en/index.html` (« Common Content Creator Proletarian ») +
`cccp.education/magic-stick/en/index.html` (« Standardized portable environment »).

**Gradle 9.8.0** : 97 wrappers bumpés (properties only, méthode S-095), zéro
9.7.1, 34 repos commités/poussés. Validations : bakery, slider, conventions,
agent-contracts, runner VERT.

== Mission Session 241

*Absorber le split-brain cheroliv (D5)* — `office/sites/cheroliv.com` possède sa
copie de `VariantBakeAssembler`/`VariantServePlan`/`VariantDeployability` (et
`BakeLocalSite`/`LangSwitchOfferedLanguages`). La nouvelle brique
`bakery.i18n.variant` est générique. Faire converger : soit le site consomme
`bakery-plugin` (includeBuild ou Central), soit la copie est retirée. **Attention** :
cheroliv est un build auto-contenu (pas d'`includeBuild` bakery) et son arbre de
travail a de nombreux fichiers sales préexistants — périmètre à cadrer d'abord.

Alternative : *FLAKY-5* — resync fixture cheroliv.com chrome FR (5 templates
`blog/blog_section/footer/header/menu`) + régénération golden master
(procédure S-222), purement bakery-local.

== Reprendre à

. `git status` dans bakery-gradle ET office (attention : 133+ fichiers sales
   préexistants dans office, NE PAS les committer).
. `.agents/sessions/240-…` (ce rapport) + `.agents/CADRAGE_EPIC_BKY_LANG_NAV.adoc`.
. `.agents/sessions/239-…` (brique `bakeVariants`) et 240 pour la chaîne release.
. `src/main/kotlin/bakery/i18n/variant/` (la brique) + `office/sites/cheroliv.com/src/main/kotlin/com/cheroliv/i18n/`.
. Gate Économie d'Encre : ne relancer `test`/`cucumber` que si le code source change.

== Rappel

* Gate métier : `grep -rwE "REAC|AFNOR|SPG|SPD|FPA|RNCP|CDA" src/` = 0.
* **JAMAIS lire un `site.yml`** (gitignoré, contient un token) — `git check-ignore -v` d'abord.
* *Modèle LLM* : `gemma4:31b-cloud` + `OLLAMA_POOL_PORTS=11439,11440,11441,11442`.
* *Règle 0* : `./gradlew -q publishToMavenLocal` OBLIGATOIRE après toute modif source.
* *Version dérivée* : bumper le toml local (`bakery = "0.0.X"`) est obligatoire pour
  qu'une release porte les changements — une version dérivée du catalog n'est PAS
  une version publiée (leçon S-240, piège HTOWN S-218).
* *Pattern BDD runner dédié* : tout tag non couvert doit avoir son runner (S-082).
* Commit/push autorisés (décision pilote 2026-09-17). Zéro secret.
* Baby-step DDD/TDD/BDD strict — 1 fichier à la fois, gate `test` complète.
