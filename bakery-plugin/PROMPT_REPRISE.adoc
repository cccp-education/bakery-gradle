= PROMPT_REPRISE — Bakery Gradle Plugin
:session: 239
:date: 2026-09-25
:etat: Session 238 ✅ — **BKY-LANG-NAV-8 rollout partiel** : chaîne `materializeTemplates` → `injectLangSwitch` câblée (delta layout `i18n/{lang}`) + bug d'idempotence corrigé (`LangSwitcherContainerMask`) + conteneur marqueur posé sur cccp/magic.
:epic_actif: **BKY-LANG-NAV** (🟡 NAV-0→7 ✅, NAV-8 🟡 enabler + rollout partiel).
:epic_suivant: **NAV-8 rollout site complet** (tree EN déployable cccp/magic + CI `deploy-i18n-site` cross-borough + bake variantes). Bloquant magic : `feed.thyme` exige du content (météoré).
:dette: FLAKY-5 (drift fixture cheroliv.com) + ktlint baseline dépôt (rouges à HEAD, préexistants).

== État S-238

Trois blocs livrés (baby-step DDD/TDD/BDD strict) :

. **Delta layout** `bakery.langswitch.LangSwitchMenuLayout` (pur) : l'i18n
   matérialisé `i18n/{lang}/templates/` prime, fallback `{lang}/templates/`,
   défaut au root. `InjectLangSwitchTask` délègue (RED FT/BDD prouvés).
. **Bug idempotence** (dogfooding) : `LangSwitcherContainerMask.strip` (pur)
   retire le fragment généré avant keyage → `materializeTemplates` reste
   idempotent après `injectLangSwitch` (plus de `menu.9`/`menu.10` orphelins).
. **Rollout partiel** : conteneur vide `lang-switcher-container` posé dans les
   `menu.thyme` réels cccp.education + magic-stick (inerte en FR, rempli au bake) ;
   fixtures + golden masters resynchronisés.

Dogfooding `/tmp` : cccp 11 templates + magic 12, 0 clé non résolue, variant EN
**baké** (sélecteur page-aware, UTF-8 correct), **zéro LLM**.

== Mission Session 239 — NAV-8 rollout site complet

*Câbler la chaîne de bake des variantes EN pour cccp.education / magic-stick + CI.*

Constats de périmètre (S-238) :
. cccp/magic = consommateurs `site.yml` purs (pas de build), via runner-gradle N3 ;
   le wiring `materializeTemplates` + `injectLangSwitch` + bake variantes + CI
   (`deploy-i18n-site.yml`) est un chantier cross-borough.
. **cccp** : variant EN **déployable** (chrome gelé + content partagé).
. **magic** : variant EN **non déployable** tel quel — `feed.thyme` rend
   `published_posts[0]` → `IndexOutOfBounds` sur content vide. Le content EN
   métier exige la traduction LLM metered (**accord pilote requis**).
. Le rollout n'écrit jamais le contenu métier sans accord : conteneur marqueur
   seulement en S-238.

*Attention boundary* : `office/sites/*` hors `foundry/`. Travail sur copie/fixture
d'abord, jamais de modification destructive du site réel sans accord pilote.

== Reprendre à

. `git status` dans bakery-gradle ET office.
. `.agents/sessions/238-…` (ce rapport) + `.agents/CADRAGE_EPIC_BKY_LANG_NAV.adoc` (§ NAV-8).
. `.agents/237-…` (enabler bundles gelés) + `.agents/236-…` (NAV-7 talaria, patron client).
. Golden masters : `src/test/resources/i18n-fixtures/{cccp-education,magic-stick}/`.
. Gate Économie d'Encre : ne relancer `test`/cucumber que si le code source change.

== Rappel

* Gate métier : `grep -rwE "REAC|AFNOR|SPG|SPD|FPA|RNCP|CDA" src/` = 0.
* *Modèle LLM* : `gemma4:31b-cloud` + `OLLAMA_POOL_PORTS=11439,11440,11441,11442`.
* *Règle 0* : `./gradlew -q publishToMavenLocal` OBLIGATOIRE après toute modif source.
* *Pattern BDD runner dédié* : tout tag non couvert doit avoir son runner (S-082).
* Commit/push autorisés (décision pilote 2026-09-17). Zéro secret.
* Baby-step DDD/TDD/BDD strict — 1 fichier à la fois, gate `test` complète.
