= PROMPT_REPRISE — Session 063 : Nettoyage FT Legacy BKG + LENS-6 E2E Playwright

== Mission (Session 063)

1. **Nettoyer FT legacy BKG** — `CollectRelatedArticlesFunctionalTest` (3 échecs préexistants, référence tâche supprimée LENS-3.3)
   - Supprimer ou marquer `@Disabled` les 3 tests de `CollectRelatedArticlesFunctionalTest`
   - Corriger `ConfigResolverCascadeTest.CONV-1 defaults` qui attend `relatedArticlesMaxResults=4` (supprimé en LENS-3.3)
2. **BKY-LENS-6.3** — E2E Playwright : vérifier rendu augmenté sur une page bake
   - Scénario Playwright : naviguer sur une page bake → vérifier `data-augmented-context` attribute sur `aside.augmented-articles`
3. Vérifier que 129/129 Cucumber PASS après nettoyage

== État actuel (Session 062)

- **BKY-LENS-6.1 TERMINÉ** ✅ — `augmented-articles.thyme` créé avec badges RAG/KG/Docs/EAGER
- **BKY-LENS-6.2 TERMINÉ** ✅ — `post.thyme` et `page.thyme` référencent `augmented-articles.thyme`
- 21/21 UT AugmentedArticlesTemplateTest PASS
- 10/10 Cucumber Feature 24 PASS
- 129/129 Cucumber total PASS
- 1/1 FT CollectAugmentedContextFunctionalTest PASS
- 3 FT legacy BKG en échec (préexistant, non lié à LENS-6)

== Architecture LENS — Rappel

```
graph.json (203k nœuds) ← ✅ LENS-1
    ↓ SubgraphExtractor → site-subgraph (~200 nœuds)
    ↓ + RAG pgvector + règles métier ← ✅ LENS-2
site-graph (~200 nœuds, ~500 edges)
    ↓ LensBudget (4-6 articles/page, minSimilarity=0.7) ← ✅ LENS-3
jbake.properties (augmentedContextData) ← ✅ LENS-3
    ↓
composite-context.json (runner-gradle N3) ← ✅ LENS-5
    ↓
AugmentedContextResolver → augmentedEntries in metadata.json ← ✅ LENS-5
    ↓
post.thyme / page.thyme → badges RAG/KG/Docs ← ✅ LENS-6
```

== Points d'attention

- Les badges sont rendus par JavaScript côté client (data-attributes injectés par Thymeleaf)
- `CollectRelatedArticlesFunctionalTest` référence la tâche `collectRelatedArticles` supprimée en LENS-3.3
- Ne pas lancer `./gradlew test` sans permission explicite