= PROMPT REPRISE — Bakery — Session 161
:session: 161
:date: 2026-06-24

== Session 160 terminée — Architecture README Plugin + Consommateur

Déplacement README.plugin → bakery-plugin/README.plugin/ (README.md master
remonté à racine plugin). Déplacement README.consommateurs → racine borough.
Liens i18n recréés (10 langues, `README.plugin/` + `README.consommateurs/`).

== Mission en cours

Reprise backlog pivot YAML :

1. **Loi de l'Économie d'Encre sur generatePivotYaml** :
   - `GeneratePivotYamlTask` doit lire le YAML existant avant d'écrire
   - Si le YAML généré == YAML existant → skip l'écriture (ne pas écraser)
   - Test : invoquer 2× la tâche, vérifier que la 2e fois ne réécrit pas

2. **CLI option natif `--input`/`--output`** :
   - Migrer `-Pinput`/`-Poutput` vers `@Option` sur la tâche

3. **Étendre golden masters** : 4e golden master avec listes imbriquées + plantuml

4. **Bug préexistant cucumber** (optionnel) : features 9, 16 steps non chargés

== Contexte parser pivot

* Package : `bakery.pivot` (6 classes main + 5 classes test)
* Golden master : `src/test/resources/pivot-golden-master/` (3 .adoc + 3 .yaml)
* 41 UT + 3 functional + 2 BDD scénarios PASS
* `./gradlew test` = 1m 1s SUCCESS

== Notes techniques

* Ports Ollama autorisés : 11437-11465 uniquement. Modèles :
  `gpt-oss:120b-cloud`, `gemma4:31b-cloud`.
* Timeout cucumberTest 5min (EPIC 5) — ne pas lancer `check` complet sans
  permission.
* *Loi de l'Économie d'Encre* : toute tâche Gradle LLM DOIT load-before-write +
  calcul delta + fusion existing+new.