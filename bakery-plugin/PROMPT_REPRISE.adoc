= PROMPT_REPRISE — Session 074 : Bakery Polish Suite (CS-4 ou CS-5)

== Mission (Session 074)

1. **CS-4 — Pattern Strategy injectConfig** (P2, 1 session) : Extraire `injectResolvedConfigIntoJbakeProperties()` en `Map<String, ConfigInjector>` avec `fun interface ConfigInjector`. Réduction 70→~25 lignes.
2. **OU CS-5 — Générifier ConfigResolver** (P2, 1 session) : Remplacer 9 méthodes `resolve*` quasi-identiques par `inline fun <reified T> resolve()`. Réduction ~200→50 lignes.

== État actuel (Session 073)

- **CS-11** ✅ TERMINÉ — Logger uniformisé (println+logger → logger.lifecycle), 3 fichiers, 0 régression.
- **CS-9** ⏭️ SKIPPED — Faux positif (DSL builders Gradle idiomatiques avec var).
- **Coverage** : 77.09% (seuil 75% ✅).
- **Bakery** : 95% complet. 2-3 CS restants (CS-4, CS-5, CS-8 XML fragile).

== Points d'attention

- Ne pas lancer `./gradlew test` sans permission explicite
- Priorité : CS-4 > CS-5 (CS-4 réduit la duplication visible, CS-5 est du refactoring interne)
- Méthodologie baby-step : 1 fichier à la fois, compile + test entre chaque étape
- Si CS-4 trop gros → switch borough (bakery considéré clos à 95%)
