= PROMPT_REPRISE — Bakery Gradle Plugin
:session: 225
:date: 2026-09-17
:etat: Session 224 ✅ — **BKY-I18N-JS-6 TERMINÉ** : le contenu du catalogue imbriqué (`TALARIA.I18N.CATALOG`) est enfin **traduit** (writer imbriqué + garde structurelle), pas seulement mesuré en couverture. Gate `test` **1842/0 FAIL** (baseline 1804/0 → +38 net).
:epic_actif: Aucun EPIC bakery actif sans accord pilote. BKY-I18N-JS est désormais **réellement 6/6 US** (couverture S-223 + écriture S-224).

== Contexte de fin

Session 224 — clôture du chantier distinct acté S-223 décision §1 (« la
couverture n'est pas l'écriture ») :

- Le catalogue imbriqué n'était **jamais traduit** : le parser plat n'y voit
  aucune clé et `I18nCatalog` ne faisait que compter les lacunes.
- Livré : `I18nJsObjectParser` (parseur structurel récursif, byte-ranges),
  `I18nCatalogDocument` (aplatissement pathé : `fpa.title`,
  `fpa.modules[0].desc`), `I18nCatalogWriter` (`translate` in-place +
  `complete` clone de la référence, contrat S-054), `I18nCatalogDelta`,
  `I18nCatalogFormatGuard` (unbalanced/structure-drift/round-trip-drift).
- Câblage `TranslateI18nClientTask` : partition plat/catalogue étendue au
  **delta** ; complétion des formations/champs manquants ; création d'une
  langue absente ; garde en production ; échec total = zéro écriture.
- REFACTOR `I18nJsStringCodec` (escape/unescape partagé plat+imbriqué).

Gate : `test` **1842/0 FAIL**, `functionalTest --rerun-tasks` 132/132,
feature 64 **17/17**, dogfooding réel talaria (22 langues, no-op strict),
`publishToMavenLocal` VERT, grep métier = 0.

== Mission Session 225 — à la demande

Les 3 pistes restantes exigent toutes un **accord pilote** :

1. **BKY-LANG-22-8/9** : dogfooding LLM réel 22 langues (slider corpus + capsule
   vidéo) — coût LLM metered.
2. **BKY-LANG-22-10** : publication Central consommateurs (BOM/catalog 0.0.43) —
   `bakery-plugin` ≥ 0.0.13 débloque le wiring talaria en CI.
3. **MASS-6** : montée en charge 108 articles × 10 langues — coût LLM ~675h.

Option locale actionnable (si aucune des 3 n'est autorisée) : **dogfooding dry
réel du catalogue talaria complet** avec un `TranslationService` factice
(22 langues × 2 formations, ~300 littéraux) pour valider la complétion à
l'échelle — zéro coût LLM, zéro écriture (copie temporaire).

## Reprendre à

. `git status` dans bakery-gradle — vérifier qu'aucune session concurrente n'écrit.
. `.agents/sessions/224-BKY-I18N-JS-6-catalogue-traduction.adoc` (rapport)
. `./gradlew test` — non-régression (baseline 1842/0 attendue)
. `publishToMavenLocal` après chaque modification code source (règle INDEX 0)

## Rappel

- Gate permanente : `grep -rwnE "REAC|AFNOR|SPG|SPD|FPA|RNCP|CDA" src/` = 0
- **Décision pilote (S-054)** : pas de migration `LlmBuildService` (codebase N1) —
  socle LLM bakery **local** (`bakery.llm.PooledOllamaLlmService`), hors périmètre.
- **Config pool LLM consommateur** : `site.yml → ollama:` résolue par `IaConfigResolver`
  (S-221), prioritaire sauf DSL explicitement activé.
- **Collision de sessions** : vérifier `git status` — d'autres boroughs écrivent parfois
  dans le même arbre (graphify, codex).
- Note environnement : `:test` peut mourir en `EOFException` sous charge (load 20+,
  swap plein) — incident infra, relancer ; `functionalTest --rerun-tasks` si un
  FAILED sans échec de test.

(PROMPT_REPRISE — ~58 lines)
