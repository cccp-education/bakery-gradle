= PROMPT_REPRISE — Bakery Gradle Plugin
:session: 208
:date: 2026-08-19
:etat: Session 207 ✅ — MASS-5 CLÔTURÉ (8/8 lots, 108/108 articles FR→EN, idempotence validée Delta=0, validation LENIENT 0 invalide). Aucun code modifié (dogfooding only). bakery 0.0.9 + document 0.0.3 déjà publiés Central (S206 antérieur).
:epic_actif: MASS-6 🟡 reporté (108 × 10 langues, ~675h LLM, accord pilote explicite requis). Tous les autres EPICs bakery clos.

== Contexte de fin

Session 207 — Dogfooding LLM réel + clôture MASS-5 :
- Lot 2026 déjà traduit (28/28 articles, timestamps août 18 run antérieur)
- Headers JBake préservés (0 cassé), `:summary:` traduits (fix S206 appliqué)
- Ratio FR résiduel < 7% (24/28 < 5%, faux positifs mots courts)
- Idempotence Loi Économie d'Encre : re-run `blog/2026` → Delta=0, 108 préservés
- Validation LENIENT : 0 table invalide, 0 plantuml invalide
- Bug chemin dogfooding : `--contentI18nSource=content/blog/2026` double
  le `content/` (srcPath site.yml préfixé) → workaround `blog/2026`

## Mission Session 208

1. **MASS-6 montée en charge** : 108 articles × 10 langues
   (zh/hi/es/ar/bn/pt/ru/ur) — session dédiée, **accord pilote explicite
   requis**. Coût LLM ~675h (108 × 9 langues × ~50 segments × 5s).
2. **Bug chemin dogfooding** : `--contentI18nSource=content/blog/YYYY`
   produit `content/content/blog/YYYY` (double `content/`). Fix dans
   `MigrateContentI18nTask` — ne pas préfixer si le chemin commence déjà
   par `srcPath`. P3 (non bloquant, workaround existe).
3. **Re-traduction cheroliv.com/en/** avec fix `:summary:` (impact SEO —
   articles S181 ont leur `:summary:` en français, déjà corrigés S206
   sur les copies dogfooding, à propager en prod).

## Reprendre à

. `git status` dans bakery-gradle
. Lire `BACKLOG.adoc` section BKY-I18N-MASS MASS-6
. Vérifier Ollama ports 11437-11465 + modèle `gemma4:31b-cloud`
. `/tmp/bakery-dogfood` projet consommateur (buildscript classpath 0.0.9)
. `publishToMavenLocal` après chaque modification code source

## Ordre des EPICs (priorité)

. MASS-6 montée en charge 108 × 10 langues (accord pilote)
. Bug chemin dogfooding (P3, non bloquant)
. Re-traduction cheroliv.com/en/ prod (fix `:summary:` impact SEO)

## Fichiers touchés S207

Aucun fichier source modifié. Dogfooding via `/tmp/bakery-dogfood/`
(projet consommateur includeBuild, hors repo git). Gouvernance uniquement
(archive session, PROMPT_REPRISE, INDEX, SESSIONS_HISTORY, BACKLOG sync).