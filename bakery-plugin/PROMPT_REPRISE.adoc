= PROMPT REPRISE — Bakery — Session 163
:session: 163
:date: 2026-06-24

== Session 162 terminée — BKY-PERF-6 : Migration FT→UT @TestInstance(PER_CLASS)

Session d'optimisation des tests fonctionnels. Investigation BKY-PERF-1→5 :
profiling 13 classes, baseline 4min10s, bottleneck incompressible GradleRunner.

**BKY-PERF-6** : Migration de 6 classes FT vers UT avec `@TestInstance(PER_CLASS)`
+ champs `project`/`ext` partagés via `@BeforeEach` + helpers configure*() privés.
6 UT créées (IaConfig, ArticleIntention, ThemeIntention, ScaffoldIntention,
CollectSiteContext, FirebaseAuthConfig). 6 FT supprimées. 1 nouveau FT
(IntegrationRestoredFunctionalTests) conserve les tests réels d'exécution.

`./gradlew test` BUILD SUCCESSFUL (11 tasks, 1m14s). Zéro code production modifié.

== Mission en cours

Reprise des pistes reportées de S161 (S162 a dévié pour BKY-PERF) :

1. **Loi de l'Économie d'Encre sur generatePivotYaml** :
   - load-before-write + skip si identique
   - Test : invoquer 2× la tâche, vérifier que la 2e fois ne réécrit pas

2. **Étendre golden masters** : 4e golden master avec listes imbriquées + plantuml

3. **Bug préexistant cucumber** (optionnel) : features 9, 16 steps non chargés

== Contexte tests

* 6 nouvelles UT PER_CLASS : `src/test/kotlin/bakery/{llm,article,theme,scaffold,lens}/`
* `IntegrationRestoredFunctionalTests` : tests GradleRunner réels dans 3 inner classes
* `./gradlew test` = 1m14s SUCCESS

== Notes techniques

* Ports Ollama autorisés : 11437-11465. Modèles : `gpt-oss:120b-cloud`, `gemma4:31b-cloud`.
* BKY-PERF EPIC ajouté dans BACKLOG (section synthèse optimisation FT).
* maxParallelForks fixé à 3 (build.gradle.kts:158, BKY-PERF-5).
