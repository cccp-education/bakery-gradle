= PROMPT_REPRISE — Bakery Gradle Plugin
:session: 236
:date: 2026-09-25
:etat: Session 235 ✅ — **BKY-LANG-NAV-6 TERMINÉ — rollout cheroliv.com** (sélecteur same-page, premier rollout site). Le sélecteur préserve la page, split-brain tué (D5), vecteurs partagés (D3), régression bake corrigée. Antérieur S-234 : NAV-5 (cœur clos). S-233 : re-pin catalogue ws 0.0.57.
:epic_actif: **BKY-LANG-NAV** (🟡 NAV-0 ✅, NAV-1 ✅, NAV-2 ✅, NAV-3 ✅, NAV-4 ✅, NAV-5 ✅, NAV-6 ✅, NAV-7→8 À FAIRE) — rollout sites en cours.
:epic_suivant: **NAV-7** (phase site — talaria.school : harden). Dépend de NAV-3 ✅.
:dette: null-safety `bakery.langswitch.LangSwitchPath` (bug latent, session dédiée si un site scaffoldé le rencontre) + FLAKY-5 (drift fixture cheroliv.com).

== État S-235

**BKY-LANG-NAV-6 livré** — premier rollout site. Travail **cross-borough** (repo
`office/sites/cheroliv.com`, build autonome), **zéro source bakery modifiée**.
Le site dupliquait la règle page-blind (`com.cheroliv.i18n`) ; D5 a tué le
split-brain : `LangSwitchLink` **délègue** à `com.cheroliv.i18n.LangSwitchPath`
(frère du `bakery.langswitch.LangSwitchPath`), prouvé contre les **mêmes vecteurs
partagés** (D3). Une **régression de rollout** (NPE `lastIndexOf` sur pages
synthétiques `content.uri` null) a été découverte et corrigée (règle null-safe,
dégradation index D8). **17 menus régénérés**, idempotence prouvée, dogfooding
bake réel (page imbriquée FR↔EN préservée, `hreflang` conservé D9).

Gates **site** : `test` 185/0, `functionalTest` 29/0 (1 skipped), `nodeTest` 10/0,
`cucumberTest` 5 échecs = baseline S-052 (prose, pool `ok=0`). Détail complet
`.agents/sessions/056-bky-lang-nav-6-same-page.adoc` du site.

== Mission Session 236 — BKY-LANG-NAV-7 (phase site talaria.school)

*NAV-7 — harden talaria.school.* Déjà client-side (pas de bug page) : aligner le
module JS sur la spec commune + garde de parité + test de comportement (page
préservée). Dépend de NAV-3 ✅ (hôte JS `lang-switch.js` + `LangSwitchClientParity`).

*Couverture* : test node du résolveur contre les vecteurs partagés + garde de
parité `maquette/js` ↔ `jbake/assets/js` + vérification que `TALARIA.I18N.setLang`
conserve la page (mode `dictionary`).

*Attention boundary* : `office/sites/talaria.school` est hors `foundry/`. Travail
sur fixture/copie, jamais de modification destructive du site réel sans accord
pilote.

== Reprendre à

. `git status` dans bakery-gradle ET office/sites/cheroliv.com — sessions concurrentes ?
. `.agents/CADRAGE_EPIC_BKY_LANG_NAV.adoc` (§ NAV-7) + `.agents/sessions/235-bky-lang-nav-6-rollout-cheroliv.adoc`.
. `.agents/sessions/056-bky-lang-nav-6-same-page.adoc` (site — le livré NAV-6).
. `office/sites/talaria.school/maquette/js/i18n.js` (`TALARIA.I18N`) + le module bakery `lang-switch.js`.
. Gate Économie d'Encre : ne relancer `test` que si le code source change.

== Dette découverte (S-235)

* **Null-safety `bakery.langswitch.LangSwitchPath.thymeleafHref()`** — l'expression
  self-link `${content.uri.substring(content.uri.lastIndexOf('/') + 1)}` n'est pas
  null-safe ; `content.uri` est null sur les pages synthétiques (`archive`,
  `tags`, masterindex). Le modèle bakery `site/templates/archive.thyme` rend
  `menu.thyme` → un site scaffoldé qui bake son archive échouerait au NPE. Aligner
  sur la forme null-safe du site + FT qui bake l'archive. **Hors périmètre** tant
  qu'aucun site scaffoldé ne le rencontre.
* **FLAKY-5** — `FixtureAlignmentAuditorTest` + `RealSiteI18nMigrationIntegrationTest`
  (2 FAIL préexistants, drift fixture cheroliv.com `blog_section.thyme`, chrome FR
  S-053/055) — stash-prouvés indépendants de NAV-6. Session dédiée : resync
  fixture + régénération golden master (`PostMigrationSnapshotGenerator`).
* `koverThresholdCheck` + `ktlintKotlinScriptCheck` rouges à HEAD (baseline dépôt).

== Rappel

* Gate métier : `grep -rwE "REAC|AFNOR|SPG|SPD|FPA|RNCP|CDA" src/` = 0.
* *Modèle LLM* : `gemma4:31b-cloud` + `OLLAMA_POOL_PORTS=11439,11440,11441,11442`.
* *Publication Central* : ordre DAG réel (publier le consommateur absent AVANT
   de l'ajouter au catalogue MEMPHIS — garde D8 anti-ghost).
* *Règle 0* : `./gradlew -q publishToMavenLocal` OBLIGATOIRE après toute
   modification du code source.
* *Pattern BDD runner dédié* : tout tag de feature non couvert par le runner
   principal doit avoir son runner dédié (S-082) — sinon il est skippé en silence.
* Commit/push autorisés (décision pilote 2026-09-17). Zéro secret.
* Baby-step DDD/TDD/BDD strict — 1 fichier à la fois, gate `test` complète.
