= PROMPT_REPRISE — Session 052 : CONV-1.8 Dette Cucumber + Nettoyage

== Mission (Session 052)

1. **CONV-1.8** : Résoudre le `DuplicateStepDefinitionException` Cucumber (renommage step `createBakeryProjectWithSiteConfigured`)
2. **CONV-1.8** : Investiguer les ~58 échecs Cucumber préexistants et classifier bloquants vs non-bloquants
3. **CONV-1.7** : ✅ TERMINÉ — 2 FT + 2 Cucumber scénarios PASS

== État actuel (Session 051 terminée)

- CONV-1.7 : ✅ 4 UT + 2 FT + 2 Cucumber scénarios PASS
  - FT : `SaveConfigurationTest` dans `ScaffoldFunctionalTests.kt`
    - `should save push credentials into site yml when collectSiteConfig is invoked with CLI properties`
    - `should preserve existing bake configuration when saving new credentials`
  - Cucumber : `21_save_configuration.feature` — 2 scénarios PASS
    - `Save push credentials into site.yml via collectSiteConfig`
    - `Preserve existing bake configuration when saving push credentials`
  - Steps : `SaveConfigurationSteps.kt` (Given + When)
- Dette Cucumber identifiée : `DuplicateStepDefinitionException` entre `ConfigResolverSteps` et `GenerateArticleSteps`
  - Même step `@Given("a new Bakery project with site configured")` défini 2 fois
  - Impact : ~58 échecs Cucumber cascading
- Solution proposée : Renommer le step dans `GenerateArticleSteps` en `@Given("a new Bakery project with site configured and IA enabled for article generation")`

== Architecture saveConfiguration — Rappel

```
collectSiteConfig → collecte (username, repo, token, configPath)
                  → File.saveConfiguration(site, siteYmlFile, username, repo, token)
                  → FileSystemManager.yamlMapper.writeValue(siteYmlFile, updatedConfig)
```

== Attention

- Ne pas lancer `./gradlew test` sans permission explicite
- 58 échecs Cucumber préexistants (DuplicateStepDefinitionException — à résoudre en CONV-1.8)