= PROMPT REPRISE — Bakery — Session 168
:session: 168
:date: 2026-06-27

== Session 167 terminée — BKY-TREE-5+6 (13 pts, 80 UT GREEN)

=== ✅ TREE-5 — Theme inheritance (5 pts)
- `ThemeResolver.kt` : `effectiveTheme(node)` remonte chaîne d'ancêtres,
  premier override gagne, sinon default. Override = remplacement total.
  `ResolvedTheme(theme, resolvedAtPath)`. Détection orphelins.
- 12 UT + 8 scénarios BDD PASS

=== ✅ TREE-6 — i18n migration par sous-arbre (8 pts)
- `SubtreeI18nPlanner.kt` : `computeDelta(after, subtreePath?)` →
  `I18nDelta(modifiedArticles, untouchedArticles, updatedChecksums)`.
  Delta ciblé par article. Idempotence (Loi Économie d'Encre par feuille).
  Backward compat (liste plate si pas de tree:).
- 11 UT + 6 scénarios BDD PASS

=== Bugs corrigés (préexistants S166)
- Feature 39/41 : steps singulier/pluriel manquants (Cucumber no auto-match)
- DuplicateStepDefinition : fusion SubtreeI18nDeltaSteps → ThemeResolverSteps
- TreeCucumberTestRunner : isole features @tree des échecs infrastructure

== Prochaine mission — BKY-TREE-7 (5 pts, clôture EPIC)

**US TREE-7 — Scaffold IA retourne un arbre**
`ScaffoldOutput.tree: SiteNode` au lieu de `templates: List<String>`.
Adapter flatten pour backward compat (si `tree:` absent, `templates:` accepté).
LLM prompt mis à jour pour retourner structure hiérarchique JSON.
Feature Cucumber bout en bout. Baby steps DDD/TDD/BDD.

== Contexte technique
- 80 UT tree package GREEN (6 classes de test)
- 42 scénarios BDD tree (6 features 39→44)
- Version : 0.0.2 Maven Central + Gradle Portal (LIVE)
- EPIC BKY-TREE : 31/36 pts (86%). TREE-7 = clôture (36/36).
- Ports Ollama : 11437-11465. Modèles : gpt-oss:120b-cloud, gemma4:31b-cloud
- Anglais imposé : nommage, commentaires, Gherkin, steps
- `TreeCucumberTestRunner` : runner dédié `@tree` pour validation isolée