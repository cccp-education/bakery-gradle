= PROMPT_REPRISE — Session 028 prête : EPIC 16 (Scaffolding Cucumber) en priorité

== Mission accomplie (Session 027)

Analyse exhaustive des 12 échecs de test préexistants + 3 EPICs cadrés dans BACKLOG :

- **EPIC 15** (3pts, P0) : PluginApplicationTest NPE — cause : `siteType` non stubbé
- **EPIC 16** (2pts, P0) : Cucumber Scaffolding 7 UndefinedStepException — cause : 1 step manquant
- **EPIC 17** (5pts, P0) : generateArticle graceful fail — cause : 3 bugs superposés (scénario + prod + assertion)

Modifications live :
- Ports LLM test modifiés : 11434→11465/64/63/62 selon fichier
- `IaConfig.kt` prod : défaut 11434→11464 aligné
- `gradle/libs.versions.toml` : readme plugin 0.0.0→0.0.1

== Prochaine mission (028) — EPIC 16 (le + rapide)

Ordre recommandé : EPIC 16 → EPIC 15 → EPIC 17

**EPIC 16 — Baby step ①** (1 pt, ~10 min) :
```
6_site_scaffolding.feature lignes 15, 60, 79 :
  remplacer "the gradle project does not have"
  par "does not have"
→ ./gradlew cucumberTest --tests "*CucumberTestRunner*"
→ 3 scénarios verts (UJ1, UJ4, UJ5)
```

== Rappels

- `./gradlew -q publishToMavenLocal` après modif code source
- Bakery = expert JBake site statique
- BACKLOG.adoc = 3 nouveaux EPICs (15, 16, 17) avec User Journeys Gherkin + baby-steps
