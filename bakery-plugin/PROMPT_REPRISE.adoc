= PROMPT_REPRISE — Session 053 : EPIC 17 generateArticle graceful fail + IaConfig.enabled=false

== Mission (Session 053)

1. **EPIC 17** : Corriger `IaConfig.enabled = false` par défaut (actuellement `true` — code mort)
2. **EPIC 17** : Corriger le scénario Cucumber "generateArticle fails gracefully without IA" (2 échefs restants)
3. **EPIC 17** : Ajouter le step Cucumber `@Given("a new Bakery project with site configured but without IA")` dans `GenerateArticleSteps.kt`

== État actuel (Session 052 terminée)

- CONV-1.8 : ✅ TERMINÉ — DuplicateStepDefinitionException résolu (1 step supprimé dans GenerateArticleSteps.kt)
- 437 UT PASS, 53 FT PASS, 68/70 Cucumber PASS
- 2 échefs Cucumber restants = EPIC 17 (`9_generate_article.feature` scénarios 1 et 2)
  - Scénario 1 : `generateArticle task is registered` — attend `task "generateArticle"` dans output mais `generateArticle` n'est pas registred car IA non configuré
  - Scénario 2 : `generateArticle fails gracefully without IA` — attend "LlmService" mais reçoit "Connection refused" car `IaConfig.enabled = true` par défaut

== Architecture EPIC 17 — Rappel

```
IaConfig.enabled = false (par défaut)
  ↓
Si enabled = false → generateArticle ne s'enregistre PAS (task absente)
Si enabled = true + LLM disponible → article créé
Si enabled = true + LLM indisponible → IllegalStateException("LlmService")
```

== Baby Steps DDD/TDD/BDD (EPIC 17)

. **Étape 1 — Production** : `IaConfig.enabled = false` par défaut (`IaConfig.kt`)
. **Étape 2 — Step Cucumber** : Ajouter `@Given("a new Bakery project with site configured but without IA")` dans `GenerateArticleSteps.kt`
. **Étape 3 — Feature Cucumber** : Mettre à jour `9_generate_article.feature` avec les nouveaux steps
. **Étape 4 — Vérification** : Lancer UT + FT + Cucumber → 70/70 PASS

== Attention

- Ne pas lancer `./gradlew test` sans permission explicite
- EPIC 17 baby steps déjà cadrés dans BACKLOG.adoc