= PROMPT_REPRISE — Bakery

== Mission accomplie

*Session 118 TERMINÉE* — BKY-CR-V5 US-1+2.
US-CR-V5-1 (P0) : FAUX POSITIF — ordre replace() actuel correct pour JSON Jackson.
US-CR-V5-2 (P1) : slugify() extrait dans `bakery/util/Slugify.kt` + parser JSON maison
remplacé par Jackson dans ValidateFirebaseConfigTask. 5 fichiers modifiés, net -60 lignes.

== Statut

- EPICs : 30/30 ✅ (100%)
- Code smells résolus : 24/24 ✅ (V2-V4)
- Code smells V5 : 58 ☐ (US-1 faux positif, US-2 résolue)
- BKY-CR-V5 : 4/24 pts (US-1 1pt + US-2 3pts)
- BKY-I18N-MIG : 0/16 pts ☐
- Version publiée: 0.0.1 (Maven Central)

== Prochaine session (119)

*Priorité P1* : BKY-CR-V5 — US-CR-V5-3 (3 pts).
Fix entries.first{} unsafe → firstOrNull{}, println() → logger.lifecycle(),
masquage auth.apiKey dans prompt LLM, yamlMapper → lazy {}.

*Fichiers cibles* :
- `GenerateArticleTask.kt:208` — `entries.first{}` → `firstOrNull{}` avec fallback
- `SiteTaskRegistrar.kt:201` — `println()` → `logger.lifecycle()`
- `ValidateFirebaseConfigTask.kt:140-161` — masquer `auth.apiKey` dans buildLlmPrompt()
- `FileSystemManager.kt:165` — `yamlMapper` → `lazy {}`

*Vérification* : `./gradlew test` → GenerateArticleTaskTest, SiteTaskRegistrar tests,
ValidateFirebaseConfigTaskTest, FileSystemManagerTest PASS.
