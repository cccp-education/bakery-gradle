= PROMPT_REPRISE — Bakery Gradle Plugin
:session: 226
:date: 2026-09-17
:etat: Session 225 ✅ — **Fix `ArticleGenerator` 22 langues** (`f6cc35b`) : le prompt de génération avait 2 branches pour 22 langues validées → **20 langues produisaient du français**. `languageInstruction(code)` via `LanguageCatalog` N0. Suite complète **1 845 tests / 0 échec**, `publishToMavenLocal` VERT.
:epic_actif: Aucun EPIC bakery actif sans accord pilote. Fix isolé issu de la revue globale S-217 (workspace racine, GBL-006 § 7).

== Session 225 — Fix `ArticleGenerator` (TDD strict)

*Bug* : `ArticleGenerator.buildPrompt(intention)` avait
`when(lang) { "en" -> English; else -> français }` alors que `SUPPORTED_LANGS`
= 22 (N0 `LanguageCatalog`).

*Fix* : `languageInstruction(code)` (nom + nom natif + code, source N0).
RED (3 tests : hi, bn, 22 langues) → GREEN → REFACTOR. Commit `f6cc35b`.
Rapport : `.agents/sessions/225-article-generator-22-langues.adoc`.

== Le vrai sujet : GBL-006 COPY MULTI-MARCHÉ (workspace racine)

Le fix rend la langue *correcte*. Il ne rend pas la copy *native*.
Demander un article « en hindi » ≠ écrire « pour le marché hindi ». Le
cadrage `CADRAGE_EPIC_GBL_006_COPY_MULTIMARKET.adoc` (15e doc jardin secret)
tranche :

----
ContentSeed (1/sujet) → MarketProfile[22] (one-time) → PlatformFormat[3] → CopyPiece
----

*Distinction* : **traduire ≠ écrire** (un hook français traduit sonne
étranger, le SEO est faux). *Générer nativement coûte ~le même prix que
traduire, mais convertit 2-3× mieux.* 8 US ~31 pts, boundary recommandée
`bakery.copy` (bakery écrit, capsule rend).

== Prochaine session (226) — pistes (accord pilote requis)

. *GBL-006 US-1→3* : `ContentSeed` + `PlatformFormat` + `CopyPiece` +
   `MarketProfile[22]` — débloque la portée réelle des 22 marchés. Zéro coût
   LLM pour US-1 (modèles purs) ; US-2 demande une revue pilote des profils.
2. *BKY-LANG-22-8/9* : dogfooding LLM 22 langues (coût LLM metered).
3. *BKY-LANG-22-10* : publication Central bakery (accord pilote).
4. *MASS-6* : ~675h LLM (bloqué par accord pilote).
5. *Dogfooding dry catalogue complet* (zéro coût).

== Règles rappelées

* *Règle 0* : `./gradlew -q publishToMavenLocal` OBLIGATOIRE après toute
  modification du code source (fait S-225).
* Commit/push autorisés (décision pilote 2026-09-17). Zéro secret.
* Baby-step DDD/TDD/BDD strict — 1 fichier à la fois, gate `test` complète.
* Gate métier : `grep -rwE "REAC|AFNOR|SPG|SPD|FPA|RNCP|CDA" src/` = 0.
