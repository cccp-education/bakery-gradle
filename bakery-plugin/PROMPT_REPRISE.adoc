= PROMPT REPRISE — Bakery — Session 162
:session: 162
:date: 2026-06-24

== Session 161 terminée — Migration @Option natif --input/--output

Migration CLI `generatePivotYaml` des propriétés Gradle `-Pinput`/`-Poutput`
vers des options natives `@Option` (`--input`/`--output`). Conforme à la
convention des autres tâches bakery (`MigrateToI18nTask`, `GenerateArticleTask`).

Baby steps DDD/TDD/BDD :
- **DDD** : `PivotOutputResolver` (concept pur, 9 UT) — résout chemin sortie
  défaut `{stem}.pivot.yaml` + priorité explicite > défaut.
- **TDD** : `GeneratePivotYamlTask` migré `RegularFileProperty` →
  `Property<String>` + `@Option`. 5/5 functional tests GREEN.
- **BDD** : Feature 38 étendue (4 scénarios). 4/4 cucumber PASS.
  Bug `DuplicateStepDefinitionException` résolu (renommage step pivot).

49 UT pivot + 5 functional + 4 BDD scénarios PASS.

== Mission en cours

Reprise backlog pistes #2 et #3 :

1. **Loi de l'Économie d'Encre sur generatePivotYaml** :
   - `GeneratePivotYamlTask` doit lire le YAML existant avant d'écrire
   - Si le YAML généré == YAML existant → skip l'écriture (ne pas écraser)
   - Test : invoquer 2× la tâche, vérifier que la 2e fois ne réécrit pas

2. **Étendre golden masters** : 4e golden master avec listes imbriquées + plantuml

3. **Bug préexistant cucumber** (optionnel) : features 9, 16 steps non chargés
   (`UndefinedStepException`, 108 failures, cucumber-picocontainer DI)

== Contexte parser pivot

* Package : `bakery.pivot` (7 classes main + 6 classes test)
* `PivotOutputResolver` (S161) — convention chemin sortie YAML
* Golden master : `src/test/resources/pivot-golden-master/` (3 .adoc + 3 .yaml)
* 49 UT + 5 functional + 4 BDD scénarios PASS
* `./gradlew test` = 1m 1s SUCCESS

== Notes techniques

* Ports Ollama autorisés : 11437-11465 uniquement. Modèles :
  `gpt-oss:120b-cloud`, `gemma4:31b-cloud`.
* Timeout cucumberTest 5min (EPIC 5) — ne pas lancer `check` complet sans
  permission.
* *Loi de l'Économie d'Encre* : toute tâche Gradle LLM DOIT load-before-write +
  calcul delta + fusion existing+new.