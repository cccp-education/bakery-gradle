= PROMPT_REPRISE — Bakery

== Mission accomplie

*Session 127 TERMINÉE* — BKY-I18N-MIG US-3 (5 pts).
DDD/TDD/BDD baby-step :
- `I18nMigrationService.kt` — implémentation complète (scanTemplates, extractHardcodedText,
  generateMessageFiles, replaceHardcodedWithMessageKeys, writeMessageFiles, injectSiteLanguage)
- `I18nMigrationResult.kt` — ajout champ `templatesModified`
- `MigrateToI18nTask.kt` — suppression dépendance LlmService (US-3 n'en a pas besoin)
- `I18nMigrationServiceTest.kt` — 34 UT (ScanTemplates 4, ExtractHardcodedText 11,
  GenerateMessageFiles 3, ReplaceHardcodedWithMessageKeys 7, WriteMessageFiles 1,
  InjectSiteLanguage 3, Migrate 5) PASS
- `MigrateToI18nTaskTest.kt` — 19 UT (suppression test llmService, nettoyage imports) PASS
- `32_i18n_migration.feature` — enrichi 8→12 scénarios Cucumber
- `I18nMigrationSteps.kt` — nettoyé (suppression steps non-testables via TestKit)
- publishToMavenLocal OK

== Statut

- EPICs : 30/30 ✅ (100%)
- Code smells résolus : 24/24 ✅ (V2-V4) + 52/52 ✅ (V5)
- BKY-CR-V5 : 24/24 pts ✅
- BKY-I18N-MIG : 10/16 pts (US-1 ✅ S125, US-2 ✅ S126, US-3 ✅ S127)
- Version publiée: 0.0.1 (Maven Central)

== Prochaine session (128)

*Priorité P2* : BKY-I18N-MIG — US-I18N-MIG-5 (3 pts).
Cucumber BDD + tests unitaires finaux :
- Feature `i18n_migration.feature` : scénarios dry run preview, migration EN+AR,
  fallback LLM indisponible, site sans templates, site déjà i18n (no-op).
- Tests unitaires : I18nMigrationServiceTest (scan, extract, replace, dryRun).
  *NOTE* : La couverture unitaire est déjà complète (34 UT). US-5 = enrichir
  les scénarios Cucumber existants avec des assertions post-exécution.

*Fichiers cibles* :
- `32_i18n_migration.feature` (ENRICHIR, 12→15+ scénarios)
- `I18nMigrationSteps.kt` (AJOUTER steps Then/And)

*Vérification* : `./gradlew cucumberTest` → 120+ PASS.

== Note — US-I18N-MIG-4 (⏸️ EN ATTENTE)

La traduction LLM des 9 langues non-fr est mise en attente. Décision architecturale
(2026-06-16) : la traduction est une capacité transverse qui sera implémentée dans
**codebase-gradle (N1)** comme `TranslationService` partagé (koog StateGraph +
langchain4j OllamaChatModel), ou dans un artefact N0 dédié (`translation-contracts`).
Bakery consommera ce service, ne l'implémentera pas.
