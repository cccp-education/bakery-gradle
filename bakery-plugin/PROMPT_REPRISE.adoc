= PROMPT_REPRISE — Bakery Gradle Plugin
:session: 238
:date: 2026-09-25
:etat: Session 237 ✅ — **BKY-LANG-NAV-8 débloqué** (null-safety cœur bakery soldé S-235 + `materializeTemplates` : bundles gelés cccp/magic-stick réutilisables **sans LLM**). Antérieur S-236 : NAV-7 (rollout talaria).
:epic_actif: **BKY-LANG-NAV** (🟡 NAV-0→7 ✅, NAV-8 🟡 enabler livré — rollout site restant).
:epic_suivant: **NAV-8 rollout site** (intégrer sélecteur dans `menu.thyme` cccp/magic + wiring `materializeTemplates`/`injectLangSwitch` + bake variantes + CI, cross-borough). Dépend S-237 ✅.
:dette: FLAKY-5 (drift fixture cheroliv.com) + ktlint baseline dépôt (rouges à HEAD, préexistants).

== État S-237

**NAV-8 enabler livré, zéro LLM.** Deux blocs :

. **Null-safety cœur** (dette S-235 soldée) : `bakery.langswitch.LangSwitchPath`
   `thymeleafHref()` + `thymeleafEachHref()` gardent `content.uri != null`
   (Thymeleaf 3.0.14 OGNL sans Elvis) → dégradation index de langue (D8). Un site
   scaffoldé ne casse plus son archive au bake. Byte-identique au correctif
   cheroliv S-235 (D3). Modèle ex-nihilo màj.
. **`materializeTemplates`** : `MessageBundleResolver` (pur) + tâche Gradle qui
   matérialise `i18n/{lang}/templates/` **déployable** depuis un bundle gelé.
   Preuve réelle sur copie `/tmp` : cccp 11 + magic 12 templates, **0 clé non
   résolue**, UTF-8 OK, **zéro appel LLM**.

Le gisement (challenge pilote) : golden masters `i18n-fixtures/{cccp-education,
magic-stick}/post-migration/templates/*` **byte-identiques** aux templates réels
(11/11, 12/12), leurs `#{key}` couverts 100 % par `translations_en.properties`
gelés, homepage EN incluse dans le chrome.

== Mission Session 238 — NAV-8 rollout site

*Intégrer le sélecteur same-page dans `office/sites/cccp.education` et
`office/sites/magic-stick`, puis câbler le pipeline de bake des variantes EN.*

Constats de périmètre (S-237) :
. Les deux sites (et leurs golden masters) **n'ont pas de `lang-switcher-container`**
   → à intégrer dans `menu.thyme` (marqueur requis par `injectLangSwitch`).
. `InjectLangSwitchTask.resolveMenuThyme` résout `site/{lang}/templates` alors que
   l'i18n vit sous `i18n/{lang}/templates` → delta de layout à traiter.
. cccp/magic = consommateurs `site.yml` purs (pas de build), via runner-gradle N3.

*Attention boundary* : `office/sites/*` hors `foundry/`. Travail sur copie/fixture
d'abord, jamais de modification destructive du site réel sans accord pilote.

== Reprendre à

. `git status` dans bakery-gradle ET office/sites/*.
. `.agents/sessions/237-…` (ce rapport) + `.agents/CADRAGE_EPIC_BKY_LANG_NAV.adoc` (§ NAV-8).
. `.agents/236-…` (NAV-7 talaria, patron client) + `.agents/235-…` (NAV-6 cheroliv, patron tree).
. Golden masters : `src/test/resources/i18n-fixtures/{cccp-education,magic-stick}/`.
. Gate Économie d'Encre : ne relancer `test`/cucumber que si le code source change.

== Rappel

* Gate métier : `grep -rwE "REAC|AFNOR|SPG|SPD|FPA|RNCP|CDA" src/` = 0.
* *Modèle LLM* : `gemma4:31b-cloud` + `OLLAMA_POOL_PORTS=11439,11440,11441,11442`.
* *Règle 0* : `./gradlew -q publishToMavenLocal` OBLIGATOIRE après toute modif source.
* *Pattern BDD runner dédié* : tout tag non couvert doit avoir son runner (S-082).
* Commit/push autorisés (décision pilote 2026-09-17). Zéro secret.
* Baby-step DDD/TDD/BDD strict — 1 fichier à la fois, gate `test` complète.
