= PROMPT_REPRISE — Bakery

== Mission accomplie

*Session 120 TERMINÉE* — BKY-CR-V5 US-4 (3 pts).
US-CR-V5-4a : toResolver() when 35 branches → propertyMap lazy + lookup.
US-CR-V5-4b : FileSystemManager.read() → Either<Exception, SiteConfiguration> + logger.warn.
US-CR-V5-4c : resolveWithoutPrompt() throw → return null, fromCli → ConfigPromptM<String?>.
US-CR-V5-4d : FirebaseContactFormConfig + 6 sous-classes → tous champs avec defaults.
10 fichiers modifiés, compilation OK, tests cibles PASS.

== Statut

- EPICs : 30/30 ✅ (100%)
- Code smells résolus : 24/24 ✅ (V2-V4)
- Code smells V5 : 50 ☐ (US-1 faux positif, US-2+3+4 résolues)
- BKY-CR-V5 : 10/24 pts (US-1 1pt + US-2 3pts + US-3 3pts + US-4 3pts)
- BKY-I18N-MIG : 0/16 pts ☐
- Version publiée: 0.0.1 (Maven Central)

== Prochaine session (121)

*Priorité P2* : BKY-CR-V5 — US-CR-V5-5 (5 pts).
Déduplication SUPPORTED_LANGS 3× → BakeryConstants.SUPPORTED_LANGS,
updateProperty() dupliqué 2× → supprimer copie GenerateThemeTask, importer bakery.injection.updateProperty,
pattern résolution CLI>DSL>defaults dupliqué 3× → extraire ResolveIntention<T> helper.

*Fichiers cibles* :
- `ScaffoldIntention.kt` + `ArticleIntention.kt` — SUPPORTED_LANGS → BakeryConstants
- `GenerateThemeTask.kt` — supprimer updateProperty() dupliqué
- `ContentTaskRegistrar.kt` — pattern résolution → ResolveIntention<T>

*Vérification* : `./gradlew test` → ScaffoldIntentionTest, ArticleIntentionTest,
GenerateThemeTaskTest, ContentTaskRegistrar tests PASS.
