= Prompt de Reprise — Session 19

_Règles absolues_ : voir `AGENT.adoc` section "Règles Absolues"

____
**Mission** : EPIC 10 Scaffolding — Finir phase green (3 scénarios Cucumber en échec) + Couverture Kover >= 75%.
____

'''

== Sessions Récentes

**Session 018** (2026-05-07) : EPIC 11 Optimisation durée tests. Suppression JVM args anti-perf (`TieredStopAtLevel=1`, `SerialGC`, `MaxMetaspaceSize=256m`), `forkEvery=0` sur cucumberTest, refactor ArchitectureValidationTest avec pattern `@Nested` + projet partagé. Durée totale -52% (8m → 3m50s).

**Session 017** (2026-05-07) : EPIC 10 Scaffolding TDD complet. `sitesBaseDir`/`siteName`, `SiteScaffolder.kt`, 10 tests unitaires + 5 fonctionnels + 5 Cucumber (3 en échec). Kover 0.9.1.

'''

== Session 018 — Réalisations

* [x] `build.gradle.kts` : suppression `-XX:TieredStopAtLevel=1`, `-XX:+UseSerialGC`, `-XX:MaxMetaspaceSize=256m`
* [x] `cucumberTest` : `forkEvery=1` → `forkEvery=0`
* [x] `ArchitectureValidationTest.kt` : refactor pattern `@Nested` + `@JvmStatic @TempDir` + projet partagé
  - 7 cold starts Gradle → 1 daemon réutilisé
  - 2 classes `@Nested` : `BakeTasks` + `TaskInspection`
* [x] Durée tests : 8m → 3m50s (-52%)

**Fichiers modifiés** :
* `build.gradle.kts` — JVM args + forkEvery
* `src/functionalTest/kotlin/bakery/ArchitectureValidationTest.kt` — refactor

'''

== Session 19 — Priorités

=== Priorité 0 : EPIC 10 — Finir phase green Cucumber
* 3 scénarios encore en échec dans `6_site_scaffolding.feature`
* Cause probable : `ScaffoldSteps` conflit avec `InitSiteSteps` existant
* Investiguer le conflit de state entre les steps Cucumber

=== Priorité 1 : Couverture Kover >= 75%
* Couverture actuelle : 57%
* Une fois les 3 scénarios Cucumber verts, recalculer

'''

== Règles

* Règle 0 : `publishToMavenLocal` après chaque modif code plugin
* Jamais de commit sans permission
* Racine de travail = `bakery-plugin/`
* Archive + PROMPT_REPRISE mis à jour en fin de session
* Ne pas lancer `check` complet — lancer tests individuels
* Utiliser `--tests` filter pour cibler les tests pertinents
