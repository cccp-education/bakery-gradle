= PROMPT REPRISE — Bakery
:session: 151
:date: 2026-06-23

== Mission en cours (session 151)

*BKY-A11Y-1 — Accessibilité WCAG/RGAA* démarré.
- DDD : package `bakery.a11y` créé avec `ContrastResult`, `evaluateContrast()`, `AccessibilityFinding`, `AccessibilityReport`, `AccessibilityAuditor`, `AccessibilityHtmlScanner`.
- TDD : 13 UT GREEN — `ColorContrastRuleTest` (5), `AccessibilityAuditorTest` (4), `AccessibilityHtmlScannerTest` (4).
- Pas encore de tâche Gradle ni de BDD Cucumber.

== Statut

* BKY-I18N-PROD-AUDIT : ✅ TERMINÉ — Session 150.
* BKY-A11Y-1 : 🔄 EN COURS — baby step 3/?, 13/13 UT GREEN.

== Prochaine session (suite 151)

1. Baby step 4 : tâche Gradle `accessibilityAudit` dans `ContentTaskRegistrar` / `SiteTaskRegistrar`.
2. Baby step 5 : BDD Cucumber Feature 37 `accessibility_audit.feature`.
3. Baby step 6 : scan des templates Thymeleaf (pas seulement inline colors) — règles alt/aria/headings.

== Notes techniques

* `./gradlew test --tests "bakery.a11y.*"` = validation rapide a11y.
* `./gradlew test` = validation complète.
* `publishToMavenLocal` obligatoire après toute modif code source.
