= PROMPT_REPRISE — Session 060 : BKY-LENS-4 Validation Cucumber + Fix 6 échecs

== Mission (Session 060)

1. **Valider Feature 17** — Lancer `./gradlew cucumberTest` et vérifier les 3 scénarios LENS injection jbake
   - Si UndefinedStepException : vérifier que les steps refactorisés matchent la Feature Gherkin
2. **Valider Feature 18** — Vérifier que le Background `Given a new Bakery project` résout les `IllegalStateException`
3. **Valider Feature 19** — Déjà 3/3 PASS, confirmer stabilité
4. **Fix** les échecs restants (objectif : 71/71 PASS ou < 3 échecs)

== État actuel (Session 059)

- LENS-1 ✅ SubgraphExtractor (7 src, 30 UT)
- LENS-2 ✅ AugmentedArticlesService + LensRules + AugmentedContextResolver (52+ UT)
- LENS-3 ✅ LensBudget + injection JBake + nettoyage BKG (82+ UT)
- LENS-4 : Features 17-19 réécrites, 6 échecs Cucumber à valider
- Build compile ✅, publishToMavenLocal ✅
- **Fix majeur** : DuplicateStepDefinition supprimé (53→6 échecs Cucumber)

== Architecture LENS — Rappel

```
graph.json (203k nœuds) ← ✅ LENS-1
    ↓ SubgraphExtractor → site-subgraph (~200 nœuds)
    ↓ + RAG pgvector + règles métier ← ✅ LENS-2
site-graph (~200 nœuds, ~500 edges)
    ↓ LensBudget (4-6 articles/page, minSimilarity=0.7) ← ✅ LENS-3
jbake.properties (augmentedContextData)
    ↓
post.thyme (AUGMENTÉ) (LENS-6)
```

== Points d'attention

- Les 6 échecs sont probablement des problèmes de step matching (Feature 17) et d'initialisation (Feature 18)
- Le step `the file {string} should contain {string}` est maintenant UNIQUEMENT dans ConfigResolverSteps
- Ne pas lancer `./gradlew test` sans permission explicite
- RAG scores viendront de pgvector en LENS-5 — pour l'instant RAG=emptyMap