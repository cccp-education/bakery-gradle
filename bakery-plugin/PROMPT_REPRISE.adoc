= PROMPT REPRISE — Bakery — Session 177
:session: 177
:date: 2026-07-15
:status: i18n EN déployé — edge cases + 8 langues restantes

== Session 176 terminée — i18n parallèle + JbakeNativeRenderer + déploiement

S176 a livré : prompt LLM corrigé, JbakeNativeRenderer, parallélisation ×4,
sélecteur de langue dans menu.thyme, 106/109 articles EN traduits et déployés
sur cheroliv.com/en/.

== Prochaine mission — Edge cases + 8 langues

1. *3 fichiers edge case* : `:jbake-title:` sans `= Title` — le parser ne
   détecte pas le header. Corriger `parseJbakeHeader` pour gérer les fichiers
   sans `= Title` en première ligne.
2. *Traduire 8 autres langues* : AR, BN, ES, HI, PT, RU, UR, ZH. Configurer
   `targetLanguages` dans build.gradle.kts, relancer `migrateContentI18n`.
3. *`<title>` global EN* : Le titre du site est encore en FR dans les templates
   EN. Vérifier `post.thyme`/`page.thyme` et utiliser `#{site.title}`.
4. *Cucumber feature 50* : Lancer `cucumberTest` pour valider les 3 scénarios
   BDD du JbakeNativeRenderer (non exécutés en S176 — timeout).

== Contexte technique
- Version : 0.0.3 Maven Central (LIVE)
- Traduction : 106/109 EN, 0 autres langues
- Déploiement : cheroliv.github.io (FR + /en/)
- LLM : gemma4:31b-cloud port 11441, parallelism=4