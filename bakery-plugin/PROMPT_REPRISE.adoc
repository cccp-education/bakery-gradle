= PROMPT_REPRISE — Bakery Gradle Plugin
:session: 199
:date: 2026-08-15
:etat: Session 198 ✅ — BKY-DNS-3 TERMINÉ (domaine `bakery.dns` enrichi : DnsDiff/DnsChange/DnsProvider port + OvhDnsProvider + NoOpDnsProvider + DnsProviderFactory/DnsProvisioner, 31 tests nouveaux, 83 dans `bakery.dns`, 0 FAIL, publishToMavenLocal VERT).
:epic_actif: BKY-DNS — BKY-DNS-0 ✅, BKY-DNS-1 ✅ S196, BKY-DNS-2 ✅ S197, BKY-DNS-3 ✅ S198. BKY-DNS-4 next (ProvisionDnsTask + wiring). BKY-I18N-MASS en pause (MASS-5→6 restants).

== Contexte de fin

Session 198 — BKY-DNS-3 :
- Domaine `bakery.dns` enrichi (9 fichiers prod) : `DnsChange` (sealed
  Create/Update(id, record, previous)/Delete), `ExistingDnsRecord`,
  `DnsDiff` (object pur, slot (type, name) + jeu de valeurs : manquant →
  Create, présent + TTL différent → Update, actuel sans désiré → Delete,
  drift de valeur → Create + Delete), `DnsReconciliationResult` (plan/applied/
  skipped/noop), `DnsProvider` (port listRecords/create/update/delete/
  refreshZone/isAvailable/name), `OvhDnsProvider` (adapter → OvhDnsClient),
  `NoOpDnsProvider` (dégradé zone vide mutations no-op),
  `DnsProviderFactory.resolve` (ovh avec client → Ovh, sinon NoOp),
  `DnsProvisioner.reconcile(desired, dryRun=true, allowDelete=false)` (noop si
  plan vide, refresh seulement si applied non vide)
- 31 tests nouveaux (10 diff + 9 provisioner + 4 noop + 4 factory + 4 ovh
  provider) — 83 dans `bakery.dns`, 0 FAIL
- REFACTOR : 4 fixtures tests corrigées (sémantique assumée drift de valeur =
  Create + Delete, pas Update)
- Non-régression : 3 échecs PRÉEXISTANTS environnementaux identiques
  S196/197 (RealSiteI18nMigrationIntegrationTest / FixtureAlignmentAuditorTest /
  I18nDeltaWireCucumberTest)
- `publishToMavenLocal` VERT

== Mission Session 199

1. **BKY-DNS-4** — `ProvisionDnsTask` + wiring :
   - Tâche `provisionDns` groupe deployment, chaînée `mustRunAfter`/`dependsOn`
     avant `deploySite` (si section dns présente)
   - Enregistrée dans `BakeryPlugin`/registrar dédié
   - Lit DnsConfig (site.yml) → DnsProvisioner.reconcile → report console
   - functional (task), ~4 pts

== Reprendre à

. `git status` dans bakery-gradle
. Lire `BACKLOG.adoc` section EPIC BKY-DNS (US BKY-DNS-4)
. `publishToMavenLocal` après chaque modification code source
. Fallback opérationnel : `~/workspace/configuration/scripts/ovh.py` (jusqu'au dogfooding DNS-6)

== Ordre des EPICs (priorité)

. **BKY-DNS** (priorité 2, ~33 pts) — BKY-DNS-0 ✅, BKY-DNS-1 ✅ S196, BKY-DNS-2 ✅ S197, BKY-DNS-3 ✅ S198, BKY-DNS-4→6 restants
. BKY-I18N-MASS (priorité 1, ~32 pts) — MASS-1→4 ✅, MASS-5→6 restants (en pause)
. BKY-SEO (priorité 2, 27 pts) — généralisation features SEO
. BKY-CONTACT-SEC (priorité 3, 26 pts) — durcissement formulaire contact

== Fichiers touchés S198

*bakery-gradle/bakery-plugin* :
- `src/main/kotlin/bakery/dns/` — 9 fichiers prod BKY-DNS-3 (DnsChange, ExistingDnsRecord, DnsDiff, DnsReconciliationResult, DnsProvider, OvhDnsProvider, NoOpDnsProvider, DnsProviderFactory, DnsProvisioner)
- `src/test/kotlin/bakery/dns/` — 5 fichiers test BKY-DNS-3 (DnsDiffTest 10, DnsProvisionerTest 9, NoOpDnsProviderTest 4, DnsProviderFactoryTest 4, OvhDnsProviderTest 4)
- `BACKLOG.adoc` — BKY-DNS-3 ✅ S198 (version 24.7)
- `.agents/INDEX.adoc` — entrée session 198
- `.agents/SESSIONS_HISTORY.adoc` — entrée session 198
- `.agents/TEST_COVERAGE_ANALYSIS.adoc` — mise à jour session 198
- `.agents/sessions/198-bky-dns-3-dnsprovisioner.adoc` — rapport de session
