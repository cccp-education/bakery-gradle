= PROMPT_REPRISE — Session 055 : BKY-LENS-1 SubgraphExtractor (Ségrégation)

== Mission (Session 055)

1. **BKY-LENS-1.1** — Importer `codebase-contracts` (N0) dans bakery : `CompositeContext`, `ChannelBudget`, `ContextChannel`
2. **BKY-LENS-1.2** — Créer `LensConfig` data class + DSL `bakery { augmentedContext { lens { ... } } }`
3. **BKY-LENS-1.3** — Créer `SubgraphExtractor` : lit `graph.json`, filtre par communautés/types/edges/depth
4. **BKY-LENS-1.4** — Tests : `SubgraphExtractorTest` avec fixtures `graph.json` mock (3 communautés, 50 nœuds)

== État actuel (Session 054 terminée — Audit)

- EPIC BKY-BKG **supersédé** par BKY-LENS (Pattern LENTILLE)
- 3 problèmes identifiés : KG maison inférieur, dépendance graphify morte, pipeline N3 non branché
- Architecture LENTILLE cadrée : ségrégation → enrichissement → budget
- EPIC BKY-LENS : 21 pts, 6 sous-EPICs (LENS-1 à LENS-6)
- Rapport détaillé : `.agents/sessions/054-audit-lentille-contexte-augmente.adoc`
- BACKLOG mis à jour avec BKY-LENS

== Architecture BKY-LENS — Rappel

```
graph.json (203k nœuds)
    ↓ SubgraphExtractor (filtre communautés + types + edges + depth)
site-subgraph (~200 nœuds, ~300 edges)
    ↓ + RAG pgvector (top-K=20, sim>0.7) + règles métier
site-graph (~200 nœuds, ~500 edges)
    ↓ LensBudget (4-6 articles/page, minSimilarity=0.7)
jbake.properties (relatedArticlesData)
    ↓
post.thyme (AUGMENTÉ)
```

== Contrat DAG

- bakery (N2) importe codebase-contracts (N0) ✅
- bakery (N2) importe graphify-plugin (N0) ✅ (déjà en implementation)
- codebase (N1) importe graphify-plugin (N0) ✅ (canal GRAPHIFY existant)

== Attention

- Ne pas lancer `./gradlew test` sans permission explicite
- `graphify-plugin` est déjà en `implementation` mais AUCUN code n'importe ses classes
- Commencer par LENS-1.1 (ajout codebase-contracts) AVANT LENS-1.3 (SubgraphExtractor)
- Le scoring hybride (LENS-2) viendra après — ne pas anticiper
- BKY-LENS supersède BKY-BKG — ne pas toucher au code RelatedArticles* existant avant LENS-3.3