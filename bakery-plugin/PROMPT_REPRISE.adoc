= PROMPT REPRISE — Bakery
:session: 152
:date: 2026-06-23

== Mission en cours (session 152)

*BKY-A11Y-1 — Accessibilité WCAG/RGAA* baby step 5 en cours.
- Domaine pur + tâche Gradle `accessibilityAudit` créés (session 151 + 151b).
- 8 UT GREEN sur `AccessibilityAuditTaskTest`.
- Feature 37 Cucumber : 1/4 PASS (registration), 3/4 FAILED.

== Problème à résoudre

Les 3 scénarios Feature 37 échouent car `AccessibilityTaskRegistrar` configure `auditDir` via `layout.buildDirectory.dir(...)` qui n'est pas résolu correctement dans le contexte TestKit. Le `dependsOn(bake)` empêche l'exécution isolée.

== Prochaine session

1. Corriger `AccessibilityTaskRegistrar` : permettre à `auditDir` d'être surchargé via CLI/DSL, et retirer/conditionner `dependsOn(bake)`.
2. Corriger les 3 scénarios Feature 37 restants (compliant, non-compliant, empty dir).
3. Baby step 6 : règles structurelles alt/aria/headings dans `AccessibilityHtmlScanner`.
4. `publishToMavenLocal` obligatoire après modif code source.

== Notes techniques

* Ports Ollama autorisés : 11437-11465 uniquement. Modèles : gpt-oss:120b-cloud, gemma4:31b-cloud.
* `./gradlew test --tests "bakery.a11y.*"` = validation rapide a11y.
* `./gradlew cucumberTest -Dcucumber.features="src/test/features/37_accessibility_audit.feature" -Dcucumber.filter.tags="@a11y"`.