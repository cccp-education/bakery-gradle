= PROMPT_REPRISE — Session 049 : CONV-1.4 migration SiteManager

== Mission (Session 049)

1. **CONV-1.4** : Brancher ConfigResolver dans SiteManager.
   - Remplacer les 9 méthodes `inject*IntoJbakeProperties` par une seule méthode
     générique utilisant la cascade 4 couches
   - Créer les méthodes `resolve*Config` manquantes : resolveFirebaseConfig,
     resolveFirebaseAuthConfig, resolveCommentsConfig, resolveNewsletterConfig,
     resolveThemeConfig, resolveLayoutConfig
   - `copySiteResources` doit passer `project.properties` et `BakeryExtension`
     aux méthodes d'injection

2. **CONV-1.5** : 3 scénarios Cucumber sur l'ordre de résolution.

3. Optionnel : commencer CONV-1.6 (documentation de l'ordre de surcharge dans AGENT.adoc)

== État actuel (Session 048 terminée)

- Code review complète → `.agents/CODE_REVIEW.adoc`
- Clarification périmètre : BKY-TRN-9 → training-gradle TRN-9 🔒, BKY-IA-5 → runner-gradle RUN-IA-5 🔒
- SiteManager.kt pointé comme god class (819 lignes)
- ConfigResolver (50 UT) non branché dans SiteManager
- `saveConfiguration()` toujours vide

== Architecture ConfigResolver — Rappel

```
CLI (-Pbakery.xxx) > gradle.properties > DSL (bakery { xxx { ... } }) > site.yml > defaults
```

== Attention

- ConfigResolver N'est PAS encore branché dans SiteManager — c'est CONV-1.4
- Les 9 méthodes inject* sont du copier-coller — une méthode générique les remplacera
