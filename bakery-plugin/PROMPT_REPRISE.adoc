= PROMPT_REPRISE — Session 047 : Backlog priorisé après BKG

== Mission (Session 047)

1. Dépiler le backlog priorisé : BKY-CONV-1 (surcharge configuration multi-couche, P0) ou BKY-IA-4 (génération article IA, P0).
2. Optionnel : BKG-2 ontologie émergente (P2, 8 pts) si les P0 sont bloqués.

== État actuel (Session 046 terminée)

BKG-1.4 TERMINÉ :
- `related-articles.thyme` consomme le graphe via `data-related-articles` JSON + JavaScript
- `SiteManager` injecte `relatedArticlesData` (JSON du graphe) dans jbake.properties
- 2 UT template PASS + 2 UT injection PASS + 2 scénarios Cucumber PASS
- Build complet : 397 UT + 53 FT + 66 Cucumber PASS ✅

== Backlog priorisé

| ÉPIC | Priorité | Description |
|------|----------|-------------|
| BKY-CONV-1 | P0 | Surcharge configuration multi-couche (CLI > gradle.properties > DSL > site.yml > defaults) |
| BKY-IA-4 | P0 | Génération d'article assistée IA (JB-8 ✅, reste IA-4) |
| BKG-2 | P2 | Ontologie émergente — clustering cosine similarity (8 pts, optionnel) |
| BKY-TRN-9 | P0 | Intégration formation : assembleTrainingSite |

== Attention

- Les tests Cucumber de `9_generate_article.feature` sont en état d'échec (3 bugs EPIC 17 non résolus). Ne pas les modifier sans EPIC 17.
- BKG EPIC complet : BKG-0 (4 étapes) + BKG-1 (4 étapes) ✅. BKG-2 reste optionnel.