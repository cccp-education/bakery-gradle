= PROMPT_REPRISE — Bakery Gradle Plugin
:session: 230
:date: 2026-09-24
:etat: Session 229 ✅ — **BKY-LANG-NAV-1 TERMINÉ** (règle pure `LangSwitchPath` + 14 vecteurs partagés JSON). Antérieur S-228 : EPIC BKY-LANG-NAV cadré.
:epic_actif: **BKY-LANG-NAV** (🟡 NAV-0 ✅, NAV-1 ✅, NAV-2→8 À FAIRE) — P1, cœur NAV-2→5 (~16 pts restants) puis rollout sites NAV-6→8 (~13 pts).

== État S-229

`LangSwitchPath.resolveSamePage(currentPageUri, currentLang, targetLang, defaultLang,
pageExists)` — objet pur, unique source de vérité. Retourne une URL relative
**depuis le répertoire de la page courante** (`../../ar/blog/foo.html`). Self-link
→ nom de fichier seul. Dégradation vers `<lang>/index.html` si `pageExists=false`.

Vecteurs partagés : `src/main/resources/bakery/langswitch/lang-switch-path-vectors.json`
(14 vecteurs, `version: 1`) — **le contrat que l'hôte JS rejouera** (parade D3).

Tests : `LangSwitchPathTest` (17 UT) + `LangSwitchPathVectorsTest` (fixture-driven).
RED prouvé → GREEN 19/19. Gate langswitch + functional injection VERTS.
`publishToMavenLocal` OK.

== Mission Session 230 — BKY-LANG-NAV-2 (+ NAV-4 si le temps)

*NAV-2 — Hôte Thymeleaf : lien par défaut exact.* Consommer `LangSwitchPath`
dans `LangSwitchUrl`/`LangSwitchMenu`/`LangSwitchThymeleafRenderer` : émettre un
`th:href` **page-aware** au lieu d'`index.html` en dur. Backward compat : sans
config i18n, no-op. `data-lang` conservé (D4). Le `th:href` reste la cible par
défaut exacte et crawlable ; le JS (NAV-3) ne le réécrit pas.

*NAV-4 — Modèle ex-nihilo.* `site-basic/templates/menu.thyme` (sélecteur
`data-lang` page-aware) + `maquette/js/lang-switch.js` embarqué → un site
scaffoldé naît conforme (garde D6/D7).

Tests : UT render page-aware racine/imbriquée, no self-loop, langue défaut ;
FT injection fixture FR+EN multi-pages + idempotence.

== Reprendre à

. `git status` dans bakery-gradle — vérifier qu'aucune session concurrente n'écrit.
. `.agents/CADRAGE_EPIC_BKY_LANG_NAV.adoc` (cadrage complet, D1→D10).
. `src/main/kotlin/bakery/langswitch/LangSwitchPath.kt` + vecteurs JSON (socle).
. `.agents/sessions/229-bky-lang-nav-1-spec-pure.adoc` (rapport S-229).
. Gate règle Économie d'Encre : ne relancer `test` que si le code source change.

== Dette découverte (FLAKY-5)

`FixtureAlignmentAuditorTest` + `RealSiteI18nMigrationIntegrationTest` (cheroliv-com)
= **2 FAIL préexistants** : drift fixture ↔ site réel (chrome nativement FR,
S-053/055, 5 templates). Stash-prouvé non introduit par NAV-1. Session dédiée :
resync fixture + régénération golden master (procédure `PostMigrationSnapshotGenerator`).

== Rappel

* Gate métier : `grep -rwE "REAC|AFNOR|SPG|SPD|FPA|RNCP|CDA" src/` = 0.
* *Modèle LLM* : `gpt-oss:120b-cloud` épuisé → `gemma4:31b-cloud` +
   `OLLAMA_POOL_PORTS=11439,11440,11441,11442`.
* *Publication Central* : ordre DAG réel (publier le consommateur absent AVANT
   de l'ajouter au catalogue MEMPHIS — garde D8 anti-ghost).
* *Règle 0* : `./gradlew -q publishToMavenLocal` OBLIGATOIRE après toute
   modification du code source.
* Commit/push autorisés (décision pilote 2026-09-17). Zéro secret.
* Baby-step DDD/TDD/BDD strict — 1 fichier à la fois, gate `test` complète.
