= PROMPT_REPRISE — Bakery Gradle Plugin
:session: 218
:date: 2026-09-15
:etat: Session 217 ✅ — **EPIC BKY-LANG-22 (parité talaria.school) 7/10 US TERMINÉ** : contrat N0 `LanguageCatalog` 10 → 22 langues + `i18n-contracts:0.0.4` Central (bundle `c1a9de57`) + BOM/catalog `0.0.43` Central (bundle `eb310d04`) + propagation bakery/capsule/slider/document. Rapports `.agents/sessions/217-BKY-LANG-22-languagecatalog-22.adoc`.
:epic_actif: BKY-LANG-22 🟡 EN COURS — reste LANG-22-8 (dogfooding slider corpus 22 langues), LANG-22-9 (dogfooding capsule vidéo 22 langues), LANG-22-10 (publication Central consommateurs).

== Contexte de fin

Session 217 — **Extension LanguageCatalog 10 → 22 (parité talaria.school)** :
- Demande pilote : aligner bakery/N0 sur les 22 langues servies par talaria.school
  (`i18n.js` + `i18n-extra-langs.js` + `i18n-content.js`).
- 12 langues ajoutées : `it nl de el tr vi th id ko ja sr` + `fa` (**RTL**).
  RTL passe de 2 (`ar ur`) à 3 (`ar ur fa`).
- Option A (accord pilote) : étendre le **contrat N0 partagé**
  `contracts.i18n.LanguageCatalog` — pas de liste locale (anti split-brain).

Livré (baby-step DDD/TDD/BDD strict) :
- **N0** `i18n-contracts:0.0.4` (bundle Central `c1a9de57`) — 52 tests 0 FAIL.
- **MEMPHIS** `workspace-bom`/`workspace-catalog:0.0.43` (bundle `eb310d04`).
- **bakery** : `SUPPORTED_LANGS` dérivé de `LanguageCatalog.supportedCodes()`
  (split-brain tué) + tests alignés. `test` 1686/2 FAIL préexistants.
- **capsule** : `VoiceMapping` +12 voix Piper (validées rhasspy) + espeak ;
  BDD `cucumberTestMultiLang` **22/22 languages rendered**.
- **slider** : `RevealUiMessageCatalog` +12 libellés natifs ; `test` 947/3 FAIL
  Playwright préexistants.
- **document** : dep alignée 0.0.4 + BOM 0.0.43 ; compile VERT.

== Mission Session 218 — à la demande

1. **LANG-22-8** : dogfooding slider corpus 22 langues — régénérer les 3 decks
   démo (fr/en/ar) vers les 12 nouvelles langues (27 → 63 fichiers). **LLM metered**
   (`gemma4:31b-cloud` ports 11437-11465) — accord pilote déjà donné (Option A).
   `TranslatedDemoDeckTest` pinné sur le corpus S-030 en attendant ; lever le pin
   après régénération + étendre feature 14.
2. **LANG-22-9** : dogfooding capsule vidéo 22 langues (PipeTTS/espeak réels).
3. **LANG-22-10** : publication Central consommateurs (bakery/capsule/slider
   bumpés sur N0 0.0.4 + BOM 0.0.43).
4. *(hors EPIC)* MASS-6 (accord pilote) OU re-traduction cheroliv.com/en/ prod.

## Reprendre à

. `git status` dans bakery-gradle
. Lire `BACKLOG.adoc` (EPIC BKY-LANG-22 en tête)
. Vérifier Ollama ports 11437-11465 + modèle `gemma4:31b-cloud`
. `/tmp/bakery-dogfood` projet consommateur
. `publishToMavenLocal` après chaque modification code source

## Rappel

- Gate permanente : `grep -rwn "REAC|AFNOR|SPG|SPD|FPA|RNCP|CDA" src/`
  doit rester = 0
- Golden masters = artefacts gelés — régénérer UNIQUEMENT si les fixtures changent
- **Règle gouvernance** : `office/sites/talaria.school/site.yml` est gitignoré
  (credentials réels). Ne pas le lire/modifier — repo `office` privé, aucun incident.
- Note environnement : suites cucumber complètes timeout sous charge (load 14+).

(End of file — 62 lines)
