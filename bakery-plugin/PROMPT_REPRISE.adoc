= PROMPT REPRISE — Bakery
:session: 158
:date: 2026-06-24

== Mission en cours (session 158)

*Session 157 TERMINÉE — Parser AsciiDoc→YAML Pivot (DDD/TDD/BDD) + READMEs badges.*
Parser pivot complet : 3 classes de domaine (PivotModel, PivotYamlRenderer,
AsciiDocParser) + 30 UT PASS. Golden master test créé mais bloqué par un
problème de découverte Gradle. READMEs mis à jour avec badges build/coverage
et tableau i18n 10 langues du CSV (~5.4 milliards de locuteurs).

== Problème à résoudre

Débugger le problème de découverte Gradle de `PivotGoldenMasterTest` (3 tests
golden master byte-identique) :
- Le test compile (bytecode présent dans `build/classes/kotlin/test/`)
- Le test n'est PAS exécuté par `--tests "bakery.pivot.PivotGoldenMasterTest"`
- Le filtre `filter { excludeTestsMatching("bakery.scenarios.*") }` du build.gradle.kts
  n'exclut pas `bakery.pivot`, mais le test n'est pas découvert
- `PivotYamlRendererTest` et `AsciiDocParserTest` s'exécutent correctement avec `--tests`
- Hypothèses : conflit avec `useJUnitPlatform { excludeEngines("cucumber") }`,
  ou problème de pattern `--tests` avec noms de méthodes backtick

Puis si le golden master passe :
- Finaliser le parser sur les edge cases restants (tables multi-lignes, listes imbriquées)
- Câbler le parser dans une tâche Gradle `generatePivotYaml` (N2 → YAML pivot)

== Contexte parser pivot

* Package : `bakery.pivot` (3 classes main + 4 classes test + 6 ressources golden master)
* Ressources golden master : `src/test/resources/pivot-golden-master/`
  (3 .adoc + 3 .yaml attendus de magic-stick)
* Bac à sable YAML pivots : `/tmp/opencode/bakery-i18n-lab/magic-stick/content/`
* Contrat pivot figé : 8 types blocs, 4 types inline, `translatable: true/false`,
  cellules table = `inline[]`

== Notes techniques

* Ports Ollama autorisés : 11437-11465 uniquement. Modèles : `gpt-oss:120b-cloud`, `gemma4:31b-cloud`.
* `./gradlew test --tests "bakery.pivot.AsciiDocParserTest" --tests "bakery.pivot.PivotModelTest" --tests "bakery.pivot.PivotYamlRendererTest"` = 30/30 PASS.
* `./gradlew cucumberTest -Dcucumber.features="src/test/features/37_accessibility_audit.feature"` = 7/7 PASS.
* Timeout cucumberTest 5min (EPIC 5) — ne pas lancer `check` complet sans permission.
* `publishToMavenLocal` fait (jar 0.0.2).
* *Loi de l'Économie d'Encre* : encodée dans 10 boroughs. Toute tâche Gradle LLM DOIT
  load-before-write + calcul delta + fusion existing+new.
* Bac à sable `/tmp/opencode/bakery-i18n-lab/` = volatile (recréer depuis
  `office/sites/magic-stick/jbake/` + les YAML dans le rapport session 155).