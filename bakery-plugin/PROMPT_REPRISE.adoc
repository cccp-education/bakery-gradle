= PROMPT REPRISE — Bakery — Session 165
:session: 165
:date: 2026-06-24

== Session 164 terminée — Fix OGNL + Optimisation E2E

=== ✅ Bug OGNL #sets.contains dans 10 templates
- `#sets.contains({'ar','ur'}, ...)` -> `#lists.contains` (ArrayList vs Set en OGNL)
- 10 templates Thymeleaf corrigés (site/ + site-basic/)
- 19/19 E2E PASS (avant : 2 FAILED i18n)

=== ✅ Cache installPlaywright
- `onlyIf` sur `~/.cache/ms-playwright/chromium-*` — SKIPPED si présent
- Pas de marker dans build/, vérification directe du cache système

=== ✅ Singleton PlaywrightPool
- 1 Playwright.create() + 1 chromium().launch() pour tout (avant 5×)
- Shutdown hook JVM pour cleanup
- forkEvery=1 supprimé (1 JVM partagée)
- ThymeleafRenderingTestFactory mutualisé en singleton companion

== Prochaine mission

Bakery production-ready + E2E optimisés. Pistes :
1. Vérifier `./gradlew koverThresholdCheck` passe maintenant (E2E vert)
2. Nouvelle priorité roadmap ou cross-borough (delta Loi Économie d'Encre 9 boroughs)

== Contexte technique
- Ports Ollama : 11437-11465. Modèles : `gpt-oss:120b-cloud`, `gemma4:31b-cloud`
- E2E : 19/19 PASS, installPlaywright SKIPPED sur second run
- Cache Playwright persistant : `~/.cache/ms-playwright/`