= PROMPT REPRISE — Bakery
:session: 159
:date: 2026-06-24

== Mission en cours (session 159)

*Session 158 TERMINÉE — Golden Masters Pivot GREEN + TextTranslatableClassifier.*
3/3 golden masters PASS (quick-start, ab-partition, scripts). Parser supporte
listes numérotées (1. 2. 3.). Renderer corrige indentation listes/tables +
yamlScalar pour language. TextTranslatableClassifier (10 UT) heuristique
translatable true/false pour segments Text. 32 UT pivot PASS.

== Problème à résoudre

Finaliser le parser pivot sur les edge cases restants, puis câbler la tâche
Gradle `generatePivotYaml` :

1. **Edge cases parser** :
   - Tables multi-lignes (cellules qui span plusieurs lignes)
   - Listes imbriquées (sous-items avec indentation)
   - plantuml pass-through (blocs plantuml non parsés)
   - Verifier que les 3 golden masters restent GREEN après chaque fix

2. **Tâche Gradle generatePivotYaml** :
   - Créer `GeneratePivotYamlTask` (DefaultTask) dans `bakery.pivot`
   - DSL `bakery { pivotYaml { sourceDir = ...; outputDir = ... } }`
   - @TaskAction : parse AsciiDoc → render YAML pivot → write file
   - Loi de l'Économie d'Encre : load-before-write, ne pas écraser si inchangé

3. **BDD Cucumber** :
   - Feature 38 `38_generate_pivot_yaml.feature`
   - Scénarios : parse simple adoc, parse avec tables, idempotence économique

== Contexte parser pivot

* Package : `bakery.pivot` (4 classes main + 4 classes test)
  - `PivotModel.kt` — sealed classes (PivotBlock, PivotInline, PivotArticle)
  - `PivotYamlRenderer.kt` — rendu YAML déterministe (136 lignes)
  - `AsciiDocParser.kt` — parser AsciiDoc→PivotArticle (287 lignes)
  - `TextTranslatableClassifier.kt` — heuristique translatable (59 lignes)
* Ressources golden master : `src/test/resources/pivot-golden-master/`
  (3 .adoc + 3 .yaml)
* Contrat pivot figé : 8 types blocs, 4 types inline, `translatable: true/false`,
  cellules table = `inline[]`

== Notes techniques

* Ports Ollama autorisés : 11437-11465 uniquement. Modèles :
  `gpt-oss:120b-cloud`, `gemma4:31b-cloud`.
* `./gradlew test --tests "bakery.pivot.AsciiDocParserTest" --tests
  "bakery.pivot.PivotModelTest" --tests "bakery.pivot.PivotYamlRendererTest" --
  tests "bakery.pivot.TextTranslatableClassifierTest"` = 41/41 PASS.
* Golden master : `--tests "bakery.pivot.PivotGoldenMasterTest.golden master
  quick start adoc to yaml is byte identical"` (nom complet requis, bug Gradle
  noms backtick).
* `./gradlew cucumberTest -Dcucumber.features="src/test/features/37_..."` =
  7/7 PASS.
* Timeout cucumberTest 5min (EPIC 5) — ne pas lancer `check` complet sans
  permission.
* `publishToMavenLocal` fait (jar 0.0.2).
* *Loi de l'Économie d'Encre* : toute tâche Gradle LLM DOIT load-before-write +
  calcul delta + fusion existing+new.