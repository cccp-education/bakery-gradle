= PROMPT_REPRISE — Bakery

== Mission accomplie

*Session 126 TERMINÉE* — BKY-I18N-MIG US-2 (3 pts).
DDD/TDD/BDD baby-step :
- `MigrateToI18nTask.kt` (abstract class, 4 CLI options, cascade CLI>DSL>defaults, llmService + dslIntention)
- `I18nMigrationService.kt` (stub, US-3 fera l'implémentation complète)
- `I18nMigrationResult.kt` (data class)
- `ContentTaskRegistrar.kt` : `registerMigrateToI18nTask()`
- `BakeryPlugin.kt` : wiring dans `registerFullPipelineTasks()`
- 20 UT (MigrateToI18nTaskTest) PASS
- 5 Cucumber (32_i18n_migration.feature) PASS
- publishToMavenLocal OK

== Statut

- EPICs : 30/30 ✅ (100%)
- Code smells résolus : 24/24 ✅ (V2-V4) + 52/52 ✅ (V5)
- BKY-CR-V5 : 24/24 pts ✅
- BKY-I18N-MIG : 5/16 pts (US-1 ✅ S125, US-2 ✅ S126)
- Version publiée: 0.0.1 (Maven Central)

== Prochaine session (127)

*Priorité P1* : BKY-I18N-MIG — US-I18N-MIG-3 (5 pts).
I18nMigrationService — scan + extraction + messages.properties :
- `scanTemplates()` → liste les fichiers `.thyme` dans `templates/`
- `extractHardcodedText()` → parse le texte hors `#{...}` et `th:*`
- `generateMessageFiles()` → crée `messages_{lang}.properties` par langue
- `replaceHardcodedWithMessageKeys()` → remplace texte par `#{key}`
- `writeMessageFiles()` → écrit les fichiers properties
- `injectSiteLanguage()` → ajoute `site.language` dans `jbake.properties`
- Retourne `I18nMigrationResult` (keysExtracted, filesGenerated, dryRun)

*Fichiers cibles* :
- `I18nMigrationService.kt` (REMPLACER le stub)
- `I18nMigrationServiceTest.kt` (NOUVEAU, ~15 UT)
- `MigrateToI18nTaskTest.kt` (MODIFIÉ, enrichir tests executeMigration)

*Vérification* : `./gradlew compileKotlin` → BUILD SUCCESSFUL.
