= PROMPT_REPRISE — Bakery Gradle Plugin
:session: 196
:date: 2026-08-12
:etat: Session 195 ✅ — Cadrage EPIC BKY-DNS (provisionnement DNS OVH depuis site.yml). Suite mise en prod talaria.school (S-004/005 office) : DNS fait à la main via script Python jetable `~/workspace/configuration/scripts/ovh.py`. Zéro code modifié — gouvernance pure.
:epic_actif: BKY-DNS — cadré. US BKY-DNS-0→6 (~33 pts). BKY-DNS-1 next (DnsConfig + mapping Jackson site.yml). BKY-I18N-MASS en pause (MASS-5→6 restants).

== Contexte de fin

Session 195 — Cadrage EPIC BKY-DNS :
- Problème : `provisionDns` fait à la main via script Python jetable (`configuration/scripts/ovh.py`) non testable, non idempotent, non versionné
- Section `dns:` dans site.yml (mapping Jackson `SiteConfiguration`)
- Port `DnsProvider` / `OvhDnsProvider` / `NoOpDnsProvider` (pattern capsule `resolve*Provider`)
- Réconciliation create/update/delete des diffs (idempotent), dry-run par défaut
- Wiring `provisionDns` → `deploySite`
- BACKLOG.adoc + INDEX.adoc + SESSIONS_HISTORY.adoc mis à jour
- Pattern : S-057 gouvernance pure, zéro code modifié

== Mission Session 196

1. **BKY-DNS-1** — DDD pur : `DnsConfig` (domaine `bakery.dns`) + mapping Jackson section `dns:` dans `SiteConfiguration` :
   - Section dns dans site.yml : `provider`, `domain`, `records` (type/name/value/ttl), dry-run
   - Merger 4 sources (pattern `mergeOutputConfig`)
   - TDD strict RED → GREEN → REFACTOR (~8 unit)

== Reprendre à

. `git status` dans bakery-gradle
. Lire `BACKLOG.adoc` section EPIC BKY-DNS (US BKY-DNS-1)
. `publishToMavenLocal` après chaque modification code source

== Ordre des EPICs (priorité)

. **BKY-DNS** (priorité 2, ~33 pts) — BKY-DNS-0 ✅ cadré, BKY-DNS-1→6 restants
. BKY-I18N-MASS (priorité 1, ~32 pts) — MASS-1→4 ✅, MASS-5→6 restants (en pause)
. BKY-SEO (priorité 2, 27 pts) — généralisation features SEO
. BKY-CONTACT-SEC (priorité 3, 26 pts) — durcissement formulaire contact

== Fichiers touchés S195

*bakery-gradle/bakery-plugin* :
- `BACKLOG.adoc` — EPIC BKY-DNS ajouté (US-0→6, ~33 pts)
- `.agents/INDEX.adoc` — entrée session 195 + EPIC BKY-DNS en attente
- `.agents/SESSIONS_HISTORY.adoc` — entrée session 195
