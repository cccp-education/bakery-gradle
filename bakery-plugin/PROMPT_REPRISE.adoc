= PROMPT_REPRISE — Session 027 prête : BKY-JB-0 GenerateArticle (User Journey IA complète)

== Mission accomplie (Session 026)

BKY-JB-0 Scaffolding site type ✅ :
- SiteType enum (BLOG/BASIC) + extension DSL `siteType`
- Ressources `site-basic/` (12 fichiers templates minimaux)
- 22 nouveaux tests (17 unitaires + 4 fonctionnels + 1 blog explicite)

== Prochaine mission (027) — generateArticle : user journey IA complète

Boucler la première user journey de bout en bout :
```
generateSite → generateArticle → bake → deploySite
```

Implémenter la tâche `generateArticle` :
1. Prendre un topic via `-Ptopic="..."` (CLI)
2. Appeler `OllamaLlmService` via `bakery { ia { ... } }` (BKY-IA-0 existant)
3. Générer un article AsciiDoc structuré (titre, description, body, date, tags)
4. L'injecter dans `content/blog/YYYY/MM/article-slug.adoc`
5. TDD baby-step : FakeLlmService → tests unitaires → tests fonctionnels
6. Architecture : bakery = expert JBake, pas IA. BKY-IA-0 = MVP, future délégation codebase.

Patterns koog (KOOG_AGENTIC_PATTERNS.adoc) : **Pattern 10 Self-Reflection** pour la qualité article.

Ordre P0 :
  BKY-JB-0 ✅ → generateArticle (MVP) → BKY-JB-1 → BKY-JB-8 (via codebase)

== Rappels

- GroupId: `education.cccp` — Plugin ID: `cccp.education.bakery`
- `./gradlew -q publishToMavenLocal` après toute modification du code source
- Discipline baby-step : RED (test) → GREEN (implémente) → REFACTOR
- Bakery = expert JBake site statique, PAS formation (training-gradle)
- KOOG_AGENTIC_PATTERNS.adoc = référence architecture agentique
