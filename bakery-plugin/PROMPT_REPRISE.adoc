= PROMPT_REPRISE — Bakery Gradle Plugin
:session: 209
:date: 2026-09-01
:etat: Session 208 ✅ — MEM-CAT-3 pilote workspace-catalog (cross-borough MEMPHIS S-026) : pin `ws` 0.0.29, version dérivée du catalogue, document-plugin sans version via platform 0.0.29, PublicationTest migré. Gates non-régression VERT (test 1665 PASS/3 FAIL baseline, functionalTest, cucumberTest 7 FAIL baseline). Fix-0 buildscript force kotlin-stdlib (conflit Gradle 9.6.1).
:epic_actif: MASS-6 🟡 reporté (108 × 10 langues, ~675h LLM, accord pilote explicite requis).

== Contexte de fin

Session 208 — MEM-CAT-3 (priorité pilote : centralisation versions cross-borough) :
- settings bakery-plugin : `create("ws") { from("education.cccp:workspace-catalog:0.0.29") }` (pin unique)
- build : `version = ws.versions.bakery.plugin.get()` (0.0.9 dérivé du catalogue MEMPHIS)
- `document-plugin` SANS version — contraint par platform workspace-bom 0.0.29
- platforms implementation/functionalTest/e2eTest : 0.0.20 → 0.0.29
- `BakeryPluginPublicationTest` migré : dérivation ws + cohérence toml local ↔ catalogue ws (2/2 PASSED)
- Fix-0 : buildscript force `kotlin-stdlib:2.4.10` + `annotations:26.0.2-1`
  (Gradle 9.6.1 épingle strictly 2.3.21/13.0 en conflit avec platform transitive
  bakery → document → plantuml → workspace-bom)

## Mission Session 209

1. **MASS-6 montée en charge** : 108 articles × 10 langues (zh/hi/es/ar/bn/pt/ru/ur)
   — session dédiée, **accord pilote explicite requis**. Coût LLM ~675h.
2. **Bug chemin dogfooding** : `--contentI18nSource=content/blog/YYYY` produit
   `content/content/blog/YYYY` (double `content/`). Fix dans `MigrateContentI18nTask`
   — ne pas préfixer si le chemin commence déjà par `srcPath`. P3.
3. **Re-traduction cheroliv.com/en/** avec fix `:summary:` (impact SEO).
4. **MEM-CAT-4/5 côté MEMPHIS** : propagation + BDD — coordination avec la
   session MEMPHIS (workspace-bom PROMPT_REPRISE).

## Reprendre à

. `git status` dans bakery-gradle
. Lire `BACKLOG.adoc` section BKY-I18N-MASS MASS-6
. Vérifier Ollama ports 11437-11465 + modèle `gemma4:31b-cloud`
. `/tmp/bakery-dogfood` projet consommateur (buildscript classpath 0.0.9)
. `publishToMavenLocal` après chaque modification code source

## Ordre des EPICs (priorité)

. MASS-6 montée en charge 108 × 10 langues (accord pilote)
. Bug chemin dogfooding (P3, non bloquant)
. Re-traduction cheroliv.com/en/ prod (fix `:summary:` impact SEO)

## Fichiers touchés S208

- `build.gradle.kts` (racine) — buildscript force kotlin-stdlib/annotations
- `bakery-plugin/settings.gradle.kts` — pin ws catalog 0.0.29
- `bakery-plugin/build.gradle.kts` — version dérivée ws + platform 0.0.29 + document-plugin sans version
- `bakery-plugin/src/test/kotlin/bakery/BakeryPluginPublicationTest.kt` — migré D3
- `bakery-plugin/gradle/libs.versions.toml` — WIP 0.0.9 (absorbé, inchangé)