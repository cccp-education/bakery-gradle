= PROMPT_REPRISE — Bakery Gradle Plugin
:session: 217
:date: 2026-09-10
:etat: Session 216 ✅ — **transfert BKY-FPA-SITE → TRN-11 (training) + arbitrage licence FPA tranché** : corpus FPA = document personnel (zéro droit de tiers), corpus brut privé office/, produits formation dérivés Apache 2.0. Étape 7 backlog synchro (BKY-FPA-SITE ✅ TRANSFÉRÉ, en-tête 216). Rapport `.agents/sessions/216-transfer-bky-fpa-site-trn11.adoc`.
:epic_actif: MASS-6 🟡 reporté (accord pilote, ~675h LLM) + re-traduction cheroliv.com/en/ prod (fix `:summary:`). Backlog bakery propre — BKY-FPA-SITE ✅ transféré S-216 → TRN-11 training (arbitrage licence FPA tranché).

== Contexte de fin

Session 216 — **transfert BKY-FPA-SITE → TRN-11 training** (gouvernance pure, zéro code) :
- Arbitrage licence FPA tranché par le pilote (2026-09-10) : le corpus FPA est
  un *document personnel* (zéro droit de tiers) → corpus brut (scan → AsciiDoc)
  reste privé dans `office/metiers/FPA/`. Les produits formation (SPG, SPDs,
  slides, PDFs, quiz) sont des travaux originaux dérivés = Apache 2.0 → publiables.
- Le site navigable héberge uniquement les produits formation, jamais le texte
  intégral brut → livrable formation (GBL-003.5, anneau A4), pas un livrable bakery.
- Pattern de transfert : TRN-9 (ex BKY-TRN-9) / TRN-TEMP (ex BKY-JB-1). Bakery =
  mécanisme JBake (publishSite), training = propriétaire du portail formation.
- Étape 7 backlog synchro : BKY-FPA-SITE ✅ TRANSFÉRÉ (S-216), en-tête mise à jour.
- Backlog bakery : reste MASS-6 (reporté polarité) + re-traduction cheroliv.com/en/.

Session 215 — résolution du bug chemin dogfooding (P3, item S-215) :
- Cause racine : `SiteConfiguration.resolvePaths` rend `contentRoot` absolu
  (projectDir + srcPath) ; `--contentI18nSource` déjà préfixé par `srcPath`
  doublait (`jbake/content/blog/2026` → « répertoire source n'existe pas »).
- Nouveau domaine `bakery.i18n.path` : `ContentSourcePathResolver` objet pur
  segment-aware (3 règles : absolu tel quel / préfixé → projectDir.resolve /
  sinon projectDir.resolve(srcPath).resolve). Rétro-compatibilité totale :
  forme non-préfixée `content/blog` inchangée, forme préfixée `jbake/content/blog`
  corrigée. Workaround S-207 (`blog/2026`) devenu inutile.
- Wiring : `contentSrcPath` relatif dérivé par le registrar (le registrar connaît
  la vérité issue de `resolvePaths`), tâche `resolveSourceDir()` câblée au resolver.
- Tests : 8 UT resolver + 2 UT wiring (9 `ExecuteContentMigration`) + 2 FT TestKit
  (9/9) + feature 63 BDD 5 scénarios (runner dédié, 5/5). Gates : test 1682/0 FAIL,
  functionalTest 119/0, cucumberTest 2 FAIL préexistants environnementaux (0 nouveau).
- Découverte runner : le `CucumberTestRunner` standard filtre
  `@pipeline-contracts and not @wip` — la feature 63 (`@source-path`) a son
  runner dédié `I18nSourcePathCucumberTest`.

== Mission Session 217 — à la demande

1. **MASS-6 montée en charge** : 108 articles × 10 langues — accord
   pilote explicite requis. Coût LLM ~675h.
2. **Re-traduction cheroliv.com/en/ prod** (fix `:summary:` impact SEO).
3. **Portail formation (anneau A4)** : quand les plans de formation
   existeront (Newark TRN-1→6), dogfood le portail via assembleTrainingSite
   (TRN-9/TRN-11 — BKY-FPA-SITE ✅ transféré S-216).

## Reprendre à

. `git status` dans bakery-gradle
. Lire `BACKLOG.adoc` (S-215 ✅ — item P3 clôturé, backlog propre)
. Vérifier Ollama ports 11437-11465 + modèle `gemma4:31b-cloud`
. `/tmp/bakery-dogfood` projet consommateur (buildscript classpath 0.0.9)
. `publishToMavenLocal` après chaque modification code source

## Ordre des EPICs (priorité)

. MASS-6 (accord pilote requis)
. Re-traduction cheroliv.com/en/ prod (fix `:summary:` impact SEO)
. Portail formation anneau A4 (quand les SPDs existeront)

## Rappel S212-S214

- Gate permanente : `grep -rwn "REAC|AFNOR|SPG|SPD|FPA|RNCP|CDA" src/`
  doit rester = 0 (bakery + tous boroughs transverse)
- Golden masters = artefacts gelés : `PostMigrationSnapshotGenerator`
  reste `@Disabled`, régénérer UNIQUEMENT si les fixtures changent
  (pattern REAL-4 : retirer @Disabled, run, re-valider, re-@Disabled).
- Note environnement : suite `cucumberTest` complète (1866 scénarios)
  timeout à 5min sous charge machine élevée (load 14+) — baselines
  futures à mesurer machine déchargée ou via runners dédiés.

(End of file - total 63 lines)