= PROMPT_REPRISE — Bakery Gradle Plugin
:session: 219
:date: 2026-09-17
:etat: Session 218 ✅ — **BKY-I18N-JS-2 TERMINÉ** : `I18nJsDictionary` (object pur parser/writer JS client) livré, 21 tests 0 FAIL dont dogfooding réel talaria (205 clés `fr`, 11+11+22 langues, round-trip byte-identique).
:epic_actif: BKY-I18N-JS 🟢 EN COURS 2/5 US — reste JS-3 (task `translateI18nClient`), JS-4 (propagation + garde). Débloque talaria `T-I18N-BAKERY` US-2/US-3.

== Contexte de fin

Session 218 — **BKY-I18N-JS-2** (bloquant talaria `T-I18N-BAKERY`, cadré S-054) :
- `I18nJsDictionary` object pur (`parse` / `languagesOf` / `insertTranslations`),
  miroir du harness `tools/translate-i18n.mjs` (S-044).
- 2 invariants supérieurs au harness : scanner braces **string-aware** (une valeur
  `"{0}"` ne tronque plus un bloc) + regex bloc ancrée (entrées hors bloc ignorées).
- Loi Économie d'Encre : seules les clés manquantes sont insérées ; corpus complet
  = **no-op byte-identique** (prouvé sur les 3 dictionnaires talaria réels).
- Bug attrapé par TDD : `String.replace(Regex, "\\n")` perdait le backslash
  (Java Matcher) → lambda de remplacement.

Gate : `test` **1707/2 préexistants** (baseline 1686/2, +21 exactement),
`publishToMavenLocal` VERT. Zéro fichier existant modifié.

== Mission Session 219 — à la demande

1. **BKY-I18N-JS-3** : task `translateI18nClient` (domaine `bakery.i18n.js`) —
   `I18nClientMigrationIntention` + DSL `bakery { i18nClient { ... } }` + CLI +
   `ContentTaskRegistrar.registerTranslateI18nClientTask` ; brancher
   `LlmServiceTranslationAdapter` → port N0 `contracts.i18n.TranslationService` ;
   delta des clés manquantes ; dry-run ; **tests unit + functional Oven + Cucumber**.
2. **BKY-I18N-JS-4** : propagation `maquette/js/` → `jbake/assets/js/`
   byte-identique + garde parse↔write round-trip + failover zéro appel HTTP direct.
3. *(hors EPIC)* BKY-LANG-22-8/9/10 (dogfooding LLM 22 langues, publication Central
   consommateurs) OU MASS-6 (accord pilote).

## Reprendre à

. `git status` dans bakery-gradle
. Lire `BACKLOG.adoc` (EPIC BKY-I18N-JS en tête)
. `src/main/kotlin/bakery/i18n/js/I18nJsDictionary.kt` (API parser/writer US-2)
. `src/main/kotlin/bakery/i18n/MigrateContentI18nTask.kt` (pattern intention/DSL/CLI)
. `/tmp/bakery-dogfood` projet consommateur + corpus talaria
   `office/sites/talaria.school/maquette/js/` (lecture seule)
. `publishToMavenLocal` après chaque modification code source (règle INDEX 0)

## Rappel

- Gate permanente : `grep -rwn "REAC|AFNOR|SPG|SPD|FPA|RNCP|CDA" src/` doit rester = 0
- **Décision pilote (S-054)** : pas de migration `LlmBuildService` (codebase N1) —
  socle LLM bakery **local** (`bakery.llm.PooledOllamaLlmService`), hors périmètre.
- **Ordre maquette** : `maquette/js/` = source de dev, `jbake/assets/js/` = copie de
  publication byte-identique (`MaquetteParityGuard`).
- **Règle gouvernance** : `office/sites/talaria.school/site.yml` est gitignoré
  (credentials réels). Ne pas le lire/modifier — repo `office` privé.
- Note environnement : suites cucumber complètes timeout sous charge (load 14+).

(End of PROMPT_REPRISE — 60 lines)
