= PROMPT_REPRISE — Session 068 : EPIC 6 publishProfil finalisation + BKY-IA-2 Thème IA

== Mission (Session 068)

1. **EPIC 6** — Vérification + finalisation publishProfil (P1, ~1h)
   - Vérifier que `deployProfile` marche dans un projet consommateur
   - Nettoyer build consommateur cheroliv.com si besoin
   - MAJ statut EPIC 6 dans INDEX → 100%
2. **BKY-IA-2** — Thème IA paramétrique (P1, 8 pts, ~3-4h)
   - Catalogue de 5-10 thèmes Thymeleaf (minimal, magazine, documentation, portfolio, formation)
   - Variables de thème (couleurs, polices, logo, favicon) — déjà dans le DSL `bakery { theme { ... } }`
   - Tâche `generateTheme` : intention → sélection thème + variables
   - Tests : vérifier que les templates générés compilent avec Thymeleaf
   - Cucumber : scénarios de régression
3. **Priorité P2** si temps : BKY-IA-3 Validation Firebase IA

== État actuel (Session 067)

- **CODE_REVIEW.adoc v2** ✅ — Mise à jour complète avec 12 code smells, analyse vélocité, projection
- **CODE_REVIEW.adoc EAGER** ✅ — Ajouté dans AGENT.adoc, chargé automatiquement
- EPIC 6 : code existant, statut documentaire à corriger (~3 pts réels)
- Vrai total restant : ~14-17 pts

== Points d'attention

- Lire `.agents/CODE_REVIEW.adoc` en premier — contient l'analyse complète
- Ne pas lancer `./gradlew test` sans permission explicite
- `test` task exclut Cucumber (38s) — lancer `./gradlew cucumberTest` séparément
- Configuration-cache bloqué par `koverThresholdCheck` (non sérialisable)
