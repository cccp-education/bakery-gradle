= PROMPT_REPRISE — Bakery Gradle Plugin
:session: 235
:date: 2026-09-25
:etat: Session 234 ✅ — **BKY-LANG-NAV-5 TERMINÉ — BDD de clôture du cœur** (fixture deux hôtes, 6/6 scénarios, accord injecté ∪ modèle ∪ règle). Antérieur S-233 : re-pin catalogue ws 0.0.57. S-232 : NAV-4. S-231 : NAV-3. S-230 : NAV-2. S-229 : NAV-1. S-228 : cadrage.
:epic_actif: **BKY-LANG-NAV** (🟡 NAV-0 ✅, NAV-1 ✅, NAV-2 ✅, NAV-3 ✅, NAV-4 ✅, NAV-5 ✅, NAV-6→8 À FAIRE) — cœur CLOS, reste rollout sites (NAV-6→8, ~13 pts).
:epic_suivant: **NAV-6** (phase site — cheroliv.com : fix bug + kill split-brain + `hreflang` conservé). Dépend de NAV-5 ✅.

== État S-234

**Le cœur `BKY-LANG-NAV` est clos.** NAV-5 (BDD bout en bout) livre la preuve
exécutable de la décision D3 « une règle, deux hôtes, vecteurs partagés » :
feature `66_same_page_language_switch.feature` (`@same-page`, 6 scénarios) +
`SamePageLanguageSwitchSteps` + runner dédié `SamePageCucumberTestRunner`
(pattern S-082). Une page imbriquée (`blog/foo.html` FR ↔ `en/blog/foo.html` EN)
est injectée par la chaîne de production `injectLangSwitch` **et** rendue par
l'hôte modèle ex-nihilo, les deux étant évalués au `TemplateEngine` réel et
comparés à `LangSwitchPath.resolveSamePage`. Baby-step BDD strict RED (6×
`UndefinedStepException`) → GREEN (6/6 PASSED). **Zéro production modifiée**
(3 fichiers de test créés).

Baseline : `test` 2007 (+0, 2 FAIL = FLAKY-5 préexistants), `cucumberTest`
2864/5 = baseline exacte (stash-prouvé 2464/5), `functionalTest` VERT,
`nodeTest` 7/7, `publishToMavenLocal` VERT.

== Mission Session 235 — BKY-LANG-NAV-6 (phase site cheroliv.com)

*NAV-6 — premier rollout site.* Migrer `office/sites/cheroliv.com` vers la règle
commune : le sélecteur préserve la page (fin du retour à l'accueil) et le
**split-brain est tué** — `com.cheroliv.i18n.LangSwitchLink.thymeleafHref()` +
`LangSwitchMenuRenderer`/`LangSwitchSiteApplier`/`LangSwitchBlockInjector`/
`InjectLangSwitchMain` consomment la règle commune (ou sont retirés au profit du
task bakery `injectLangSwitch`). Décision D5 du cadrage à confirmer au cadrage
de la phase site.

*Couverture* : préservation de page (page imbriquée réelle) + `hreflang`/SEO
conservés (D9, les deux mécanismes ne doivent pas diverger).

*Attention boundary* : `office/sites/*` sont des sites hors `foundry/`. Le
cadrage D-pré-requis impose de travailler sur fixture/copie (`dry-run` par
défaut), jamais de modification destructive du site réel sans accord pilote.

== Reprendre à

. `git status` dans bakery-gradle — vérifier qu'aucune session concurrente n'écrit.
. `.agents/CADRAGE_EPIC_BKY_LANG_NAV.adoc` (cadrage complet, D1→D10 + NAV-6).
. `.agents/sessions/234-bky-lang-nav-5-bdd-same-page.adoc` (rapport S-234).
. `office/sites/cheroliv.com/src/main/kotlin/com/cheroliv/i18n/LangSwitchLink.kt`
   (le split-brain à tuer) + `InjectLangSwitchMain` (la chaîne à replier sur bakery).
. Gate Économie d'Encre : ne relancer `test` que si le code source change.

== Dette découverte (FLAKY-5)

`FixtureAlignmentAuditorTest` + `RealSiteI18nMigrationIntegrationTest` (cheroliv-com)
= **2 FAIL préexistants** : drift fixture ↔ site réel (chrome nativement FR,
S-053/055, 5 templates — `blog_section.thyme` golden master « Close » vs
« Fermer »). Stash-prouvé non introduit par NAV-2/3/4/5 ni par le re-pin S-233.
Session dédiée : resync fixture + régénération golden master
(`PostMigrationSnapshotGenerator`). `koverThresholdCheck` + `ktlintKotlinScriptCheck`
rouges à HEAD (baseline dépôt).

== Rappel

* Gate métier : `grep -rwE "REAC|AFNOR|SPG|SPD|FPA|RNCP|CDA" src/` = 0.
* *Modèle LLM* : `gemma4:31b-cloud` + `OLLAMA_POOL_PORTS=11439,11440,11441,11442`.
* *Publication Central* : ordre DAG réel (publier le consommateur absent AVANT
   de l'ajouter au catalogue MEMPHIS — garde D8 anti-ghost).
* *Règle 0* : `./gradlew -q publishToMavenLocal` OBLIGATOIRE après toute
   modification du code source.
* *Pattern BDD runner dédié* : tout tag de feature non couvert par le runner
   principal doit avoir son runner dédié (S-082) — sinon il est skippé en silence.
* Commit/push autorisés (décision pilote 2026-09-17). Zéro secret.
* Baby-step DDD/TDD/BDD strict — 1 fichier à la fois, gate `test` complète.
