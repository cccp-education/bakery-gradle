= PROMPT REPRISE — Bakery — Session 167
:session: 167
:date: 2026-06-26

== Session 166 terminée — BKY-TREE-1+2+3+4 (18 pts, 57 UT GREEN)

=== ✅ TREE-1 — SiteNode sealed interface
- `SiteNode.kt` : sealed interface + Site/Section/Article data classes
- Identity par `path`, validation non-blank, `isLeaf()`/`isSection()`
- 14 UT + 7 BDD scénarios PASS

=== ✅ TREE-2 — SiteTree aggregate + visiteurs
- `SiteTree.kt` : walk (pre/post-order), filter, leaves, sections,
  findByPath, findSubtree, visit. No mutation.
- 17 UT + 9 BDD scénarios PASS

=== ✅ TREE-3 — Content wrapper sur PivotModel
- `Content.kt` : wraps PivotArticle, exposes blocs()/inlineTexts()/
  translatableSegments(). Article.content optional (backward compat).
- 13 UT + 6 BDD scénarios PASS

=== ✅ TREE-4 — YAML serialization tree: section
- `SiteNodeDto.kt` + `SiteTreeYaml.kt` : Jackson polymorphic DTO,
  round-trip déterministe, `type:` discriminator, content @JsonIgnore
- 13 UT + 6 BDD scénarios PASS

== Prochaine mission — BKY-TREE-5 + BKY-TREE-6 (13 pts)

**US TREE-5 — Theme inheritance (5 pts)**
`effectiveTheme(node)` : résout thème effectif par nœud (héritage parent
si non surchargé, override explicite sinon). `ThemeConfig` existant (S068).
Pas de mutation — retourne `ResolvedTheme`. Détection conflits (override
partiel interdit). Baby steps DDD/TDD/BDD, domaine pur.

**US TREE-6 — i18n migration par sous-arbre (8 pts)**
Remplace walk flat `templates/` par `tree.walk().filter { it is Article }`.
Delta ciblé : AsciiDoc modifié → re-traduire seulement ses clés. Intégrité
hors-sous-arbre préservée (checksums). Implémente Loi Économie d'Encre par
article (S153 global → S167 par feuille). Backward compat (walk flat si
pas de `tree:`).

== Contexte technique
- 57 UT tree package GREEN (SiteNode 14 + SiteTree 17 + Content 13 + Yaml 13)
- 4 features Cucumber (28 scénarios) — cucumberTest complet à valider
- Version : 0.0.2 Maven Central + Gradle Portal (LIVE)
- Ports Ollama : 11437-11465. Modèles : gpt-oss:120b-cloud, gemma4:31b-cloud
- TREE-5+6 = fusion (consume TREE-2 + TREE-3 + TREE-4) — gros morceau EPIC
- Anglais imposé : nommage, commentaires, Gherkin, steps