= PROMPT REPRISE — Bakery
:session: 142
:date: 2026-06-22

== Mission accomplie (session 141)

*BKY-CR-V6 Cadrage EPIC Duplications Structurelles* — audit + gouvernance.
- Intuition utilisateur : duplications structurelles malgré 5 audits précédents.
- 3 duplications réelles confirmées par lecture code :
  1. `resolveIntention()` dupliqué ×4 (GenerateArticleTask, GenerateThemeTask, GenerateSiteFromIntentionTask, MigrateToI18nTask) — 23 occurrences pattern CLI>DSL>defaults.
  2. `DomainConfigInjectors.kt` 9 injecteurs répétitifs (113 lignes, 3 variantes).
  3. `ConfigResolver.resolveAll()` 10 blocs `resolveDomain` identiques (ligne 332-401).
- BACKLOG V5 corrigé : US-CR-V5-5 et US-CR-V5-7 repositionnées PARTIEL (⚠️), ✅ étaient faux.
- EPIC BKY-CR-V6 créé : 3 US, 8 pts, P1.
- Aucun changement code source production (cadrage uniquement).
- Archive session 141 créée.

== Statut

- BKY-CR-V6 : US-1, US-2, US-3 à faire (8 pts)
- BKY-PUB-2 : publication 0.0.2 dry-run OK, en attente permission
- BKY-COV-2 : DeployProfileTask branches couvertes S140

== Prochaine session (142)

Démarrer **BKY-CR-V6 US-1** : Helper `ResolveIntention<T>` — pattern CLI>DSL>defaults dupliqué ×4.
Baby steps DDD/TDD/BDD :
- DDD : `ResolveIntention<T>` helper pur (zéro Gradle) — `fromCli(prov, dslField, default)` + `fromCliNullable(prov, dslField)`.
- TDD : tests unitaires helper RED→GREEN (CLI blank, CLI set, DSL fallback, default).
- Refactor : remplacer 23 occurrences dans les 4 Task, vérifier comportement identique (tests existants GREEN).
- `publishToMavenLocal` après modif code.
- `./gradlew test` pour validation (avec permission).

Fichiers cibles :
- NOUVEAU `bakery/intention/ResolveIntention.kt`
- `GenerateArticleTask.kt:178` `resolveIntention()`
- `GenerateThemeTask.kt:205` `resolveIntention()`
- `GenerateSiteFromIntentionTask.kt:160` `resolveIntention()`
- `MigrateToI18nTask.kt:96` `resolveIntention()`

== Note technique

- `./gradlew test` reste la commande de validation rapide en session.
- `./gradlew check -x e2eTest` OK mais timeout Cucumber 5min (EPIC 5 connu).
- Kover threshold 85% maintenu.