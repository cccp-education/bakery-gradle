= PROMPT_REPRISE — Bakery Gradle Plugin
:session: 212
:date: 2026-09-08
:etat: Session 211 ✅ — RENAME-3 TERMINÉ (EPIC transverse RENAME, baby-step 3/6 bakery) : 2 entrées "FPA" sorties des listes I18nMigrationService + fixtures neutralisées. Gate grep src/ = 0, baselines test (1670/3 FAIL préexistants) + cucumberTest (1866/6 FAIL préexistants) exactes. 7 fichiers, 1 pt. `publishToMavenLocal` VERT.
:epic_actif: RENAME ✅ bakery TERMINÉ (reste capsule + plantuml + graphify/codex, autres boroughs) + BKY-FPA-SITE 🟡 STANDBY (arbitrage pilote) + MASS-6 🟡 reporté (accord pilote, ~675h LLM).

== Contexte de fin

Session 211 — RENAME-3 (baby-step TDD, 1 pt) :
- 2 UT verrou whitelist/blacklist ajoutées (terme conservé SPG — la gate
  grep interdit littéralement "FPA" dans src/, pattern codebase RENAME-1).
- `I18nMigrationService.kt` : "FPA" retiré de `extractionWhitelist` +
  `substringBlacklist` — le terme redevient traductible (nommage public
  neutre, cadrage RENAME D6/D7).
- Fixtures scaffold "Formation FPA" → "Formation métier" (4 fichiers) +
  tag "fpa" → "pédagogie" + fixture blog 0115 (2 mentions CDA/FPA → "métier").
- Zéro régression : échecs test (3) et cucumberTest (6) identiques sur
  baseline stashed — préexistants environnementaux (S196 + FLAKY-1/2/3).

== Mission Session 212 — à la demande

1. **BKY-FPA-SITE-0** : arbitrage licence publication publique corpus FPA
   (décision pilote — après GBL-003.3/003.4 codex/codebase livrés).
2. **Portail formation (anneau A4)** : quand les SPDs existeront (Newark
   TRN-1→6), dogfood le portail via assembleTrainingSite.
3. **MASS-6 montée en charge** : 108 articles × 10 langues — accord
   pilote explicite requis. Coût LLM ~675h.
4. **FLAKY-1/2/3** : dette cucumber i18n (P1 — 6 FAIL préexistants
   cucumberTest, documentés S196/S201).

## Reprendre à

. `git status` dans bakery-gradle
. Lire `BACKLOG.adoc` section EPIC RENAME (bakery ✅) + BKY-FPA-SITE (STANDBY)
. Vérifier Ollama ports 11437-11465 + modèle `gemma4:31b-cloud`
. `/tmp/bakery-dogfood` projet consommateur (buildscript classpath 0.0.9)
. `publishToMavenLocal` après chaque modification code source

## Ordre des EPICs (priorité)

. RENAME boroughs restants (capsule fixtures, plantuml registre — autres repos)
. FLAKY-1/2/3 (dette cucumber i18n, P1)
. Bug chemin dogfooding (P3, non bloquant)
. Re-traduction cheroliv.com/en/ prod (fix `:summary:` impact SEO)

## Rappel S211

- `I18nMigrationServiceTest` 42 UT (dont 2 verrou whitelist SPG)
- Gate permanente : `grep -ri "fpa" src/` doit rester = 0