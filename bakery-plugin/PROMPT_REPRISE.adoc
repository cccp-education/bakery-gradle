= PROMPT REPRISE — Bakery
:session: 134
:date: 2026-06-22

== Mission accomplie

*Session 132 TERMINÉE* — BKY-I18N-PROD-GEN (8 pts) ✅.
DDD/TDD/BDD sur fixtures cccp-education et cheroliv-com :
- 2 nouvelles fixtures i18n en test resources (12 + 10 templates, jbake.properties)
- `translations_en.properties` pour cccp-education (17 clés) et cheroliv-com (14 clés)
- `I18nMigrationService` — whitelist/blacklist enrichies de noms propres, identifiants de projet, termes métier
- `CccpEducationI18nMigrationIntegrationTest` — 5 tests d'intégration
- `CherolivComI18nMigrationIntegrationTest` — 5 tests d'intégration
- Feature 34 `i18n_migration_prod_gen.feature` — 4 scénarios Cucumber (migration réelle + dry-run × 2 fixtures)
- 119/119 tests i18n unitaires/intégration PASS, 120/120 bakery Cucumber scenarios PASS

*Transition 133* — Codebase-gradle N1 a livré `TranslationService` (contrat N0) + `LlmTranslator` (impl N1) + `FakeLlmTranslator`. BKY-I18N-MIG US-4 est maintenant débloquée. Gouvernance bakery réajustée en conséquence.

== Statut

- BKY-I18N : 23/23 pts ✅
- BKY-I18N-MIG : 13/16 pts (US-1→3 ✅, US-5 ✅, US-4 🟢 débloquée par codebase-gradle N1)
- BKY-I18N-PROD-VAL : 3/3 pts ✅
- BKY-I18N-PROD-MIG : 5/5 pts ✅
- BKY-I18N-PROD-GEN : 8/8 pts ✅

== Prochaine session (134) — BKY-I18N-MIG US-4 : Traduction automatisée FR→EN

*Prérequis* : `codebase-plugin` publié en mavenLocal (`./gradlew :codebase-plugin:publishToMavenLocal` ✅).

*Mission* (3 pts pour clore BKY-I18N-MIG) :
1. **US-4-1** : Ajouter dépendance `education.cccp:codebase-plugin` (ou contrat) dans `bakery-plugin/build.gradle.kts` pour consommer `contracts.i18n.TranslationService` + `codebase.i18n.LlmTranslator`.
2. **US-4-2** : Créer `TranslationApplier` dans `bakery.i18n` — service bakery qui prend un `Map<String, String>` (clés FR), appelle `TranslationService` pour traduire FR→EN, et retourne `Map<String, String>` traduit. Gérer `TranslationResult.Failure` par fallback clé FR (pas de crash).
3. **US-4-3** : Brancher `I18nMigrationService` pour utiliser `TranslationApplier` quand aucun `translations_en.properties` n'est fourni (mode auto-LLM). Garder le mode manuel existant comme override.
4. **US-4-4** : Tests unitaires `TranslationApplierTest` avec fake `TranslationService` (deterministe) + tests d'intégration + Cucumber feature 35 `i18n_migration_auto_translation.feature`.

*Contraintes* :
- Baby-step DDD/TDD/BDD — un fichier à la fois, compilation entre chaque étape.
- Pas de vrai appel LLM dans les tests : utiliser un fake implémentant `TranslationService`.
- `TranslationService` reste dans codebase ; bakery ne duplique pas la logique LLM.

== Note technique

- `./gradlew test` déclenche `CucumberTestRunner.initializationError` (runner suite contracts sans engine cucumber, exclu du task test). Ce n'est pas une régression — la tâche `cucumberTest` est la référence BDD.
- Imports graphify workaround temporaire — revenir à `graphify.model.*` quand graphify-plugin 0.0.3 corrigera le package.
