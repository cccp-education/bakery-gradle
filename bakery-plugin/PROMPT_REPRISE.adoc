= PROMPT_REPRISE — Session 051 : CONV-1.7 saveConfiguration() FT + Cucumber

== Mission (Session 051)

1. **CONV-1.7** : Ajouter des tests fonctionnels (FT) pour saveConfiguration()
2. **CONV-1.7** : Ajouter un scénario Cucumber pour la persistence site.yml
3. **Dette** : Les 58 échecs Cucumber préexistants à investiguer

== État actuel (Session 050 terminée)

- CONV-1.6 : ✅ Documentation ordre de surcharge (AGENT.adoc, BACKLOG)
- CONV-1.7 : ✅ Implémentation `File.saveConfiguration()` — 4 UT PASS
  - `saves push credentials into existing site yml`
  - `creates pushPage section if missing in site yml`
  - `preserves existing bake configuration when saving credentials`
  - `does not overwrite site yml when all credentials are empty`
- SiteManager mis à jour : `collectSiteConfig` appelle `File.saveConfiguration`
- Ancien no-op `Project.saveConfiguration` gardé comme `@Deprecated`
- Graphify : dépendance RÉTABLIE (nécessaire pour BKG)

== Architecture ConfigResolver — Rappel

```
CLI (-Pbakery.xxx) > gradle.properties > DSL (bakery { xxx { ... } }) > site.yml > defaults
```

== Architecture saveConfiguration — Rappel

```
collectSiteConfig → collecte (username, repo, token, configPath)
                   → File.saveConfiguration(site, siteYmlFile, username, repo, token)
                   → FileSystemManager.yamlMapper.writeValue(siteYmlFile, updatedConfig)
```

== Attention

- Ne pas lancer `./gradlew test` sans permission explicite
- 58 échecs Cucumber préexistants (pas liés à nos changements)