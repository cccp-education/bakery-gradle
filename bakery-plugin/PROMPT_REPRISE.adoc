= PROMPT_REPRISE — Bakery

== Mission accomplie

*Session 125 TERMINÉE* — BKY-I18N-MIG US-1 (2 pts).
DDD/TDD/BDD baby-step :
- `I18nMigrationIntention.kt` (data class, validation init{}, toPromptContext())
- `I18nMigrationIntentionDsl.kt` (open class, var mutables, toIntention())
- `BakeryExtension.kt` : `val i18nMigration` + `fun i18nMigration(action)`
- 10 UT (I18nMigrationIntentionTest + I18nMigrationIntentionDslTest) PASS
- 3 Cucumber (31_i18n_migration.feature) PASS
- 8 fichiers (4 nouveaux + 2 modifiés + 2 steps/world). Compilation OK.

== Statut

- EPICs : 30/30 ✅ (100%)
- Code smells résolus : 24/24 ✅ (V2-V4) + 52/52 ✅ (V5)
- BKY-CR-V5 : 24/24 pts ✅
- BKY-I18N-MIG : 2/16 pts (US-1 ✅)
- Version publiée: 0.0.1 (Maven Central)
- 3 échecs ThymeleafRenderingTest pré-existants (OGNL bug header.thyme, non lié)

== Prochaine session (126)

*Priorité P1* : BKY-I18N-MIG — US-I18N-MIG-2 (3 pts).
MigrateToI18nTask + ContentTaskRegistrar :
- Tâche `@DisableCachingByDefault abstract class MigrateToI18nTask : DefaultTask()`
- Options CLI (`--i18nSite`, `--i18nLangs`, `--i18nDefaultLang`, `--i18nDryRun`)
- Résolution cascade CLI > DSL > defaults
- Injection `dslIntention` + `llmService`
- `ContentTaskRegistrar.registerMigrateToI18nTask()`
- Enregistrement dans `BakeryPlugin.registerFullPipelineTasks()`

*Fichiers cibles* :
- `MigrateToI18nTask.kt` (NOUVEAU)
- `ContentTaskRegistrar.kt` (MODIFIÉ)
- `BakeryPlugin.kt` (MODIFIÉ)

*Vérification* : `./gradlew compileKotlin` → BUILD SUCCESSFUL.
