= PROMPT_REPRISE — Bakery Gradle Plugin
:session: 233
:date: 2026-09-25
:etat: Session 232 ✅ — **BKY-LANG-NAV-4 TERMINÉ** (modèle ex-nihilo conforme). Antérieur S-231 : NAV-3 (hôte JS + garde de parité). S-230 : NAV-2 (hôte Thymeleaf page-aware). S-229 : NAV-1 (`LangSwitchPath` règle pure + vecteurs). S-228 : cadrage.
:epic_actif: **BKY-LANG-NAV** (🟡 NAV-0 ✅, NAV-1 ✅, NAV-2 ✅, NAV-3 ✅, NAV-4 ✅, NAV-5→8 À FAIRE) — P1, clôture du cœur NAV-5 (~3 pts), puis rollout sites NAV-6→8 (~13 pts).
:epic_suivant: **NAV-5** (BDD Cucumber bout en bout, fixture FR+EN multi-pages) — clôture du cœur. Dépend de NAV-2 ✅ et NAV-4 ✅.

== État S-232

Le **modèle ex-nihilo** est conforme : `site-basic/templates/menu.thyme` et
`site/templates/menu.thyme` portent le sélecteur page-aware (`lang-option` +
l'expression exacte de la règle). Un site scaffoldé ne naît plus buggé (D7).

Nouveau frère symbolique dynamique `LangSwitchPath.thymeleafEachHref(defaultLang)`
(projection de la règle sur un `th:each`) + `LangSwitchModelGuard` (garde pure :
présence **ET** page-awareness, pas seulement `lang-option`).

Preuves : `LangSwitchPathEachHrefTest` (4 UT, rejeu **11/14 vecteurs partagés**
via le `TemplateEngine`) ; `LangSwitchModelGuardTest` (9 UT, dont **rendu réel**
des deux menus embarqués) ; `LangSwitchScaffoldFunctionalTest` 1 → 3 FT (BLOG +
BASIC conformes). `test` 2006 (+13 nets, 2 FAIL = FLAKY-5 préexistants) ;
cucumber baseline exacte ; FT BUILD SUCCESSFUL.

*Limitation notée* : l'expression du modèle embarque `'fr'` (défaut de
`SiteConfiguration`) — pas de `config.default_language` injecté. Suite possible
en NAV-5 ou US dédiée.

== Mission Session 233 — BKY-LANG-NAV-5 (BDD de clôture du cœur)

*NAV-5 — Cucumber bout en bout.* Fixture FR+EN multi-pages → assert :
`en/blog/foo.html` garde `data-lang`, le lien FR = `../blog/foo.html` (page
préservée), pas d'accueil, pas de self-loop, langue active. Couvre les deux
hôtes (Thymeleaf NAV-2, modèle/JS NAV-4).

*Tests* : feature BDD (page FR→EN, EN→FR, page imbriquée, no self-loop, active
class) + runner dédié si nécessaire (pattern S-082).

== Reprendre à

. `git status` dans bakery-gradle — **vérifier qu'aucune session concurrente
   n'écrit** (un drift MEM-CAT-3 non committé a été observé en S-232 :
   `build.gradle.kts`, `libs.versions.toml`, `settings.gradle.kts`,
   `BakeryPluginPublicationTest.kt` — hors périmètre NAV-4, non committés par
   cette session).
. `.agents/CADRAGE_EPIC_BKY_LANG_NAV.adoc` (cadrage complet, D1→D10).
. `src/test/features/51_lang_switch_url_resolver.feature` (BDD existante à
   étendre ou compléter par une feature multi-pages).
. `.agents/sessions/232-bky-lang-nav-4-modele-ex-nihilo.adoc` (rapport S-232).
. Gate Économie d'Encre : ne relancer `test` que si le code source change.

== Dette découverte (FLAKY-5)

`FixtureAlignmentAuditorTest` + `RealSiteI18nMigrationIntegrationTest` (cheroliv-com)
= **2 FAIL préexistants** : drift fixture ↔ site réel (chrome nativement FR,
S-053/055, 5 templates). Stash-prouvé non introduit par NAV-2/3/4. Session
dédiée : resync fixture + régénération golden master
(`PostMigrationSnapshotGenerator`). `koverThresholdCheck` + `ktlintKotlinScriptCheck`
rouges à HEAD (baseline dépôt).

== Rappel

* Gate métier : `grep -rwE "REAC|AFNOR|SPG|SPD|FPA|RNCP|CDA" src/` = 0.
* *Modèle LLM* : `gemma4:31b-cloud` + `OLLAMA_POOL_PORTS=11439,11440,11441,11442`.
* *Publication Central* : ordre DAG réel (publier le consommateur absent AVANT
   de l'ajouter au catalogue MEMPHIS — garde D8 anti-ghost).
* *Règle 0* : `./gradlew -q publishToMavenLocal` OBLIGATOIRE après toute
   modification du code source.
* Commit/push autorisés (décision pilote 2026-09-17). Zéro secret.
* Baby-step DDD/TDD/BDD strict — 1 fichier à la fois, gate `test` complète.
