= PROMPT_REPRISE — Session 080 : CS-FIN-1 (fix critique) ou Switch Borough

== Mission (Session 080)

**Deux options au choix de l'utilisateur :**

*Option A — Finir bakery* : Résoudre CS-FIN-1 (P0, 2 pts) → corriger le bug dormant
`configPath.get()` sans `isPresent` + `isGradlePropertiesEnabled` timing dans
`BakeryPlugin.kt`. ~30 min. Puis CS-FIN-2 (P1, JSON escape, 1.5 pts).

*Option B — Switch borough* : Le backlog est propre, bakery est à 28/28 EPICs.
Reprendre un autre borough du workspace (New Jersey, Newark, Brooklyn, Manhattan...).

== État bakery (Session 079)

- **EPICs** : 28/28 ✅ (100%)
- **Backlog** : Nettoyé — 200 lignes (était 1375, -85%)
- **Code smells historiques** : 12/12 résolus (CS-1→12 ✅)
- **Nouveaux smells** : 10 (CS-13→22) → 3 EPICs de finalisation (4.5 pts)
- **Build** : ✅ Zéro warning
- **Coverage** : 77.25% (seuil 75% ✅)

== CS-FIN-1 — Bug critique (P0, 2 pts)

*Fichier* : `BakeryPlugin.kt`

Deux bugs superposés :
1. **CS-21** : `configPath.get()` (ligne 47) lève `MissingPropertyException` si configPath jamais défini
2. **CS-16** : `isGradlePropertiesEnabled` (ligne 40) calculé dans `apply()` → toujours `false`,
   écrasement silencieux de la config DSL

*Correction* :
- Ajouter `isPresent` guard avant `configPath.get()`
- Déplacer `isGradlePropertiesEnabled` dans `afterEvaluate`

== Boroughs candidats (si switch)

| Borough | Projet | DAG | Statut | Pourquoi |
|---|---|---|---|---|
| New Jersey | `runner-gradle` | N3 | Sess 011 | Orchestration DAG, cœur MVP0 |
| Newark 🔴 | `training-gradle` | N2 | Sess 010 (6/8) | Pipeline formation, livrable Graal |
| Brooklyn | `codex-gradle` | N2 | Sess 018 | Asciidoctor→PDF, pipeline doc |
| Manhattan | `planner-gradle` | N2 | Sess 008 | LLM SPG/SPD, point d'entrée MVP0 |

== Méthodologie obligatoire — Baby Steps DDD/TDD/BDD

1. DDD — Identifier le comportement manquant
2. TDD — Test unitaire AVANT implémentation
3. BDD — Cucumber si comportement utilisateur
4. Vérification — `./gradlew test` après chaque fichier
5. Coverage — `./gradlew koverXmlReport koverPrintCoverage` après chaque ajout
