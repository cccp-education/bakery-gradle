= PROMPT REPRISE — Bakery
:session: 157
:date: 2026-06-24

== Mission en cours (session 157)

*Session 156 TERMINÉE — README i18n 10 langues (consumer + plugin).*
20 fichiers README créés dans `foundry/public/bakery-gradle/` :
- `README.consommateurs/` (Consumer Guide) — 10 langues
- `README.plugin/` (Plugin Internals) — 10 langues
Langues : EN master, FR, ZH, HI, ES, AR (RTL), BN, PT, RU, UR (RTL).
Badges shields.io, markers version 0.0.2, Cucumber 129/129 PASS, switcher i18n.
Aucun code source modifié.

== Problème à résoudre

Reprendre la piste 155 : aligner les 2 premiers YAML pivots
(`quick-start.yaml`, `ab-partition.yaml`) pour figer le contrat homogène :
convertir leurs tables de `text:` simple vers `inline[]` (mix texte+code),
comme révélé par le 3e fichier `scripts.yaml`.

Puis coder le parser AsciiDoc→YAML en baby steps DDD/TDD/BDD :
- **Golden master test** : parser AsciiDoc→YAML→régénérer AsciiDoc = byte-identique
- Le test pilote le parser sans toucher au code production
- Domaine pur d'abord (data classes / sealed classes), puis tests unitaires, puis BDD

== Contexte schéma YAML pivot

* Bac à sable : `/tmp/opencode/bakery-i18n-lab/magic-stick/` (volatile, recréer).
* 3 YAML pivots : `quick-start.yaml`, `ab-partition.yaml`, `scripts.yaml`.
* 8 types blocs : `heading`, `paragraph`, `list`, `table`, `admonition`, `source`, `hr`, `frontmatter`
* 4 types inline : `text`, `bold`, `code`, `link`
* Règle : `translatable: true` = feuille éditoriale à traduire ; `false` = structure/code/URL
* `plantuml` = pass-through (`translatable: false`), plugin plantuml-gradle gère son cycle
* Cellules table : `inline[]` (pas `text:` simple) — mix texte+code
* Table sans `[cols=...]` : `cols: null` (auto-détection)

== Notes techniques

* Ports Ollama : 11437-11465. Modèles : `gpt-oss:120b-cloud`, `gemma4:31b-cloud`.
* `./gradlew test --tests "bakery.i18n.*"` = validation rapide i18n (3/3 AutoTranslation PASS).
* Timeout cucumberTest 5min (EPIC 5) — ne pas lancer `check` complet sans permission.
* `publishToMavenLocal` fait (jar 0.0.2).
* *Loi de l'Économie d'Encre* : encodée dans 10 boroughs. Toute tâche LLM DOIT
  load-before-write + calcul delta + fusion existing+new.
* Bac à sable `/tmp/opencode/bakery-i18n-lab/` = volatile (recréer depuis
  `office/sites/magic-stick/jbake/` + les YAML dans le rapport session 155).