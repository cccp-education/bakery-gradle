= PROMPT_REPRISE — Session 026 prête : BKY-JB-0 Scaffolding site JBake
:toc:

== Mission accomplie (Session 025)

BKY-BKG Step 1-3 (18 UT + 3 FT PASS) :
- Modèle domaine ArticleGraph + RelatedArticlesService (scoring tags/titres/stop words)
- Tâche collectRelatedArticles (COLLECT_GROUP, dependsOn collectSiteContext)
- Fix SiteContextCollector : tags `<category>` + préfixe `/` URLs

== Nouveau cap — Roadmap Expert Métier JBake

*Document de référence* : `.agents/BAKERY_ROADMAP_JBake.adoc`

La vision a changé. Bakery n'est pas un assembleur de formation. Bakery est **l'expert métier JBake** (site statique). On monte en puissance sur son domaine avant de l'utiliser dans la formation.

*IA centralisée* : codebase (N1) est le SEUL provider IA (koog orchestre, langchain4j exécute). BKY-IA-0 à dépiler.

== Prochaine mission (026) — BKY-JB-0 Scaffolding site JBake

Objectif : enrichir `generateSite` pour qu'il propose une structure JBake solide (choix du type de site, templates par défaut, services optionnels).

Phases roadmap JBake :
  A. Scaffolding + templates + composants (JB-0→2, ~18 pts)
  B. Services : Google Forms, Firebase, Analytics (JB-3→5, ~13 pts)
  C. Thème + layouts (JB-6→7, ~8 pts)
  D. IA via codebase : génération article assistée (JB-8, ~13 pts)

Ordre P0 :
  BKY-JB-0 → BKY-JB-1 → BKY-JB-2 → BKY-JB-3 → BKY-JB-8 (IA via codebase)

== Rappels

- GroupId: `education.cccp` — Plugin ID: `cccp.education.bakery`
- `./gradlew -q publishToMavenLocal` après toute modification du code source
- Pool Docker 11437-11465 (gpt-oss:120b-cloud) géré par runner-gradle N3, pas bakery N2
