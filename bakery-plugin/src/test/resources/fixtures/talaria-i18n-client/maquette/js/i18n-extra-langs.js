/* ------------------------------------------------------------------ *
 * talaria-i18n-client — BDD fixture — additive patch
 *
 * `de` owns a block with no key yet — the delta must fill it entirely.
 * ------------------------------------------------------------------ */
(function(){
  TALARIA.I18N.extend({
    de: {
    }
  });
})();
