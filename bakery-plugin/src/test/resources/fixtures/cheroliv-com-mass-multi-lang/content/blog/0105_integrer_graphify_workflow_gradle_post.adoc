= Intégrer Graphify dans un workflow Gradle : du Knowledge Graph au diagramme PlantUML en une commande
:jbake-title: Intégrer Graphify dans un workflow Gradle : du Knowledge Graph au diagramme PlantUML en une commande
:jbake-date: 2026-04-19
:jbake-type: post
:jbake-status: published
:jbake-tags: gradle, graphify, plantuml, knowledge-graph, automatisation, documentation, devops
:jbake-author: Cheroliv
:jbake-description: Comment intégrer Graphify dans un pipeline Gradle pour générer automatiquement des diagrammes PlantUML à partir du Knowledge Graph de votre codebase. Pipeline déterministe, zéro LLM, diagrammes versionnés.
:jbake-summary: Graphify extrait un Knowledge Graph de votre codebase. Le plugin Gradle PlantUML transforme ce graphe en diagrammes PlantUML déterministes. Pipeline complet : pip install graphify → graphify . --no-viz → ./gradlew generateKnowledgeGraphDiagram → diagrammes PNG versionnés dans Git.
:jbake-slug: integrer-graphify-workflow-gradle-plantuml
:imagesdir: ./images
:source-highlighter: highlight.js
:icons: font
:idprefix:
:toc: left
:toclevels: 4
:toc-title: Table des matières
:sectnums:
:jbake-reading-time: 12 minutes

.temps de lecture : {jbake-reading-time}

[.lead]
Votre codebase grandit. Les dépendances entre modules se multiplient. La documentation architecture devient obsolète avant même d'être écrite. Et si votre build Gradle pouvait _automatiquement_ générer des diagrammes à jour à partir de la structure réelle du code ? C'est exactement ce que fait le pipeline Graphify + PlantUML Gradle Plugin : `graphify . --no-viz` extrait le Knowledge Graph, `./gradlew generateKnowledgeGraphDiagram` le transforme en diagrammes PlantUML. Zéro LLM, zéro manuel, 100% déterministe.

toc::[]

== Le problème : des diagrammes toujours en retard sur le code

Tout projet qui dépasse quelques milliers de lignes connaît ce syndrome :

[arabic]
. On dessine un diagramme d'architecture au début du projet
. Le code évolue, les dépendances changent
. Le diagramme devient un mensonge décoratif
. Personne ne le met à jour parce que c'est pénible
. Les nouveaux arrivants se basent dessus et font des erreurs

[plantuml,probleme-diagrammes-obsoletes,svg]
----
@startuml
skinparam backgroundColor #FEFEFE
skinparam ActivityBackgroundColor #FDEDEC
skinparam ActivityDiamondBackgroundColor #FFF3CD

start

:Diagramme d'architecture créé\n(manuellement, à la main);

:Le code évolue\n(nouvelles classes, nouvelles dépendances);

:Diagramme obsolète\n(ne reflète plus le code);

if (Qui met à jour ?) then (Personne)
    :Documentation mensongère;
    note right: Les nouveaux développeurs\nse basent dessus... et font des erreurs
else (Quelqu'un)
    :Mise à jour manuelle\n(2-3 heures de travail);
    note right: Déjà obsolète\nà la prochaine PR
endif

stop

@enduml
----

La question n'est pas _faut-il des diagrammes ?_ — tout le monde sait que oui. La question est : **qui les maintient à jour ?**

La réponse : personne. Sauf si c'est automatique.

== La solution : un pipeline déterministe Knowledge Graph → PlantUML

Le principe est simple : au lieu de dessiner les diagrammes à la main, on les **génère depuis la structure réelle du code**.

[plantuml,pipeline-complet,svg]
----
@startuml
skinparam backgroundColor #FEFEFE
skinparam componentStyle rectangle

actor Développeur
component "Graphify\n(pip install graphifyy)" as Graphify
collections "graphify-out/graph.json\n(Knowledge Graph)" as KGJSON
component "PlantUML Gradle Plugin\n(generateKnowledgeGraphDiagram)" as Plugin
component "KnowledgeGraphParser" as Parser
component "KnowledgeGraphRenderer" as Renderer
component "PlantumlService\n(validation + rendu PNG)" as PS
collections "diagrams/knowledge-graph/\n(.puml + .png)" as Output

Développeur --> Graphify : graphify . --no-viz
Graphify --> KGJSON : extrait la structure\ndu code source
Développeur --> Plugin : ./gradlew generateKnowledgeGraphDiagram
Plugin --> Parser : parse(graph.json)
Parser --> Plugin : KnowledgeGraph\n(noeuds + arêtes + communautés)
Plugin --> Renderer : render(graph, filters)
Renderer --> Plugin : code PlantUML\ndéterministe
Plugin --> PS : validateSyntax + generateImage
PS --> Output : .puml + .png

note bottom of Plugin
  Pipeline DÉTERMINISTE
  Aucun appel LLM
  Résultat reproductible
end note

@enduml
----

Deux commandes. C'est tout.

[source,bash]
----
# Étape 1 : extraire le Knowledge Graph
graphify . --no-viz

# Étape 2 : générer les diagrammes PlantUML
./gradlew generateKnowledgeGraphDiagram
----

Le résultat ? Des fichiers `.puml` et `.png` dans `diagrams/knowledge-graph/`, versionnés dans Git, toujours à jour avec le code.

== Étape 1 : Installer Graphify et extraire le Knowledge Graph

=== Installation

Graphify est un outil Python qui analyse votre codebase et construit un graphe de connaissances structuré :

[source,bash]
----
# Méthode recommandée
uv tool install graphifyy && graphify install --platform opencode

# Alternative avec pip
pip install graphifyy && graphify install --platform opencode
----

=== Configurer les exclusions

Créer un fichier `.graphifyignore` à la racine du projet pour exclure les fichiers qui ne font pas partie de la logique métier :

[source,gitignore]
----
# Secrets — JAMAIS dans le graphe
*-context.yml
*.env

# Fichiers générés
build/
.gradle/

# Tests fonctionnels
src/functionalTest/
----

=== Extraire le Knowledge Graph

[source,bash]
----
graphify . --no-viz
----

Le flag `--no-viz` saute la génération HTML (inutile dans un pipeline Gradle). Le résultat est un fichier `graphify-out/graph.json` contenant :

* **Noeuds** : classes, fonctions, fichiers — avec leur type et communauté
* **Arêtes** : relations entre noeuds (EXTRACTED depuis le code, INFERRED par l'LLM)
* **Communautés** : regroupements automatiques de noeuds liés

.Exemple de structure `graph.json`
[source,json]
----
{
  "nodes": [
    {"id": "0", "label": "LlmService", "file_type": "code", "community": 0},
    {"id": "1", "label": "ApiKeyPool", "file_type": "code", "community": 0},
    {"id": "2", "label": "PlantumlService", "file_type": "code", "community": 1}
  ],
  "links": [
    {"source": "1", "target": "0", "relation": "uses", "confidence": "EXTRACTED", "weight": 0.9},
    {"source": "0", "target": "2", "relation": "calls", "confidence": "INFERRED", "weight": 0.7}
  ]
}
----

[NOTE]
====
Le code source (`.kt`, `.java`) est analysé localement par tree-sitter, **sans appel LLM**. Seuls les fichiers de documentation (`.adoc`, `.md`) nécessitent un appel LLM pour l'extraction sémantique. Donc `--update` sur du code Kotlin est quasi instantané.
====

== Étape 2 : Le plugin Gradle transforme le Knowledge Graph en PlantUML

=== Architecture du pipeline

Le plugin `com.cheroliv.plantuml` intègre une tâche `generateKnowledgeGraphDiagram` qui transforme `graph.json` en diagrammes PlantUML de manière **totalement déterministe** :

[plantuml,architecture-pipeline-kg,svg]
----
@startuml
skinparam backgroundColor #FEFEFE

participant "Gradle" as G
participant "GenerateKnowledgeGraphDiagramTask" as Task
participant "KnowledgeGraphParser" as Parser
participant "KnowledgeGraphRenderer" as Renderer
participant "PlantumlService" as PS

collections "graphify-out/graph.json" as JSON

G -> Task : execute
Task -> JSON : read
Task -> Parser : parse(graph.json)
Parser --> Task : KnowledgeGraph\n(noeuds + arêtes + communautés)
Task -> Renderer : render(graph, filters)
Renderer --> Task : code PlantUML\n(déterministe)
Task -> PS : validateSyntax(plantumlCode)
alt syntaxe valide
    Task -> PS : generateImage → .png
else syntaxe invalide
    Task -> Task : save .puml avec warning
end

@enduml
----

=== Les composants internes

[cols="1,3", options="header"]
|===
| Composant | Rôle

| `KnowledgeGraphParser`
| Parse `graph.json` — supporte 3 formats : graphify natif (`nodes`+`links`), legacy (`communities`), flat. Résout les IDs numériques en labels.

| `KnowledgeGraphRenderer`
| Transforme déterministement un `KnowledgeGraph` en code PlantUML. Groupes par type, communautés en packages, légende automatique.

| `GenerateKnowledgeGraphDiagramTask`
| Tâche Gradle qui orchestre : parse → render → validate → PNG. Configurable via propriétés Gradle.

| `kgmodels.kt`
| Modèles de données : `KnowledgeGraph`, `KnowledgeGraphNode`, `KnowledgeGraphEdge`, `KnowledgeGraphCommunity`, `EdgeType`.

| `PlantumlService`
| Validation syntaxique + rendu PNG (réutilisé par toutes les tâches du plugin).
|===

=== Règles de rendu

Le renderer applique des conventions visuelles déterministes :

[cols="2,2,3", options="header"]
|===
| Type d'arête | Notation PlantUML | Signification

| EXTRACTED | `-->` (trait plein, noir) | Relation extraite du code source (certitude)
| INFERRED | `..>` (trait pointillé) | Relation inférée par l'LLM (score de confiance)
| AMBIGUOUS | `--x` (trait rouge pointillé) | Relation ambiguë (à vérifier)
|===

Les communautés sont rendues comme des packages PlantUML avec une palette de couleurs automatique.

== Étape 3 : Utilisation au quotidien

=== Diagramme complet

[source,bash]
----
# Générer le diagramme du Knowledge Graph complet
./gradlew generateKnowledgeGraphDiagram
----

Sortie : `diagrams/knowledge-graph/knowledge-graph-full.puml` + `.png`

=== Filtrer par communauté

[source,bash]
----
# Une seule communauté
./gradlew generateKnowledgeGraphDiagram \
  -Pplantuml.kg.community=community_0

# Limiter le nombre de noeuds (lisibilité)
./gradlew generateKnowledgeGraphDiagram \
  -Pplantuml.kg.community=community_0 \
  -Pplantuml.kg.maxNodes=15
----

=== Filtrer par type d'arête

[source,bash]
----
# Uniquement les relations certaines (EXTRACTED)
./gradlew generateKnowledgeGraphDiagram \
  -Pplantuml.kg.edgeTypes=EXTRACTED

# Relations certaines + inférées
./gradlew generateKnowledgeGraphDiagram \
  -Pplantuml.kg.edgeTypes=EXTRACTED,INFERRED
----

=== Filtrer par type de noeud et confiance

[source,bash]
----
# Uniquement les classes de code
./gradlew generateKnowledgeGraphDiagram \
  -Pplantuml.kg.nodeTypes=code

# Seuil de confiance minimum (pour les INFERRED)
./gradlew generateKnowledgeGraphDiagram \
  -Pplantuml.kg.minConfidence=0.7
----

=== Répertoire de sortie personnalisé

[source,bash]
----
./gradlew generateKnowledgeGraphDiagram \
  -Pplantuml.kg.outputDir=docs/architecture
----

=== Référence complète des propriétés

[cols="1,1,3", options="header"]
|===
| Propriété | Défaut | Description

| `plantuml.kg.community`
| _(toutes)_
| Filtrer les communautés par nom (correspondance sous-chaîne)

| `plantuml.kg.edgeTypes`
| _(tous)_
| Types d'arêtes séparés par virgules : `EXTRACTED`, `INFERRED`, `AMBIGUOUS`

| `plantuml.kg.minConfidence`
| `0.0`
| Seuil minimum de confiance pour les arêtes

| `plantuml.kg.maxNodes`
| _(illimité)_
| Nombre maximum de noeuds à afficher

| `plantuml.kg.nodeTypes`
| _(tous)_
| Types de noeuds séparés par virgules (ex. `class`, `code`)

| `plantuml.kg.outputDir`
| `diagrams/knowledge-graph`
| Répertoire de sortie pour les fichiers `.puml` et `.png`
|===

== Le pipeline complet dans un workflow Gradle

=== Workflow type

[plantuml,workflow-complet,svg]
----
@startuml
skinparam backgroundColor #FEFEFE
skinparam ActivityBackgroundColor #E8F4FD
skinparam ActivityDiamondBackgroundColor #FFF3CD
skinparam ActivityBorderColor #2C3E50

start

partition "EXTRACTION" {
    :graphify . --no-viz;
    note right: Analyse le code source\ntree-sitter (local, pas de LLM)
    
    :graphify-out/graph.json;
    note right: Noeuds + arêtes\n+ communautés
}

partition "GÉNÉRATION" {
    :./gradlew generateKnowledgeGraphDiagram;
    note right: Pipeline déterministe\nparse → render → validate → PNG
    
    if (Filtres actifs ?) then (Oui)
        :Appliquer filtres\n(community, edgeTypes,\nmaxNodes, nodeTypes);
    else (Non)
        :Diagramme complet;
    endif
    
    :diagrams/knowledge-graph/\nknowledge-graph-full.puml\nknowledge-graph-full.png;
}

partition "INTÉGRATION" {
    :git add diagrams/knowledge-graph/;
    :git commit;
    note right: Diagrammes versionnés\ntoujours à jour avec le code
}

stop

@enduml
----

=== Intégration dans le cycle de développement

Le pipeline s'intègre naturellement dans les étapes clés du développement :

[plantuml,cycle-developpement,svg]
----
@startuml
skinparam backgroundColor #FEFEFE

state "Développement" as dev
state "Extraction\ngraphify . --no-viz" as extract
state "Génération\n./gradlew generateKnowledgeGraphDiagram" as generate
state "Commit\ndiagrammes versionnés" as commit

[*] --> dev
dev --> extract : Code modifié
extract --> generate : graph.json à jour
generate --> commit : .puml + .png générés
commit --> dev : Diagrammes dans le repo

note right of extract
  Quasi instantané sur du code Kotlin
  (tree-sitter, pas de LLM)
end note

note right of generate
  Déterministe
  Pas de LLM
  Résultat reproductible
end note

@enduml
----

=== Mise à jour incrémentale

Quand le code change, on ne reconstruit pas tout le graphe depuis zéro :

[source,bash]
----
# Mise à jour incrémentale (fichiers modifiés uniquement)
graphify . --update

# Puis régénérer les diagrammes
./gradlew generateKnowledgeGraphDiagram
----

[NOTE]
====
`--update` re-extrait uniquement les fichiers modifiés (détectés par SHA256 dans `graphify-out/cache/`). Sur du code Kotlin, c'est quasi instantané car tree-sitter travaille localement sans appel LLM.
====

== Le dogfooding : le plugin se documente lui-même

Le plugin PlantUML existe pour transformer des prompts en diagrammes. Il peut aussi transformer le Knowledge Graph de sa __propre__ codebase en diagrammes de documentation. C'est du _dogfooding_ : le plugin consomme son propre service.

[plantuml,dogfooding-pipeline,svg]
----
@startuml
skinparam backgroundColor #FEFEFE
skinparam componentStyle rectangle

package "Pipeline normal\n(utilisateur → diagrammes)" as normal {
    [Fichier .prompt] as prompt
    [LlmService\n+ ApiKeyPool] as llm1
    [ProcessPlantumlPromptsTask] as task1
    [Diagramme PNG] as out1
    
    prompt --> task1
    task1 --> llm1
    llm1 --> out1
}

package "Pipeline Knowledge Graph\n(déterministe, pas de LLM)" as kg {
    [graphify-out/graph.json] as kgjson
    [KnowledgeGraphParser] as parser
    [KnowledgeGraphRenderer] as renderer
    [PlantumlService] as ps
    [Diagramme PNG\n(documentation du plugin)] as out2
    
    kgjson --> parser
    parser --> renderer
    renderer --> ps
    ps --> out2
}

package "Pipeline Dogfooding\n(LLM → documentation du plugin)" as dogfood {
    [GraphifyPromptAdapter] as gpa
    [Fichiers .prompt\nauto-générés] as auto_prompt
    [LlmService\n+ ApiKeyPool] as llm2
    [ProcessPlantumlPromptsTask] as task2
    [Diagramme PNG\n(documentation LLM)] as out3
    
    kgjson --> gpa
    gpa --> auto_prompt
    auto_prompt --> task2
    task2 --> llm2
    llm2 --> out3
}

note "Même LlmService, même ApiKeyPool,\nmême PlantumlService\n— ZÉRO duplication" as N

@enduml
----

Tâches Gradle associées :

[cols="1,1,3", options="header"]
|===
| Tâche | LLM ? | Description

| `generateKnowledgeGraphDiagram`
| Non
| Transforme `graph.json` en PlantUML (déterministe)

| `generateDiagramDocs`
| Oui
| Génère des `.prompt` depuis le graphe, les traite via LLM (dogfooding)
|===

[source,bash]
----
# Documentation déterministe (rapide, pas de LLM)
./gradlew generateKnowledgeGraphDiagram

# Documentation LLM (plus riche, consomme des tokens)
./gradlew generateDiagramDocs
----

== Pourquoi c'est déterministe (et pourquoi ça compte)

Le point clé du pipeline `generateKnowledgeGraphDiagram` : **il n'appelle aucun LLM**. La transformation `graph.json` → PlantUML est une fonction pure.

[plantuml,determinisme-vs-llm,svg]
----
@startuml
skinparam backgroundColor #FEFEFE
skinparam componentStyle rectangle

rectangle "Pipeline DÉTERMINISTE\n(generateKnowledgeGraphDiagram)" as det {
    [graph.json] as json
    [KnowledgeGraphParser] as parser
    [KnowledgeGraphRenderer] as renderer
    [PlantUML code] as puml
    
    json --> parser : parse
    parser --> renderer : KnowledgeGraph
    renderer --> puml : render (fonction pure)
}

rectangle "Pipeline LLM\n(processPlantumlPrompts)" as llm {
    [.prompt] as prompt
    [LlmService] as llmSvc
    [ChatModel] as model
    [PlantUML code] as puml2
    
    prompt --> llmSvc
    llmSvc --> model : API call
    model --> puml2 : réponse non-déterministe
}

note bottom of det
  Même entrée → même sortie
  Pas de latence réseau
  Pas de coût en tokens
  Reproductible en CI
end note

note bottom of llm
  Même entrée → sortie variable
  Latence réseau (1-5s)
  Coût en tokens
  Nécessite une clave API
end note

@enduml
----

Les avantages concrets :

[cols="1,3", options="header"]
|===
| Avantage | Impact

| Reproductibilité
| Même `graph.json` → même diagramme. Exactement. Tous les fois.

| Zéro coût
| Pas d'appel LLM = pas de tokens = pas de facture API.

| Zéro latence
| Parse + render prend ~100ms, pas 1-5 secondes.

| Compatible CI
| Pas de clé API nécessaire. Pas de flaky test dû à une réponse LLM variable.

| Versionnable
| Le `.puml` généré est du texte. On peut le `diff`, le reviewer en PR, le versionner dans Git.
|===

== Diagrammes du pipeline lui-même

Pour boucler la boucle, voici le diagramme du pipeline tel qu'il serait généré par le plugin :

[plantuml,pipeline-sequence,svg]
----
@startuml
skinparam backgroundColor #FEFEFE

participant "Développeur" as Dev
participant "Gradle" as G
participant "GenerateKnowledgeGraphDiagramTask" as Task
participant "KnowledgeGraphParser" as Parser
participant "KnowledgeGraphRenderer" as Renderer
participant "PlantumlService" as PS
collections "graphify-out/graph.json" as JSON
collections "diagrams/knowledge-graph/" as Out

Dev -> G : ./gradlew generateKnowledgeGraphDiagram
G -> Task : execute
Task -> JSON : read
Task -> Parser : parse(graph.json)
Parser --> Task : KnowledgeGraph

alt filtres actifs
    Task -> Task : appliquer filtres\n(community, edgeTypes, maxNodes…)
end

Task -> Renderer : render(graph, filters)
Renderer --> Task : code PlantUML

Task -> PS : validateSyntax()
alt valide
    Task -> PS : generateImage()
    PS --> Out : .puml + .png
else invalide
    Task -> Out : .puml (avec warning)
end

@enduml
----

== Intégration dans la gouvernance de projet

Dans notre projet, ce pipeline s'intègre dans une stratégie de gestion de contexte EAGER/LAZY pour l'agent IA. Le Knowledge Graph remplace la documentation architecture manuelle par un graphe structuré, queryable, et auto-mis à jour.

[plantuml,strategie-hybride,svg]
----
@startuml
skinparam backgroundColor #FEFEFE
skinparam componentStyle rectangle

package "EAGER\n(toujours chargé)" as eager {
    [PROMPT_REPRISE.adoc\n(contexte session)] as pr
    [INDEX.adoc\n(vue d'ensemble)] as idx
    [GRAPH_REPORT.adoc\n(~50 lignes)] as gr
}

package "LAZY — Stratégie\n(sur demande)" as lazy_strat {
    [Méthodologies] as meth
    [Archives sessions] as sessions
}

package "LAZY — Graphify\n(requêtes ciblées)" as lazy_graph {
    [graph.json] as gj
    [Queries\n(query/path/explain)] as queries
}

package "Pipeline PlantUML\n(auto-généré)" as puml {
    [generateKnowledgeGraphDiagram] as kgTask
    [generateDiagramDocs] as ddTask
    [Diagrammes PNG] as diagrams
}

Agent --> eager : Lit en début de session
Agent ..> lazy_strat : Charge si type détecté
Agent ..> lazy_graph : Query si besoin structurel
kgTask --> diagrams : Déterministe
ddTask --> diagrams : Via LLM

note right of gr
  Condensé du Knowledge Graph
  God nodes + communautés
  Remplace ECOSYSTEM_OVERVIEW
  (225 lignes → 50 lignes)
end note

@enduml
----

Le mariage des deux systèmes est complémentaire :

* **La stratégie gère le QUAND et le COMMENT** — gouvernance, workflow, archivage
* **Graphify gère le QUOI et le OÙ** — structure du code, relations, requêtes ciblées

[quote]
____
La stratégie de session gère le **QUAND** et le **COMMENT** (gouvernance, workflow, seuils), Graphify gère le **QUOI** et le **OÙ** (structure du code, relations, requêtes ciblées). Le pipeline PlantUML gère le **AVEC QUOI** (diagrammes déterministes, versionnés, toujours à jour).
____

== Mise en place : checklist en 5 minutes

[cols="1,3,1", options="header"]
|===
| # | Étape | Commande

| 1 | Installer Graphify | `uv tool install graphifyy`
| 2 | Configurer les exclusions | Créer `.graphifyignore`
| 3 | Extraire le Knowledge Graph | `graphify . --no-viz`
| 4 | Générer les diagrammes | `./gradlew generateKnowledgeGraphDiagram`
| 5 | Committer les résultats | `git add diagrams/knowledge-graph/`
|===

[source,bash]
----
# Script complet en 5 commandes
uv tool install graphifyy
cat > .graphifyignore << 'EOF'
*-context.yml
*.env
build/
.gradle/
EOF
graphify . --no-viz
./gradlew generateKnowledgeGraphDiagram
git add graphify-out/GRAPH_REPORT.adoc graphify-out/graph.json diagrams/knowledge-graph/
----

== Pièges et mitigations

[cols="2,3,3", options="header"]
|===
| Piège | Description | Mitigation

| Graphe trop dense
| Un gros projet génère des centaines de noeuds illisibles
| Utiliser `-Pplantuml.kg.maxNodes=30` et filtrer par communauté

| Exclusions trop larges
| Trop de fichiers dans `.graphifyignore` réduit la valeur du graphe
| Commencer par exclure seulement credentials et build

| Direction des flèches
| Les arêtes INFERRED peuvent avoir une direction ambiguë
| Filtrer par `-Pplantuml.kg.edgeTypes=EXTRACTED` pour les relations certaines uniquement

| Graphe obsolète
| Le code change mais le graphe n'est pas reconstruit
| Utiliser `graphify . --update` régulièrement ou le hook git `graphify hook install`

| Coût mis à jour
| Les mises à jour incrémentales sont quasi gratuites (tree-sitter local)
| Seules les docs (`.adoc`) consomment des tokens LLM
|===

== Ce qu'on obtient au final

[plantuml,recap-visuel,svg]
----
@startuml
skinparam backgroundColor #FEFEFE

rectangle "AVANT" as avant {
    card "Diagrammes dessinés à la main\nToujours obsolètes\nPersonne ne les met à jour" as av1 #FDEDEC
}

rectangle "APRÈS" as apres {
    card "Diagrammes générés automatiquement\nToujours à jour avec le code\nVersionnés dans Git\nDéterministes et reproductibles" as ap1 #E8F8E8
}

avant --> apres : graphify . --no-viz\n+ ./gradlew generateKnowledgeGraphDiagram

@enduml
----

Les bénéfices concrets :

[cols="1,3", options="header"]
|===
| Bénéfice | Détail

| Documentation toujours à jour
| Les diagrammes reflètent le code actuel, pas un snapshot manuel

| Zéro effort de maintenance
| Les diagrammes se régénèrent à chaque build

| Réduction de la dette technique
| Plus besoin de maintenir les diagrammes à la main

| Contexte visuel pour les nouveaux
| Un nouveau développeur comprend l'architecture en regardant les diagrammes

| Fine-tuning potentiel
| Les paires (sous-graphe → diagramme) sont des exemples d'entraînement IA

| Validation automatique
| `PlantumlService.validateSyntax()` vérifie chaque diagramme généré

| Onboarding rapide
| 5 diagrammes = vue complète de l'architecture
|===

[quote, Friedrich Nietzsche]
____
Celui qui a un _pourquoi_ peut supporter tous les _comment_.
____

Le _pourquoi_ : des diagrammes toujours à jour. Le _comment_ : deux commandes dans un pipeline Gradle.

== Liens

* https://github.com/nicholasgasior/graphify[Graphify sur GitHub]
* https://github.com/cheroliv/plantuml-gradle[PlantUML Gradle Plugin]
* link:/blog/2026/0104_fg_command_super_tips_post.html[Le super tips de la commande fg] — article précédent sur le workflow terminal