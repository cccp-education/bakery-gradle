<!DOCTYPE html SYSTEM "http://www.thymeleaf.org/dtd/xhtml1-strict-thymeleaf-4.dtd">
<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:th="http://www.thymeleaf.org"
      th:lang="${config.site_language}" data-bs-theme="light">
<body>
<!-- =============================================================================================================== -->
<!-- Contact Section -->
<!-- =============================================================================================================== -->
<section id="contact"
         class="py-5"
         th:fragment="contact-section">
    <div class="container">
        <div class="row">
            <div class="col-lg-12 text-center mb-5">
                <h2 class="display-5 fw-bold" th:text="#{nav.contact}">Contact</h2>
                <p class="text-muted" th:text="#{contact.subtitle}">Une question ? Un projet ? Contactez-moi !</p>
            </div>
        </div>
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="contact-form">
                    <form id="contact-form">
                        <div style="position:absolute;left:-9999px" aria-hidden="true">
                            <input type="text" name="hp_name" tabindex="-1" autocomplete="off" />
                        </div>
                        <div class="mb-3">
                            <input type="text"
                                   name="name"
                                   class="form-control"
                                   th:placeholder="#{contact.form.name.placeholder}"
                                   placeholder="Nom"
                                   required />
                        </div>
                        <div class="mb-3">
                            <input type="email"
                                   name="email"
                                   class="form-control"
                                   th:placeholder="#{contact.form.email.placeholder}" placeholder="Email"
                                   required />
                        </div>
                        <div class="mb-3">
                            <input type="tel"
                                   id="phone"
                                   name="phone"
                                   class="form-control"
                                   th:placeholder="#{contact.form.phone.placeholder}" placeholder="Téléphone (Optionnel)" />
                        </div>
                        <div class="mb-3">
                            <input type="text"
                                   name="subject"
                                   class="form-control"
                                   th:placeholder="#{contact.form.subject.placeholder}" placeholder="Sujet"
                                   required />
                        </div>
                        <div class="mb-3">
                            <textarea class="form-control"
                                      name="message"
                                      rows="5"
                                       th:placeholder="#{contact.form.message.placeholder}" placeholder="Votre message"
                                      required></textarea>
                        </div>
                        <div class="text-center">
                            <button type="submit" class="btn btn-primary">
                                <i class="bi bi-send me-2"></i>
                                <span th:text="#{contact.form.submit}">Envoyer le Message</span>
                            </button>
                        </div>
                    </form>
                    <div id="contact-success-message" class="alert alert-success mt-3" role="alert" style="display:none">
                        <i class="bi bi-check-circle me-2"></i>
                        <span th:text="#{contact.success.message}">Message envoyé avec succès ! Je vous répondrai dans les plus brefs délais.</span>
                    </div>
                    <div id="contact-error-message" class="alert alert-danger mt-3" role="alert" style="display:none">
                        <i class="bi bi-exclamation-triangle me-2"></i>
                        <span th:text="#{contact.error.message}">Une erreur est survenue lors de l'envoi. Veuillez réessayer.</span>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
</body>
</html>
