= Historique des Sessions — Bakery Gradle Plugin

  _Derniere mise a jour : 2026-09-25 — Session 235 ✅ — **BKY-LANG-NAV-6 TERMINÉ — rollout cheroliv.com** (sélecteur same-page, premier rollout site). Antérieur S-234 : NAV-5 (BDD de clôture du cœur, same-page deux hôtes) — cœur clos. S-233 : re-pin catalogue ws 0.0.57. S-232 : NAV-4 (modèle ex-nihilo conforme). S-231 : NAV-3 (hôte JS + garde de parité). S-230 : NAV-2 (hôte Thymeleaf). S-229 : NAV-1 (rule pure + vecteurs). S-228 : cadrage._

  |===
  | # | Date | Type | Sujet | Score
  | 235 | 2026-09-25 | cross-borough DDD/TDD/BDD — rollout site | **BKY-LANG-NAV-6 TERMINÉ — rollout cheroliv.com (sélecteur same-page)** — Premier rollout site de l'EPIC BKY-LANG-NAV (cœur clos S-234). **Travail cross-borough** dans le repo `office/sites/cheroliv.com` (build Gradle autonome, hors `foundry/` — zéro source bakery modifiée). Le site possédait sa propre chaîne `com.cheroliv.i18n` (`LangSwitchLink`/`LangSwitchMenuRenderer`/`LangSwitchSiteApplier`/`LangSwitchBlockInjector`/`InjectLangSwitchMain`) qui **dupliquait** la règle page-blind → split-brain exactement comme D5 du cadrage S-228. **Décision D5 confirmée** : consommer la règle commune plutôt que retirer la copie. **DDD** : `com.cheroliv.i18n.LangSwitchPath` règle pure page-aware (frère du `bakery.langswitch.LangSwitchPath`), expression `th:href` évaluée par page au bake. **D3** : vecteurs partagés (`bakery/langswitch/lang-switch-path-vectors.json`) épinglés côté site + `LangSwitchPathDriftGuardTest` (garde anti-fork, skip en CI isolé — jamais un faux vert) + replay ≥ 11 vecteurs via le `TemplateEngine` réel. **Régression de rollout découverte et corrigée** : le bake échouait sur les pages synthétiques (`archive`/`tags`/masterindex) qui rendent `menu.thyme` **sans** `content.uri` → `NullPointerException: target is null for method lastIndexOf` (5 templates non rendus) ; règle rendue null-safe, dégradation vers l'index de langue (D8) — **Thymeleaf 3.0.14 OGNL n'accepte pas l'opérateur Elvis `?:`** (ternaire explicite retenu). **17 menus régénérés** page-aware + **idempotence prouvée** (`changed: 0`). **Dogfooding bake réel** (`bakeLocalSite`) : page imbriquée FR `blog/2019/0004_…` → EN `../../en/blog/2019/0004_…` (page préservée, pas l'accueil), EN → FR `../../../blog/2019/0004_…` ; `hreflang`/`canonical` conservés (D9). Baby-step strict RED (`Unresolved reference 'LangSwitchPath'` → 5 tests FAILED ; puis NPE prouvée par test) → GREEN → REFACTOR. Gates **site** : `test` **185/0**, `functionalTest` **29/0** (1 skipped), `nodeTest` **10/0**, `cucumberTest` 31 tests **5 échecs = baseline S-052** (prose non convergée, pool `ok=0`), bake PASSED. `check` ROUGE piloté par le corpus non convergé. Non-régression bakery : `FixtureAlignmentAuditorTest` + `RealSiteI18nMigrationIntegrationTest` = **FLAKY-5 préexistants** (drift `blog_section.thyme`, chrome FR S-053/055), **stash-prouvés indépendants de NAV-6**. **Dette découverte** : `bakery.langswitch.LangSwitchPath.thymeleafHref()` porte le **même bug null-safety latent** (son modèle `site/templates/archive.thyme` rend `menu.thyme`) — à aligner en session dédiée (cœur clos S-234). Rapport `.agents/sessions/235-bky-lang-nav-6-rollout-cheroliv.adoc` (+ détail site `.agents/sessions/056-…`). | 9/10
  | 234 | 2026-09-25 | BDD Cucumber (bout en bout) — clôture cœur | **BKY-LANG-NAV-5 TERMINÉ — BDD de clôture du cœur (same-page, deux hôtes)** — Cinquième US du cœur de l'EPIC BKY-LANG-NAV (cadré S-228 ; NAV-1 socle S-229, NAV-2 Thymeleaf S-230, NAV-3 JS S-231, NAV-4 modèle S-232). **Diagnostic de cadrage** : NAV-5 doit prouver (D3) que les **deux hôtes** issus des sessions précédentes résolvent *la même page* — l'hôte **injecté** (`injectLangSwitch` : `LangSwitchMenu` → `LangSwitchThymeleafRenderer` → `LangSwitchInjector`, chaîne de production réelle) **et** l'hôte **modèle** ex-nihilo (`site-basic/templates/menu.thyme`, sélecteur `th:each`). Le cadrage S-228 avait écarté la dépendance serveur ; l'évaluation au `TemplateEngine` réel par page est le niveau de preuve pertinent (rendu déterministe), un bake JBake complet n'aurait rien prouvé de plus sur la règle. **Livré baby-step BDD strict** : feature `66_same_page_language_switch.feature` `@same-page` **6 scénarios** — FR→EN page imbriquée (`../en/blog/foo.html`, pas l'accueil), EN→FR page imbriquée (`../../blog/foo.html`, pas l'accueil), no self-loop (self EN = `foo.html` + classe `active`), **tous les liens == `LangSwitchPath.resolveSamePage`**, hôte modèle ex-nihilo page-aware, **accord mutuel injecté ∪ modèle**. Fixture temporaire à deux arbres (FR racine, EN sous `en/`) → `content.uri` = chemin dans l'arbre, `content.rootpath` remonte à sa racine (hypothèse NAV-2 vérifiée S-230/S-232). `SamePageLanguageSwitchSteps` (injection chaîne production + rendu réel + extraction `data-lang` + comparaison règle) + runner dédié `SamePageCucumberTestRunner` (pattern S-082, imports explicites ktlint-clean). **Baby-step strict** : RED prouvé (feature + runner **sans** steps → `358 tests completed, 6 failed` = 6× `UndefinedStepException` au XML JUnit) → GREEN (**6/6 PASSED**) → REFACTOR (`lateinit` sur propriété initialisée corrigé ; runner en imports explicites, 0 violation ktlint sur les 3 fichiers). **Gates** : `test` **2007** (2 FAIL = FLAKY-5 préexistants, baseline S-233 exacte), `cucumberTest` **2864 tests, 5 failed** = **baseline exacte stash-prouvée** (`git stash push -u` → 2464/5, delta = +400 tests de la feature, zéro nouvel échec), `functionalTest` VERT (UP-TO-DATE), `nodeTest` 7/7, `bakery.langswitch.*` 72 UT GREEN (NAV-1→4 non régressés), `publishToMavenLocal` VERT (Règle 0), gate métier = 0. 3 fichiers de test créés, **zéro production modifiée**, zéro bump, zéro publication Central, zéro secret. **Le cœur BKY-LANG-NAV est clos** (NAV-1→5) ; reste rollout sites NAV-6→8 (~13 pts). Rapport `.agents/sessions/234-bky-lang-nav-5-bdd-same-page.adoc`. | 9/10
  | 233 | 2026-09-25 | maintenance build + TDD strict (re-pin cross-borough) | **Re-pin catalogue ws 0.0.57 + réalignement drift MEM-CAT-3** — Demande pilote : « re-pin bakery ws 0.0.57 » + « réalignement drift catalogue si MEMPHIS bump ». **Contexte** : working tree occupé par une session bakery concurrente (WIP BKY-LANG-NAV-4) → pause (commit sélectif piège #25), reprise après clôture concurrente (`4320987`). **Drift mesuré** (`dependencyInsight`) : le borough pinnait `workspace-catalog:0.0.56` + platform `workspace-bom:0.0.48` (neutralisée par résolution transitive) → résolvait `document-plugin 0.0.17` alors que MEMPHIS a publié **`0.0.18`** au catalogue `0.0.57` (delta `0.0.56→0.0.57` = `document-plugin 0.0.17→0.0.18`, seule entrée). **Réalignement** : (1) `settings.gradle.kts` pin catalogue `0.0.56 → 0.0.57` ; (2) `build.gradle.kts` **3 platform pins** `workspace-bom:0.0.48 → 0.0.57` (dépendances principales + `functionalTest` + `e2eTest`) ; (3) `gradle/libs.versions.toml` 2 entrées `education.cccp` stale référencées et silencieusement neutralisées par le BOM : `codebase-plugin 0.0.12 → 0.0.16` + `i18n-contracts 0.0.3 → 0.0.4` (résolution inchangée, toml désormais truthful). **Baby-step TDD strict RED → GREEN → REFACTOR** : nouvelle garde `BakeryPluginPublicationTest` `every workspace bom platform pin matches ws catalog bom version` — RED 1 FAILED (les 3 pins `[0.0.48,0.0.48,0.0.48]` invisibles car neutralisés) → GREEN 3/3 → REFACTOR (de `doesNotContain("0.0.48")` fragile à l'extraction regex de **tous** les pins + `containsOnly(wsBomVersion)` — détecte tout drift futur). **Preuves** : `dependencyInsight` **`document-plugin → 0.0.18`** ; résolution `codebase-plugin → 0.0.16` / `i18n-contracts → 0.0.4` confirmée. Version plugin bakery **inchangée** (`0.0.19`) — re-pin de consommation, **zéro publication Central**. Gates : `test` **2007** (+1 net = nouvelle garde, 2 FAIL = FLAKY-5 préexistants stash-confirmés — drift fixture cheroliv.com `blog_section.thyme` « Close » vs « Fermer », sans rapport), `functionalTest` BUILD SUCCESSFUL 2m45s, 4 source sets compilent, `publishToMavenLocal` VERT (Règle 0), zéro secret. Piège #25 re-confirmé (WIP concurrent → commit sélectif, attendre). Rapport `.agents/sessions/233-repin-catalog-ws-0.0.57-drift-realign.adoc`. | 9/10
  | 232 | 2026-09-25 | DDD + TDD strict + functional Oven + garde de conformité (rendu réel) | **BKY-LANG-NAV-4 TERMINÉ — modèle ex-nihilo conforme (sélecteur page-aware)** — Quatrième US du cœur de l'EPIC BKY-LANG-NAV (cadré S-228 ; NAV-1 socle S-229, NAV-2 Thymeleaf S-230, NAV-3 JS S-231). **Constat S-228 soldé** : les deux menus du modèle (`site-basic/templates/menu.thyme` **et** `site/templates/menu.thyme`), copiés *verbatim* par `generateSite`, émettaient un href **page-blind** `'/' + ${lang.code} + '/'` → « un site neuf naît buggé ». Le modèle utilise un **seul `th:each` sur `${supportedLanguages}`** partagé par toutes les pages (ni page courante ni langue cible à l'injection, note S-230 conciliée) → nouveau **frère symbolique dynamique** `LangSwitchPath.thymeleafEachHref(defaultLang)` : projection de la règle unique sur une boucle, expression à 4 branches lisant `lang.code` (cible) et `config.site_language` (courante) **au bake** (self / défaut→non-défaut / non-défaut→défaut / non-défaut→non-défaut). **Preuve anti split-brain D3 étendue au 3e hôte** : `LangSwitchPathEachHrefTest` (4 UT) rejoue **11/14 vecteurs partagés** (`lang-switch-path-vectors.json`) via le `TemplateEngine` réel — la même fixture que la règle pure (NAV-1) et l'hôte JS (NAV-3). **Garde `LangSwitchModelGuard`** (objet pur, D7) : présence **ET** page-awareness (`lang-option` seul serait un faux vert), rejette `missing-selector`/`missing-data-lang`/`page-blind-href`/`missing-page-awareness`. Modèle pinné **byte-à-byte** contre `thymeleafEachHref("fr")`. **Preuves** : `LangSwitchModelGuardTest` (9 UT — garde pure 5, ressources embarquées 2, **rendu réel** des 2 menus embarqués 2 : chaque option `data-lang` résolue page-aware, attrape une `th:each`/`th:href` cassée qu'une assertion texte manquerait) ; FT `LangSwitchScaffoldFunctionalTest` 1 → **3** (site BLOG + site BASIC scaffoldés conformes, TestKit réel). Baby-step strict : RED prouvé (`Unresolved reference 'thymeleafEachHref'` puis `'LangSwitchModelGuard'`) → GREEN → REFACTOR. Gates : `bakery.langswitch.*` **72 UT GREEN** (NAV-1/2/3 non régressés), `functionalTest` BUILD SUCCESSFUL 4m54s, `test` **2006 tests, 2 FAIL = FLAKY-5 préexistants** (baseline 1993 → **+13 nets**), `cucumberTest` **2464/5 = baseline S-229/S-230/S-231 exacte**, ktlint 0 sur le delta, `publishToMavenLocal` VERT, gate métier = 0. **Limitation notée** : l'expression du modèle embarque `'fr'` (défaut `SiteConfiguration`, pas de `config.default_language` injecté) — cas nominal conforme, suite possible. **NAV-5 débloqué** (DAG `NAV-2, NAV-4 → NAV-5`). 3 créés, 4 modifiés, zéro bump, zéro secret. Rapport `.agents/sessions/232-bky-lang-nav-4-modele-ex-nihilo.adoc`. | 9/10
  | 231 | 2026-09-25 | DDD + TDD (Kotlin) + node + FT Oven + garde de parité | **BKY-LANG-NAV-3 TERMINÉ — hôte JS `lang-switch.js` + garde de parité (maquette ∪ jbake)** — Troisième US du cœur de l'EPIC BKY-LANG-NAV (cadré S-228 ; NAV-1 socle S-229, NAV-2 hôte Thymeleaf S-230). L'**hôte JS** de la règle pure est livré : `resolveLangPath(...)` est le **frère JS** de `LangSwitchPath.resolveSamePage`, rejouant **les mêmes 14 vecteurs partagés** (`lang-switch-path-vectors.json`) — la preuve anti split-brain D3 cesse d'être théorique côté navigateur. Adaptateur DOM `attachLangSwitch` : mode `tree` (réécrit les liens `data-lang` page-aware au load + marque la langue active ; **ne navigue jamais** — le `th:href` serveur reste la cible) / mode `dictionary` (talaria : relocalise **en place**, `preventDefault`, délègue `TALARIA.I18N.setLang`). `bootstrap()` auto-câble sur `DOMContentLoaded` (mode/langue déclarés par `<html data-lang-switch lang>`). Module navigateur plain (zéro build step), copié **byte-identique** dans `maquette/js/` et `site/assets/js/`. **Garde `LangSwitchClientParity`** (objet pur, D6 « présence ET comportement ») : présence dans les **deux** arbres + byte-identité + surface de contrat (`resolveLangPath` **et** `attachLangSwitch`) — `setLang` seul serait un faux vert. **Preuves** : node `lang-switch.test.mjs` (7 tests, double DOM-free — `fakeElement`/`fakeDocument`, **pas de jsdom**) rejoue chaque vecteur + adaptateur ; `LangSwitchClientParityTest` (7 UT, dont `ShippedResources` lit les ressources réellement embarquées du classpath) ; `LangSwitchScaffoldFunctionalTest` (1 FT Oven : un site scaffoldé naît conforme D7, module des deux côtés byte-identique). **Infrastructure** : tâche `nodeTest` (`node --test`, groupe `verification`, `download=false` pattern talaria) + `check.dependsOn(nodeTest)` + CI `actions/setup-node@v4` (node 22). Baby-step strict : RED prouvé (`compileTestKotlin` `Unresolved reference 'LangSwitchClientParity'` puis node `ERR_TEST_FAILURE`) → GREEN → REFACTOR. Gates : `bakery.langswitch.*` 10 classes GREEN (NAV-1/2 non régressés), `functionalTest` BUILD SUCCESSFUL 2m32s, `test` **1993 tests, 2 FAIL = FLAKY-5 préexistants** (baseline 1986 → **+7 nets**), `cucumberTest` **2464/5 = baseline S-229/S-230 exacte**, ktlint 0 sur le delta, `publishToMavenLocal` VERT, gate métier = 0. **Incident méthodologique** : `ktlintFormat` global a reformaté **126 fichiers hors périmètre** (baseline de style rouge sur tout le dépôt) → tous revertés (`git checkout`), seuls les fichiers du delta conservés, ligne `version =` préexistante restaurée à la main. **NAV-4 débloqué** (DAG `NAV-3 → NAV-4`). 5 créés, 2 modifiés, zéro bump, zéro secret. Rapport `.agents/sessions/231-bky-lang-nav-3-js-host-parity.adoc`. | 9/10
  | 230 | 2026-09-24 | DDD + TDD strict + FT + BDD | **BKY-LANG-NAV-2 TERMINÉ — hôte Thymeleaf page-aware (lien par défaut exact)** — Seconde US de l'EPIC BKY-LANG-NAV (cadré S-228 ; socle NAV-1 S-229). `bakery.langswitch.LangSwitchPath.thymeleafHref(currentLang, targetLang, defaultLang)` : le **frère symbolique** de `resolveSamePage` — émet un `th:href` en `${content.rootpath}` / `${content.uri}`, évalué **par page au bake**. Contrainte DDD structurante : `injectLangSwitch` injecte dans `menu.thyme`, un template **partagé par toutes les pages** d'une langue → aucune « page courante » n'existe à l'injection ; le lien doit donc être une *expression*, pas une valeur concrète (le choix symbolique n'est pas cosmétique, il est imposé par le point d'injection). 4 cas : self-link → `${content.uri.substring(content.uri.lastIndexOf('/') + 1)}` (nom de fichier seul, pas de relance de navigation) ; défaut → non-défaut → `${content.rootpath + 'en/' + content.uri}` ; non-défaut → défaut → `|../${content.rootpath}${content.uri}|` (sort de l'arbre de langue puis rejoue la page) ; non-défaut → non-défaut → `|../${content.rootpath}ar/${content.uri}|`. Note technique : **Thymeleaf 3.0.14 ne dispose pas de `#strings.substringAfterLast`** (sources du jar vérifiées) → expression OGNL `content.uri.substring(...)` retenue et **prouvée par évaluation réelle** dans le `TemplateEngine`. Hôtes consommateurs : `LangSwitchUrl.thymeleafHref()` délègue à la règle unique (D3), `resolve()` reste la cible de dégradation page-blind (arbre incomplet, D8) ; `LangSwitchThymeleafRenderer` émet `link.thymeleafHref()` au lieu de `link.resolve()` (fin de l'`index.html` en dur), `data-lang` conservé (D4). **Preuve anti split-brain D3** : `LangSwitchPathThymeleafHrefTest` (9 UT) rejoue **11 des 14 vecteurs partagés via le TemplateEngine réel** (les 3 vecteurs `existingPages` = dégradation au build, hors de portée d'une expression statique, D8 documenté) + `LangSwitchRendererEvaluationTest` (4 UT) prend la **sortie réelle du renderer**, l'évalue par page et l'égale à `resolveSamePage` — la preuve D3 au niveau de l'hôte réellement câblé. `LangSwitchUrlThymeleafHrefTest` (5 UT). `LangSwitchThymeleafRendererTest` réécrit page-aware (13 UT). FT `InjectLangSwitchFunctionalTest` réécrit (8/8 : sortie page-aware, plus de `'index.html'` hardcodé, idempotence OK). BDD feature 51 réécrite (5/5 page-aware PASSED), feature 55 alignée + step `FR menu page-aware` (runner dédié 0 échec). Baby-step strict : RED unit prouvé (2× `Unresolved reference 'thymeleafHref'`, puis 4× pour `LangSwitchUrl`) → GREEN → REFACTOR. Gates : `bakery.langswitch.*` 63 UT GREEN (19 NAV-1 non régressés), `functionalTest` BUILD SUCCESSFUL 3m35s, `test` **1986 tests, 2 FAIL = FLAKY-5 préexistants** (stash-prouvé : les 2 tombent aussi sur arbre propre), `cucumberTest` **2464/5 = baseline S-229 exacte** (aucun échec dans Language Switcher), ktlint 0 sur le delta (263 violations préexistantes sur fichiers non touchés), `publishToMavenLocal` VERT, gate métier `grep -rwE "REAC\|AFNOR\|SPG\|SPD\|FPA\|RNCP\|CDA" src/` = 0. 3 créés, 9 modifiés, zéro bump, zéro secret. **NAV-4 reporté** (cadrage DAG `NAV-3 → NAV-4` ; le `site-basic/templates/menu.thyme` utilise en outre un `th:each` dynamique `${supportedLanguages}` — design symbolique distinct). Rapport `.agents/sessions/230-bky-lang-nav-2-thymeleaf-host.adoc`. | 9/10
  | 229 | 2026-09-24 | DDD + TDD strict | **BKY-LANG-NAV-1 TERMINÉ — règle pure `LangSwitchPath` + vecteurs partagés (socle du cœur NAV)** — Première US de l'EPIC BKY-LANG-NAV (cadré S-228, décision pilote S2 + hybride). Objet pur `bakery.langswitch.LangSwitchPath.resolveSamePage(currentPageUri, currentLang, targetLang, defaultLang, pageExists)` : normalise (trim + `trimStart('/')`), retire le préfixe de langue courante, rejoue la page sous la langue cible, relativise **depuis le répertoire de la page courante**. Self-link → nom de fichier seul (`foo.html`), jamais de relance de navigation. Dégradation `<lang>/index.html` si la cible n'existe pas (D8, prédicat `pageExists` injectable). **14 vecteurs partagés JSON** (`src/main/resources/bakery/langswitch/lang-switch-path-vectors.json`, `version: 1`) = le contrat anti-split-brain D3 que rejouera l'hôte JS (NAV-3) : une divergence Kotlin ↔ JS fait rougir la fixture d'un côté. Tests : `LangSwitchPathTest` 17 UT explicites (self, racine FR→EN, page imbriquée EN→FR, `en/blog/foo`→`ar/blog/foo`, langue défaut, profondeur, slash, invariants de validation) + `LangSwitchPathVectorsTest` 2 UT fixture-driven. Baby-step strict : RED prouvé (4× `Unresolved reference 'LangSwitchPath'` au `compileTestKotlin`) → GREEN 19/19 → REFACTOR (fix indentation ktlint). Gates : `bakery.langswitch.*` 17 UT GREEN (12 `LangSwitchUrlTest` historiques non régressés), `InjectLangSwitchFunctionalTest` 7/7 + idempotence, ktlint 0 sur les 4 fichiers NAV-1, `publishToMavenLocal` VERT. 4 fichiers créés, **0 fichier suivi modifié**, zéro bump, zéro secret. **Découverte FLAKY-5** : `test` complet = 2 FAIL préexistants (`FixtureAlignmentAuditorTest` + `RealSiteI18nMigrationIntegrationTest`, drift fixture cheroliv.com — chrome nativement FR S-053/055, 5 templates : blog/blog_section/footer/header/menu) — **stash-prouvé identique sur arbre propre** (idem `cucumberTest` `2464 tests, 5 failed, 2011 skipped`), zéro régression. Rapport `.agents/sessions/229-bky-lang-nav-1-spec-pure.adoc`. | 9/10
  | 228 | 2026-09-24 | cadrage EPIC (gouvernance pure) + audit cross-site | **EPIC BKY-LANG-NAV CADRÉ — changement de langue sans retour à l'accueil (same-page switching)** — Demande pilote : en changeant de langue, arriver sur la **traduction de la page courante**, pas l'accueil de la langue demandée. **Audit** : le défaut est **structurel** (résolution d'URL, pas JS) — `LangSwitchUrl.resolve()` (`src/main/kotlin/bakery/langswitch/LangSwitchUrl.kt:23-31`) connaît la langue mais jamais la page (`index.html` en dur) ; le même bug est **copié** sur cheroliv.com (`com.cheroliv.i18n.LangSwitchLink.thymeleafHref()` + `LangSwitchMenuRenderer`/`LangSwitchSiteApplier`/`LangSwitchBlockInjector`/`InjectLangSwitchMain`) → **split-brain**. Le mapping page→page existe déjà sur cheroliv **côté SEO** (`jbake/templates/header.thyme:8-16`, `hreflang` via `content.uri`) — patron à réutiliser. **Cartographie 4 sites** : cheroliv (arbres de pages → bug réel), talaria (dictionnaire client `TALARIA.I18N.setLang` → marche déjà, relocalise en place), cccp.education + magic-stick (**aucun i18n** → feature à créer). Constat : le modèle ex-nihilo (`site-basic`/`maquette`) n'embarque aucun sélecteur → un site neuf naît buggé. **Arbitrage pilote (3 tours)** : pilote préfère **S2** (JS) ; correction de prémisse (Thymeleaf sur générateur statique ne demande **pas** de serveur — tâche `bake` testée sans serveur) → le bon motif de S2 est l'**uniformité maquette ∪ JBake ∪ cross-site** ; pilote valide l'**hybride** (SEO + FOUC) et confirme **garder Thymeleaf**. **Décision actée** : arbre → Thymeleaf résout au build (exact, crawlable, no-JS, sans FOUC), JS surenchérit (persistance/active) ; dict client → JS résout ; parade anti-split-brain = **une règle pure, deux hôtes, vecteurs de test partagés**. **Livrable** : doc `.agents/CADRAGE_EPIC_BKY_LANG_NAV.adoc` (10 décisions, 9 US ~32 pts, DAG, projection, risques) + BACKLOG (section + status final + v24.13) + TODO + INDEX (en-tête + EPIC en attente). **Zéro code** (Economie d'Encre : sources inchangées, aucun build/test). Projection S-229 NAV-1 (spec pure) → S-235+ NAV-8. Rapport `.agents/sessions/228-bky-lang-nav-cadrage.adoc`. | 9/10
  | 227 | 2026-09-17 | cross-borough + DDD/TDD/BDD (slider) + diagnostic | **Dette `asciidoctorRevealJs` SOLDÉE (cross-borough slider S-072)** — Reprise bakery à la demande pilote : les 2 missions du PROMPT_REPRISE 227 (GBL-006 COPY MULTI-MARCHÉ ~31 pts, MASS-6 ~675h LLM) étaient **bloquées par accord pilote** ; reste la dette locale actionnable zéro coût externe — rendu `asciidoctorRevealJs` Gradle cassé (gems JRuby, préexistant S-023/S-034). Arbitrage pilote : mission B. **Constat de périmètre** : `asciidoctorRevealJs` est un task asciidoctor-gradle appliqué/configuré **côté slider** (`RevealJsTaskRegistrar`) ; bakery n'use AsciidoctorJ que via JBake + diagram, sans revealjs — la dette listée en gouvernance bakery était en réalité **slider**. **Diagnostic (côté propriétaire)** : le symptôme « gems » recouvre deux causes. (1) **Double JRuby sur le worker OUT_OF_PROCESS** : la tâche résout le défaut asciidoctor-gradle `asciidoctorj:3.0.0` → `jruby-complete:9.4.8.0` (gem `jar-dependencies` **0.4.1**) tandis que le classpath plugin (workspace-bom / codebase-plugin) contraint `asciidoctorj:3.0.1` → `jruby-stdlib:9.4.14.0` (**0.5.4**) ; JRuby active 0.4.1 puis échoue sur 0.5.4 → `Gem::LoadError: can't activate jar-dependencies-0.5.4, already activated jar-dependencies-0.4.1`, `asciidoctorGemsPrepare` avorte, jar de gems **vide** (261 o) → `LoadError: no such file to load -- asciidoctor`. (2) JRuby isolé GradleRunner (3 tests Playwright, distinct, S-034). **Correctif baby-step DDD/TDD/BDD strict (repo slider)** : US-1 `slider.extension.AsciidoctorJPin` value object pur (défaut 3.0.1, invariant non-blank, RED 3 `Unresolved` → GREEN 3/3) ; US-2 `SliderExtensionRegistrar.pinAsciidoctorJ` (pose `AsciidoctorJExtension.setVersion`) ; US-3 `AsciidoctorJPinFunctionalTest` (TestKit, configuration `__$$asciidoctorj_asciidoctorRevealJs$$__r`, **RED prouvé par stash** → GREEN, second test « single JRuby » retiré non discriminant) ; US-4 `18_extension_decomposition.feature` 9 → 12 scénarios. **Preuve dogfooding from-scratch** : nettoyage complet puis `asciidoctorRevealJs` → `Successfully installed asciidoctor-revealjs-5.2.0` + BUILD SUCCESSFUL + **80 decks HTML** (RTL ar/fa/ur), jar de gems 261 o → **10,9 Mo** ; contre-preuve stash (LoadError réapparaît). **Bonus** : `generatePlaywrightFixtures` débloqué → les **3 tests Playwright préexistants S-217 passent**. **Bump slider 0.0.19 → 0.0.20** (0.0.19 Central immuable S-226). Gates slider post-bump `--rerun-tasks` : `test` **1102/0**, `functionalTest` **46/0**, `cucumberTest` **273/0** (1 skip), `check` VERT. Zéro code bakery modifié (Economie d'Encre : gate bakery non relancée, sources inchangées). Rapport bakery `.agents/sessions/227-asciidoctorj-pin-cross-borough.adoc` + rapport slider `.agents/sessions/072-asciidoctorj-pin-revealjs-repare.adoc`. | 9/10
  | 226 | 2026-09-17 | dogfooding LLM réel + fix cross-borough + publication Central + clôture EPIC | **BKY-LANG-22-8/9/10 TERMINÉ — EPIC BKY-LANG-22 clos 10/10 US** (accord pilote LLM + publication accordés). **8 — dogfooding slider 22 langues** : `gpt-oss:120b-cloud` à sa limite mensuelle → bascule `gemma4:31b-cloud` (ports 11439-11444, `OLLAMA_POOL_PORTS`). 3 sources fr/en/ar × 12 nouvelles langues = **36 paires** générées via `translateDeck` réel (19s + 3×~1m). Corpus final **63 adoc + 63 context = 126 fichiers** (21 cibles/source). Pin levé : `TranslatedDemoDeckTest.dogfoodTargetCodes` **dérivé de `LanguageCatalog.supportedCodes()`** (RED 8 assertions pinnées → GREEN 261/0). Drift préexistant corrigé : `7_rtl_layout.feature` (« RTL exactly Arabic and Urdu », S-217 a ajouté `fa` sans maj — stash-confirmé) → catalog-driven. Feature 14 : 9→21 adoc/source, totaux 27→63, exemples `fa`. Gates : 261 UT + cucumber 270/0 + FT 45/0. **9 — dogfooding capsule 22 langues** : **bug P0 réel exposé** (les tests NoOp le masquaient) — chaîne réelle slider `translateDeck` → `extractSpeakerNotes` écrit `<slug>_<lang>-deck-script.txt` (suffixe `-deck`), or `CapsuleVideoPlanner` cherchait `<deckName>_<lang>-script.txt` → `no matching deck + script pair` fail-fast (S-082 contournait par `mv` manuel). Fix baby-step DDD/TDD/BDD : `CapsuleVideoPlanner.resolveScript` préfère l'exact, tolère le suffixe `-deck` (miroir S-082 côté sortie WebM). RED prouvé → GREEN. BDD 14/14. Dogfooding réel consumer composite : rendu HTML via Ruby système (blocage gems JRuby Gradle préexistant S-023/S-034, orthogonal) → `generateCapsuleVideoAllLanguages --rerun-tasks` → **21/22 rendered + 1 skipped (Économie d'Encre) = 22/22 vidéos** espeak+Playwright, 26-81s. Dégradation propre `th`/`ja`/`ar` (voix espeak absentes → vidéo silencieuse, zéro crash). Gate capsule 1168/0. **10 — publication Central** ordre DAG réel révélé par les gardes MEMPHIS D8 (anti-ghost) : **capsule d'abord** (infra nmcp créée : settings `com.gradleup.nmcp.settings:1.5.0` + publishing mavenCentral + version 0.0.1 + fix POM marker `name`/`description` sur `withType<MavenPublication>`) → bundle `54a58526` ; puis **MEMPHIS 0.0.48** (bakery 0.0.13 + capsule 0.0.1 + slider 0.0.19 ; gardes `PlatformCatalogAlignmentTest`/`WorkspaceCatalogStructureTest`/`VersionCatalogSteps` 12→13 plugins) → `9d43c5b7` ; puis **bakery 0.0.13** (`3d53b77e`, pin ws 0.0.48 + drift BOM 0.0.42 purgé) ; **slider 0.0.19** (`03289d70`). Propagation repo1 HTTP 200 vérifiée (~6-13 min). Piège nmcp : re-run pendant validation → collision deployment. **EPIC BKY-LANG-22 clos 10/10.** Rapport `.agents/sessions/226-bky-lang-22-8-9-10-cloture-epic.adoc`. | 9/10
  | 225 | 2026-09-17 | fix + TDD strict | **Fix `ArticleGenerator` — 22 langues au lieu de 2 (GBL-006 § 7)** — Découvert lors de la revue globale S-217 (workspace racine) en auditant le moteur de copy pour la stratégie « tous les marchés en même temps » (22 langues × 3 plateformes). **Le bug** : `ArticleIntention.lang` est validé contre les **22 langues** du contrat N0 (`BakeryConstants.SUPPORTED_LANGS = LanguageCatalog.supportedCodes()`) mais `buildPrompt(intention)` n'avait que **2 branches** (`when(lang) { "en" -> "Write the article in English."; else -> "Rédige l'article en français (langue : fr)." }`) → l'article était demandé **en français** pour hi/bn/zh/ar/ur/es/pt/ru/it/nl/de/el/tr/vi/th/id/ko/ja/sr/fa, soit **20 langues sur 22** (la validation promettait 22, l'exécution en faisait 2). **Fix baby-step TDD strict** : RED (3 tests — `names the Hindi language for lang hi`, `does not claim French for a non French language` (bn), `names each of the 22 catalog languages` itérant `LanguageCatalog.ALL` → 3 `AssertionFailedError`) → GREEN (`languageInstruction(code)` : nom anglais + nom natif `Hindi (हिन्दी)` + code ISO, `findByCode` N0, défensif sur code inconnu) → REFACTOR (suite complète **1 845 tests / 0 échec / 0 erreur**, `publishToMavenLocal` VERT règle 0). Diff : +25/-5 prod, +48 test. Commit `f6cc35b`. **Ce que le fix ne fait pas** : il rend la langue *correcte*, pas la copy *native* — demander un article « en hindi » ≠ écrire « pour le marché hindi » (c'est l'EPIC transverse **GBL-006 COPY MULTI-MARCHÉ** : `ContentSeed` → `MarketProfile[22]` → `PlatformFormat[3]` → `CopyPiece`, distinction **traduire ≠ écrire**, 8 US ~31 pts, cadré `CADRAGE_EPIC_GBL_006_COPY_MULTIMARKET.adoc` au workspace racine). Zéro bump BOM, zéro secret, zéro publication Central. Rapport `.agents/sessions/225-article-generator-22-langues.adoc`. | 9/10
  | 224 | 2026-09-17 | DDD + TDD + BDD + functional Oven + dogfooding réel + REFACTOR | **BKY-I18N-JS-6 TERMINÉ — traduction du contenu catalogue imbriqué (writer + garde de structure)** — Reprise bakery à la demande pilote : les 3 pistes du PROMPT_REPRISE S-223 (LANG-22-8/9 dogfooding LLM, LANG-22-10 publication Central, MASS-6 ~675h LLM) **toutes bloquées par accord pilote** ; mission = le chantier distinct acté S-223 décision §1 (« la couverture n'est pas l'écriture »). **Diagnostic** : le catalogue imbriqué (`i18n-content.js`, 98881 octets, 22 langues → formations → scalaires + listes `objectives[]`/`modules[]` à objets inline) n'était **jamais traduit** — le parser plat y voit 0 clé et `I18nCatalog` ne faisait que compter les lacunes. Le writer plat (`insertTranslations`) corromprait la structure (append de paires quotées dans un objet à clés non quotées). **Implémentation baby-step DDD/TDD/BDD strict** : (1) `I18nJsObjectParser` (internal, parseur récursif descendant objet/tableau/chaîne JS, chaîne-aware, skippe commentaires, conserve les byte-ranges des littéraux). (2) `I18nCatalogDocument` (aplatissement pathé `fpa.title`/`fpa.objectives[0]`/`fpa.modules[0].desc` + `missingLiterals`). (3) `I18nCatalogWriter` (`translate` substitution in-place par byte-ranges ; `complete` contrat S-054 : langue absente → clone de la structure de référence, langue incomplète → complétée, valeur existante > traduction > référence). (4) `I18nCatalogDelta` (plan de delta imbriqué). (5) `I18nCatalogFormatGuard` (`unbalanced-catalogue-block`, `language-structure-drift`, `round-trip-drift`, rewriter injectable). (6) `I18nJsStringCodec` REFACTOR (escape/unescape partagé plat+imbriqué — le bug historique du backslash n'est pas dupliqué). (7) **Câblage** `TranslateI18nClientTask` : partition plat/catalogue étendue au **delta** (report+traduction), complétion des formations/champs manquants, création d'une langue absente, garde en production (résultat invalide jamais écrit), échec total = zéro écriture (Économie d'Encre). 2 bugs attrapés par TDD : raw string Kotlin ≠ saut de ligne (test d'échappement) et le writer initial ne faisait que substituer (le contrat S-054 exige la complétion). **Tests** : 28 DDD UT (document 7 + writer 11 + delta 5 + guard 5) + 5 UT task + 5 UT dogfooding réel talaria (22 langues, no-op strict, garde verte) + 3 FT Oven (11/11) + feature 64 14 → **17 scénarios**. Gate `test` **1842/0 FAIL** (baseline 1804/0 → +38), `functionalTest --rerun-tasks` 132/132, feature 64 17/17, `publishToMavenLocal` VERT, grep métier = 0. **BKY-I18N-JS réellement 6/6 US** (couverture S-223 + écriture S-224). | 9/10
  | 223 | 2026-09-17 | DDD + TDD + BDD + dogfooding réel + REFACTOR | **BKY-I18N-JS-5 TERMINÉ — catalogue structurel : ownership + couverture (l'EPIC BKY-I18N-JS était 4/4 à tort)** — Reprise bakery à la demande pilote : les 3 pistes du PROMPT_REPRISE S-222 (LANG-22-8/9 dogfooding LLM, LANG-22-10 publication Central, MASS-6 ~675h LLM) **toutes bloquées par accord pilote** ; mission = dette locale actionnable zéro coût externe (profil FLAKY-4 S-222). **Diagnostic** : le cadrage talaria verrouillé S-054 (`office/sites/talaria.school/.agents/CADRAGE_EPIC_T_I18N_BAKERY.adoc`) déclare **3** dictionnaires i18n JS ; le 3e, `maquette/js/i18n-content.js` (`TALARIA.I18N.CATALOG`), est **imbriqué** (langues → formations `fpa`/`cda` → scalaires + listes `objectives[]`/`modules[]`) avec un contrat explicite (« bloc absent = langue entière manquante ; si le bloc existe, les champs manquants »). Or `I18nJsDictionary` est **plat** (regex clés quotées `"key": "value"`) → dans le catalogue les clés sont **non quotées** (`title:`, `fpa:`) donc **0 clé** extraite : le catalogue n'était **jamais traduit**. **Bug latent plus grave** : le catalogue réutilise l'indentation 4 espaces, donc `I18nClientDelta.languageOwners` (« premier fichier qui possède le bloc ») le désignait **propriétaire des 22 langues** car le tri réel `walkTopDown().sortedBy { relativePath }` place `i18n-content.js` en tête — une clé chrome `fr` ajoutée aurait été insérée **dans le catalogue**. Dormant tant que le corpus est complet (plan vide). La tâche ne partitionnait pas (`--i18nClientSource=…/maquette/js` inclut le catalogue) et les tests ne le voyaient pas (fixture chrome+patch seulement). **Fix baby-step DDD/TDD/BDD strict** : (1) `I18nJsFormat` (enum pur `FLAT`/`CATALOG`, `of(source)` sur marqueur `TALARIA\.I18N\.CATALOG\s*=`, marqueur autoritaire, 6 UT) + `I18nClientDelta.languageOwners` saute les sources CATALOG (RED prouvé : garde retirée → 2 UT `LanguageOwners` FAILED ; `+2 UT`). (2) `I18nCatalog` (object pur `languagesOf`/`formationsOf`/`fieldsOf` — parser imbriqué indentation 4/6/8, scanner string-aware — + `plan(source, reference, targets): I18nCatalogPlan`) + `I18nCatalogPlan` (languages/missingLanguages/missingFormations/missingFields + compteurs + `hasGap` + `EMPTY` ; plan vide si la référence n'a pas de bloc catalogue), 11 UT. (3) `TranslateI18nClientTask` : partition `catalogueFiles`/`flatFiles` **avant** garde de format et delta plat (le catalogue ne passe **jamais** au writer plat ni à `I18nClientFormatGuard`), report `logCatalogueCoverage` (lacune → `warn` détaillé langue/formation/champ, complète → `lifecycle`, sans catalogue → silence Économie d'Encre), seam `internal catalogueCoverage(intention)`, 4 UT. (4) REFACTOR : `I18nJsBlockScanner` (scanner balanced-brace string-aware partagé) extrait — `I18nJsDictionary` délègue (-37 lignes), fin de la duplication. (5) **Dogfooding réel** talaria lecture seule : 4 UT (catalogue 22 langues × 2 formations `fpa`/`cda`, chaque langue porte les mêmes champs de référence, classification, ownership réel = chrome/patch jamais le catalogue). (6) **BDD feature 64 étendue 10 → 14 scénarios** (`@i18n-client` : capture, langue manquante, formation+champs, no-op) + 4 steps + générateur de catalogue. Gate `test` **1804 tests, 0 FAIL, 0 erreur, 3 skipped** (baseline S-222 1777/0 → **+27 net**), feature 64 **14/14**, `functionalTest --rerun-tasks` **130/130 BUILD SUCCESSFUL 4m28s**, `publishToMavenLocal` VERT, gate métier `grep -rwE "REAC|AFNOR|SPG|SPD|FPA|RNCP|CDA" src/` = 0. Incident infra : 2× `:test` `java.io.EOFException` (worker mort, load 21-27 + swap plein, sessions concurrentes) — non lié au code, relance verte. Décision : le catalogue est une **couverture** (le report), pas une écriture plate — la traduction du contenu imbriqué (writer + validation de structure) est un chantier distinct. 5 créés, 8 modifiés. Zéro secret, zéro bump BOM. Rapport `.agents/sessions/223-BKY-I18N-JS-5-catalogue-ownership-coverage.adoc`. | 9/10
  | 222 | 2026-09-17 | diagnostic + resync fixture + régénération golden master | **FLAKY-4 résolu — drift fixture `contact.thyme` (baseline 1777/0, fin des « 2 FAIL préexistants »)** — Reprise bakery à la demande pilote (méthodo baby-step DDD/TDD/BDD). Les 3 pistes du PROMPT_REPRISE S-222 (BKY-LANG-22-8/9 dogfooding LLM metered, LANG-22-10 publication Central, MASS-6 ~675h LLM) étaient **toutes bloquées par accord pilote**. Mission retenue : la seule dette actionnable sans coût externe — les **2 FAIL « préexistants »** traînant depuis S-218 et mentionnés dans toutes les gates cross-borough MEMPHIS depuis S-196. **Diagnostic** : ce n'est pas un flake mais un **drift réel**. Le site `office/sites/cheroliv.com` a retiré le champ `subject` du formulaire contact (commit `d96dcec`, 2026-09-14, US-35 « remove the subject field from the contact form ») ; la fixture bakery `i18n-fixtures/cheroliv-com/jbake/templates/contact.thyme` (mtn 2026-09-09) n'a pas été resynchronisée → `FixtureAlignmentAuditorTest` (compare fixture ↔ site réel) + `RealSiteI18nMigrationIntegrationTest` (migre une copie fraîche du réel et compare au golden master `post-migration/` figé sur l'ancienne forme avec `subject`) tombent. Même famille que l'EPIC BKY-TEST-FLAKY S-214 — la resync S-214 n'avait pas anticipé la modification cheroliv.com postérieure de 5 jours. **Périmètre vérifié par diff brut** : seul `contact.thyme` drift réellement ; `footer.thyme` ne diverge que par les secrets (apiKey/appId réels vs placeholders) déjà neutralisés par `FixtureAlignmentAuditor.neutralizeSecrets()` ; aucun drift magic-stick/cccp-education. **Fix chirurgical (5 fichiers, +6/-23)** : (1) resync fixture `contact.thyme` (bloc `<input name="subject">` 7 lignes retiré, diff = 0 avec le réel) ; (2) recâblage clés messages — le retrait du champ **recompose la numérotation** (`contact.6=Subject` disparaît, `Votre message` passe `contact.7`→`contact.6`) dans `translations_en.properties` + golden master `messages_fr`/`messages_en` ; (3) régénération golden master via la **procédure one-shot documentée** (`PostMigrationSnapshotGenerator` : retrait `@Disabled` → `--rerun-tasks` → re-`@Disabled` mention S-222), **churn timestamp réverté** (les snapshots magic-stick/cccp ne différaient que par la ligne de date `Properties.store` — loi d'Économie d'Encre appliquée au diff, pas seulement aux appels LLM ; date cheroliv-com réécrite à l'identique S-214). Gate `test` **1777 tests, 0 FAIL, 3 skipped** (baseline S-221 1775/2 → les 2 FAIL disparaissent), `FixtureAlignmentAuditorTest` 8/0, `RealSiteI18nMigrationIntegrationTest` 2/2, `publishToMavenLocal` VERT (règle INDEX 0), gate grep métier src/ = 0. Zéro secret, zéro bump BOM, zéro publication Central, zéro coût LLM. Note : le couple atomique graphify pré-session (bump toml `graphify 0.0.3→0.0.5` + 2 UT tolérance `SubgraphExtractorTest.kt`, session concurrente graphify S-028 commit `9486663`, publié Central bundle `895d2e53`) est **inclus au commit** — sans lui le toml bakery référence une API absente (`schemaVersion`). Rapport `.agents/sessions/222-flaky-4-contact-thyme-drift-resolu.adoc`. | 9/10
  | 221 | 2026-09-17 | DDD + TDD + functional Oven + cross-borough | **Cross-borough T-I18N-BAKERY US-2/US-3 — `IaConfigResolver` + harness talaria audit-only** — Mission pilote « T-I18N-BAKERY » (US-2 wiring build talaria + US-3 retrait harness) restaient bloquées par l'EPIC bakery `BKY-I18N-JS` (clos S-220). **Audit d'ouverture** : `translateI18nClient` n'appelle le LLM que si `bakery { ia { enabled = true } }` est posé là où le plugin est appliqué — or talaria n'applique pas bakery (c'est le **runner N3 partagé**). 3 chemins de config mesurés : `-Pia.enabled` **documenté mais jamais lu** (`grep '"ia\.'` = 0), `site.yml → ollama:` (`OllamaConfig`) **parsé mais ignoré** par `iaConfig`, seul le DSL marchait. **Option C retenue** (recommandation agent, validation pilote) : dériver `iaConfig` de `site.yml → ollama:`, résolu **localement** à la tâche (périmètre US-2 strict, zéro impact `BakeryPlugin` cross-borough). **Domaine pur** `bakery.llm.IaConfigResolver` (`resolve(dsl, ollama)`, précédence DSL explicitement activé > section `ollama:` (rotation 11437-11465, modèle, timeout) > inactif backward-compat) + `IaConfigResolverTest` **7 UT** (RED `Unresolved reference` → GREEN) + **2 FT** `TranslateI18nClientOllamaConfigFunctionalTest` (**RED prouvé par `git stash`** du câblage : 1 FAILED/1 PASSED → 2/2 après restauration) + câblage `ContentTaskRegistrar.registerTranslateI18nClientTask` (`resolvedIaConfig`). Bug TDD : **fixture YAML `trimIndent` avec interpolation multi-ligne** produisait `ollama:` mal indenté → construction ligne par ligne. **Collision de session concurrente graphify** (`GF-SCHEMA-VERSION`) : bump `graphify 0.0.3→0.0.5` dans le toml bakery (propagation cross-borough) + `GraphModel` gagnant `schemaVersion` en 1er paramètre cassait la compilation de `LensSteps.kt` → fix compatible **arguments nommés** (`GraphModel(nodes=…, edges=…, communities=…)`) ; fichiers graphify (toml + `SubgraphExtractorTest.kt`) **laissés non committés** par cette session. Gate `test` **1775/2 préexistants** (baseline 1768/2 → **+7 exactement**), `I18nClientCucumberTest` 10/10, FT 2/2, `publishToMavenLocal` VERT. Livrable talaria documenté côté talaria (`.agents/sessions/057-*.adoc`) : US-2 task `Exec` → runner (pattern `serve`) + `site.yml.example` section `ollama:` ; US-3 `tools/translate-i18n.mjs` réduit à un **audit couverture read-only** + garde d'architecture `i18n-harness-audit-only.test.mjs` (4 tests). Zéro secret, zéro Python, zéro bump BOM, zéro publication Central. | 9/10
  | 220 | 2026-09-17 | DDD + TDD + functional Oven + BDD | **BKY-I18N-JS-4 TERMINÉ — propagation byte-identique + garde de format + frontière zéro HTTP (clôture EPIC BKY-I18N-JS 4/4 US)** — US-4 (dernière) de l'EPIC BKY-I18N-JS (bloquant talaria `T-I18N-BAKERY`, contrat figé S-054). Baby-step DDD/TDD/BDD strict. (1) **Domaine pur** `bakery.i18n.js` : `I18nClientPropagation` object (`targetFor` mappe `maquette/js/…` → `jbake/assets/js/…` en ne remplaçant que le **premier** segment, profondeur préservée, `null` hors arbre ; `pairs` ; `drift` = sources dont la copie de publication est absente/non byte-identique — miroir du `propagate()` du harnais) + `I18nClientFormatGuard` object (`unbalanced-language-block`, `round-trip-drift`, rewriter injectable) + `I18nJsDictionary.unbalancedLanguageBlocks` (scanner string-aware). (2) **Task** `TranslateI18nClientTask` : CLI `--i18nClientPropagate` + intention/DSL `propagate` (défaut `true`), `propagateToPublication()` delta-only (arbre aligné = strict no-op, Loi Économie d'Encre), appelé dans les 3 chemins non-dry-run (delta traduit / delta vide / sans `TranslationService`), jamais en dry-run, **garde de format câblée en production** (fichiers invalides écartés du plan avant planning + résultat invalide jamais écrit). (3) **Wiring** : `ContentTaskRegistrar` (`i18nClientPropagate`). (4) **9 functional Oven** (5 S-219 + 4 : propagation après no-op, no-op copie alignée `0 fichier(s) aligne(s)`, dry-run ne touche pas la copie, flag `--i18nClientPropagate=false`). (5) **BDD feature 64** 7 → **10 scénarios** (@i18n-client) + 3 steps (propagation source→copie, no-op copie alignée, dry-run préserve la copie). (6) **Test d'architecture** `I18nClientNoDirectHttpBoundaryTest` (3 UT) : scanne `src/main/kotlin/bakery/i18n/js`, échoue sur toute surface HTTP directe (`api/generate`, `java.net.http`, `HttpClient`, `fetch(`, `localhost:114`…), vérifie que la traduction ne passe que par le port N0 `contracts.i18n.TranslationService`. Bugs attrapés par TDD : `targetFor` rejetait à tort le chemin racine `maquette/js/i18n.js` (garde `index <= 0` → `index < 0`) ; type `Map<String,String?>` sur les paires conditionnelles ; garde appliquée trop tard (bloc non balancé faisait lever le writer avant la garde). Gate `test` **1768 tests, 2 FAIL préexistants** (baseline S-219 1743/2 → **+25 exactement** ; FAIL = drift `contact.thyme` cheroliv.com, identiques S-218/S-219), `functionalTest` 9/9, Cucumber 64 10/10, `publishToMavenLocal` VERT (règle INDEX 0), gate grep métier src/ = 0. 3 fichiers créés, 8 modifiés. Zéro secret, zéro bump BOM, zéro publication Central. **EPIC BKY-I18N-JS ✅ TERMINÉ 4/4 US.** Rapport `.agents/sessions/220-BKY-I18N-JS-4-propagation-format-guard.adoc`. | 9/10
  | 219 | 2026-09-17 | DDD + TDD + functional Oven + BDD | **BKY-I18N-JS-3 TERMINÉ — task `translateI18nClient` (traduction dictionnaires i18n JS client)** — US-3 de l'EPIC BKY-I18N-JS (bloquant talaria `T-I18N-BAKERY`, contrat figé S-054). Baby-step DDD/TDD/BDD strict. (1) **Domaine pur** `bakery.i18n.js` : `I18nClientMigrationIntention` (invariants `sourceDirs` non vide/éléments non blank, référence ∈ N0 `LanguageCatalog`, cibles non vide ∈ N0, référence ∉ cibles) + `I18nClientMigrationIntentionDsl` (dryRun défaut `true`) + `I18nClientDelta` object pur planificateur (`referenceFloor` plancher `fr` moins variantes `.html`, `missingKeys` par langue, `languageOwners` premier fichier propriétaire du bloc **découvert par parsing** au lieu des listes hardcodées du harness, `plan` → `List<I18nClientFilePlan>`). (2) **Task** `TranslateI18nClientTask` (`@DisableCachingByDefault`) : CLI `@Option` `--i18nClientSource`/`--i18nClientTargetLangs`/`--i18nClientSourceLang`/`--i18nClientDryRun` (pattern `ResolveIntention` CLI > DSL > défauts), fusion sources, no-op strict si delta vide (jamais d'appel LLM), dry-run sans écriture, écriture des seuls fichiers modifiés, `TranslationResult.Failure` → clé conservée absente + document intact (dégradation). (3) **Wiring** : `ContentTaskRegistrar.registerTranslateI18nClientTask` (pattern `registerMigrateContentI18nTask`) + `BakeryExtension.i18nClient` + `BakeryPlugin.registerFullPipelineTasks`. (4) **5 functional Oven** `TranslateI18nClientFunctionalTest` (TestKit réel : dry-run delta 1 rien écrit, tâche groupe `transform`, sans IA source intacte, dictionnaire complet no-op, source absente non bloquante). (5) **BDD feature 64** `64_i18n_client_dictionary.feature` `@i18n-client` 7 scénarios + `I18nClientDictionarySteps` + runner dédié `I18nClientCucumberTest` (pattern S-082) + fixture `talaria-i18n-client` (chrome fr plancher/en complet/fa incomplet, patch de vide) — fake translator label-aware zéro LLM. Bugs attrapés par TDD : 2 assertions de test erronées (le `en` du fixture n'était pas complet), interpolation `trimMargin` cassant l'indentation du bloc `en` (delta 0) → `buildString`, assertion fonctionnelle sur `nav.cart` présent dans `fr` → affinée sur le bloc `en`, regex step `requests?` invalide → 2 steps singulier/pluriel. Gate `test` **1743 tests, 2 FAIL préexistants** (baseline S-218 1707/2 → **+36 exactement** ; FAIL = drift `contact.thyme` cheroliv.com, identiques S-218, reproduits isolément), `functionalTest` 5/5, Cucumber 64 7/7, `publishToMavenLocal` VERT (règle INDEX 0), gate grep métier src/ = 0. 3 fichiers existants modifiés (wiring additif), 12 créés. Zéro secret, zéro bump BOM, zéro publication Central. Rapport `.agents/sessions/219-BKY-I18N-JS-3-translate-i18n-client.adoc`. | 9/10
  | 218 | 2026-09-17 | DDD + TDD + dogfooding réel | **BKY-I18N-JS-2 TERMINÉ — `I18nJsDictionary` (parser/writer JS pur)** — US-2 de l'EPIC BKY-I18N-JS (bloquant talaria `T-I18N-BAKERY`, cadré S-054). Object pur `bakery.i18n.js.I18nJsDictionary` miroir du harness node `tools/translate-i18n.mjs` (S-044) : `parse(vararg sources)` (fusion chrome+patch, LinkedHashMap déterministe), `languagesOf(source)`, `insertTranslations(source, language, translations)` (delta clés manquantes — Loi Économie d'Encre, no-op byte-identique si corpus complet, idempotent). 2 invariants supérieurs au harness : **scanner de braces string-aware** (`{`/`}` dans un littéral double-quote ignorés — une valeur `"{0}"` ne tronque plus un bloc ; le harness naïf comptait tous les braces) + regex bloc ancrée `^\s{4}[a-z]{2}: \{` (entrées hors bloc ignorées). Bug attrapé par TDD : `String.replace(Regex, "\\n")` perdait le backslash (Java Matcher interprete `\n` comme escape) → lambda de remplacement `{ "\\n" }`. Tests baby-step TDD strict RED (compileTestKotlin FAILED `Unresolved reference 'I18nJsDictionary'` ×13) → GREEN → REFACTOR : **16 UT** `I18nJsDictionaryTest` (parse 6, languagesOf 3, insert 7) + **5 intégration** `I18nJsDictionaryTalariaIntegrationTest` (dogfooding réel lecture seule, skip `assumeTrue` CI : `i18n.js` 11 langues + plancher `fr` 205 clés hors `.html`, `i18n-extra-langs.js` 11 langues + plancher identique, catalogue 22 langues, round-trip byte-identique chrome+patch toutes langues). Gate `test` **1707 tests, 2 FAIL préexistants** (baseline S-217 1686/2 → **+21 exactement** ; FAIL `FixtureAlignmentAuditorTest` + `RealSiteI18nMigrationIntegrationTest` = drift `contact.thyme` cheroliv.com, reproduits isolément). `publishToMavenLocal` VERT (règle INDEX 0). Gate grep métier src/ = 0. Zéro fichier existant modifié (3 nouveaux). Zéro secret, zéro bump BOM, zéro publication Central. Rapport `.agents/sessions/218-BKY-I18N-JS-2-js-dictionary.adoc`. | 9/10
  | 217 | 2026-09-15 | DDD + TDD + BDD + publication Central | **EPIC BKY-LANG-22 — Extension LanguageCatalog N0 10 → 22 langues (parité talaria.school) — 7/10 US** — Demande pilote : aligner bakery/N0 sur les 22 langues servies par talaria.school (`i18n.js` + `i18n-extra-langs.js` + `i18n-content.js`). Constat split-brain : `BakeryConstants.SUPPORTED_LANGS` + N0 `contracts.i18n.LanguageCatalog` = 10 langues (`fr en zh hi es ar bn pt ru ur`), talaria = 22. Option A actée (accord pilote) : étendre le **contrat N0 partagé** (pas de liste locale) + propager + BOM + Central. 12 langues ajoutées : `it nl de el tr vi th id ko ja sr` + `fa` (**RTL**, RTL 2→3 `ar ur fa`). (1) N0 `LanguageCatalog` 10→22 (DDD/TDD baby-step RED→GREEN, 52 tests 0 FAIL) + `LanguageCatalogTest` (22, 3 RTL, parité set talaria) + `I18nConfigTest` renommé. Publication Central `i18n-contracts:0.0.4` bundle `c1a9de57` (repo1 200). (2) MEMPHIS BOM/catalog `0.0.43` bundle `eb310d04` (constraint i18n-contracts 0.0.4, repo1 200). (3) bakery : `SUPPORTED_LANGS = LanguageCatalog.supportedCodes()` (dérivation — split-brain tué), dep 0.0.4 + platform/catalog 0.0.43, tests alignés (`accepts all supported` dérivé du const, code invalide `de`→`xx`), `test` 1686/2 FAIL préexistants (drift `contact.thyme`, stash-prouvé). (4) capsule : `VoiceMapping` +12 modèles Piper validés rhasspy/piper-voices `voices.json` (`it_IT-paola-medium`, `nl_NL-pim-medium`, `el_GR-rapunzelina-medium`, `tr_TR-dfki-medium`, `vi_VN-vais1000-medium`, `th_TH-tsync2-medium`, `id_ID-news_tts-medium`, `ko_KR-kss-medium`, `ja_JA-hi_fi_captain-medium`, `sr_RS-serbski_institut-medium`, `fa_IR-amir-medium`) + 12 espeak codes (espeak-ng couvre 22) ; BDD `multi_language_capsule.feature` catalog-driven + runner dédié `CapsuleMultiLangCucumberRunner` + task `cucumberTestMultiLang` (pattern S-082) — **22/22 languages rendered** (11m18s). (5) slider : `RevealUiMessageCatalog` +12 libellés natifs (nav + controls) + `fa` RTL ; features 5/6/13 + steps catalog-driven ; tests alignés (`ar ur fa`) ; `TranslatedDemoDeckTest` **pinné sur corpus S-030** (27 fichiers gelés — régénération 22 langues = dogfooding LLM metered, reporté S-218) ; `test` 947/3 FAIL Playwright préexistants (stash-prouvé). (6) document : dep 0.0.4 + BOM/catalog 0.0.43, `SiteTranslationPlan` déjà N0-driven, compile VERT. Zéro publication plugin (LANG-22-10 en cours), zéro secret commité. 5 repos. Rapport `.agents/sessions/217-BKY-LANG-22-languagecatalog-22.adoc`. | 9/10
  | 216 | 2026-09-10 | gouvernance | **Transfert BKY-FPA-SITE → TRN-11 (training) + arbitrage de licence FPA tranché — GBL-003.5 (anneau A4 critical path)** — Arbitrage pilote : corpus FPA = document personnel (zéro droit de tiers), corpus brut privé office/, produits formation dérivés Apache 2.0. BKY-FPA-SITE ✅ TRANSFÉRÉ vers training comme EPIC TRN-11 (2 pts, ex BKY-FPA-SITE, pattern TRN-9, dépend GBL-003.3/003.4 + document S-082 + bakery). Travaux : bakery BACKLOG (marquage + en-tête S-216), CADRAGE_EPIC_BKY_FPA_SITE.adoc (statut final), INDEX :last_update:, PROMPT_REPRISE S-217 ; training BACKLOG (TRN-11) + INDEX ; racine BACKLOG.adoc (GBL-003.5→training) + AGENTS.adoc (table Bushwick + A4 + historique). Gouvernance pure, zéro code. Numérotation locale bakery (S-216, dernière 215). Rapport `.agents/sessions/216-transfer-bky-fpa-site-trn11.adoc`. | 8/10
  | 215 | 2026-09-09 | DDD + TDD + BDD | **Bug chemin dogfooding CLÔTURÉ (P3, item S-215) — résolution `srcPath`-aware du doublement `content/`** — Cause racine : `SiteConfiguration.resolvePaths` (BakeryPlugin.kt:245-251) rend `contentRoot` absolu (projectDir + srcPath) ; `--contentI18nSource` déjà préfixé par `srcPath` doublait (`jbake/content/blog/2026` → « répertoire source n'existe pas »), workaround S-207 `blog/2026`. (1) DDD : nouveau domaine `bakery.i18n.path` — `ContentSourcePathResolver` objet `internal` segment-aware (match par segments, `jb`≠`jbake`), 3 règles : chemin absolu tel quel / préfixé par les segments `srcPath` → `projectDir.resolve(sourceDir)` sans doublement / sinon `projectDir.resolve(srcPath).resolve(sourceDir)` (comportement historique préservé). Rétro-compatibilité totale : forme non-préfixée `content/blog` inchangée, préfixée `jbake/content/blog` corrigée, workaround devenu inutile. (2) Wiring TDD : `MigrateContentI18nTask.contentSrcPath: String?` + `resolveSourceDir()` câblé au resolver ; `ContentTaskRegistrar` dérive `contentSrcPath` relatif au projectDir (`relativeTo`, sinon null) — le registrar connaît la vérité issue de `resolvePaths`, la tâche consomme le chemin relatif. Sémantique copie : arbre préservé relatif au dossier source → `build/i18n-en/en/2020/0016_simple_post.adoc` (assertions FT). 8 UT resolver + 2 UT wiring (9 `ExecuteContentMigration`) + 2 FT TestKit (9/9, assertions chemin + `doesNotContain("répertoire source n'existe pas")`). (3) BDD : feature `63_i18n_source_path_resolution.feature` (5 scénarios, tags `@cucumber @bakery @i18n @i18n-mass @source-path`) + `I18nSourcePathSteps.kt` (glue `io.cucumber.java.en.*`) + runner dédié `I18nSourcePathCucumberTest` — 5/5. Gates : `test` **1682 tests 0 FAIL** (baseline S-214 1671 → +11 : 8 resolver + 2 wiring + 1 runner dédié), `functionalTest` BUILD SUCCESSFUL 5m9s 119/0, `cucumberTest` 581 completed **2 FAIL = préexistants environnementaux** (BKY-I18N-PROD-GEN dry-run cheroliv-com + JBake native roundtrip, baseline historique 1866/6 S-196/S-211 CLOS flaky) — 0 échec nouveau, feature 63 hors runner standard. Découverte runner : `CucumberTestRunner` standard filtre `@pipeline-contracts and not @wip` → la feature 63 (`@source-path`) passe par son runner dédié (5/5 dans `test`). `publishToMavenLocal` VERT (règle INDEX 0, après chaque modif main). Zéro bump BOM, zéro secret, zéro régression. 9 artefacts (4 modifiés +137/−2, 5 nouveaux). Rapport `.agents/sessions/215-contenti18nsource-double-chemin-resolu.adoc`. | 9/10
  | 214 | 2026-09-09 | BDD + fixtures + sécurité | **EPIC BKY-TEST-FLAKY CLÔTURÉ 3/3 US — stabilisation des 3 FAIL i18n récurrents + neutralisation secrets** — Baseline RED 1670/3 FAIL (XML JUnit) : (1) FLAKY-2 `FixtureAlignmentAuditorTest` 11 templates fixture cheroliv-com désynchronisés (site réel évolué post-S150 : SRI integrity, JSON-LD @graph, lang="fr", head(null), footer-bottom-links supprimé, magic-stick URL cccp.education) → copie des 11 templates du site réel vers la fixture, auditor 7/7 PASS. (2) FLAKY-1 `RealSiteI18nMigrationIntegrationTest` sitemap.thyme golden master périmé (cascade FLAKY-2) → `PostMigrationSnapshotGenerator` re-activé one-shot (pattern REAL-4 S170) puis re-`@Disabled` mention S-214, `translations_en.properties` étendu (header.1-6 réalignés nouvelles clés header.5=og:site_name/header.6=robots noindex, footer.15-18 supprimés) — 0 clé EN vide, 2/2 PASS. (3) FLAKY-3 feature 57 : fixture a 4 articles depuis S-190 (diagrammes-mixtes.adoc ajouté pour feature 55) mais feature 57 attendait 3 → feature + Given step mis à jour 3→4 (fresh 4/0, idempotence 0/4, delta 1/3), 3/3 scénarios PASS. **Découverte sécurité** : la resync a introduit la vraie clé API Firebase (`AIzaSy...`) + appId du site cheroliv.com dans les fixtures trackées (ancienne fixture = placeholders) → neutralisation baby-step TDD : placeholders dans fixtures/golden master (grep AIzaSy = 0) + `FixtureAlignmentAuditor.neutralizeSecrets()` (regex apiKey/appId → placeholder canonique avant hash, secrets-only divergence ignorée, +1 UT) + comparaison neutralisée dans `RealSiteI18nMigrationIntegrationTest.assertSnapshotsMatch` (copie site réel vraie clé vs golden master placeholders). Gates : `test` **1671 tests 0 FAIL** (1670+1, baseline 1670/3), runners dédiés 55/56/60/62/57 5/5 PASS, `PostMigrationGoldenMasterTest` 3/3 PASS, `publishToMavenLocal` VERT, gate D16 = 0, grep secrets = 0. 1 fichier production métier (`FixtureAlignmentAuditor` — neutralisation secrets). Les gates cross-borough MEMPHIS n'ont plus besoin de la mention "baseline exacte 3 FAIL". Rapport `.agents/sessions/214-flaky-123-stabilisation-i18n.adoc`. | 9/10
  | 213 | 2026-09-09 | DDD + gouvernance transverse | **RENAME Vague 3 CLÔTURÉ — convergence sessions concurrentes + purge résidus drift** — Constats d'ouverture : 4/6 US déjà livrées par sessions concurrentes (planner S-254, document prod S-253, codebase prod S-254, plantuml S-216, hyperframes S-254). 2 résidus réels corrigés : (1) document `AssembleBookTaskContentRichTocTest.kt:100` assertion `REAC` → `"enquête terrain"` (corps OCR page 22 absent du TOC — assertion plus discriminante, gate D16 src/ satisfaite, functionalTest 1/1 GREEN 21s skipped=0) ; (2) codebase docs courantes — commit CB-FPA-RAG S-214 (`ea2e5db`) postérieur au RENAME-10 avait réintroduit 4 mentions SPG d'usage (BACKLOG ×3 : tableau canaux + GenerateSPGTask + W-0 + référence V16 ; PROMPT_REPRISE ×1) → "plans de formation"/"GenerateContentPlanTask"/"generateContentPlan" (grille D12). Gate transverse finale : 0 usage métier sur planner/document/codebase/plantuml/capsule/hyperframes/bakery (matches résiduels = doc des EPICs RENAME eux-mêmes + historique CAP-SPD, precedent S-212/S-254). Zéro publication (functionalTest hors JAR + docs gouvernance), zéro bump BOM, zéro secret. Rapport `.agents/sessions/213-rename-v3-convergence-transverse.adoc`. | 9/10
  | 212 | 2026-09-09 | DDD + TDD + BDD | **RENAME-9 TERMINÉ — Confiner "SPG" dans bakery (EPIC RENAME Vague 3, baby-step 4/6)** — (1) `I18nMigrationService.kt` : "SPG" retiré de `extractionWhitelist` + `substringBlacklist` (cadrage V3 D12 — le terme redevient traductible). (2) 2 UT verrou whitelist/blacklist migrées (SPG → USB / Magic Stick — verrous testent des termes publics). (3) Fixture `vocabulaire-metier.adoc` : REAC/AFNOR/RNCP/DC/TS/CP/ECF → QUALIOPI/ISO/ISO 9001/PDCA/NFP (registres publics neutres, D11/D13) + tag "reac"→"referential". (4) Features 53/55/56 scénarios BorrowVocabulary alignés. (5) Fixture 0115 mass-multi-lang : SPG→plans de formation, SPG_A2SP→plan_formation, RNCP/rncp→CERTIF/certification (aucune assertion ne ciblait ces strings). (6) AGENT.adoc portée transverse "training (SPG/SPD)" → "training (plans pédagogiques)". Boundary : `borrowedVocabulary` déjà config-injectée côté document-plugin (D14 en place — bakery n'a AUCUNE liste hardcodée REAC/AFNOR). Gate baseline exacte : grep word-boundary `REAC|AFNOR|SPG|SPD|FPA|RNCP|CDA` src/ = 0 ; `test` 1670/3 FAIL FLAKY-1/2/3 identiques sur HEAD propre (stash preuve) ; features 55/56 runners dédiés 31s GREEN ; feature 53 main runner 9/9 PASSED ; `cucumberTest` full timeout 5min sous charge machine (load 14+, aucune échec de scénario). `publishToMavenLocal` VERT. Runner dédié créé puis supprimé (Économie d'Encre). 10 fichiers, 2 pts. EPIC RENAME Vague 3 bakery ✅ TERMINÉ (reste planner RENAME-7 + document RENAME-8 + codebase RENAME-10 + plantuml RENAME-11 + capsule/hyperframes RENAME-12). | 9/10
  | 211b | 2026-09-08 | release cross-borough | **Cycle release complet S-211 (rattrapage pilote)** — Ordre DAG : (1) document-plugin 0.0.17 publié Central bundle `d07c4444` (prérequis BOM — WIP MEMPHIS référençait 0.0.17 inexistant, 404 Central). (2) BOM/catalog 0.0.36 publiés Central bundle `2b4da514` (bakery-plugin 0.0.10 + document 0.0.17). (3) bakery-plugin 0.0.10 publié Central bundle `9e02b0ad` (pin catalog 0.0.36, BOM 0.0.36 ×3, toml local aligné D3, BakeryPluginPublicationTest 2/2 PASS). (4) Consommateurs : runner-gradle pin catalog 0.0.31→0.0.36 (buildEnvironment confirme 0.0.10), magic-stick migration groupId `com.cheroliv.bakery:0.1.9` → `education.cccp.bakery:0.0.10` (marker Central 200, compile OK). Commits/push 4 repos. | 9/10
  | 211 | 2026-09-08 | DDD + TDD | **RENAME-3 TERMINÉ — Neutralisation FPA dans bakery (EPIC transverse RENAME, baby-step 3/6)** — (1) 2 UT verrou whitelist/blacklist dans `I18nMigrationServiceTest` (terme conservé "SPG" — la gate `grep -ri "fpa" src/` = 0 interdit littéralement FPA dans src/, pattern codebase RENAME-1 : modification data-only verrouillée par baseline + grep gate). (2) `I18nMigrationService.kt` : "FPA" retiré de `extractionWhitelist` + `substringBlacklist` — le terme redevient traductible (nommage public neutre, cadrage RENAME D6/D7 : backward compat non préservée, breaking change assumé). (3) Fixtures scaffold "Formation FPA" → "Formation métier" (4 fichiers, JSON fake LLM + ScaffoldIntention) + tag "fpa" → "pédagogie" + fixture blog `0115_anonymiseur_*.adoc` (2 mentions éditoriales CDA/FPA → "experts métier"). Aucune assertion ne ciblait ces strings (vérifié avant). Gate baseline exacte : `test` 1670/3 FAIL préexistants + `cucumberTest` 1866/6 FAIL préexistants — identiques sur baseline stashed (S196 environnementaux + FLAKY-1/2/3). `publishToMavenLocal` VERT. Zéro secret, zéro bump BOM. 7 fichiers, 1 pt. EPIC RENAME bakery ✅ (reste capsule + plantuml + graphify/codex). | 9/10
  | 210 | 2026-09-07 | cadrage | **Cadrage EPIC BKY-FPA-SITE (GBL-003.5 critical path MVP0)** — site navigable du corpus FPA. Arbitrage pilote 2026-09-07 : priorité aux boroughs qui rapprochent de la génération de formation. EPIC en STANDBY : 2 US ~2 pts (BKY-FPA-SITE-0 arbitrage licence public = décision pilote, BKY-FPA-SITE-1 run JBake si GO). Constat : livre FPA navigable déjà produit (document S-082), bakery n'ajoute de valeur qu'avec contenu consolidé post GBL-003.3/003.4. Zéro code modifié. Doc `.agents/CADRAGE_EPIC_BKY_FPA_SITE.adoc`. | 10/10
  | 209 | 2026-09-07 | governance | **Sync gouvernance post-S-208** — TODO.adoc dégelé (S-194 → S-209) : EPIC PUB marqué ✅ TERMINÉ S-203 (bakery 0.0.7 Central puis 0.0.8/0.0.9), MASS-5 ✅ CLÔTURÉ S-207, DEPLOY-4→7 ✅ (EPIC BKY-I18N-DEPLOY CLOS 35/35 S-183), doublon BKY-TREE-7 supprimé. Vérification post-sync : 4 unchecked légitimes restants (FLAKY-1/2/3 + MASS-6 reporté). Rapport `.agents/sessions/209-sync-gouvernance-post-208.adoc`. Zéro code production, zéro bump BOM, zéro secret. | 9/10
  | 207 | 2026-08-19 | dogfooding LLM réel + clôture EPIC | **MASS-5 CLÔTURÉ — 8/8 lots, 108/108 articles FR→EN, idempotence validée** — Lot 2026 déjà traduit (28/28 articles, timestamps août 18 run antérieur). Headers JBake préservés (0 cassé), `:summary:` traduits (fix S206 appliqué). Ratio FR résiduel < 7% (24/28 < 5%, faux positifs mots courts le/la/les/a/on/y). Idempotence Loi Économie d'Encre : re-run `migrateContentI18n --contentI18nSource=blog/2026` → `[en] Delta : 0 à traduire, 108 préservés.` Zéro appel LLM, zéro fichier modifié, BUILD SUCCESSFUL 856ms. Validation LENIENT : 0 table invalide, 0 plantuml invalide. Bug chemin dogfooding : `--contentI18nSource=content/blog/2026` double `content/` (srcPath site.yml préfixé) → workaround `blog/2026` (P3, non bloquant). Aucun code source modifié (dogfooding only). bakery 0.0.9 + document 0.0.3 déjà publiés Central (S206 antérieur). **EPIC BKY-I18N-MASS : MASS-1→5 ✅ TERMINÉ, MASS-6 🟡 reporté.** | 8/10
  | 206 | 2026-08-18 | DDD + TDD + BDD + publication | **Fix plugin ID + asciidoc :summary: + bump/pub cross-borough** — (1) Constat 1 S205 : 19 fichiers corrigés `education.ccp.bakery` (2 "c") → `education.cccp.bakery` (3 "c") via `sed -i` (18 README + AGENTS/INDEX/SESSIONS_HISTORY/archives/session 205). Vérif post-fix : 0 match. (2) Constat 2 S205 : `DocumentTranslator.translateFrontmatter` (document-gradle) ne traduisait que `jbakeAttributes["summary"]`/`["description"]`, pas `asciidocAttributes["summary"]`/`["description"]`. Or articles cheroliv.com utilisent `:summary:` (sans préfixe `:jbake-`) → `asciidocAttributes`. Fix étend la traduction aux asciidocAttributes (pattern identique, +10 lignes). Baby-step TDD strict RED (AssertionFailedError ligne 427) → GREEN (test PASSED) → REFACTOR (gate `document.translation.*` 10 classes BUILD SUCCESSFUL 37s, 0 FAIL). BDD Cucumber nouveau scénario `translateDocument translates asciidoc summary and description attributes without jbake prefix` (5/5 steps PASSED, cucumber.json confirmé). Échecs préexistants `bookPipeline`/`applyDocumentTemplate`/`batchConvertDocuments` indépendants (AsciidoctorJ/env). (3) Bump pub : document-plugin 0.0.1→0.0.2 (bundle `def2f207` Central AUTOMATIC), bakery-plugin 0.0.5→0.0.8 (bundle `a3931440` Central AUTOMATIC — aligne catalog sur Central 0.0.7, ajoute fix document 0.0.2), workspace-bom 0.0.19→0.0.20 (Central, bakery 0.0.6→0.0.8). (4) Consommateurs màj foundry/public : bakery (BOM 0.0.20 ×3), codex (BOM 0.0.20), plantuml (BOM 0.0.20), runner (bakery 0.0.4→0.0.8 + ID corrigé `education.ccp.bakery`→`education.cccp.bakery`). `BakeryPluginPublicationTest` 2/2 PASS. `publishToMavenLocal` VERT. Compilation bakery avec BOM 0.0.20 + document-plugin 0.0.2 : `compileKotlin --rerun-tasks` BUILD SUCCESSFUL. Autorisation commit + push. Zéro secret. | 9/10
  | 205 | 2026-08-18 | DDD + TDD + BDD + dogfooding LLM réel | **MASS-5 dogfooding lot annuel 7/8 lots (80/108 articles FR→EN) + BDD feature 62 batch annual** — (1) Dogfooding LLM réel `gemma4:31b-cloud` port 11437 sur cheroliv.com via `/tmp/bakery-dogfood` (buildscript classpath `education.cccp.bakery:0.0.7` mavenLocal + symlink content). 8 lots annuels (2021→2026), 7 complétés : 2021 (1 art, 2m10s), 2024 (6 art, 7m22s), 2022 (7 art, 11m33s), 2023 (14 art, 11m31s), 2019 (13 art, 5m28s), 2020 (15 art, 3m58s), 2025 (24 art, ~9m30s). Lot 2026 (28 art, ~15000 lignes) en cours au-delà timeout 1h — limite pratique ~20-25 art/lot. Idempotence validée (2e run = 0 à traduire, delta vide). Delta inter-lots validé (checksums globaux préservent lots précédents). Validation LENIENT : 0 table/plantuml invalide, 0 erreur. (2) BDD feature `62_i18n_mass_batch_annual.feature` `@batch-annual` (6 scénarios : fresh lot 2025, lot 2026 après 2025 delta inter-lots, idempotence intra-lot, idempotence inter-lots, marqueur `[en]`, headers JBake préservés). Steps `I18nMassBatchSteps.kt` + runner `I18nMassBatchCucumberTest.kt` + fixture `cheroliv-com-mass-batch` (2 lots × 2 articles FR). Baby-step TDD/BDD strict RED → GREEN (6/6 PASS en 1.1s) → REFACTOR. Fake translator label-aware (zéro LLM en CI). Non-régression `bakery.i18n.mass.*` PASS (features 60 + 62). `publishToMavenLocal` VERT. Constat : plugin ID réel = `education.cccp.bakery` (3 "c"), pas `education.ccp.bakery` documenté (incohérence gouvernance — corrigée S206). MASS-6 reporté (coût LLM ~675h, session dédiée pilote). | 8/10
  | 204 | 2026-08-17 | DDD + TDD + BDD | **BKY-SEO CLOS 27/27 + BKY-CONTACT-SEC CLOS 26/26** — Double clôture EPIC. (1) BKY-SEO US-6 `SitemapHreflangBuilder` (domaine `bakery.seo`, object pur, 9 UT) génère XML sitemap avec `<xhtml:link rel="alternate" hreflang>` fr/en/x-default. Câblage `InjectSeoTask` + `collectPageUris` + 2 FT. (2) BKY-SEO US-7 BDD `59_seo_injection.feature` `@seo` (6 scénarios) + runner `SeoCucumberTestRunner` + `SeoInjectionSteps`. 6/6 PASSED. **EPIC BKY-SEO CLOS 27/27 pts (7/7 US).** (3) BKY-CONTACT-SEC US-6 `ContactSecRenderer` (3 méthodes : footer fragment, contact.js durci, firestore.rules) + `ScaffoldContactSecTask` + câblage. 20 UT + 7 FT. (4) BKY-CONTACT-SEC US-7 fixture `cheroliv-com-contact` (mocks : `0xTESTKEY`, `script.example.com`, `turnstile.placeholder.js`). 2 FT. (5) BKY-CONTACT-SEC US-8 BDD `61_contact_sec.feature` `@contact` (4 scénarios) + runner `ContactSecCucumberTestRunner` + `ContactSecSteps`. 4/4 PASSED. **EPIC BKY-CONTACT-SEC CLOS 26/26 pts (8/8 US).** Baby-step TDD/BDD strict. 29 UT + 9 FT + 10 BDD nouveaux, 0 FAIL. `publishToMavenLocal` VERT. Zéro régression, zéro secret. Bug Cucumber YAML : `trimIndent`/`trimMargin` → `StringBuilder` ligne par ligne. MASS-5→6 reporté (dogfooding LLM). | 9/10
  | 203 | 2026-08-17 | DDD + TDD + BDD + publication | **EPIC PUB TERMINÉ 3/3 US + BKY-SEO US-1→5 + BKY-CONTACT-SEC US-1→5** — Triple EPIC. (1) EPIC PUB : bakery-plugin 0.0.7 publié sur Maven Central (bundle `dedd92b5` AUTOMATIC) via `publishAggregationToCentralPortal` nmcp. POM + signing GPG via convention plugin v0.0.3. Tags v0.0.8/v0.0.9 orphelins supprimés, tag v0.0.7 poussé. Cross-borough fix : plantuml-plugin 0.0.4 publié sur Central (bundle `87b3614e`) après fix kotlin-stdlib strictly 2.3.21 dans buildscript (force() ajouté). BOM 0.0.19 exigeait plantuml 0.0.4 inexistant — blocage résolu. (2) BKY-SEO US-1→5 (19/27 pts) : domaine `bakery.seo` créé — `SeoConfig`+`Person` POJO (8 UT), `SeoPageMeta`+`SeoPageType`+`HreflangAlternate` (11 UT), `SeoThymeleafRenderer` (9 UT), `SeoInjector` (3 UT), `InjectSeoTask` (5 FT) + câblage. 24 UT + 5 FT PASSED. (3) BKY-CONTACT-SEC US-1→5 (13/26 pts) : domaine `bakery.contact` créé — `ContactSecConfig` (5 UT), `TurnstileClient` (6 UT), `RateLimiter` (6 UT), `SessionTokenManager` (6 UT), `PowVerifier`+`ContentHeuristics` (5 UT). 28 UT PASSED. Baby-step TDD strict. Zéro secret, zéro régression. Reste : BKY-SEO US-6→7 (8 pts) + BKY-CONTACT-SEC US-6→8 (13 pts). | 9/10
  | 202 | 2026-08-17 | DDD + TDD + intégration réelle | **BKY-DNS-6 TERMINÉ (dogfooding réel talaria.school + 2 bugs DnsDiff corrigés)** — `DnsDogfoodTalariaIntegrationTest` : lit `site.yml` réel talaria (gitignoré), parse en `SiteConfiguration`, construit vrai `OvhDnsClient`+`OvhDnsProvider` (credentials depuis `e3p0.yaml` via script Python, jamais affichées), exécute `DnsProvisioner.reconcile` dry-run contre la vraie API OVH. `assumeTrue` skip propre si site.yml/creds absents (CI/fresh checkout). Dogfood a révélé **2 bugs de conception DnsDiff** : (1) **scoped orphan cleanup** — le diff marquait `Delete` pour tous records zone non désirés (NS/MX/SPF/CNAME ftp), un provisioner scoped GitHub Pages ne doit pas purger mail/nameserver → fix : delete seulement si slot `(type,name)` ∈ desired ; (2) **TTL=0 zone default** — OVH convention `ttl=0` = "zone default", traité à tort comme drift vs `desired.ttl=3600` → fix : `actual.ttl != d.ttl && actual.ttl != 0`. 2 nouveaux tests unitaires `DnsDiffTest` + 4 tests `DnsProvisionerTest` ajustés + test `combined` ajusté. Baby-step TDD strict RED → GREEN → REFACTOR. Dogfood report : `noop=true, plan=0, applied=0, skipped=0` — zone talaria.school (12 records live) correspond au desired state (4 A + 1 CNAME www), idempotence prouvée. Gate `bakery.dns.*` BUILD SUCCESSFUL (85 tests, 0 FAIL). publishToMavenLocal VERT. Sécurité : dry-run uniquement, credentials jamais loggées, `e3p0.yaml` jamais lu par agent, sauvegarde `site.yml.bak.bky-dns-6`. **EPIC BKY-DNS TERMINÉ 7/7 US**. | 10/10
  | 201 | 2026-08-16 | BDD | **BKY-DNS-5 TERMINÉ (Cucumber BDD dns_provision.feature)** — Feature 8 scénarios @dns (noop section absente = aucune réconciliation — tâche non enregistrée BKY-DNS-4, create CNAME www manquant, create 4 A apex, update TTL drift id préservé, replace valeur changée = 1 create + 1 delete sémantique S198, delete orphelin skipped dry-run → appliqué purge flag, idempotence 2nd run no-op, verify mapping zone = desired exactement). Runner dédié `DnsCucumberTestRunner` (pattern TreeCucumberTestRunner, filtre `@dns and not @pipeline-contracts`). Steps `DnsProvisionSteps` (24 step defs, état par instance pattern SiteTreeSteps) + `FakeDnsProvider` in-memory (id auto, zone mutable), `DnsProvisioner(provider, "talaria.school")`. Baby-step BDD strict RED (UndefinedStepException 8 scénarios ×2 runners) → GREEN (8/8 PASSED ×2 runners = 16 exécutions vertes, 3m37s) → REFACTOR. Bug expression Cucumber : `{string} {string}` ne matche pas `CNAME "www"` (CNAME sans guillemets) → `{word} {string}` (2 sites). Gate cucumberTest : 1180 tests, 3 FAILED = préexistants environnementaux I18nDeltaWire (identiques S196/197/198, hors BKY-DNS). Zéro production modifié — pure BDD. Zéro secret. | 9/10
  | 200 | 2026-08-16 | DDD + TDD + functional | **BKY-DNS-4 TERMINÉ (ProvisionDnsTask + wiring)** — `ProvisionDnsTask.kt` (domaine `bakery.dns`, `@DisableCachingByDefault`) : inputs non-secrets (`providerName`/`domain`/`records`/`dryRun`/`allowDelete` @Input), `credentials` @Internal (jamais hashées, jamais loggées). `@TaskAction` : skip si domain blank → `OvhDnsClient` seulement si credentials complètes (`isComplete` privé) → `DnsProviderFactory.resolve` (NoOp fallback CI) → `DnsProvisioner.reconcile(desired, dryRun, allowDelete)` → report console `[dns] provider/domain/dryRun/noop/plan/applied/skipped` + ligne par change (create/update/delete type name value ttl). `DnsTaskRegistrar.kt` : `registerProvisionDnsTask(site)` — return si `site.dns == null` (backward compat), enregistre `provisionDns` groupe deploy, `tasks.named("deploySite") { dependsOn("provisionDns") }`. `BakeryPlugin.kt` : appel dans `registerFullPipelineTasks` après `registerDeploySiteTask`. 3 functional `ProvisionDnsFunctionalTest` (TestKit, pattern VerifyConfigurationMappingFunctionalTest) : provisionDns success + report dry-run avec section dns, tâche absente sans section, dépendance avant deploySite (fix : `help --task` n'affiche pas les deps → `deploySite --dry-run` liste `:provisionDns` si câblé, sans side-effect git push). TDD strict RED (2 FAILED + 1 PASSED) → GREEN (3/3 PASSED 20s) → REFACTOR. Gate `test --tests "bakery.dns.*" --rerun-tasks` BUILD SUCCESSFUL 0 FAIL. publishToMavenLocal VERT (règle INDEX 0). Zéro secret commité (policy credentials @Internal). Working tree pré-session bump 0.0.6→0.0.7 + BOM 0.0.18→0.0.19 laissé intact (EPIC PUB 0.0.5 au backlog). | 9/10
  | 199 | 2026-08-15 | gouvernance | **Audit version + EPIC PUB (bump 0.0.5)** — delta 0.0.5 déclaré vs 0.0.4 Central, EPIC PUB ajouté au BACKLOG (3 US), zéro code modifié (gouvernance pure). | 8/10
  | 198 | 2026-08-15 | DDD + TDD | **BKY-DNS-3 TERMINÉ (DnsProvisioner : réconciliation idempotente)** — Domaine `bakery.dns` enrichi (9 fichiers prod purs, zéro I/O/Gradle) : `DnsChange` (sealed Create/Update(id, record, previous)/Delete), `ExistingDnsRecord`, `DnsDiff` (object pur, slot (type, name) + jeu de valeurs : valeur manquante → Create, présente + TTL différent → Update garde l'id, actuelle sans désirée → Delete, drift de valeur → Create + Delete), `DnsReconciliationResult` (plan/applied/skipped/noop), `DnsProvider` (port listRecords/create/update/delete/refreshZone/isAvailable/name pattern capsule), `OvhDnsProvider` (adapter → OvhDnsClient), `NoOpDnsProvider` (dégradé zone vide mutations no-op), `DnsProviderFactory.resolve` (ovh avec client → Ovh sinon NoOp pattern resolve*Provider), `DnsProvisioner.reconcile(desired, dryRun=true, allowDelete=false)` (noop si plan vide, refresh seulement si applied non vide). 31 tests nouveaux (10 diff + 9 provisioner + 4 noop + 4 factory + 4 ovh provider) — 83 dans `bakery.dns`, 0 FAIL. TDD strict RED (compileTestKotlin FAILED) → GREEN → REFACTOR (4 fixtures tests corrigées : sémantique assumée drift de valeur = Create + Delete, pas Update). Non-régression `./gradlew test` : 3 échecs PRÉEXISTANTS environnementaux identiques S196/197 (RealSiteI18nMigrationIntegrationTest / FixtureAlignmentAuditorTest / I18nDeltaWireCucumberTest). publishToMavenLocal VERT (règle INDEX 0). Zéro secret commité. | 9/10
  | 197 | 2026-08-15 | DDD + TDD | **BKY-DNS-2 TERMINÉ (OvhDnsClient : signature HMAC + endpoints + fake HTTP)** — Domaine `bakery.dns` enrichi (7 fichiers prod purs, zéro I/O dans le domaine) : `OvhSignature` (object pur, `$1$` + sha1 minuscule AS+CK+method+url+body+ts, vecteurs figés miroir ovh.py), `OvhEndpoint` (BASE_URL `https://eu.api.ovh.com/1.0` + path builders), `OvhRecord` (wire fieldType/subDomain/target/ttl, id `@JsonInclude(NON_NULL)` jamais envoyé en body, mapping ↔ `DnsRecord` apex `@` ↔ `subDomain=""`), `OvhHttp` (port `call` → `OvhHttpResponse`), `OvhDnsException`, `OvhDnsClient` (listRecordIds/getRecord/createRecord/updateRecord/deleteRecord/refreshZone, non-2xx → exception), `JavaOvhHttp` (transport prod java.net.http.HttpClient injectable timeout 10s, headers `X-Ovh-*`). 32 tests nouveaux (5 signature + 4 endpoint + 6 mapper + 9 client + 8 functional HTTP réel local `com.sun.net.httpserver` port éphémère FIFO) — 52 dans `bakery.dns`, 0 FAIL. TDD strict RED (compileTestKotlin FAILED) → GREEN → REFACTOR. functional test dans `src/test` (le source set `src/functionalTest` TestKit n'a pas le classpath main) ; headers OVH lowercase au wire (contrainte java.net.http.HttpClient). Non-régression `./gradlew test` : 1538 tests, 3 échecs PRÉEXISTANTS environnementaux identiques S196 (RealSiteI18nMigrationIntegrationTest / FixtureAlignmentAuditorTest / I18nDeltaWireCucumberTest). `publishToMavenLocal` VERT (règle INDEX 0). Zéro secret commité. | 9/10
  | 196 | 2026-08-15 | DDD + TDD | **BKY-DNS-1 TERMINÉ (DnsConfig + mapping Jackson site.yml)** — Domaine `bakery.dns` ouvert (4 fichiers prod purs, zéro I/O) : `DnsConfig` (provider défaut `ovh`, domain, records, dryRun défaut true, credentials — `@JsonIgnoreProperties`), `DnsRecord` (type/name/value/ttl 3600, apex `@`), `OvhCredentials` (applicationKey/applicationSecret/consumerKey + toString masqué pattern `RepositoryCredentials` + `maskSecret`, zéro fuite de secret), `DnsConfigMerger` object pur 4 sources CLI > YAML > Props > ENV (pattern capsule `mergeAudioPostConfig` — mergeStr isNotBlank heuristic, mergeBoolean, records liste native YAML, creds champ à champ). `SiteConfiguration.dns: DnsConfig? = null` (section absente = pas de provisionnement, backward compat). 16 unit TDD strict RED → GREEN (RED compileTestKotlin `Unresolved reference` × 4 types + champ dns → GREEN BUILD SUCCESSFUL 29s) : 7 `DnsConfigTest` (defaults, masquage, mapping Jackson records/creds/absent→null/unknown ignored) + 9 `DnsConfigMergerTest` (précédences CLI/YAML/Props/ENV, boolean, records, creds, cli blank). Non-régression `./gradlew test` : 1506 tests, 3 échecs PRÉEXISTANTS environnementaux (Fixtures i18n vs sites réels `office/sites/*` — prouvé par `git stash -u` + rerun ciblé identique). `publishToMavenLocal` VERT (règle INDEX 0). Zéro secret commité. | 9/10 — Suite mise en prod talaria.school (S-004/005 office) : DNS fait à la main via script Python jetable `~/workspace/configuration/scripts/ovh.py` (signature HMAC OVH, list/get/create/update/delete/refresh, création CNAME www → `pages-content.github.io.` + 4 A apex GitHub Pages). Réflexion architecture : transposer en tâche Kotlin bakery — testable, idempotent (réconciliant), versionné. Section `dns:` dans site.yml (mapping Jackson `SiteConfiguration`), port `DnsProvider`/`OvhDnsProvider`/`NoOpDnsProvider` (pattern capsule `resolve*Provider`), réconciliation create/update/delete diffs, dry-run par défaut, wiring `provisionDns` → `deploySite`. 7 US BKY-DNS-0→6, ~33 pts, projection S-N→S-N+3. Zéro code modifié — gouvernance pure (pattern S-057). BACKLOG.adoc + INDEX.adoc mis à jour. | 8/10
  | 194 | 2026-08-04 | DDD + TDD + BDD + dogfooding LLM | **EPIC BKY-I18N-MASS — MASS-4 CLOS (BDD golden master multi-langue + dogfooding LLM réel)** — Feature 60 `60_i18n_mass_multi_lang.feature` (7 scénarios, 7/7 PASS) : JBake headers préservés, RTL ar/ur, LTR zh, `[source]`/`[plantuml]` blocks inchangés, end-to-end 9 langues, translation marker. Steps `I18nMassMultiLangSteps.kt` (noms préfixés `mass-multi-lang`). Runner `I18nMassMultiLangCucumberTest.kt` standalone. Fixture `cheroliv-com-mass-multi-lang/` (3 articles pilotes réels). Dogfooding LLM réel : 3 articles × 9 langues via `gemma4:31b-cloud` port 11437, projet consommateur `/tmp/bakery-dogfood` mavenLocal 0.0.5. Fix DuplicateStepDefinition préexistant (I18nDeltaWireSteps renommé). | 8/10
  | 193 | 2026-08-04 | DDD + TDD + BDD | **EPIC BKY-I18N-MASS — MASS-3 CLOS (validation STRICT plantuml/table configurable)** — `ContentMigrationIntention.validation: String` (STRICT/LENIENT/OFF, default LENIENT) + `ContentMigrationIntentionDsl.validation`. `MigrateContentI18nTask` : CLI `--contentI18nValidation` + DSL propagation + `PlantUmlTranslationAdapter(plantUmlValidationMode = validationMode)`. `ValidationReport` data class (Jackson JSON consolidé table + plantUml). `writeValidationReport()` → `validation-report.json` post-passe. `ContentTranslationService.drainTableValidationResults()` + `drainPlantUmlValidationResults()` (document-gradle). 15 UT ContentMigrationIntentionTest (+6 validation) + 17 FT MigrateContentI18nDeltaFunctionalTest (+6 ValidationMode) + 5 BDD feature 59 (5/5 PASS). publishToMavenLocal document-gradle + bakery-gradle OK. | 9/10
  | 192 | 2026-08-04 | DDD + TDD + BDD | **EPIC BKY-I18N-MASS — MASS-2 CLOS (Issue 191-1 fixée)** — Copy loop `MigrateContentI18nTask:124-129` déplacée dans branche `translationService==null`. `translateSingleFileWithBlockDelta` écrit lui-même `targetFile`. 12/12 FT `MigrateContentI18nDeltaFunctionalTest` verts (Fresh 1, Delta 3, BlockDelta 8). 1/1 `BlockDeltaWireDebugTest` vert. Debug `println` nettoyés (4 fichiers, 24 lignes). publishToMavenLocal document-gradle + bakery-gradle OK. | 9/10
  | 191 | 2026-08-04 | DDD + TDD + BDD | **EPIC BKY-I18N-MASS — MASS-1 ✅ + MASS-2 🟠** — MASS-1 : `BlockChecksum` (enum `BlockTranslationStatus` TRANSLATED/PENDING, `BlockChecksumEntry` parse/serialize — legacy `"hash"`→PENDING, `sha256`/`computeForBlocks`) + `BlockDelta` (`compute(previous,current)` → modified/preserved, statut explicite Loi Économie d'Encre). DDD pur `document/translation/delta/`. 26 UT BlockDeltaTest + 4 UT DocumentTranslatorBlockDeltaTest + 1 debug (verts). MASS-2 : `DocumentTranslator.translateArticleWithDelta` (preserve blocs indexés, shift table/plantuml, isEmpty→retourne previous) + `ContentTranslationService.translateSingleFileWithBlockDelta` (fresh/delta path, écrit target). `MigrateContentI18nTask` boucle bloc-delta + `load/storeBlockChecksums` `.bakery-block-checksums/*.checksums` (`idx=hash:STATUS`). FT fresh ✅ + idempotence ✅ + 4 debug ✅. **Issue 191-1** : FT « second run with one modified block preserves other blocks » ❌ — copy loop `MigrateContentI18nTask:124-129` `sourceFile.copyTo(targetFile)` écrase la cible avec la source AVANT traduction (blocs préservés héritent du texte source). publishToMavenLocal OK. | 6/10
  | 190 | 2026-08-04 | DDD + TDD + BDD | **Renfort couverture tests i18n deploy** — Fake translator fixé (fragment-level, préfixe `[lang]` systématique). Feature 55 8/8 PASS (+1 mixed strategies). Nouvel article fixture `diagrammes-mixtes.adoc` (2 blocs plantuml stratégies différentes). Standalone runner `I18nDeployEndToEndCucumberTest` créé. Dictionnaire labels aligné 20 entrées. EPIC BKY-I18N-MASS cadré (priorité 1, ~32 pts, 6 US). publishToMavenLocal OK. | 9/10
  | 189 | 2026-08-04 | DDD + TDD + BDD | **EPIC BKY-OUTPUT CLOS — OUTPUT-5 ThemeResolver wiring (5 pts)** — `OutputConfigResolver` accepte `ThemeResolver?` optionnel, résout le thème dans `effectiveConfig`. `TreeBakeService` simplifié (ThemeResolver passé à OutputConfigResolver, buildConfigMap sans paramètre theme). +6 UT OutputConfigResolverTest. 12/12 OutputConfigResolverTest + 7/7 TreeBakeServiceTest + 100+ tree PASS. Cucumber features 39-48 tous PASS. publishToMavenLocal OK. 49/49 pts EPIC (100%). Tous les EPICs bakery sont clos. Backlog vide. | 9/10
  | 188 | 2026-08-03 | DDD + TDD + BDD + CI | **EPIC BKY-I18N-CI CLOS (19/19 pts)** — CI-4 site.yml.example 29 Device Keys template créé dans office/sites/cheroliv.com/. CI-5 workflows translate-cheroliv-com.yml + translate-i18n-content.yml déjà existants. CI-6 BDD feature 58 (4 scénarios, 4/4 PASS). CI-2 gap comblé : buildMaskedSummary étendu ollama (model, ports, deviceKeys count). validateRequiredFields rendu internal. 1 UT + 4 BDD ajoutés. publishToMavenLocal OK. | 9/10
  | 187 | 2026-07-31 | refactoring | **Alliance i18n — Suppression 10 shadow copies contrats BOM** — 5 fichiers main (SupportedLanguage, LanguageCatalog, I18nConfig, MessageBundle, LocaleResolver) + 5 tests identiques au BOM supprimés. Dépendance `i18n-contracts:0.0.2` déjà dans build.gradle.kts. compileKotlin + compileTestKotlin BUILD SUCCESSFUL. Zéro régression. | 9/10
  | 186 | 2026-07-25 | feat + fix | **DocKnowledgeBase JBake câblé dans LlmServiceTranslationAdapter** — DocKnowledgeBase.kt keyword matching in-memory sur doc officielle JBake. resources/docs/jbake-reference.adoc (structure, templates, variables, CLI). LlmServiceTranslationAdapter.buildPrompt() injecte doc avant traduction. 3 articles EN cheroliv.com corrigés (0100, 0105, 0115). | 9/10
  | 185 | 2026-07-24 | DDD + TDD + BDD + CI | **EPIC BKY-I18N-CI CI-1/CI-2/CI-3 livrés** — N0 OllamaConfig (portStart/portEnd Jackson-compatible, 29 deviceKeys) + OllamaDeviceKey (toString masqué). N2 SiteConfiguration.ollama: OllamaConfig? (backward-compat). N2 SecretField.DeviceKey + maskSecret. TDD: OllamaConfigYamlTest 6 UT + SecretFieldTest.DeviceKey 4 UT. BDD: 57_ollama_config.feature 4 scénarios. N2 document: PooledOllamaTranslationAdapter rotation 11437-11465. CI: translate-i18n-content.yml + translate-cheroliv-com.yml. 4 repos commités+pushés. | 9/10
  | 029 | 2026-07-21 | DDD + TDD + BDD + dogfooding LLM + cadrage EPIC | **document/bakery/office-cheroliv.com cross-borough** — Bug `+` continuation fixé (PivotInline.LineBreak, 50/50 PASS). Batch 3 articles pilotes FR→EN via Ollama gemma4:31b-cloud (14min, validé). BDD @jbake-native PASSED. :summary: + :description: traduits (TDD vert). EPIC BKY-I18N-CI cadré (6 US, 19 pts, S185→S187). EPIC BKY-I18N-BATCH documenté (stratégie lot annuel). | 8/10
  | 184 | 2026-07-19 | DDD + TDD + BDD | **EPIC BKY-I18N-DELTA-WIRE CLOS (12/12 pts) + BKY-OUTPUT-5 livré (5 pts)** — 2 EPICs en 1 session. DELTA-WIRE : US-1 I18nDeltaApplier DDD (8 UT), US-2 câblage MigrateContentI18nTask FT (4 FT, 0 régression 23/23), US-3 BDD Feature 57 (3 scénarios Cucumber). ContentChecksum SHA-256 + stockage .bakery-checksums.properties. ContentTranslationService.translateFiles(). OUTPUT-5 : ThemeResolver câblé dans TreeBakeService.injectTreeConfig() (themeOverrides + defaultTheme, 2 UT). 17 nouveaux tests. | 9/10
  | 183 | 2026-07-19 | DDD + TDD + BDD + CI | **EPIC BKY-I18N-DEPLOY CLOS — DEPLOY-7 Généralisation multi-sites + SiteTranslationPlan + CI réutilisable (7 pts)** — 5 baby steps. (1) DDD : `SiteTranslationPlan` (52 lignes, 4 méthodes `rtlTargets`/`ltrTargets`/`missingLanguages`/`isComplete`, délègue `LanguageCatalog`). (2) TDD FT : câblage `mustRunAfter` dans `BakeryPlugin.wireI18nDeployTaskOrdering` (3 tâches ordonnées sans dépendance dure, 4 FT). (3) BDD : Feature 56 (7 scénarios multi-sites : 3 sites × 9 langues, dry-run, RTL ar/ur, headers JBake, PlantUml ×3 stratégies, SiteTranslationPlan). Runner standalone `I18nDeployMultiSiteCucumberTest` via `LauncherDiscoveryRequestBuilder`. (4) CI : workflow réutilisable `deploy-i18n-site.yml` (paramétré `workflow_call`), `deploy-cheroliv-com.yml` migré (-68 lignes). (5) Fix : bump catalog root + plugin 0.0.3/0.0.2 → 0.0.4, `BakeryPluginPublicationTest` 2/2 PASS. 30 nouveaux tests (19 UT + 4 FT + 7 BDD). `./gradlew test` SUCCESS 2m27s. EPIC CLOS 35/35 pts (100%). | 9/10
  | 182 | 2026-07-19 | DDD + TDD + BDD | **EPIC BKY-I18N-DEPLOY — DEPLOY-6 Fixture 10 langues + BDD end-to-end (5 pts)** — 1 US livrée. Fixture `cheroliv-com-i18n-deploy/` (3 articles FR couvrant 3 stratégies PlantUml : PreserveTechnical class diagram, TranslateLabels use case diagram, BorrowVocabulary REAC/AFNOR) + `i18n/{9 langues}/` vides + `site.yml` 10 langues + `menu.thyme`. Feature `55_i18n_deploy_end_to_end.feature` 7 scénarios BDD GREEN (FR→EN headers préservés, FR→AR RTL, FR→ZH LTR, PlantUml ×3 stratégies, end-to-end 10 langues + lang switcher). Steps `I18nDeployEndToEndSteps.kt` (fake translator label-aware, zéro LLM en CI — Loi Économie d'Encre, dictionnaire FR→EN 17 entrées + remplacement in-texte). Step names préfixés `the deploy pipeline` pour éviter DuplicateStepDefinition. `cucumberTest` GREEN 2m43s. Plugin 0.0.4 mavenLocal. Déploiement cheroliv.com CI étendu 10 langues (workflow `deploy-cheroliv-com.yml` boucle 9 langues swap/bake/inject). Bug préexistant `BakeryPluginPublicationTest` version mismatch (0.0.4 vs catalog 0.0.3) non introduit. | 9/10
  | 180 | 2026-07-18 | DDD + TDD + BDD | **EPIC BKY-I18N-DEPLOY — DEPLOY-2 PlantUmlTranslationAdapter + DEPLOY-3 RtlDirectionInjector (10 pts)** — 2 US parallélisées (DAG indépendants). DEPLOY-3 : `RtlDirective(lang, rtl)` value object + `RtlDirectionInjector.inject(frontmatter, lang)` délègue `LanguageCatalog.findByCode` (i18n-contracts MEM-3). RTL (ar/ur) → `:jbake-lang:` + `:lang: rtl`, LTR → drop `:lang:` (idempotence inverse), lang inconnue → no-op. Pattern inspiré `DeckTranslator.ensureRtlDirection` MIAMI S030 adapté bakery. 11 UT + 9 BDD GREEN (feature 52). DEPLOY-2 : `PlantUmlTranslationAdapter` dispatch sur `PlantUmlClassifier` S179 — `PreserveTechnical` no-op, `TranslateLabels` traduit labels via port `TranslationService`, `BorrowVocabulary` masque vocab métier (REAC/AFNOR/DC/TS/RNCP/CP/ECF) derrière placeholders PUA `\uE000...\uE001` avant traduction puis restaure verbatim. I/O isolée dans port. Inspiré HTOWN PLT-BOUNDARY S163-166. 11 UT + 9 BDD GREEN (feature 53). Aucun fichier production existant modifié (additif — DEPLOY-4 câblera). `cucumberTest` complet GREEN 2m42s. Plugin 0.0.4 mavenLocal (signature GPG). 18/35 pts EPIC (51%). | 9/10
  | 179 | 2026-07-18 | DDD + TDD + BDD | **EPIC BKY-I18N-DEPLOY cadré + DEPLOY-0 + DEPLOY-1** — Constat : 108 articles `office/sites/cheroliv.com/content-i18n/en/blog/` encore en FR (headers JBake cassés `title=/date=/~~~~~~`, corruption `cp`+`sed` externe). EPIC BKY-I18N-DEPLOY cadré (35 pts, 7 US, S179→S183). Boundary bakery↔sites documenté (`.agents/I18N_DEPLOY_BOUNDARY.adoc`). DEPLOY-0 : 7 FT smoke `MigrateContentI18nDeploySmokeFunctionalTest` (mini-site JBake natif représentatif, dry-run parsing 3 articles simple+plantuml+source, 10 langues, source dir inexistant, `configPath=site.yml` requis). DEPLOY-1 : domaine pur `PlantUmlClassifier.kt` — sealed interface `PlantUmlStrategy` (TranslateLabels/PreserveTechnical/BorrowVocabulary) + `PlantUmlBlock` (extraction labels entre guillemets, skip identifiants dotted, détection vocabulaire REAC/AFNOR/DC/TS). 15 UT. Inspiré HTOWN PLT-BOUNDARY S163-166. BACKLOG v20.0. Plugin 0.0.4 mavenLocal. 0 secret compromis. | 9/10
  | 178 | 2026-07-18 | DDD + TDD + BDD | **EPIC BKY-LANG-SWITCH CLOS — Renderer + Task + BDD** — Fix build préexistant (bump codebase 0.0.6, BOM transitif résolu). LANG-SWITCH-3 `LangSwitchThymeleafRenderer` (13 UT, 10 langues natives labels). LANG-SWITCH-5 `LangSwitchInjector` (5 UT, domaine pur idempotent) + `InjectLangSwitchTask` (7 FT, câblage ContentTaskRegistrar + BakeryPlugin, @Internal sur File). LANG-SWITCH-6 BDD Cucumber feature 51 (5 scénarios, 11 steps, paths site/). 18 UT + 7 FT + 5 BDD ajoutés. cucumberTest complet GREEN. Plugin 0.0.4 mavenLocal. Bug sélecteur de langue cheroliv.com/en/ résolu. | 9/10
  | 176 | 2026-07-15 | DDD + TDD + BDD + déploiement | **i18n parallèle + JbakeNativeRenderer + déploiement cheroliv.com/en/** — Prompt LLM corrigé (rôle translator, interdiction bavardage/options). JbakeNativeRenderer préserve format `= Title / @Author / :jbake-*:`. PivotFrontmatter étendu (author, jbakeAttributes, asciidocAttributes). ContentTranslationService parallélisé (Dispatchers.IO + Semaphore ×4). Exclusion paths dans traduction. Sélecteur de langue (10 langues) dans menu.thyme. 106/109 articles EN traduits et déployés sur cheroliv.github.io/en/. 7 commits. 17 UT + 3 BDD + 3 Cucumber. Golden master régénéré. | 9/10
  | 175 | 2026-07-04 | gouvernance + audit | **Réconciliation code/gouvernance BKY-OUTPUT** — Audit complet : code OUTPUT-3/4/6/7/8 livré S174 mais BACKLOG/INDEX/PROMPT_REPRISE/TODO non mis à jour. Vérification compile + tests (119 tree/OUTPUT ✅, 1483/1484 total). Réévaluation : 44/49 pts (90%), 1 US restante (OUTPUT-5 ThemeResolver wiring, 5pts). 4 fichiers gouvernance mis à jour, 1 session report créé. Zéro code source modifié. | 8/10
  | 174 | 2026-06-29 | dev + publication | **BKY-I18N-REAL content migration + BKY-OUTPUT tree + pub 0.0.3** — 5 classes i18n (ContentCopyService, ContentMigrationIntention, ContentMigrationIntentionDsl, ContentTranslationService, MigrateContentI18nTask) + 4 classes tree (AssetRef, PageAssets, TreeBakeService, AsciiDocRenderer). 2 features Cucumber + 2 functional tests + 1 E2E. Messages i18n 10 langues + menu.thyme. bakery-plugin 0.0.3 publié Maven Central (bundle 49be80ef). 59 fichiers (33 modifiés + 26 nouveaux). | 8/10
  | 173 | 2026-06-29 | migration | **CNV-6 Migration conventions plugins** — `build.gradle.kts` migré vers `education.cccp.build.gradle-plugin` + `education.cccp.build.publishing` 0.0.1. 60 lignes supprimées (POM 53 lignes + signing 6 + toolchain 4 + testLogging 8), 5 ajoutées (net -55). `publishingConventions { publicationType = "PLUGIN" }`. `./gradlew check` BUILD SUCCESSFUL 9m46s, zéro régression, coverage 92.94%. 1er borough consommateur de la généralisation CNV-6 (gold standard). | 9/10
  | 172 | 2026-06-29 | DDD + TDD | **BKY-OUTPUT-1+2 : NodeMetadata + OutputConfig (13 pts)** — Cadrage EPIC BKY-OUTPUT (structure générique output theme/template/article/page/html/css/js). OUTPUT-1 : `NodeMetadata` (title/description/tags/layout by Section/Article, DDD pur, 5 UT). OUTPUT-2 : `OutputConfig` (template/layout/cssFiles/jsFiles/theme overridable per node, DDD pur, 5 UT). `NodeMetadataResolver` (6 UT) + `OutputConfigResolver` (6 UT) — héritage en cascade ancêtre→enfant. `SiteNode` + `SiteNodeDto` étendus (metadata/outputConfig champs optionnels). 22 nouveaux UT. 105/105 tree PASS. `./gradlew test` BUILD SUCCESSFUL (1m5s). Backward compat YAML round-trip préservée. | 9/10
  | 171 | 2026-06-29 | closing | **Clôture S171 + commit/push EPIC BKY-I18N-REAL** — S170 work committed: 9 new files, 7 modified files. Procédure 6 étapes exécutée. Git commit + push. Backlog vide. | 8/10
  | 170 | 2026-06-29 | DDD + TDD + BDD | **BKY-I18N-REAL CLOS — Real i18n migration of 3 sites on copies (24 pts, 6 US)** — Golden masters persistants figent le résultat post-migration. REAL-1 : `PostMigrationSnapshot` (DDD pur, 13 UT). REAL-2 : `PostMigrationSnapshotLoader` (I/O isolée, 8 UT). REAL-3 : Golden master test TDD rouge→vert (3 sites). REAL-4 : 3 snapshots produits (magic-stick 12, cccp 11, cheroliv 24 templates). REAL-5 : BDD feature 46 + `I18nRealMigrationSteps` (6 scénarios GREEN, `TreeCucumberTestRunner` filtre étendu `@i18n-real`). REAL-6 : Idempotence Ink Economy Law (3 sites, 0 keys re-extracted). 27 UT + 6 BDD GREEN. `./gradlew test` SUCCESS, `./gradlew cucumberTest` SUCCESS (4m47s). Anglais imposé. | 9/10
  | 169 | 2026-06-28 | maintenance | **Maintenance CI : 3 bugs résolus, `./gradlew clean build` SUCCESSFUL** — Bug 1 : `settings.gradle.kts` `error("ossrhUsername")` → `?: ""` (credentials optionnelles, CI build bloqué à settings evaluation). Bug 2 : `CucumberTestRunner.kt` filtre `not @personal` (scénarios cheroliv.com 655 fichiers 12MB exclus du run default, timeout 5min dépassé). Bug 3 : `AccessibilityAuditTask.kt` `@PathSensitive(RELATIVE)` sur `auditDir` (Gradle 9.5 validatePlugins). Bug 4 NON résolu : `org.jetbrains:annotations:{strictly 13.0}` dans codebase-plugin POM (website.yml cassé, hors périmètre). `./gradlew clean build` BUILD SUCCESSFUL 26/26 tasks 8m57s. Coverage 93,09% (was 92,90%). 3 fichiers modifiés, 0 nouveau code production. | 9/10
  | 168 | 2026-06-27 | DDD + TDD + BDD | **BKY-TREE-7 — Scaffold IA retourne un arbre (5 pts, clôture EPIC 36/36)** — `ScaffoldOutput.tree: SiteNode?` (additif, null=legacy). `ScaffoldOutputDto.tree: SiteNodeDto?` parsing Jackson. `ScaffoldGenerator` buildPrompt demande `tree` hiérarchique (type discriminator) + parseJsonOutput tree→flattenTemplates (tree prioritaire sur templates legacy). `GenerateSiteFromIntentionTask.buildSiteYml` sérialise `tree:` section. `ScaffoldTreeAdapter.kt` (nouveau) `flattenTemplates()` domaine pur zero I/O. 19 UT (6 adapter + 4 DTO + 9 generator) + 4 UT existants étendus + 1 UT task. 5 scénarios BDD (22 steps) Feature 45. 99 UT tree + 186 BDD steps tree (features 39→45) tous GREEN. Backward compat préservée (tree absent=legacy, tree présent=tree wins). 3 sessions au lieu de 4 projetées. | 9/10
  | 167 | 2026-06-26 | DDD + TDD + BDD | **BKY-TREE-5+6 — Theme inheritance + i18n par sous-arbre (13 pts)** — 2 US livrées. TREE-5 : `ThemeResolver` (effectiveTheme, ancestorChain, override total replacement, orphelins rejetés, resolveAll, 12 UT). TREE-6 : `SubtreeI18nPlanner` (computeDelta par sous-arbre, I18nDelta/ArticleModification, idempotence Loi Économie d'Encre par feuille, backward compat liste plate, 11 UT). 3 bugs corrigés : features 39/41 steps singulier/pluriel manquants + DuplicateStepDefinition (fusion SubtreeI18nDeltaSteps→ThemeResolverSteps). `TreeCucumberTestRunner` isole features @tree. 80/80 UT tree + 42/42 BDD tree GREEN. publishToMavenLocal OK. 31/36 pts EPIC (86%). | 9/10
  | 166 | 2026-06-25 | DDD + TDD + BDD | **BKY-TREE-1+2+3+4 — SiteNode + SiteTree + Content + YAML (18 pts)** — 4 US livrées en 1 session. TREE-1 : `SiteNode` sealed interface (Site/Section/Article, identity par path, 14 UT). TREE-2 : `SiteTree` aggregate (walk pre/post-order, filter, leaves, findByPath, findSubtree, visit, 17 UT). TREE-3 : `Content` wrapper sur PivotArticle (blocs/inlineTexts/translatableSegments, Article.content optional, 13 UT). TREE-4 : `SiteNodeDto` + `SiteTreeYaml` (Jackson polymorphic DTO, round-trip déterministe, `type:` discriminator, content @JsonIgnore, 13 UT). 57/57 UT GREEN. 4 features Cucumber (28 scénarios) + 4 step files. Anglais imposé. Backward compat préservée. | 9/10
  | 165 | 2026-06-25 | vérification + investigation + cadrage | **koverThresholdCheck PASS + Cadrage EPIC BKY-TREE** — (1) `./gradlew koverThresholdCheck` BUILD SUCCESSFUL 6m28s, instruction coverage 92,90%. (2) Investigation i18n prod : fixtures `.thyme` 100% alignées (SHA-256) pour 3 sites, gaps identifiés (`jbake.properties` diverge, `assets/`/`content/` absents fixtures), prod = zéro migration. (3) Cadrage EPIC BKY-TREE — concept unifiant `SiteTree` (Composite Site→Section→Article→PivotModel AST) manquant. 7 US (TREE-1→7), ~36 pts, 4 sessions projetées (S166→S169). Backlog rouvert. Détail dans `BACKLOG.adoc`. Aucun code source modifié. | 8/10
  | 164 | 2026-06-24 | TDD + refactoring | **Fix OGNL templates + Optimisation E2E Playwright** — (1) Fix #sets.contains -> #lists.contains dans 10 templates (ArrayList vs Set OGNL, 2 E2E i18n FAILED -> 19/19 PASS). (2) Cache installPlaywright via onlyIf sur ~/.cache/ms-playwright/chromium-* (SKIPPED si present). (3) PlaywrightPool singleton JVM-wide (1 Playwright + 1 Browser pour tout, avant 5x). (4) forkEvery=1 supprime. (5) ThymeleafRenderingTestFactory singleton. 13 fichiers modifies/crees. `./gradlew e2eTest` 19/19 PASS 1m44s. | 9/10
  | 163 | 2026-06-24 | DDD + TDD + BDD | **Loi Économie d'Encre generatePivotYaml + 4e golden master + bug cucumber** — (1) GeneratePivotYamlTask load-before-write + skip si identique. 1 nouveau FT idempotence, 6/6 FT PASS. (2) 4e golden master nested-plantuml (listes `**`/`***` + plantuml pass-through). 4/4 GM PASS. (3) Bug cucumber features 9/16 — déjà résolu par fix S161. 0 FAILED. `./gradlew test` 1m SUCCESS. | 9/10
  | 162 | 2026-06-24 | Refactoring + DDD/TDD | **BKY-PERF-6 : Migration FT→UT avec @TestInstance(PER_CLASS) + @BeforeEach** — Investigation BKY-PERF-1→5 (profiling 13 classes, baseline 4min10s, bottleneck incompressible GradleRunner, `maxParallelForks` fixé à 3). Migration de 6 classes FT vers UT ProjectBuilder : `IaFunctionalTests`→`IaConfigUnitTest`, `ArticleIntentionFunctionalTests`→`ArticleIntentionUnitTest`, `ThemeIntentionFunctionalTests`→`ThemeIntentionUnitTest`, `ScaffoldIntentionFunctionalTests`→`ScaffoldIntentionUnitTest`, `CollectSiteContextFunctionalTest`→`CollectSiteContextUnitTest`, `ValidateFirebaseConfigFunctionalTests` DSL tests→`FirebaseAuthConfigUnitTest`. Pattern commun : `@TestInstance(PER_CLASS)`, champs `project`/`ext` partagés via `@BeforeEach`, helpers configure*() privés. Tests GradleRunner réels conservés dans `IntegrationRestoredFunctionalTests` (3 inner classes, 4 FT). 6 UT créées, 6 FT supprimées, 1 FT créé. `./gradlew test` BUILD SUCCESSFUL (11 tasks, 1m14s). Zéro code production modifié. | 9/10
  | 161 | 2026-06-24 | DDD + TDD + BDD | **Migration @Option natif --input/--output sur generatePivotYaml** — DDD : `PivotOutputResolver` (concept pur, 9 UT) — résout chemin sortie défaut `{stem}.pivot.yaml` + priorité explicite > défaut. TDD : `GeneratePivotYamlTask` migré `RegularFileProperty` → `Property<String>` + `@Option` (convention `MigrateToI18nTask`/`GenerateArticleTask`). 5/5 functional tests GREEN (`--input`/`--output`, défaut output, missing input, missing option, group). BDD : Feature 38 étendue (4 scénarios). Bug `DuplicateStepDefinitionException` résolu (renommage step `the pivot build should succeed` pour éviter collision `MinimalSteps`). `PivotTaskRegistrar` nettoyé (résolution `gradleProperty` supprimée). 49 UT pivot + 5 functional + 4 BDD PASS. | 8/10
  | 160 | 2026-06-24 | refactor README | **Architecture README Plugin + Consommateur** — Déplacement `README.plugin/` → `bakery-plugin/README.plugin/` (master EN remonté à racine plugin). Déplacement `README.consommateurs/README.md` → racine borough. Liens i18n recréés (10 langues × 2 audiences). Aucun code Kotlin modifié. | 8/10

