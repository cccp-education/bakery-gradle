= Archive des Taches Terminees — Juin 2026

== Session 166 — 2026-06-25 — BKY-TREE-1+2+3+4 : SiteNode + SiteTree + Content + YAML (18 pts)

**Contexte** : Démarrage EPIC BKY-TREE (Arbre de Site Composite). 4 US livrées
en une session (TREE-1→4, 18 pts sur 36). Méthode baby step DDD/TDD/BDD
préservée. Domaine pur, testable sans Gradle.

=== BKY-TREE-1 — SiteNode sealed interface (5 pts) ✅

* `src/main/kotlin/bakery/tree/SiteNode.kt` — sealed interface + Site/Section/
  Article data classes immutable. Identity par `path`. Validation non-blank.
  `isLeaf()`/`isSection()` avec `@JsonIgnore`. `Article.content` optional.
* `src/test/kotlin/bakery/tree/SiteNodeTest.kt` — 14 UT PASS.
* `src/test/features/39_site_tree_node.feature` — 7 scénarios BDD.
* `SiteTreeSteps.kt` — domaine pur, pas de GradleRunner.

=== BKY-TREE-2 — SiteTree aggregate + visiteurs (5 pts) ✅

* `src/main/kotlin/bakery/tree/SiteTree.kt` — walk (pre/post-order via
  `TraversalOrder`), filter, leaves, sections, findByPath, findSubtree,
  visit. No mutation.
* `src/test/kotlin/bakery/tree/SiteTreeTest.kt` — 17 UT PASS.
* `src/test/features/40_site_tree_traversal.feature` — 9 scénarios BDD.
* `SiteTreeTraversalSteps.kt` — état partagé via `BakeryWorld.siteTree`.

=== BKY-TREE-3 — Content wrapper sur PivotModel (3 pts) ✅

* `src/main/kotlin/bakery/tree/Content.kt` — wraps PivotArticle. `blocs()`,
  `inlineTexts()` (récursif Paragraph/ListBlock/Table/Admonition),
  `translatableSegments()` (filtre TextTranslatableClassifier). PivotModel
  inchangé.
* `src/test/kotlin/bakery/tree/ContentTest.kt` — 13 UT PASS.
* `src/test/features/41_site_tree_content.feature` — 6 scénarios BDD.
* `SiteTreeContentSteps.kt`.

=== BKY-TREE-4 — YAML serialization (5 pts) ✅

* `src/main/kotlin/bakery/tree/SiteNodeDto.kt` — DTO intermédiaire avec
  `@JsonTypeInfo` + `@JsonSubTypes` (Jackson polymorphic ne fonctionne pas
  sur sealed interface Kotlin). Mappers `toDto()`/`toDomain()`.
* `src/main/kotlin/bakery/tree/SiteTreeYaml.kt` — serialize/parse/
  parseOrNull. `USE_NATIVE_TYPE_ID` disabled pour `type:` property.
* `src/test/kotlin/bakery/tree/SiteTreeYamlTest.kt` — 13 UT PASS.
* `src/test/features/42_site_tree_yaml.feature` — 6 scénarios BDD.
* `SiteTreeYamlSteps.kt`.

=== Métriques

* 57/57 UT tree package GREEN (14+17+13+13)
* 4 features Cucumber (28 scénarios) — compile OK, cucumberTest complet à
  valider (timeout 5min)
* Backward compat : 31 UT TREE-1/2 préservées après ajout `content` à Article
* Anglais imposé : nommage, Gherkin, steps (correction utilisateur)

=== Fichiers

* 17 fichiers créés (5 main + 4 test + 4 feature + 4 step)
* 1 fichier modifié (`BakeryWorld.kt` — ajout `siteTree` field)

== Session 165 — 2026-06-24 — koverThresholdCheck PASS + Investigation Loi Économie d'Encre

**Contexte** : Session de vérification post-S164 (fix OGNL + E2E optimisation).
La piste 1 du PROMPT_REPRISE 165 demandait de valider que `koverThresholdCheck`
passe maintenant que les E2E i18n sont verts. La piste 2 ouvrait sur la Loi
de l'Économie d'Encre (transverse cross-borough).

=== Tâche 1 — koverThresholdCheck — ✅ PASS

* `./gradlew koverThresholdCheck --console=plain` — BUILD SUCCESSFUL en 6m28s.
* *Instruction coverage* : **92,90%** (missed=11540, covered=150915).
* Largement au-dessus du seuil Kover historique (85,88%, S104 BKY-KOV-1)
  et du seuil configuré (70%).
* La piste 1 de PROMPT_REPRISE 165 est close.

=== Tâche 2 — Investigation Loi de l'Économie d'Encre — état des lieux

Audit transverse des 10 boroughs LLM-metered (bakery + 9 autres) pour préparer
le prolongement code de la Loi (conceptualisée S153, propagée S154 aux
`AGENT.adoc` des 9 autres boroughs mais pas au code applicatif).

* *Code applicatif bakery* : 2 implémentations réelles uniquement —
  `I18nMigrationService.loadExistingTranslations()` (S153, delta traduction) +
  `GeneratePivotYamlTask` load-before-write + skip si identique (S163).
* *9 autres boroughs* : Loi uniquement documentée dans `AGENT.adoc`
  (gouvernance proclamée S154). Zéro ligne de code applicatif.
  boroughs : codex, training, capsule, plantuml, readme, document, agile,
  ticket, review.
* *workspace-bom* : `pipeline-contracts` (MEM-2, code prêt 33/33 PASS) n'est
  pas encore publié sur Maven Central (PUB-3.06 à faire). `llm-pool-contracts`
  (5 classes : LlmInstance, LlmInstancePool, QuotaConfig, ResetPolicy,
  RotationStrategy) publié 0.0.1.
* *Audit grep* : 9 `AGENT.adoc` contiennent la section Loi avec référence
  canonique vers `bakery-gradle/bakery-plugin/AGENT.adoc` (S153).

=== Cadrage identifié (non exécuté)

Trois options d'attaque pour prolonger la Loi, non départées (l'utilisateur
a clôturé la session avant décision) :

* *Option A — Contrat N0* : sealed `MeteredOperation<T>` + `Delta<T>` +
  `InkEconomyPolicy` dans workspace-bom (pipeline-contracts ou nouveau
  module `ink-economy-contracts`). DDD-first, transverse, évite 10
  implémentations divergentes. Baby steps DDD/TDD/BDD + publication
  Maven Central.
* *Option B — Pilote borough* : implémenter load-before-write dans un
  borough concret (codex RAG / readme / training SPG). TDD-first, démontre
  la valeur immédiatement, extraction du contrat ensuite.
* *Option C — A puis B* : contrat + pilote dans la même session 166.

=== Vérifications

* `./gradlew -q publishToMavenLocal` OK (working tree déjà propre, JAR 0.0.2).
* `git status` : working tree propre, branche `main` à jour avec `origin/main`.
* `./gradlew koverThresholdCheck` : BUILD SUCCESSFUL, 92,90% instruction coverage.

=== Fichiers modifiés

* Aucun code source. Gouvernance uniquement (5 fichiers) :
  - `.agents/sessions/165-kover-threshold-verification.adoc` (créé)
  - `PROMPT_REPRISE.adoc` (mise à jour session 166 → mission TREE-1)
  - `.agents/INDEX.adoc` (session 165 ajoutée + EPIC BKY-TREE dans roadmap)
  - `.agents/SESSIONS_HISTORY.adoc` (entrée 165)
  - `.agents/archives/COMPLETED_TASKS_ARCHIVE_2026-06.adoc` (ce fichier, entrée 165)
  - `.agents/TEST_COVERAGE_ANALYSIS.adoc` (métrique 92,90% MAJ)
  - `BACKLOG.adoc` (EPIC BKY-TREE cadré — 7 US, ~36 pts, 4 sessions projetées)

=== Tâche 3 — Cadrage EPIC BKY-TREE (réouverture backlog)

Après investigation i18n prod et découverte que `SiteConfiguration` est plate
(pas d'arborescence éditoriale), l'utilisateur a proposé le concept unifiant
manquant : `SiteTree` (Composite Site→Section→Article→PivotModel AST).

* *Motivation* : `SiteConfiguration` est plate (infra only). `ScaffoldOutput.
  templates: List<String>` est plate. La migration i18n walk `templates/` à
  plat. Aucun concept ne modélise l'arborescence éditoriale. Conséquence : la
  Loi de l'Économie d'Encre ne peut pas calculer un delta ciblé par sous-arbre.
* *Vision* : agregat `SiteTree` à 4 niveaux — `Site` (root) → `Section` →
  `Article` (feuille) → `PivotModel` (AST déjà construit S157-163). Chaque
  niveau manipulable indépendamment (theme, i18n, accessibilité, Économie
  d'Encre par sous-arbre).
* *7 US cadrées* : TREE-1 (SiteNode sealed, 5 pts) → TREE-2 (SiteTree aggregate,
  5) → TREE-3 (Content wrapper PivotModel, 3) → TREE-4 (YAML serialization,
  5) → TREE-5 (Theme inheritance, 5) → TREE-6 (i18n par sous-arbre, 8) →
  TREE-7 (Scaffold IA arbre, 5). Total ~36 pts.
* *Projection* : S166 TREE-1+2 (socle domaine pur), S167 TREE-3+4 (AST+YAML),
  S168 TREE-5+6 (theme+i18n), S169 TREE-7 (scaffold, clôture EPIC).
* *Pré-requis* : ne pas réinventer PivotModel (fait, testé, golden-master) ;
  backward compat (`templates: List<String>` reste accepté via adapter) ;
  baby steps DDD/TDD/BDD à chaque US.

'''

== Session 162 — 2026-06-24 — BKY-PERF-6 : Migration FT→UT avec @TestInstance(PER_CLASS) + @BeforeEach

**Contexte** : L'investigation BKY-PERF (profiling, 4min10s baseline) a montré
que le bottleneck des tests fonctionnels est incompressible via l'API GradleRunner
(chaque `.build()` fork un daemon). Solution : remplacer les tests DSL triviaux
(compilation + enregistrement tâche) par des tests unitaires ProjectBuilder in-process.

**Baby steps BKY-PERF-1→5** :
. *BKY-PERF-1* : Profiling 13 classes — 10 SINGLE-SHOT (35 tests), 3 MULTI-SHOT (19 tests). Baseline 4min10s.
. *BKY-PERF-2* : `withDebug(true)` → 2× plus lent. ABANDONNÉ.
. *BYK-PERF-3* : `@TestInstance(PER_CLASS)` + `@BeforeAll` → pas de gain (daemon frais à chaque `.build()`). ABANDONNÉ.
. *BYK-PERF-4* : `--configuration-cache` → pas de gain (`@TempDir` unique). ABANDONNÉ.
. *BYK-PERF-5* : `maxParallelForks` fixé à 3 (vs `availableProcessors()`). ✅ CONSERVÉ.

**BKY-PERF-6 — Migration FT→UT** :
. *IaFunctionalTests* (4 FT, 27s) → `IaConfigUnitTest` (4 UT, in-process) — helper `configureIa()`
. *ArticleIntentionFunctionalTests* (4 FT, 25s) → `ArticleIntentionUnitTest` (4 UT) — helper `configureArticleIntention()`
. *ThemeIntentionFunctionalTests* (5 FT, 27s) → `ThemeIntentionUnitTest` (4 UT) — helper `configureThemeIntention()`
. *ScaffoldIntentionFunctionalTests* (5 FT, 25s) → `ScaffoldIntentionUnitTest` (5 UT) — helper `configureScaffoldIntention()`
. *CollectSiteContextFunctionalTest* (4 FT nested, 26s) → `CollectSiteContextUnitTest` (4 UT) — `siteYamlContent` property
. *ValidateFirebaseConfigFunctionalTests* (3 DSL tests supprimés) → `FirebaseAuthConfigUnitTest` (3 UT) — inline DSL
. *IntegrationRestoredFunctionalTests* (NOUVEAU, 4 FT) — conserve les tests GradleRunner réels (injection propriétés, exécution tâche)

**Pattern commun** : `@TestInstance(PER_CLASS)`, `@TempDir projectDir`, `@BeforeEach setUp()`
avec `ProjectBuilder` + `pluginManager.apply(BakeryPlugin::class)` + configPath, helpers privés configure*()
qui configurent le DSL et appellent `(project as ProjectInternal).evaluate()`.

**Vérification** : `./gradlew test` BUILD SUCCESSFUL (11 tasks, 1m14s). Zéro code production modifié.

**Fichiers créés** (7) :
* `src/test/kotlin/bakery/llm/IaConfigUnitTest.kt` — 79 lignes, 4 UT
* `src/test/kotlin/bakery/article/ArticleIntentionUnitTest.kt` — 123 lignes, 4 UT
* `src/test/kotlin/bakery/theme/ThemeIntentionUnitTest.kt` — 125 lignes, 4 UT
* `src/test/kotlin/bakery/scaffold/ScaffoldIntentionUnitTest.kt` — 105 lignes, 5 UT
* `src/test/kotlin/bakery/lens/CollectSiteContextUnitTest.kt` — 96 lignes, 4 UT
* `src/test/kotlin/bakery/FirebaseAuthConfigUnitTest.kt` — 96 lignes, 3 UT
* `src/functionalTest/kotlin/bakery/IntegrationRestoredFunctionalTests.kt` — 251 lignes, 4 FT

**Fichiers supprimés** (6) :
* `src/functionalTest/kotlin/bakery/IaFunctionalTests.kt`
* `src/functionalTest/kotlin/bakery/ArticleIntentionFunctionalTests.kt`
* `src/functionalTest/kotlin/bakery/ThemeIntentionFunctionalTests.kt`
* `src/functionalTest/kotlin/bakery/ScaffoldIntentionFunctionalTests.kt`
* `src/functionalTest/kotlin/bakery/CollectSiteContextFunctionalTest.kt`
* `src/functionalTest/kotlin/bakery/CollectAugmentedContextFunctionalTest.kt`

**Fichiers modifiés** (2) :
* `src/functionalTest/kotlin/bakery/ValidateFirebaseConfigFunctionalTests.kt` — -3 DSL tests
* `build.gradle.kts` — maxParallelForks = 3 (BKY-PERF-5)

== Session 161 — 2026-06-24 — Migration @Option natif --input/--output sur generatePivotYaml

**Contexte** : La tâche `generatePivotYaml` (S159) utilisait `-Pinput`/`-Poutput`
(résolution manuelle `providers.gradleProperty` dans `PivotTaskRegistrar`). Les
autres tâches bakery (`MigrateToI18nTask`, `GenerateArticleTask`,
`AccessibilityAuditTask`) utilisent déjà `@Option`. Migration pour cohérence.

**Baby steps DDD/TDD/BDD** :

. *DDD* : `PivotOutputResolver` (concept pur, 9 UT) — résout chemin sortie défaut
  `{stem}.pivot.yaml` + priorité explicite > défaut. Remplace la logique inline
  de `PivotTaskRegistrar` (S159).
. *TDD* : `GeneratePivotYamlTask` migré `RegularFileProperty` →
  `Property<String>` + `@Option(option="input")`/`@Option(option="output")`.
  `@TaskAction` résout `File` via `project.file()`, valide `--input` requis,
  utilise `PivotOutputResolver.resolveOutput()`. 5/5 functional tests GREEN.
. *BDD* : Feature 38 étendue (4 scénarios). Bug `DuplicateStepDefinitionException`
  résolu (renommage step `the pivot build should succeed` pour éviter collision
  `MinimalSteps`). 4/4 scénarios cucumber PASS.
. *Nettoyage* : `PivotTaskRegistrar` réduit à l'enregistrement pur (résolution
  `gradleProperty` supprimée).

**Vérification** : 49 UT pivot + 5 functional + 4 BDD PASS.
`publishToMavenLocal` OK (jar 0.0.2).

**Fichiers** : 7 modifiés/créés (3 main + 4 test).

== Session 159 — 2026-06-24 — Fix découverte Gradle + Edge cases Parser + Tâche generatePivotYaml

**Contexte** : Session 158 avait passé les 3 golden masters GREEN via workaround
noms de méthodes complets, mais le `:test` task complet restait bloqué (timeout
5min) à cause d'un runner Cucumber non exclu. Session 159 : fix découverte Gradle,
fix 2 bugs parser (boucle infinie + IndexOutOfBounds), 3 edge cases parser TDD,
tâche Gradle `generatePivotYaml` (DDD/TDD), Cucumber BDD.

**Tâches** :

* [x] Fix découverte Gradle — diagnostic : `contracts.pipeline.scenarios.CucumberTestRunner`
  (package non exclu par `filter { excludeTestsMatching("bakery.scenarios.*") }`)
  est `@Suite @IncludeEngines("cucumber")`. Sans l'engine cucumber (exclu), ce
  runner reste bloqué indéfiniment. Fix : ajout `excludeTestsMatching("contracts.pipeline.scenarios.*")`
  au filtre `:test` dans `build.gradle.kts`. `:test` complet 1m 1s SUCCESS.
* [x] Fix boucle infinie `parseParagraph` — une ligne `----` flottante (hors bloc
  source) faisait boucler : le while s'arrêtait sur `startsWith("---")` mais
  `sb.isEmpty()` → `return null to i` (sans avancer `i`) → boucle infinie. Fix :
  `return null to i+1` (avance d'un cran) quand rien n'est consommé.
* [x] Fix `parseSourceBlock` IndexOutOfBounds — si `----` ouvrant absent,
  `contentStart` dépassait `lines.size` → `subList` crash. Fix : guard
  `if (i >= lines.size) return Source(language, "") to i`.
* [x] Tolérance `====` comme fermeture de bloc source — golden master
  `scripts.adoc` ligne 79 contient `====` au lieu de `----`. Nouvelle méthode
  `isSourceClosingDelimiter(line)` accepte `----` OU `====`.
* [x] TDD : tables multi-lignes — `parseTableRows` concatène les lignes de
  continuation (sans `|`) à la dernière cellule au lieu de les traiter comme
  nouvelles cellules. Test : `parses table cell spanning multiple lines`.
* [x] TDD : listes imbriquées `**`/`***` — `isUnorderedListMarker(line)`
  reconnaît `* `, `** `, `*** `. `listMarkerLength` retourne 2/3/4. Test :
  `parses nested unordered list with double asterisk sub-items`.
* [x] TDD : plantuml pass-through — branche `[plantuml]` dans `parseBlocks`
  redirige vers `parseSourceBlock` avec `language = "plantuml"`. Test :
  `parses plantuml block as source-like pass-through`.
* [x] DDD/TDD : tâche Gradle `generatePivotYaml` — `GeneratePivotYamlTask`
  (DefaultTask, @InputFile/@OutputFile) + `PivotTaskRegistrar` (résout
  `-Pinput`/`-Poutput`) + wiring `BakeryPlugin.apply()` avant `afterEvaluate`
  (disponible mode scaffold only, pas de site.yml requis). 3/3 functional
  tests PASS.
* [x] BDD : Cucumber feature 38 `38_generate_pivot_yaml.feature` (2 scénarios :
  task registration + YAML production). `GeneratePivotYamlSteps` (6 steps).
  2/2 scénarios GREEN.
* [x] Correction golden master YAML `scripts.yaml` : `Reseau` → `Resseau`
  (cohérence avec `scripts.adoc` ligne 200).
* [x] 7 nouveaux UT parser (41 UT total pivot) : 25 AsciiDocParser + 4
  PivotModel + 9 PivotYamlRenderer + 10 TextTranslatableClassifier + 3
  PivotGoldenMaster (via workaround noms méthodes complets).

**Fichiers modifiés/créés** :
* Main (5) : `AsciiDocParser.kt` (modifié), `BakeryPlugin.kt` (modifié),
  `GeneratePivotYamlTask.kt` (NOUVEAU), `PivotTaskRegistrar.kt` (NOUVEAU),
  `build.gradle.kts` (modifié)
* Test (5) : `AsciiDocParserTest.kt` (+7 UT),
  `GeneratePivotYamlFunctionalTest.kt` (NOUVEAU, 3 tests),
  `38_generate_pivot_yaml.feature` (NOUVEAU, 2 scénarios),
  `GeneratePivotYamlSteps.kt` (NOUVEAU, 6 steps),
  `scripts.yaml` (correction Resseau)

== Session 158 — 2026-06-24 — Golden Masters Pivot GREEN + TextTranslatableClassifier

**Contexte** : Session 157 avait créé le parser pivot AsciiDoc→YAML (30 UT) mais
le `PivotGoldenMasterTest` (3 tests golden master byte-identique) était bloqué
par un problème de découverte Gradle. Session 158 : débogage, fix, et passage
GREEN des 3 golden masters.

**Tâches** :

* [x] Débogage découverte Gradle PivotGoldenMasterTest — diagnostic : bug noms
  méthodes backtick Kotlin (espaces Java), workaround = nom méthode complet.
* [x] DDD : `TextTranslatableClassifier` (nouvelle classe de domaine, 10 UT)
  — heuristique `translatable: true/false` pour segments `Text` (codes techniques,
  noms de paquets, tailles, locale codes → false ; langage naturel, ponctuation → true).
* [x] TDD : `AsciiDocParser` support listes numérotées (`1.`, `2.`, `3.`) via
  `isNumberedListMarker` + `listMarkerLength` + refactoring `parseList` +
  `parseParagraph` arrête sur marqueurs numérotés. Test RED→GREEN.
* [x] TDD : `PivotYamlRenderer` corrections indentation inline children listes
  (10→12 espaces) + tables header (10→12) + rows `- - inline:` (deux tirets :
  row + cell) + `yamlScalar(language)` (empty→`""`, non-empty→sans quotes).
* [x] BDD : `PivotGoldenMasterTest` normalisation — suppression commentaires
  inline (` # translatable`) + standalone + lignes `translatable:` (heuristique
  testée séparément dans TextTranslatableClassifierTest).
* [x] Câblage classifier dans `parseInline` :
  `TextTranslatableClassifier.isTranslatable(text)` pour segments `Text`.
* [x] 3/3 golden masters PASS : quick-start (83l adoc → 325l yaml), ab-partition
  (126l), scripts (234l).
* [x] 32 UT pivot PASS total : 4 PivotModel + 9 PivotYamlRenderer + 18
  AsciiDocParser + 10 TextTranslatableClassifier + 3 PivotGoldenMaster.
* [x] Debug test temporaire créé puis supprimé (PivotDebugTest).

**Fichiers modifiés/créés** :
* Main (3) : `TextTranslatableClassifier.kt` (NOUVEAU), `AsciiDocParser.kt`
  (modifié), `PivotYamlRenderer.kt` (modifié)
* Test (3) : `TextTranslatableClassifierTest.kt` (NOUVEAU),
  `PivotGoldenMasterTest.kt` (modifié), `AsciiDocParserTest.kt` (modifié)

== Session 156 — 2026-06-24 — README i18n 10 langues (consumer + plugin)

**Contexte** : Création de 20 fichiers README en 10 langues (2 familles : Consumer Guide + Plugin Internals) à la racine de `foundry/public/bakery-gradle/`, alignés sur l'EPIC BKY-I18N (10 langues supportées). Documentation de bout en bout pour utilisateurs et contributeurs, avec switcher i18n réutilisant le pattern des sites JBake migrés.

**Tâches** :

* [x] Création `foundry/public/bakery-gradle/README.consommateurs/` — 10 fichiers README (EN master, FR, ZH, HI, ES, AR RTL, BN, PT, RU, UR RTL).
* [x] Création `foundry/public/bakery-gradle/README.plugin/` — 10 fichiers README (mêmes 10 langues).
* [x] Badges shields.io sur chaque README : Maven Central (`education.cccp/bakery-plugin`), Gradle Plugin Portal (`education.cccp.bakery`), CI GitHub Actions (`test.yml`), Coverage ≥85 % (Kover, consumer only), License (Apache 2.0).
* [x] Markers version : `Version: 0.0.2` · `Group: education.cccp` · `Plugin ID: education.cccp.bakery` · Cucumber `129/129 PASS`.
* [x] Switcher i18n en tête de page (10 liens relatifs `README.{code}.md`, langue courante en gras).
* [x] Marker `<!-- master source — other languages are translations of this file -->` sur `README.md` EN ; `<!-- translated from README.md rev 0.0.2 -->` sur les 9 traductions.
* [x] Consumer Guide : quick start DSL, table 12+ tâches, diagramme chaîne CCCP (content → bakery → gh-pages ; codebase RAG + graphify KG + LLM).
* [x] Plugin Internals : module layout (arbre `src/main/kotlin/bakery/`), table N0 contracts (5 artefacts workspace-bom), key dependencies (JBake, JGit, LangChain4j, Arrow-kt, Playwright, Jackson).
* [x] RTL natif pour AR (العربية) et UR (اردو) via script natif — pas de balisage `dir="rtl"` artificiel.

**Fichiers créés** : 20 fichiers README (10 dans `README.consommateurs/`, 10 dans `README.plugin/`). Aucun code source modifié.

== Session 155 — 2026-06-23 — Nettoyage BACKLOG + Schéma YAML Pivot i18n

**Contexte** : Nettoyage du BACKLOG.adoc (1269→207 lignes) après vérification que tous les EPICs réduits à une ligne sont réellement implémentés dans le code source. Découverte que les 3 sites réels (magic-stick, cccp.education, cheroliv.com) ne sont pas migrés i18n. Emergent design d'un schéma YAML pivot pour la traduction arborescente AsciiDoc nœud-par-nœud, sur bac à sable /tmp (pas de code production touché).

**Tâches** :

* [x] Historisation BACKLOG complet (1269 lignes) dans `.agents/backup/Y2026-sessions-145-154/BACKLOG_avant_nettoyage_sess154.adoc`.
* [x] Nettoyage BACKLOG → 207 lignes : synthèse 49 EPICs clos (1 ligne chacun), récap 87 code smells résolus, 3 pistes ouvertes.
* [x] Vérification EPICs réellement implémentés avant suppression du détail : BKY-CR-V6 (ResolveIntention.kt, InjectorSpec.kt, ConfigResolver.resolveAllDataDriven), BKY-I18N-DELTA (loadExistingTranslations), BKY-A11Y-1 (AccessibilityAuditTask, evaluateContrast, scanInlineColors, scanStructural), BKY-I18N (20 messages_*.properties, 4 services i18n), BKY-LENS (SubgraphExtractor, AugmentedArticlesService, LensPipelineService, BfsProximityCache), ARROW-1 (7 fichiers arrow.core.Either), BKY-PUB-2 (version 0.0.2, nmcp 1.5.0), BKY-KOV-1 (koverThresholdCheck 85%), BKY-JB-* (5 TaskRegistrars, 3 Intentions), BKY-JB-9 (5 tests E2E Playwright) — tous ✅ présents.
* [x] Audit i18n des 3 sites réels : magic-stick (12 templates, 100% FR), cccp.education (11 templates, 100% FR), cheroliv.com (19 templates, 100% FR) — 0 messages_*.properties, 0 `#{...}`, 0 `site.language`.
* [x] Création bac à sable `/tmp/opencode/bakery-i18n-lab/magic-stick/` (copie site réel).
* [x] Schéma YAML Pivot — `quick-start.yaml` (83 lignes adoc → 6 types blocs + 4 inline).
* [x] Schéma YAML Pivot — `ab-partition.yaml` (126 lignes → tables + admonitions IMPORTANT).
* [x] Schéma YAML Pivot — `scripts.yaml` (234 lignes → 3 corrections émergentes : hr, cellules table inline[], cols null).
* [x] Validation schéma : 8 types blocs (heading, paragraph, list, table, admonition, source, hr, frontmatter) + 4 types inline (text, bold, code, link) + règle translatable true/false.
* [x] Décision : blocs `plantuml` = pass-through (`translatable: false`), plugin plantuml-gradle gère son cycle.

**Fichiers modifiés** : `BACKLOG.adoc` (1269→207 lignes), `.agents/backup/Y2026-sessions-145-154/BACKLOG_avant_nettoyage_sess154.adoc` (archive).

**Bac à sable** (non versionné, volatile) : `/tmp/opencode/bakery-i18n-lab/magic-stick/content/{quick-start,ab-partition,scripts}.yaml`.

**Commits** : `b2b00a2` (nettoyage BACKLOG).

**Décision session 156** : Aligner les 2 premiers YAML pivots (tables `text:`→`inline[]`), puis coder parser AsciiDoc→YAML (golden master test, baby steps DDD/TDD/BDD).

== Session 154 — 2026-06-23 — Loi de l'Économie d'Encre transverse (9 boroughs)

**Contexte** : Suite à la conceptualisation de la Loi de l'Économie d'Encre en session 153 dans bakery, propagation de la loi aux 9 autres boroughs LLM metered du workspace. Objectif : encodage durable de la contrainte d'idempotence économique dans la littérature de gouvernance transverse.

**Tâches** :

* [x] Ajout section "Loi de l'Économie d'Encre" dans `codex-gradle/AGENT.adoc` (Règle 5) — anti-patterns : re-embedding global, ré-extraction OCR, ré-ingestion inconditionnelle.
* [x] Ajout section dans `training-gradle/training-plugin/AGENT.adoc` — anti-patterns : régénération SPG systématique, batch SPD inconditionnel.
* [x] Ajout section dans `capsule-gradle/capsule-plugin/AGENT.adoc` — anti-patterns : re-TTS systématique, re-capture vidéo inconditionnelle.
* [x] Ajout section dans `plantuml-gradle/plantuml-plugin/AGENT.adoc` — anti-patterns : régénération diagrammes, re-rendu PNG inconditionnel.
* [x] Ajout section dans `readme-gradle/readme-plugin/AGENT.adoc` — anti-patterns : régénération README, re-rendu PNG, re-commit inutile.
* [x] Ajout section dans `document-gradle/document-plugin/AGENT.adoc` — anti-patterns : régénération AsciiDoc, re-conversion inconditionnelle.
* [x] Ajout section dans `agile-gradle/agile-plugin/AGENT.adoc` — anti-patterns : re-cadrage systématique, re-dashboard inconditionnel.
* [x] Ajout section dans `ticket-gradle/ticket-plugin/AGENT.adoc` — anti-patterns : re-création Issues (doublons API), re-enrichissement inconditionnel.
* [x] Ajout section dans `review-gradle/review-plugin/AGENT.adoc` — anti-patterns : re-review systématique, re-score inconditionnel.
* [x] 9 commits créés (un par repo git), uniquement sur `AGENT.adoc`.
* [x] 9 push vers remote (cccp-education + cheroliv).

**Fichiers modifiés** : 9 × `AGENT.adoc` (gouvernance uniquement, aucun code source Kotlin).

**Commits** : codex `29705c9`, training `e5ffa0b`, capsule `74a9710`, plantuml `193254f`, readme `2108e32`, document `d4a55d5`, agile `a233513`, ticket `1463975`, review `0dfaa1b`.

**Projection d'impact** : Potentiel 50-80% d'économie LLM sur le pipeline MVP0 complet si le pattern delta est implémenté dans le code des 9 boroughs (hypothèse : 3-5 ré-exécutions par cycle de dev).

== Session 153 — 2026-06-23 — BKY-I18N-DELTA Loi de l'Économie d'Encre + delta traduction i18n

**Contexte** : Découverte d'un anti-pattern de non-idempotence économique dans `I18nMigrationService.translateNonFrenchKeys()` qui re-traduisait l'intégralité des clés FR→EN via le LLM à chaque exécution de `migrateToI18n`, écrasant les traductions validées de `messages_en.properties`.

**Tâches** :

* [x] US-DELTA-1 — Delta traduction i18n : `translateNonFrenchKeys()` reçoit `templatesDir`, charge `messages_{lang}.properties` existant via `loadExistingTranslations()`, filtre les clés manquantes (`allKeys.filterKeys { !existing.containsKey(it) }`), ne traduit que le delta, fusionne `existing + newTranslations`.
* [x] US-DELTA-2 — Test `auto-translation reuses existing translations and only translates missing keys` : pré-remplit `messages_en.properties` avec `header.1` manuel, assert conservation + traduction `footer.1` manquant + `requestsReceived.size == 1` (LLM appelé 1 seule fois).
* [x] US-DELTA-3 — Conceptualisation gouvernance : section "Non-Répétition des Opérations Coûteuses — Loi de l'Économie d'Encre" ajoutée à `AGENT.adoc` (principe, fondement metered, règle load-before-write + delta, anti-patterns, protection, leçon S153, portée transverse 10 boroughs LLM metered).
* [x] Fix `IaConfigTest.kt:20` — `deepseek-v4-pro` → `gpt-oss:120b-cloud` (dernier résidu de modèle interdit dans le code source bakery). Code prod `IaConfig.kt` déjà correct (port 11464, gpt-oss:120b-cloud).

**Tests** : 3/3 `AutoTranslationI18nMigrationIntegrationTest` PASS. 7/7 `IaConfigTest` PASS. Suite i18n complète PASS.

**Fichiers modifiés** :

. `src/main/kotlin/bakery/i18n/I18nMigrationService.kt` — delta traduction + `loadExistingTranslations()`
. `src/test/kotlin/bakery/i18n/AutoTranslationI18nMigrationIntegrationTest.kt` — 1 nouveau test réutilisation
. `src/test/kotlin/bakery/llm/IaConfigTest.kt` — fix modèle interdit
. `AGENT.adoc` — Loi de l'Économie d'Encre
. `BACKLOG.adoc` — EPIC BKY-I18N-DELTA (3/3 pts ✅)
. `PROMPT_REPRISE.adoc`, `.agents/INDEX.adoc`, `.agents/SESSIONS_HISTORY.adoc`, `.agents/TEST_COVERAGE_ANALYSIS.adoc` — clôture session 153

== Session 150 — 2026-06-23 — BKY-I18N-PROD-AUDIT alignement fixtures ↔ sites réels

**Contexte** : Suite à la session 149, alignement des fixtures i18n de test avec les templates JBake réels de cccp.education et cheroliv.com. Approche baby steps DDD/TDD/BDD.

**Tâches réalisées** :

* [x] Relecture PROMPT_REPRISE S149 + AGENT + INDEX + BACKLOG.
* [x] Baseline GREEN : `./gradlew test --tests "bakery.i18n.audit.FixtureAlignmentAuditorTest"` 5/5 infrastructure + magic-stick GREEN, 2 audits RED attendus.
* [x] Backup des fixtures avant modification (`/tmp/opencode/cccp-education-fixture-backup-s150`, `/tmp/opencode/cheroliv-com-fixture-backup-s150`).
* [x] Alignement fixture `cccp-education` : suppression de `release.thyme`, copie des 11 templates réels.
* [x] Mise à jour `translations_en.properties` cccp-education (28 → 55 clés EN).
* [x] Alignement fixture `cheroliv-com` : copie des 19 templates racine + 5 placeholders `TODO/`.
* [x] Fix TDD `FixtureAlignmentAuditor.kt` : comparaison par chemins relatifs (subdirectory `TODO/`).
* [x] Mise à jour `translations_en.properties` cheroliv-com (~120 clés EN).
* [x] Validation : `FixtureAlignmentAuditorTest` 7/7 GREEN, `CccpEducationI18nMigrationIntegrationTest` 5/5 GREEN, `CherolivComI18nMigrationIntegrationTest` 5/5 GREEN, `./gradlew test` SUCCESS.
* [x] `publishToMavenLocal` après modification du code source.
* [x] Hook fermeture : PROMPT_REPRISE, INDEX, SESSIONS_HISTORY, TEST_COVERAGE_ANALYSIS, COMPLETED_TASKS_ARCHIVE mis à jour.

**Fichiers modifiés / créés** :

[cols="1,3,2"]
|===
| Fichier | Action | Note
| `src/main/kotlin/bakery/i18n/audit/FixtureAlignmentAuditor.kt` | modifié | comparaison par chemins relatifs
| `src/test/resources/i18n-fixtures/cccp-education/jbake/templates/*.thyme` | modifié/créé | alignement site réel
| `src/test/resources/i18n-fixtures/cccp-education/jbake/templates/release.thyme` | supprimé | absent du site réel
| `src/test/resources/i18n-fixtures/cccp-education/translations_en.properties` | modifié | 55 clés EN
| `src/test/resources/i18n-fixtures/cheroliv-com/jbake/templates/*.thyme` | modifié/créé | alignement site réel + TODO/
| `src/test/resources/i18n-fixtures/cheroliv-com/translations_en.properties` | modifié | ~120 clés EN
| `.agents/sessions/150-alignement-fixtures-sites-reels.adoc` | créé | archive session
|===

**Décisions techniques** :

. *Alignement exact* : copie fidèle des templates réels (y compris placeholders vides dans `TODO/`).
. *Bug découvert* : l'auditeur comparait par `File.name`, ce qui échouait sur les sous-répertoires. Correction en comparaison par chemin relatif.
. *Traductions* : manuelles, car les modèles Ollama cloud disponibles sont inadaptés aux traductions courtes.

**Statut** : BKY-I18N-PROD-AUDIT ✅ TERMINÉ. Score : 9/10.

---

== Session 149 — 2026-06-23 — BKY-I18N-PROD-AUDIT audit d'alignement fixtures ↔ sites réels

**Contexte** : Suite à la session 148, option 4 choisie : auditer et aligner les fixtures cccp.education et cheroliv.com avec les sites réels. Approche baby steps DDD/TDD/BDD.

**Tâches réalisées** :

* [x] Relecture PROMPT_REPRISE S148 + AGENT + INDEX + BACKLOG.
* [x] Baseline GREEN : `compileKotlin` ✅, `test` ✅.
* [x] Localisation des fixtures `src/test/resources/i18n-fixtures/{cccp-education,cheroliv-com,magic-stick}` et des sites réels `office/sites/{cccp.education,cheroliv.com}/jbake/templates`.
* [x] Diff reproductible : `/tmp/opencode/cccp_fixture_vs_site.diff` (821 lignes) + `/tmp/opencode/cheroliv_fixture_vs_site.diff` (1050 lignes).
* [x] DDD : `FixtureAlignmentAuditor` pur dans `src/main/kotlin/bakery/i18n/audit/FixtureAlignmentAuditor.kt` — rapport `missingInFixture`/`extraInFixture`/`mismatchedContent`.
* [x] TDD : `FixtureAlignmentAuditorTest` dans `src/test/kotlin/bakery/i18n/audit/FixtureAlignmentAuditorTest.kt` — 7 tests (4 tests infrastructure RED→GREEN + 3 audits de sites).
* [x] Résultat des audits : magic-stick ✅, cccp-education ❌ (`release.thyme` en trop + 11 templates diff), cheroliv-com ❌ (13 templates manquants + 11 templates diff).
* [x] Hook fermeture : PROMPT_REPRISE, INDEX, SESSIONS_HISTORY, TEST_COVERAGE_ANALYSIS, COMPLETED_TASKS_ARCHIVE mis à jour.

**Fichiers modifiés / créés** :

[cols="1,3,2"]
|===
| Fichier | Action | Lignes
| `src/main/kotlin/bakery/i18n/audit/FixtureAlignmentAuditor.kt` | NOUVEAU (DDD) | ~95
| `src/test/kotlin/bakery/i18n/audit/FixtureAlignmentAuditorTest.kt` | NOUVEAU (TDD) | ~125
| `PROMPT_REPRISE.adoc` | MàJ mission S150 | ~34
| `.agents/INDEX.adoc` | MàJ session en cours + portefeuille | +3 lignes
| `.agents/SESSIONS_HISTORY.adoc` | Ajout session 149 | +1 entrée
| `.agents/TEST_COVERAGE_ANALYSIS.adoc` | MàJ compteurs tests + historique | +2 lignes
| `.agents/archives/COMPLETED_TASKS_ARCHIVE_2026-06.adoc` | Ajout session 149 | ~35 lignes
|===

**Décisions techniques** :

. *Option C (test d'audit)* choisie : créer un service d'audit reproductible avant de modifier les fixtures. Les tests RED documentent l'écart.
. *Fixture magic-stick* : déjà alignée (12/12 identiques).
. *Fixtures cccp-education/cheroliv-com* : sont des stubs de scaffolding, pas des copies fidèles. Alignement reporté session 150.
. *Aucune fixture modifiée* dans cette session : l'objectif était de mesurer et de verrouiller avec des tests.

**Statut** : BKY-I18N-PROD-AUDIT 🟠 En cours — phase mesure/contract terminée. Alignement fixtures reporté S150. Score : 8/10.

---

== Session 148 — 2026-06-23 — Audit migration i18n réelle magic-stick

**Contexte** : Suite à la demande d'analyse du backlog, investigation de l'écart entre les fixtures i18n de test et les sites réels. Test de `migrateToI18n` sur une copie temporaire du site magic-stick avec le plugin bakery 0.0.2.

**Tâches réalisées** :

* [x] Analyse du backlog bakery : tous les EPICs terminés, aucun EPIC actif prioritaire.
* [x] Vérification de l'état i18n des 3 sites : magic-stick, cccp.education, cheroliv.com — aucun n'a de `messages_*.properties` ni de clés `#{...}` dans les templates réels.
* [x] Comparaison fixtures de test vs sites réels : magic-stick fixture = site réel (12/12 templates identiques), cccp.education et cheroliv.com fixtures différentes.
* [x] Publication locale de bakery-plugin 0.0.2 (`publishToMavenLocal`).
* [x] Création d'un projet consommateur temporaire dans `/tmp/opencode/bakery-migration/`.
* [x] Exécution de `migrateToI18n` en dry-run : 45 clés extraites, aucune modification.
* [x] Exécution de `migrateToI18n` réel avec IA (gemma4:31b-cloud sur port 11440) : 12 templates modifiés, `messages_en.properties` généré, `site.language=fr` injecté.
* [x] Validation : les traductions IA gemma4 sont inutilisables (tutoriels d'explication au lieu de traductions courtes).
* [x] Test qwen3.5:397b-cloud : présent mais requiert abonnement Ollama payant.

**Fichiers modifiés** :

Aucun fichier source du repo bakery n'a été modifié. Fichiers temporaires créés dans `/tmp/opencode/bakery-migration/`.

**Décisions techniques** :

. *Ports Ollama* : tests effectués sur les ports 11437-11465 (autorisés) ; ports 11434-11436 non utilisés.
. *Modèle LLM* : gemma4:31b-cloud disponible mais inadapté aux traductions courtes. qwen3.5:397b-cloud requiert abonnement.
. *Stratégie* : avant application au site réel, il faut améliorer le pipeline de traduction ou utiliser les traductions manuelles de la fixture.

**Statut** : Session d'audit terminée — 7/10. Aucun EPIC avancé, mais base de décision établie pour la session 149.

---

== Session 147 — 2026-06-23 — BKY-FIX-1 Corriger deployProfile sans site.yml

**Contexte** : PROMPT_REPRISE session 146 identifiait une microfissure Cucumber Feature 5 : le scénario `deployProfile fails when site.yml does not exist` échouait car la tâche `deployProfile` n'était pas enregistrée quand `site.yml` était absent (mode scaffold only). L'utilisateur a rappelé la méthodologie baby steps DDD/TDD/BDD + agile software craftmanship et a validé l'approche B (UX robuste).

**Tâches réalisées** :

* [x] Baseline GREEN — relecture PROMPT_REPRISE, INDEX, AGENT, BACKLOG. Lancement ciblé de Feature 5 : 1 échec confirmé (`Task 'deployProfile' not found`).
* [x] DDD — `src/main/kotlin/bakery/DeployTaskRegistrar.kt` : `registerDeployProfileTask()` devient indépendant de `SiteConfiguration`. La tâche lit la configuration au runtime.
* [x] Refactor — `src/main/kotlin/bakery/BakeryPlugin.kt` : appel inconditionnel à `registerDeployProfileTask()` dans `registerScaffoldOnlyTasks` et `registerFullPipelineTasks` (condition `resolvedSite.pushProfile != null` supprimée). Commentaires BKY-FIX-1 ajoutés.
* [x] TDD adapté — `BakeryPluginFullPipelineTest` : `deployProfile` attendue toujours enregistrée. `DeployTaskRegistrarTest` : appel sans `SiteConfiguration`. `ProfilePublisherFunctionalTest$FailureCasesTest` : pushProfile absent attend maintenant le message métier `pushProfile section not found in site.yml`.
* [x] BDD adapté — Feature 5 scénario "not registered when pushProfile is absent" remplacé par "always registered regardless of pushProfile configuration". `PublishProfileSteps.kt` capture l'exception même quand des credentials sont fournis, permettant à `deployProfileShouldFailWithMessage` de vérifier `Failed to read site.yml`.
* [x] Safety net — `./gradlew test functionalTest` ✅, `./gradlew cucumberTest` ✅, `./gradlew check -x e2eTest` ✅ (Kover instruction coverage 92,58%), `./gradlew -q publishToMavenLocal` ✅.

**Fichiers modifiés** :

[cols="1,3"]
|===
| Fichier | Action
| `src/main/kotlin/bakery/DeployTaskRegistrar.kt` | Signature `registerDeployProfileTask()` sans `SiteConfiguration`
| `src/main/kotlin/bakery/BakeryPlugin.kt` | Appel inconditionnel dans scaffold + full pipeline
| `src/test/kotlin/bakery/BakeryPluginFullPipelineTest.kt` | Assertion `deployProfile` toujours enregistrée
| `src/test/kotlin/bakery/DeployTaskRegistrarTest.kt` | Appel sans argument + nettoyage helper inutilisé
| `src/functionalTest/kotlin/bakery/ProfilePublisherFunctionalTest.kt` | Failure case pushProfile absent → message métier
| `src/test/features/5_publish_profile_task.feature` | Scénario "always registered" + site.yml absent
| `src/test/scenarios/bakery/scenarios/PublishProfileSteps.kt` | Capture d'exception dans `@When` deployProfile
|===

**Décisions techniques** :

. *Approche B choisie* — Toujours enregistrer la tâche et laisser `DeployProfileTask` échouer au runtime. Meilleure UX que le message Gradle "Task 'deployProfile' not found".
. *Pas de nouveaux tests unitaires* — Correction par adaptation du contrat ; les tests existants suffisent après mise à jour.
. *Aucune régression* — Tous les safety nets passent, coverage inchangé.

**Statut** : BKY-FIX-1 ✅ TERMINÉ — 2 pts.

---

== Session 145 — 2026-06-23 — BKY-CR-V6 US-3 ConfigResolver.resolveAll() data-driven

**Contexte** : PROMPT_REPRISE session 144 donnait la mission US-3. L'utilisateur a rappelé la méthodologie baby steps DDD/TDD/BDD + agile software craftmanship. Refactor data-driven des 10 blocs `resolveDomain` répétitifs dans `ConfigResolver.resolveAll()` (lignes 332-401).

**Tâches réalisées** :

* [x] Baseline GREEN — `git status` propre (session 144), `compileKotlin` + `ResolvedConfigsTest` OK.
* [x] TDD caractérisation — `src/test/kotlin/bakery/DomainResolverTest.kt` (NOUVEAU, 124 lignes, 9 UT) : 2 `@Nested` (Structure 2 tests : name/resolver/fallback ; Resolve 7 tests : succès, fallback, accumulation DomainFailure, message null, capture cause Exception vs Throwable non-Exception, indépendance multi-resolvers). Tous RED contre code actuel puis GREEN avec `DomainResolver.kt`.
* [x] DDD — `src/main/kotlin/bakery/DomainResolver.kt` (NOUVEAU, 23 lignes) : `DomainResolver<T>(name, resolver, fallback)` data class + `resolve(errors: MutableList<ConfigResolutionError>): T` utilisant `Either.catch` d'Arrow. Centralise la logique d'accumulation d'erreurs qui était dupliquée 10 fois dans `ConfigResolver.resolveAll()`.
* [x] TDD `resolveAllDataDriven` — 9 tests dans `ResolvedConfigsTest.kt` (`ResolveAllDataDrivenTest`) : comparaison identité comportement `resolveAll()` vs `resolveAllDataDriven()` (defaults, CLI overrides, YAML overrides, DSL overrides, language, supportedLanguages, 10 domains sans erreurs, type ResolvedConfigs, accumulation erreurs). Tous RED puis GREEN.
* [x] Refactor — `src/main/kotlin/bakery/ConfigResolver.kt` : `resolveAll()` délègue à `resolveAllDataDriven()`. Suppression de la fonction locale `resolveDomain<T>()` et des 10 blocs répétitifs (72 lignes → 3 lignes, -96%). `resolveAllDataDriven()` itère une liste de 10 `DomainResolver<*>` et reconstruit `ResolvedConfigs`. Import `arrow.core.Either` inutilisé retiré. `@Suppress("UNCHECKED_CAST")` localisé sur le cast `List<String>` (type erasure inévitable avec liste hétérogène).
* [x] Safety net — `./gradlew test` : 1123/1123 UT PASS. `publishToMavenLocal` OK (GPG signing). Compile clean, zéro warning.
* [x] Cucumber `cucumberTest` : timeout 5min (EPIC 5 préexistant, non lié au refactor).

**Fichiers modifiés / créés** :

[cols="1,3,2,2"]
|===
| Fichier | Action | Lignes avant | Lignes après
| `src/main/kotlin/bakery/ConfigResolver.kt` | Refactor (resolveAll) | 436 | 407 (-29 net, -69 sur resolveAll)
| `src/main/kotlin/bakery/DomainResolver.kt` | NOUVEAU (DDD) | 0 | 23
| `src/test/kotlin/bakery/DomainResolverTest.kt` | NOUVEAU (TDD) | 0 | 124
| `src/test/kotlin/bakery/ResolvedConfigsTest.kt` | Complété (TDD) | 506 | 675 (+169)
|===

**Décisions techniques** :

. *Liste hétérogène `DomainResolver<*>`* — Nécessaire car les 10 domaines retournent des types différents. Le résultat est reconstruit par index, avec `@Suppress("UNCHECKED_CAST")` localisé.
. *Comportement strictement identique* — Les tests `ResolveAllDataDrivenTest` caractérisent et vérifient l'équivalence exacte avec `resolveAll()` pour tous les domaines et toutes les couches de configuration.
. *Pas de BDD Cucumber nouveau* — Refactor purement interne (API publique `resolveAll()` inchangée). Les tests Cucumber existants valident le comportement de bout en bout.

**Statut EPIC BKY-CR-V6** : US-1 ✅ S142 (3 pts), US-2 ✅ S144 (2 pts), US-3 ✅ S145 (3 pts). **EPIC BKY-CR-V6 TERMINÉ — 8/8 pts**.

---

== Session 157 — Parser AsciiDoc→YAML Pivot + READMEs badges (2026-06-24)

**Baby steps DDD/TDD/BDD — Parser pivot AsciiDoc→YAML pour traduction nœud-par-nœud.**

**Tâches terminées** :

. *Alignement YAML pivots* — `ab-partition.yaml` : 2 tables migrées `text:`→`inline[]` (16 cellules). Contrat pivot homogène sur 3 fichiers.
. *PivotModel.kt* (DDD) — sealed classes : 8 types blocs (Heading, Paragraph, ListBlock, Table, Admonition, Source, Hr) + 4 types inline (Text, Bold, Code, Link) + PivotFrontmatter + PivotArticle. 84 lignes.
. *PivotYamlRenderer.kt* (DDD) — renderer déterministe PivotArticle→YAML. 134 lignes. Produit le format pivot figé.
. *AsciiDocParser.kt* (DDD) — parser AsciiDoc→PivotArticle. 226 lignes. Gère : frontmatter (title/date/type/status + ~~~~~~), headings (==/===/====), paragraphs (multi-lignes + inline), inline (bold `**..**`, code `` `..` ``, link `link:url[label]` + direct `url[label]`), lists (`* `/`. `), source (`[source,lang]` + `----`), admonition (`[KIND]` + `====`), hr (`---`), tables (`[cols=".."]` + `|===`, cellules inline code+text, cols null auto-détection).
. *Tests* — 30 UT PASS : PivotModelTest (4), PivotYamlRendererTest (9), AsciiDocParserTest (17). PivotGoldenMasterTest (3) créé mais bloqué par bug découverte Gradle.
. *READMEs badges* — 4 fichiers (README.adoc, README_truth.adoc, README_fr.adoc, README_truth_fr.adoc) : badges shields.io (build, coverage 85.88%, i18n 10 langues, Gradle, Kotlin, Java, license) + tableau i18n avec locuteurs du CSV (~5.4B total) + section coverage.

**Fichiers créés** :

[cols="1,3,2]
|===
| Fichier | Action | Lignes

| `src/main/kotlin/bakery/pivot/PivotModel.kt` | NOUVEAU (DDD) | 84
| `src/main/kotlin/bakery/pivot/PivotYamlRenderer.kt` | NOUVEAU (DDD) | 134
| `src/main/kotlin/bakery/pivot/AsciiDocParser.kt` | NOUVEAU (DDD) | 226
| `src/test/kotlin/bakery/pivot/PivotModelTest.kt` | NOUVEAU (TDD) | 60
| `src/test/kotlin/bakery/pivot/PivotYamlRendererTest.kt` | NOUVEAU (TDD) | 160
| `src/test/kotlin/bakery/pivot/AsciiDocParserTest.kt` | NOUVEAU (TDD) | 414
| `src/test/kotlin/bakery/pivot/PivotGoldenMasterTest.kt` | NOUVEAU (TDD) | 56
| `src/test/resources/pivot-golden-master/*` | NOUVEAU (6 fichiers) | —
|===

**Statut** : Parser pivot fonctionnel (30 UT). Golden master à débugger (découverte Gradle). READMEs à jour.

---

== Session 164 — 2026-06-24 — Fix OGNL templates + Optimisation E2E Playwright

**Contexte** : Verification build complet apres S163. koverThresholdCheck revele 2 E2E FAILED (i18n).

**Fix OGNL** : `#sets.contains({'ar','ur'}, ...)` -> `#lists.contains` dans 10 templates Thymeleaf. OGNL cree ArrayList avec `{...}`, pas Set. `IllegalArgumentException: Unable to convert type java.util.ArrayList to type of java.util.Set`.

**Optimisations E2E** :
- Cache `installPlaywright` : `onlyIf` sur `~/.cache/ms-playwright/chromium-*` (SKIPPED si present)
- `PlaywrightPool` singleton JVM-wide (1 Playwright + 1 Browser pour tout, avant 5x)
- `forkEvery=1` supprime (1 JVM partagee)
- `ThymeleafRenderingTestFactory` mutualise singleton companion

**Tests** : 19/19 E2E PASS en 1m44s (avant : 2 FAILED). installPlaywright SKIPPED sur second run.

**Fichiers** : 12 modifiés + 1 créé (PlaywrightPool.kt).

**Score** : 9/10

---

== Session 163 — 2026-06-24 — Loi Économie d'Encre generatePivotYaml + 4e golden master + bug cucumber

**Contexte** : Trois tâches reportées de S161.

**DDD/TDD** : `GeneratePivotYamlTask.generate()` — load-before-write avec comparaison byte-à-byte. Skip si output existant identique.

**Tests** :
- 1 nouveau FT : `generatePivotYaml does not rewrite identical output on second invocation` → 6/6 FT PASS
- 4e golden master `nested-plantuml` (listes `**`/`***` + plantuml pass-through) → 4/4 GM PASS
- Investigation bug cucumber features 9/16 : déjà résolu par fix S161 (DuplicateStepDefinitionException)

**Build** :
- `./gradlew test` : 1m SUCCESS
- `./gradlew cucumberTest` : 37 features, 0 FAILED

**Fichiers** : 3 modifiés + 2 créés. 54 lignes ajoutées.

**Score** : 9/10

---
