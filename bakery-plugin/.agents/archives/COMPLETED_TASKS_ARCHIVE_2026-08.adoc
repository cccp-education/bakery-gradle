= Archive des Taches Terminees — Aout 2026

== Session 198 — 2026-08-15 — BKY-DNS-3 TERMINÉ (DnsProvisioner : réconciliation idempotente)

**Contexte** : Troisième US d'implémentation de l'EPIC BKY-DNS. Suite à
BKY-DNS-2 (client HTTP OVH S197), implémenter la réconciliation idempotente en
DDD pur — zéro I/O, zéro Gradle — avec un port de mutation DNS testable
(pattern capsule).

**Taches livrees** :
- Domaine `bakery.dns` enrichi (9 fichiers prod purs) :
  `DnsChange` (sealed Create/Update(id, record, previous)/Delete),
  `ExistingDnsRecord`, `DnsDiff` (object pur, slot (type, name) + jeu de
  valeurs : manquant → Create, présent + TTL différent → Update garde l'id,
  actuel sans désiré → Delete, drift de valeur → Create + Delete),
  `DnsReconciliationResult` (plan/applied/skipped/noop), `DnsProvider` (port
  listRecords/create/update/delete/refreshZone/isAvailable/name),
  `OvhDnsProvider` (adapter → OvhDnsClient), `NoOpDnsProvider` (dégradé zone
  vide mutations no-op), `DnsProviderFactory.resolve` (ovh avec client → Ovh
  sinon NoOp), `DnsProvisioner.reconcile(desired, dryRun=true,
  allowDelete=false)` (noop si plan vide, refresh seulement si applied non vide)
- 31 tests nouveaux (10 diff + 9 provisioner + 4 noop + 4 factory + 4 ovh
  provider) — 83 dans `bakery.dns`, 0 FAIL

**Fichiers crees** :
- `src/main/kotlin/bakery/dns/DnsChange.kt`
- `src/main/kotlin/bakery/dns/ExistingDnsRecord.kt`
- `src/main/kotlin/bakery/dns/DnsDiff.kt`
- `src/main/kotlin/bakery/dns/DnsReconciliationResult.kt`
- `src/main/kotlin/bakery/dns/DnsProvider.kt`
- `src/main/kotlin/bakery/dns/OvhDnsProvider.kt`
- `src/main/kotlin/bakery/dns/NoOpDnsProvider.kt`
- `src/main/kotlin/bakery/dns/DnsProviderFactory.kt`
- `src/main/kotlin/bakery/dns/DnsProvisioner.kt`
- `src/test/kotlin/bakery/dns/DnsDiffTest.kt` (10)
- `src/test/kotlin/bakery/dns/DnsProvisionerTest.kt` (9)
- `src/test/kotlin/bakery/dns/NoOpDnsProviderTest.kt` (4)
- `src/test/kotlin/bakery/dns/DnsProviderFactoryTest.kt` (4)
- `src/test/kotlin/bakery/dns/OvhDnsProviderTest.kt` (4)

**Fichiers modifies** :
- `BACKLOG.adoc` — BKY-DNS-3 ✅ S198 (version 24.7)
- `.agents/INDEX.adoc` — entrée session 198 + EPIC BKY-DNS 🟠
- `.agents/SESSIONS_HISTORY.adoc` — entrée session 198
- `.agents/TEST_COVERAGE_ANALYSIS.adoc` — mise à jour session 198
- `PROMPT_REPRISE.adoc` — session 199, mission BKY-DNS-4

**Tests** : `--tests "bakery.dns.*"` 83/83 PASSED (BUILD SUCCESSFUL).
Non-régression suite complète : 3 échecs PRÉEXISTANTS environnementaux
identiques S196/197 (RealSiteI18nMigrationIntegrationTest /
FixtureAlignmentAuditorTest / I18nDeltaWireCucumberTest). `publishToMavenLocal`
VERT (règle INDEX 0, signature GPG). Zéro secret commité.

**Décision** : sémantique diff par slot (type, name) + jeu de valeurs ; drift de
valeur = Create + Delete (pas Update en place, aligné ovh.py + multi-valeurs
apex GitHub Pages) ; drift TTL sur valeur identique = Update garde l'id.

**Prochaine session (199)** : BKY-DNS-4 — `ProvisionDnsTask` + wiring : tâche
`provisionDns` groupe deployment, chaînée avant `deploySite`, enregistrée dans
`BakeryPlugin`/registrar dédié (functional, ~4 pts).

**Score** : 9/10

== Session 197 — 2026-08-15 — BKY-DNS-2 TERMINÉ (OvhDnsClient : signature HMAC + endpoints + fake HTTP)

**Contexte** : Deuxième US d'implémentation de l'EPIC BKY-DNS. Suite à
BKY-DNS-1 (domaine `bakery.dns` ouvert S196), implémenter le client HTTP OVH en
DDD pur — zéro I/O dans le domaine — avec un transport injectable.

**Taches livrees** :
- Domaine `bakery.dns` enrichi (7 fichiers prod purs) :
  `OvhSignature` (object pur `sign(AS, CK, method, url, body, ts)` → `$1$` +
  sha1 minuscule), `OvhEndpoint` (BASE_URL `https://eu.api.ovh.com/1.0` + path
  builders), `OvhRecord` (wire, id `@JsonInclude(NON_NULL)` jamais en body,
  mapping ↔ `DnsRecord` apex `@` ↔ `subDomain=""`), `OvhHttp` (port `call` →
  `OvhHttpResponse`), `OvhDnsException`, `OvhDnsClient` (6 opérations, non-2xx →
  exception), `JavaOvhHttp` (transport prod HttpClient injectable timeout 10s +
  headers `X-Ovh-*`)
- 32 tests nouveaux (5 signature + 4 endpoint + 6 mapper + 9 client + 8
  functional HTTP réel local) — 52 dans `bakery.dns`, 0 FAIL

**Fichiers crees** :
- `src/main/kotlin/bakery/dns/OvhSignature.kt`
- `src/main/kotlin/bakery/dns/OvhEndpoint.kt`
- `src/main/kotlin/bakery/dns/OvhRecord.kt`
- `src/main/kotlin/bakery/dns/OvhHttp.kt`
- `src/main/kotlin/bakery/dns/OvhDnsException.kt`
- `src/main/kotlin/bakery/dns/OvhDnsClient.kt`
- `src/main/kotlin/bakery/dns/JavaOvhHttp.kt`
- `src/test/kotlin/bakery/dns/OvhSignatureTest.kt` (5)
- `src/test/kotlin/bakery/dns/OvhEndpointTest.kt` (4)
- `src/test/kotlin/bakery/dns/OvhRecordMapperTest.kt` (6)
- `src/test/kotlin/bakery/dns/OvhDnsClientTest.kt` (9)
- `src/test/kotlin/bakery/dns/OvhDnsClientFunctionalTest.kt` (8)

**Fichiers modifies** :
- `BACKLOG.adoc` — BKY-DNS-2 ✅ S197 (version 24.6)
- `.agents/INDEX.adoc` — entrée session 197 + EPIC BKY-DNS 🟠
- `.agents/SESSIONS_HISTORY.adoc` — entrée session 197
- `.agents/TEST_COVERAGE_ANALYSIS.adoc` — mise à jour session 197
- `PROMPT_REPRISE.adoc` — session 198, mission BKY-DNS-3

**Tests** : `--tests "bakery.dns.*"` 52/52 PASSED (BUILD SUCCESSFUL 22s).
Non-régression suite complète : 1538 tests, 3 échecs PRÉEXISTANTS
environnementaux identiques S196 (RealSiteI18nMigrationIntegrationTest /
FixtureAlignmentAuditorTest / I18nDeltaWireCucumberTest). `publishToMavenLocal`
VERT (règle INDEX 0, signature GPG). Zéro secret commité.

**Prochaine session (198)** : BKY-DNS-3 — `DnsProvisioner` réconciliation
idempotente (diff désirés vs actuels, create/update/delete, dry-run par défaut,
noop si identique, port `DnsProvider` pattern capsule, ~8 pts).

**Score** : 9/10

== Session 196 — 2026-08-15 — BKY-DNS-1 TERMINÉ (DnsConfig + mapping Jackson site.yml)

**Contexte** : Première US d'implémentation de l'EPIC BKY-DNS (provisionnement
DNS OVH depuis site.yml). Suite au cadrage S195, ouvrir le domaine `bakery.dns`
en DDD pur — zéro I/O, zéro Gradle — et mapper la section `dns:` dans
`SiteConfiguration` (Jackson).

**Taches livrees** :
- Domaine `bakery.dns` (4 fichiers prod purs) : `DnsConfig` (provider défaut
  `ovh`, domain, records, dryRun défaut true, credentials,
  `@JsonIgnoreProperties(ignoreUnknown = true)`), `DnsRecord` (type/name/value/
  ttl 3600, apex `@`), `OvhCredentials` (toString masqué pattern
  `RepositoryCredentials` + `maskSecret` — zéro fuite de secret, test dédié),
  `DnsConfigMerger` object pur 4 sources CLI > YAML > Props > ENV (pattern
  capsule `mergeAudioPostConfig` : mergeStr isNotBlank heuristic, mergeBoolean,
  records liste native YAML, creds champ à champ)
- `SiteConfiguration.dns: DnsConfig? = null` — section absente = pas de
  provisionnement (backward compat strict, les sites existants sans `dns:`
  restent inchangés)
- 16 unit TDD strict RED → GREEN : 7 `DnsConfigTest` (defaults, masquage,
  mapping Jackson records/creds/absent→null/unknown ignored) + 9
  `DnsConfigMergerTest` (précédences, boolean, records, creds, cli blank)

**Fichiers crees** :
- `src/main/kotlin/bakery/dns/DnsConfig.kt`
- `src/main/kotlin/bakery/dns/DnsRecord.kt`
- `src/main/kotlin/bakery/dns/OvhCredentials.kt`
- `src/main/kotlin/bakery/dns/DnsConfigMerger.kt`
- `src/test/kotlin/bakery/dns/DnsConfigTest.kt` (7 unit)
- `src/test/kotlin/bakery/dns/DnsConfigMergerTest.kt` (9 unit)

**Fichiers modifies** :
- `src/main/kotlin/bakery/SiteConfiguration.kt` (+1 champ `dns` + import)
- `BACKLOG.adoc` — BKY-DNS-1 ✅ S196 (version 24.5)
- `.agents/INDEX.adoc` — entrée session 196 + EPIC BKY-DNS 🟠
- `.agents/SESSIONS_HISTORY.adoc` — entrée session 196
- `.agents/TEST_COVERAGE_ANALYSIS.adoc` — mise à jour session 196

**Tests** : `--tests "bakery.dns.*"` 16/16 PASSED (BUILD SUCCESSFUL 29s).
Non-régression suite complète : 1506 tests, 3 échecs PRÉEXISTANTS
environnementaux (Fixtures i18n vs sites réels `office/sites/*` — prouvé par
`git stash -u` + rerun ciblé). `publishToMavenLocal` VERT (règle INDEX 0).
Zéro secret commité.

**Prochaine session (197)** : BKY-DNS-2 — `OvhDnsClient` : signature HMAC
SHA-1 OVH (`$1$` + AS+CK+method+url+body+ts), endpoints
list/get/create/update/delete/refresh (`eu.api.ovh.com/1.0`), http client
injectable (fake en test), zéro I/O réseau dans le domaine pur (~8 pts).

**Score** : 9/10

== Session 195 — 2026-08-12 — Cadrage EPIC BKY-DNS (provisionnement DNS OVH depuis site.yml)

**Contexte** : Suite à la mise en production de `talaria.school` (S-004/005
office), l'opération DNS (lien domaine OVH ↔ GitHub Pages : 4 A apex + 1 CNAME
www, suppression 3 TXT de redirection OVH) a été faite à la main via un script
Python jetable (`~/workspace/configuration/scripts/ovh.py`) — non testé, non
idempotent, hors écosystème Kotlin. Session de gouvernance pure (pattern
S-057, zéro code modifié) pour cadrer la transposition en tâche Kotlin bakery.

**Taches livrees** :
- EPIC BKY-DNS cadré dans BACKLOG.adoc : 7 US (US-0 cadrage → US-6 dogfooding
  talaria.school), ~33 pts, section `dns:` dans site.yml (mapping Jackson
  SiteConfiguration), port `DnsProvider`/`OvhDnsProvider`/`NoOpDnsProvider`
  (pattern capsule `resolve*Provider`), réconciliation idempotente
  create/update/delete, dry-run par défaut + `--apply` explicite, wiring
  `provisionDns` → `deploySite`
- Notes de revue (avis sur l'idée des tâches DNS) : delete protégé
  (`--apply --purge`), portée réduite US-1/2/3 = 80% (pas de contrat N0 OVH,
  YAGNI), credentials via gradle.properties/env (jamais commitées) + fake
  provider en tests (zéro OVH réel CI), priorité P2 derrière BKY-I18N-MASS
- Dashboard/historique : INDEX.adoc (session 195 + EPIC BKY-DNS P2),
  SESSIONS_HISTORY.adoc, PROMPT_REPRISE.adoc (session 196 → mission
  BKY-DNS-1 `DnsConfig` + mapping Jackson)

**Fichiers modifies/crees** :
- `BACKLOG.adoc` — EPIC BKY-DNS (US-0→6) + notes de revue + version 24.4
- `.agents/INDEX.adoc` — entrée session 195 + EPIC BKY-DNS P2
- `.agents/SESSIONS_HISTORY.adoc` — entrée session 195
- `PROMPT_REPRISE.adoc` — session 196, mission BKY-DNS-1
- `.agents/sessions/195-cadrage-bky-dns.adoc` — NOUVEAU (rapport session)

**Tests** : N/A — zéro code source modifié, aucune régression possible.

**Prochaine session (196)** : BKY-DNS-1 — DDD pur `DnsConfig` (domaine
`bakery.dns`) + mapping Jackson section `dns:` + merger 4 sources (pattern
`mergeOutputConfig`). TDD strict RED → GREEN → REFACTOR (~8 unit).

**Score** : 8/10

== Session 192 — 2026-08-04 — EPIC BKY-I18N-MASS — MASS-2 CLOS (Issue 191-1 fixée)

**Contexte** : Session 191 avait livré MASS-1 (BlockChecksum + BlockDelta DDD pur)
et le câblage MASS-2, mais 1 FT rouge bloquait la clôture : la copy loop
`MigrateContentI18nTask:124-129` écrasait la cible avec la source avant traduction
bloc-delta, violant la Loi Économie d'Encre.

**Taches livrees** :
- Fix Issue 191-1 : copy loop déplacée dans branche `translationService == null`
- `translateSingleFileWithBlockDelta` écrit lui-même `targetFile` (branche LLM)
- 12/12 FT `MigrateContentI18nDeltaFunctionalTest` verts
- 1/1 `BlockDeltaWireDebugTest` vert
- Debug `println` nettoyés (4 fichiers, 24 lignes)
- publishToMavenLocal document-gradle + bakery-gradle OK

**Fichiers modifies** :
- `bakery-plugin/src/main/kotlin/bakery/i18n/MigrateContentI18nTask.kt` — copy loop déplacée
- `bakery-plugin/src/test/kotlin/bakery/i18n/MigrateContentI18nDeltaFunctionalTest.kt` — debug println nettoyés
- `document-plugin/src/main/kotlin/document/translation/ContentTranslationService.kt` — debug println nettoyés
- `document-plugin/src/test/kotlin/document/translation/BlockDeltaWireDebugTest.kt` — debug println nettoyés

== Session 188 — 2026-08-03 — EPIC BKY-I18N-CI CLOS (19/19 pts)

**Contexte** : EPIC BKY-I18N-CI cadré Session 029 document/bakery/office-cheroliv.com
(2026-07-21). CI-1/CI-2/CI-3 déjà livrés S185. CI-5 workflows déjà existants.
Session 188 a livré CI-4 + CI-6 + gap CI-2 (buildMaskedSummary ollama).

**Taches livrees** :
- CI-4 : `site.yml.example` créé dans `office/sites/cheroliv.com/` (29 Device Keys template, ports 11437-11465, model gemma4:31b-cloud)
- CI-6 : BDD feature 58 `58_i18n_ci_ollama_device_key.feature` (4 scénarios, 4/4 PASS)
- CI-2 gap : `buildMaskedSummary` étendu pour ollama (model, ports, deviceKeys count)
- `validateRequiredFields` rendu `internal` pour testabilité
- YAML fixtures `OllamaConfigSteps` étendus (pushPage + pushMaquette requis)
- Bug BakeryPluginPublicationTest : déjà résolu (0.0.5)

**Fichiers modifies** :
- `src/main/kotlin/bakery/VerifyConfigurationMappingTask.kt` — buildMaskedSummary + ollama, validateRequiredFields internal
- `src/test/kotlin/bakery/VerifyConfigurationMappingTaskTest.kt` — +1 UT ollama masked summary
- `src/test/scenarios/bakery/scenarios/OllamaConfigSteps.kt` — +5 steps feature 58, YAML fixtures étendus
- `src/test/features/58_i18n_ci_ollama_device_key.feature` — nouveau (4 scénarios)
- `office/sites/cheroliv.com/site.yml.example` — nouveau

**Tests** : 1 UT + 4 BDD ajoutés. compileKotlin + compileTestKotlin BUILD SUCCESSFUL.
publishToMavenLocal OK.

**Score** : 9/10

== Session 189 — 2026-08-04 — EPIC BKY-OUTPUT CLOS (49/49 pts, 100%)

**Contexte** : Dernière US de l'EPIC BKY-OUTPUT. `ThemeResolver` (TREE-5, S167)
et `OutputConfigResolver` (OUTPUT-2, S172) existaient séparément.
`TreeBakeService.injectTreeConfig()` appelait les deux indépendamment.

**Taches livrees** :
- OUTPUT-5 : `OutputConfigResolver` accepte `ThemeResolver?` optionnel, résout le thème dans `effectiveConfig`
- `TreeBakeService` simplifié : `ThemeResolver` passé à `OutputConfigResolver`, `buildConfigMap` sans paramètre `theme`
- +6 UT `OutputConfigResolverTest` (thème via ThemeResolver, défaut, override article, sibling, resolveAll, backward compat)
- Fix `sampleTree()` : ajout `blog/hello` article, `resolveAll` 5→6

**Fichiers modifies** :
- `src/main/kotlin/bakery/tree/OutputConfigResolver.kt` — +themeResolver param, résolution thème
- `src/main/kotlin/bakery/tree/TreeBakeService.kt` — ThemeResolver via OutputConfigResolver, buildConfigMap simplifié
- `src/test/kotlin/bakery/tree/OutputConfigResolverTest.kt` — +6 tests, fix sampleTree

**Tests** : +6 UT. 12/12 OutputConfigResolverTest + 7/7 TreeBakeServiceTest + 100+ tree PASS.
Cucumber features 39-48 tous PASS. 2 échecs préexistants non liés.
publishToMavenLocal OK.

**Score** : 9/10

== Session 190 — 2026-08-04 — Renfort couverture tests i18n deploy + cadrage EPIC BKY-I18N-MASS

**Contexte** : Renfort TDD/BDD sur le pipeline i18n deploy. Fake translator trop simpliste
(remplacement global) — ne couvrait pas les stratégies mixtes PlantUML.

**Fait** :
* [Fake translator] ✅ — Fragment-level, préfixe `[lang]` systématique, dictionnaire labels PlantUML 20 entrées
* [Feature 55] ✅ — 8/8 PASS (+1 scénario mixed strategies)
* [Fixture] ✅ — `diagrammes-mixtes.adoc` (2 blocs plantuml stratégies différentes)
* [Standalone runner] ✅ — `I18nDeployEndToEndCucumberTest` (pattern feature 56/57)
* [EPIC BKY-I18N-MASS] ✅ — Cadré (priorité 1, ~32 pts, 6 US). 3 gaps : dogfooding unilangue, delta bloc, validation STRICT
* [publishToMavenLocal] ✅ — OK

**Fichiers modifies** :
- `src/test/scenarios/bakery/scenarios/I18nDeployEndToEndSteps.kt` — fake translator fragment-level
- `src/test/scenarios/bakery/scenarios/I18nDeployMultiSiteSteps.kt` — idem
- `src/test/features/55_i18n_deploy_end_to_end.feature` — +1 scénario mixed strategies
- `src/test/resources/fixtures/cheroliv-com-i18n-deploy/content/blog/diagrammes-mixtes.adoc` — nouveau
- `src/test/kotlin/bakery/i18n/deploy/I18nDeployEndToEndCucumberTest.kt` — nouveau

**Tests** : Feature 55 8/8 PASS. Dictionnaire labels 20 entrées aligné. publishToMavenLocal OK.

**Score** : 9/10

== Session 191 — 2026-08-04 — EPIC BKY-I18N-MASS — MASS-1 ✅ + MASS-2 🟠 (issue 191-1 à fixer)

**Contexte** : Exécution de l'EPIC BKY-I18N-MASS (priorité 1). MASS-1 = domaine delta bloc
(DDD pur), MASS-2 = câblage dans DocumentTranslator/ContentTranslationService +
MigrateContentI18nTask. Statut de traduction *explicite* imposé par l'utilisateur
(Loi Économie d'Encre).

**Fait** :
* [MASS-1] ✅ — `BlockChecksum.kt` : `BlockTranslationStatus` (TRANSLATED/PENDING),
  `BlockChecksumEntry(hash,status)` parse/serialize (legacy `"hash"`→PENDING),
  `sha256`/`computeForBlocks`. `BlockDelta.kt` : `compute(previous,current)` →
  modified/preserved (statut ≠ TRANSLATED → re-traduit). DDD pur `document/translation/delta/`.
  Cas critique : diagramme `[plantuml]` traduit préservé si le texte environnant change.
* [MASS-2] 🟠 — `DocumentTranslator.translateArticleWithDelta` (internal→public, preserve blocs
  indexés, shift table/plantuml, isEmpty→retourne previous). `ContentTranslationService
  .translateSingleFileWithBlockDelta` (fresh/delta path, écrit target). `MigrateContentI18nTask` :
  boucle bloc-delta + `load/storeBlockChecksums` (`.bakery-block-checksums/*.checksums`,
  `idx=hash:STATUS`).
* [Tests] — BlockDeltaTest 26 ✅, DocumentTranslatorBlockDeltaTest 4 ✅, BlockDeltaWireDebugTest 1 ✅
  (document-gradle). FT BlockDeltaMigration : fresh ✅ + idempotence ✅ + 4 debug ✅.
* [Issue 191-1] ❌ — FT « second run with one modified block preserves other blocks » échoue :
  copy loop `MigrateContentI18nTask:124-129` (`sourceFile.copyTo(targetFile, overwrite=true)`)
  écrase la cible avec la source AVANT la traduction bloc-delta → blocs préservés = texte source.
* [publishToMavenLocal] ✅ — OK (document-gradle puis bakery-plugin).

**Fichiers modifies** :
- `bakery-plugin/src/main/kotlin/bakery/i18n/MigrateContentI18nTask.kt` — boucle bloc-delta + load/store checksums
- `bakery-plugin/src/test/kotlin/bakery/i18n/MigrateContentI18nDeltaFunctionalTest.kt` — @Nested BlockDeltaMigration
- `bakery-plugin/BACKLOG.adoc` — cadrage EPIC BKY-I18N-MASS
- `document-plugin/src/main/kotlin/document/translation/delta/BlockChecksum.kt` + `BlockDelta.kt` — nouveaux (MASS-1)
- `document-plugin/src/main/kotlin/document/translation/DocumentTranslator.kt` + `ContentTranslationService.kt` — MASS-2
- `document-plugin/src/test/kotlin/document/translation/delta/BlockDeltaTest.kt` + `DocumentTranslatorBlockDeltaTest.kt` + `BlockDeltaWireDebugTest.kt` — nouveaux

**Prochaine session (192)** : Fix Issue 191-1 (retirer/conditionner copy loop),
nettoyer debug prints, clôturer MASS-2, enchaîner MASS-3.

**Score** : 6/10 (issue 191-1 ouverte)

== Session 193 — 2026-08-04 — EPIC BKY-I18N-MASS — MASS-3 CLOS (validation STRICT plantuml/table configurable)

**Contexte** : MASS-1 (BlockDelta DDD) + MASS-2 (câblage) livrés S191-S192.
MASS-3 = validation STRICT/LENIENT/OFF plantuml/table configurable via DSL + CLI,
rapport consolidé `validation-report.json` post-passe.

**Baby steps DDD/TDD/BDD** :

. DDD — `ContentMigrationIntention.validation: String` (STRICT/LENIENT/OFF, default LENIENT)
  + `ContentMigrationIntentionDsl.validation` + init check
. DDD — `ValidationReport` data class (Jackson JSON consolidé table + plantUml, `toJson()`)
. TDD — `ContentTranslationService.drainTableValidationResults()` + `drainPlantUmlValidationResults()` (document-gradle)
. TDD — `MigrateContentI18nTask` : CLI `--contentI18nValidation` + DSL propagation
  + `PlantUmlTranslationAdapter(plantUmlValidationMode = validationMode)`
  + `writeValidationReport()` → `validation-report.json`
. TDD — 15 UT ContentMigrationIntentionTest (+6 validation) + 17 FT MigrateContentI18nDeltaFunctionalTest (+6 ValidationMode)
. BDD — Feature 59 `59_i18n_mass_validation.feature` (5 scénarios, 5/5 PASS)
  + `I18nMassValidationSteps.kt`

**Fichiers modifiés** :

*bakery-gradle/bakery-plugin* :
- `src/main/kotlin/bakery/i18n/ContentMigrationIntention.kt` — +validation field + init check
- `src/main/kotlin/bakery/i18n/ContentMigrationIntentionDsl.kt` — +validation field
- `src/main/kotlin/bakery/i18n/MigrateContentI18nTask.kt` — +CLI @Option, +validationMode wiring, +writeValidationReport
- `src/main/kotlin/bakery/i18n/ValidationReport.kt` — NOUVEAU
- `src/test/kotlin/bakery/i18n/ContentMigrationIntentionTest.kt` — +6 UT
- `src/test/kotlin/bakery/i18n/MigrateContentI18nDeltaFunctionalTest.kt` — +6 FT ValidationMode
- `src/test/features/59_i18n_mass_validation.feature` — NOUVEAU
- `src/test/scenarios/bakery/scenarios/I18nMassValidationSteps.kt` — NOUVEAU

*document-gradle/document-plugin* :
- `src/main/kotlin/document/translation/ContentTranslationService.kt` — +drainTableValidationResults, +drainPlantUmlValidationResults

**Tests** : 15 UT + 17 FT + 5 BDD (5/5 PASS). 5 échecs préexistants non introduits.
publishToMavenLocal document-gradle + bakery-gradle OK.

**Prochaine session (194)** : MASS-4 — Dogfooding FR→N langues lot pilote (3 articles × 9 langues cibles).

**Score** : 9/10

== Session 194 — 2026-08-04 — EPIC BKY-I18N-MASS — MASS-4 CLOS (BDD golden master multi-langue + dogfooding LLM réel)

**Contexte** : MASS-4 = (a) BDD golden master multi-langue test + (b) dogfooding LLM réel
3 articles pilotes × 9 langues. Ports 11437-11465 uniquement, modèle gemma4:31b-cloud.

**Taches livrees** :
- Feature 60 `60_i18n_mass_multi_lang.feature` (7 scénarios, 7/7 PASS)
- Steps `I18nMassMultiLangSteps.kt` (noms préfixés `mass-multi-lang` pour éviter DuplicateStepDefinition)
- Runner `I18nMassMultiLangCucumberTest.kt` standalone via LauncherDiscoveryRequestBuilder
- Fixture `cheroliv-com-mass-multi-lang/` (3 articles pilotes réels + jbake.properties + site.yml)
- Dogfooding LLM réel : 3 articles × 9 langues via gemma4:31b-cloud port 11437
- Projet consommateur `/tmp/bakery-dogfood` via mavenLocal 0.0.5 (pas de includeBuild)
- Fix DuplicateStepDefinition préexistant : I18nDeltaWireSteps renommé
- publishToMavenLocal bakery-gradle OK

**Fichiers modifies/crees** :
- `src/test/features/60_i18n_mass_multi_lang.feature` — NOUVEAU
- `src/test/scenarios/bakery/scenarios/I18nMassMultiLangSteps.kt` — NOUVEAU
- `src/test/kotlin/bakery/i18n/mass/I18nMassMultiLangCucumberTest.kt` — NOUVEAU
- `src/test/resources/fixtures/cheroliv-com-mass-multi-lang/` — NOUVEAU
- `src/test/features/57_i18n_delta_wire.feature` — MODIFIÉ
- `src/test/scenarios/bakery/scenarios/I18nDeltaWireSteps.kt` — MODIFIÉ

**Tests** : 7/7 BDD PASS. Dogfooding LLM réel : 3 articles × 9 langues = 27 fichiers générés + validation-report.json.

**Prochaine session (195)** : MASS-5 — BKY-I18N-BATCH validation lot annuel (108 articles FR→EN, 8 lots).

**Score** : 8/10
