= Archive des Taches Terminees — Septembre 2026

== Session 232 — 2026-09-25 — BKY-LANG-NAV-4 : modèle ex-nihilo conforme (sélecteur page-aware)

[cols="2,3"]
|===
| Tâche | Statut

| BKY-LANG-NAV-4 — `LangSwitchPath.thymeleafEachHref(defaultLang)` (frère symbolique dynamique)
| ✅ TERMINÉ

| BKY-LANG-NAV-4 — `LangSwitchModelGuard` (garde D7 présence ET page-awareness)
| ✅ TERMINÉ

| BKY-LANG-NAV-4 — Menus modèle `site-basic` + `site` page-aware (fin du `'/' + lang.code + '/'`)
| ✅ TERMINÉ

| BKY-LANG-NAV-4 — Tests `LangSwitchPathEachHrefTest` (4) + `LangSwitchModelGuardTest` (9) + FT 1 → 3
| ✅ TERMINÉ

| Dette notée — `config.default_language` injectable dans le modèle (`'fr'` embarqué)
| 🟡 À FAIRE (NAV-5+)
|===

== Session 231 — 2026-09-25 — BKY-LANG-NAV-3 : hôte JS + garde de parité (maquette ∪ jbake)

[cols="2,3"]
|===
| Tâche | Statut

| NAV-3 (DDD) — `bakery.langswitch.LangSwitchClientParity` objet pur : garde la **présence** du module dans les 2 arbres (`maquette/js/` + `assets/js/`), le **byte-identité** (pattern `I18nClientPropagation`) et la **surface de contrat** (`resolveLangPath` **et** `attachLangSwitch`). D6 « présence ET comportement » — `setLang` seul = faux vert. 7 UT (dont `ShippedResources` sur classpath réel).
| ✅ TERMINÉ

| NAV-3 (hôte JS) — `lang-switch.js` : `resolveLangPath(...)` frère JS de `resolveSamePage`, rejouant **les mêmes 14 vecteurs partagés** ; adaptateur DOM `attachLangSwitch` mode `tree` (réécrit `data-lang` page-aware + active, ne navigue pas) / `dictionary` (relocalise en place, `preventDefault`, `TALARIA.I18N.setLang`) ; `bootstrap()` sur `DOMContentLoaded`. Copié **byte-identique** dans les 2 trees.
| ✅ TERMINÉ

| NAV-3 (preuve D3 node) — `lang-switch.test.mjs` 7 tests, double DOM-free (`fakeElement`/`fakeDocument`, **pas de jsdom**) : rejeu de chaque vecteur partagé + adaptateur tree/dictionary + no-op. La même fixture que `LangSwitchPathVectorsTest` côté Kotlin (`lang-switch-path-vectors.json`).
| ✅ TERMINÉ

| NAV-3 (FT Oven D7) — `LangSwitchScaffoldFunctionalTest` 1/1 : `generateSite` copie les 2 arbres verbatim → module présent des deux côtés, byte-identique, surface de contrat présente. Un site scaffoldé naît conforme.
| ✅ TERMINÉ

| NAV-3 (infra) — tâche `nodeTest` (`node --test`, groupe `verification`, `download=false` pattern talaria) + `check.dependsOn(nodeTest)` + CI `actions/setup-node@v4` (node 22). `build.gradle.kts` + `.github/workflows/test.yml`.
| ✅ TERMINÉ

| Gate — `bakery.langswitch.*` 10 classes GREEN (NAV-1/2 non régressés), `nodeTest` 7/7, `functionalTest` BUILD SUCCESSFUL 2m32s, `test` 1993/2 FAIL FLAKY-5 préexistants (baseline 1986 → +7 nets), `cucumberTest` 2464/5 = baseline exacte, ktlint 0 sur le delta, `publishToMavenLocal` VERT, grep métier = 0. RED prouvé (`Unresolved reference` + node) → GREEN → REFACTOR. 5 créés, 2 modifiés, zéro bump, zéro secret.
| ✅ TERMINÉ

| Incident méthodologique — `ktlintFormat` global a reformaté **126 fichiers hors périmètre** (baseline de style rouge sur tout le dépôt) → tous revertés (`git checkout`), seuls les fichiers du delta conservés. Leçon : ne jamais lancer `ktlintFormat` global sur une baseline de style rouge.
| ⚠️ DOCUMENTÉ

| NAV-4 (modèle ex-nihilo) — **débloqué** par NAV-3 (DAG `NAV-3 → NAV-4`). Prochaine session 232.
| 🟡 À FAIRE
|===

== Session 230 — 2026-09-24 — BKY-LANG-NAV-2 : hôte Thymeleaf page-aware (lien par défaut exact)

[cols="2,3"]
|===
| Tâche | Statut

| NAV-2 (DDD) — `bakery.langswitch.LangSwitchPath.thymeleafHref(currentLang, targetLang, defaultLang)` : frère **symbolique** de `resolveSamePage`, émet un `th:href` en `${content.rootpath}`/`${content.uri}` évalué par page au bake (menu.thyme partagé → pas de page courante à l'injection). Thymeleaf 3.0.14 sans `#strings.substringAfterLast` → OGNL `content.uri.substring(...)` prouvé par évaluation. `LangSwitchUrl.thymeleafHref()` délègue (D3) ; `resolve()` = dégradation page-blind (D8). `LangSwitchThymeleafRenderer` émet le href page-aware (fin de `index.html` en dur), `data-lang` conservé (D4). 9 UT + 5 UT + 4 UT.
| ✅ TERMINÉ

| NAV-2 (preuve D3) — `LangSwitchPathThymeleafHrefTest` rejoue **11/14 vecteurs partagés via le TemplateEngine réel** (les 3 `existingPages` hors expression statique, D8) + `LangSwitchRendererEvaluationTest` évalue la **sortie réelle du renderer** et l'égale à `resolveSamePage`. `LangSwitchThymeleafRendererTest` réécrit page-aware (13 UT).
| ✅ TERMINÉ

| NAV-2 (FT + BDD) — `InjectLangSwitchFunctionalTest` réécrit 8/8 (sortie page-aware, plus de `'index.html'` hardcodé, idempotence) ; feature 51 réécrite 5/5 page-aware ; feature 55 end-to-end alignée + step `FR menu page-aware` (runner dédié 0 échec).
| ✅ TERMINÉ

| Gate — `bakery.langswitch.*` 63 UT GREEN, `functionalTest` suite complète BUILD SUCCESSFUL 3m35s, `test` 1986/2 FAIL FLAKY-5 préexistants (stash-prouvé), `cucumberTest` 2464/5 = baseline S-229 exacte, ktlint 0 sur le delta, `publishToMavenLocal` VERT, grep métier = 0. RED prouvé (2× puis 4× `Unresolved reference`) → GREEN. 3 créés, 9 modifiés, zéro bump, zéro secret.
| ✅ TERMINÉ

| NAV-4 (modèle ex-nihilo) — **reporté** : cadrage DAG `NAV-3 → NAV-4` + `site-basic/templates/menu.thyme` (`th:each` dynamique `${supportedLanguages}`) = design symbolique distinct, chantier propre une fois l'hôte JS posé.
| 🟡 REPORTÉ
|===

== Session 229 — 2026-09-24 — BKY-LANG-NAV-1 : LangSwitchPath (règle pure + vecteurs partagés)

[cols="2,3"]
|===
| Tâche | Statut

| NAV-1 (DDD) — `bakery.langswitch.LangSwitchPath.resolveSamePage(currentPageUri, currentLang, targetLang, defaultLang, pageExists)` objet pur (no I/O) : normalise, retire le préfixe de langue courante, rejoue la page sous la langue cible, relativise depuis le répertoire courant. Self-link → nom de fichier seul. Dégradation `<lang>/index.html` si cible absente (D8). 17 UT.
| ✅ TERMINÉ

| NAV-1 (vecteurs partagés) — `src/main/resources/bakery/langswitch/lang-switch-path-vectors.json` (`version: 1`, 14 vecteurs) = contrat anti-split-brain D3 rejoué par l'hôte JS (NAV-3). `LangSwitchPathTest` 17 UT + `LangSwitchPathVectorsTest` 2 UT fixture-driven.
| ✅ TERMINÉ

| Gate — `bakery.langswitch.*` 17 UT GREEN (12 `LangSwitchUrlTest` non régressés), `InjectLangSwitchFunctionalTest` 7/7 + idempotence, ktlint 0 sur les 4 fichiers NAV-1, `publishToMavenLocal` VERT. RED prouvé (4× `Unresolved reference`) → GREEN 19/19. `test` complet 2 FAIL préexistants (FLAKY-5, drift fixture cheroliv.com) stash-prouvés ; `cucumberTest` `2464/5 failed/2011 skipped` idem arbre propre. 4 fichiers créés, 0 suivi modifié, zéro bump, zéro secret.
| ✅ TERMINÉ

| Dette découverte — **FLAKY-5** : drift fixture cheroliv.com (chrome nativement FR S-053/055, 5 templates : blog/blog_section/footer/header/menu). Session dédiée : resync fixture + régénération golden master (`PostMigrationSnapshotGenerator`).
| 🟡 RAPPORTÉ
|===

== Session 228 — 2026-09-24 — BKY-LANG-NAV : cadrage EPIC (gouvernance pure, zéro code)

[cols="2,3"]
|===
| Tâche | Statut

| NAV-0 (cadrage) — Audit du bug (résolution d'URL structurelle : `LangSwitchUrl.resolve()` `index.html` en dur) + split-brain cheroliv.com + cartographie 4 sites (cheroliv bug réel / talaria marche déjà / cccp+magic-stick sans i18n) + constat modèle ex-nihilo buggé. Arbitrage pilote 3 tours → **S2 + hybride, Thymeleaf conservé**. Doc `.agents/CADRAGE_EPIC_BKY_LANG_NAV.adoc` (10 décisions, 9 US ~32 pts).
| ✅ TERMINÉ

| Synchronisation backlog — BACKLOG.adoc (section détaillée EPIC + status final + version 24.13), TODO.adoc (EPIC BKY-LANG-NAV NAV-0 ✅ / NAV-1→8 🟡), `.agents/INDEX.adoc` (en-tête + EPIC en attente + prochaine session 229).
| ✅ TERMINÉ

| Session archive — `.agents/sessions/228-bky-lang-nav-cadrage.adoc` + SESSIONS_HISTORY (entrée 228).
| ✅ TERMINÉ

| Gate — **aucun build/test** (Economie d'Encre : sources bakery inchangées depuis la gate S-226 1845/0). Zéro code modifié, zéro secret.
| ✅ TERMINÉ
|===

== Session 220 — 2026-09-17 — BKY-I18N-JS-4 : propagation byte-identique + gardes (clôture EPIC BKY-I18N-JS 4/4 US)

[cols="2,3"]
|===
| Tâche | Statut

| BS-1 (DDD) — `I18nClientPropagation` object pur : `targetFor` (mappe `maquette/js/…` → `jbake/assets/js/…`, premier segment uniquement, profondeur préservée, `null` hors arbre), `pairs`, `drift` (copie absente/non byte-identique — miroir `propagate()` du harnais). 9 UT.
| ✅ TERMINÉ

| BS-2 (TDD) — `TranslateI18nClientTask` : CLI `--i18nClientPropagate` + DSL/intention `propagate` (défaut `true`), `propagateToPublication()` delta-only (arbre aligné = strict no-op), 3 chemins non-dry-run, jamais en dry-run. Wiring `ContentTaskRegistrar`. +5 UT task.
| ✅ TERMINÉ

| BS-3 (garde format) — `I18nClientFormatGuard` object pur : `unbalanced-language-block` (bloc sans accolade fermante signalé — angle mort du parser), `round-trip-drift` (writer non préservant, rewriter injectable) + `I18nJsDictionary.unbalancedLanguageBlocks` (scanner string-aware). Câblé en production : fichier invalide écarté du plan, résultat invalide jamais écrit. 6 UT + 1 UT task.
| ✅ TERMINÉ

| BS-4 (frontière HTTP) — `I18nClientNoDirectHttpBoundaryTest` (test d'architecture) : scanne `src/main/kotlin/bakery/i18n/js`, échoue sur toute surface HTTP directe (`api/generate`, `java.net.http`, `HttpClient`, `fetch(`, `localhost:114`…), vérifie la voie unique port N0 `contracts.i18n.TranslationService`. 3 UT.
| ✅ TERMINÉ

| BS-5 (functional Oven + BDD) — `TranslateI18nClientFunctionalTest` 5 → 9 FT (propagation après no-op, no-op copie alignée `0 fichier(s) aligne(s)`, dry-run ne touche pas la copie, flag `--i18nClientPropagate=false`). Feature `64_i18n_client_dictionary.feature` 7 → 10 scénarios + 3 steps (propagation source→copie, no-op copie alignée, dry-run préserve la copie).
| ✅ TERMINÉ

| Bugs attrapés par le TDD — (1) `targetFor` rejetait à tort `maquette/js/i18n.js` (chemin racine, garde `index <= 0` → `index < 0`) ; (2) type `Map<String,String?>` sur les paires conditionnelles → restructuration `mapNotNull` ; (3) garde appliquée trop tard (bloc non balancé faisait lever le writer) → déplacée avant le planning.
| ✅ TERMINÉ

| Gate finale — `./gradlew test` = **1768 tests, 2 FAIL préexistants** (baseline S-219 1743/2 → +25 net ; drift `contact.thyme` cheroliv.com reproduit isolément). `functionalTest` 9/9. Cucumber 64 10/10. `publishToMavenLocal` VERT. Gate grep métier src/ = 0. Zéro secret, zéro bump BOM, zéro publication Central.
| ✅ TERMINÉ

| **Clôture EPIC BKY-I18N-JS 4/4 US** — JS-1 cadrage S-054 · JS-2 `I18nJsDictionary` S-218 · JS-3 `translateI18nClient` S-219 · JS-4 propagation + gardes S-220. Débloque talaria `T-I18N-BAKERY` US-2 (wiring build) + US-3 (retrait du harness node `tools/translate-i18n.mjs`).
| ✅ TERMINÉ

| Clôture gouvernance — rapport `.agents/sessions/220-BKY-I18N-JS-4-propagation-format-guard.adoc`, PROMPT_REPRISE S-221, SESSIONS_HISTORY, INDEX (portefeuille 220 + EPIC ✅ 4/4 US), TEST_COVERAGE_ANALYSIS, BACKLOG (JS-4 ✅ + EPIC ✅), TODO, AGENTS racine.
| ✅ TERMINÉ

== Session 219 — 2026-09-17 — BKY-I18N-JS-3 : task `translateI18nClient` (dictionnaires i18n JS client)

[cols="2,3"]
|===
| Tâche | Statut

| BS-1 (DDD) — domaine pur `bakery.i18n.js` : `I18nClientMigrationIntention` (invariants `sourceDirs` non vide/éléments non blank, `referenceLanguage` ∈ N0 `LanguageCatalog`, cibles non vide ∈ N0, référence ∉ cibles), `I18nClientMigrationIntentionDsl` (dryRun défaut `true`), `I18nClientDelta` object pur (`referenceFloor` plancher `fr` hors `.html`, `missingKeys`, `languageOwners` découvert par parsing, `plan` → `List<I18nClientFilePlan>`). 21 UT (7 intention + 14 delta).
| ✅ TERMINÉ

| BS-2 (TDD) — `TranslateI18nClientTask` (`@DisableCachingByDefault`) : CLI `@Option` (`--i18nClientSource/-TargetLangs/-SourceLang/-DryRun`), pattern `ResolveIntention` CLI > DSL > défauts, fusion sources, no-op strict si delta vide (jamais d'appel LLM), dry-run sans écriture, écriture des seuls fichiers modifiés, `TranslationResult.Failure` → clé conservée absente + document intact. 18 UT.
| ✅ TERMINÉ

| BS-3 (wiring) — `ContentTaskRegistrar.registerTranslateI18nClientTask` (pattern `registerMigrateContentI18nTask`) + `BakeryExtension.i18nClient` + `BakeryPlugin.registerFullPipelineTasks`. 3 fichiers existants modifiés (additif).
| ✅ TERMINÉ

| BS-4 (functional Oven) — `TranslateI18nClientFunctionalTest` 5/5 (TestKit réel : dry-run delta 1 rien écrit, tâche groupe `transform`, sans IA source intacte, dictionnaire complet no-op, source absente non bloquante).
| ✅ TERMINÉ

| BS-5 (BDD) — feature `64_i18n_client_dictionary.feature` `@i18n-client` 7 scénarios + `I18nClientDictionarySteps` + runner dédié `I18nClientCucumberTest` (pattern S-082) + fixture `talaria-i18n-client`. Fake translator label-aware zéro LLM. 7/7 PASSED.
| ✅ TERMINÉ

| Bugs attrapés par le TDD — (1) 2 assertions de test erronées (le bloc `en` du fixture n'était pas complet) ; (2) interpolation `trimMargin` cassant l'indentation du bloc `en` (delta 0) → `buildString` ; (3) assertion fonctionnelle sur `nav.cart` présent dans `fr` → affinée sur le bloc `en` ; (4) regex step Cucumber `requests?` invalide (`{string}` littéral) → 2 steps singulier/pluriel explicites.
| ✅ TERMINÉ

| Gate finale — `./gradlew test` = **1743 tests, 2 FAIL préexistants** (baseline S-218 1707/2 → +36 net ; drift `contact.thyme` cheroliv.com reproduit isolément). `functionalTest` 5/5. Cucumber 64 7/7. `publishToMavenLocal` VERT. Gate grep métier src/ = 0. Zéro secret, zéro bump BOM, zéro publication Central.
| ✅ TERMINÉ

| Clôture gouvernance — rapport `.agents/sessions/219-BKY-I18N-JS-3-translate-i18n-client.adoc`, PROMPT_REPRISE S-220, SESSIONS_HISTORY, INDEX (portefeuille 219 + EPIC 3/4 US), TEST_COVERAGE_ANALYSIS, BACKLOG (JS-3 ✅ TERMINÉ), AGENTS racine.
| ✅ TERMINÉ
|===

*Clôture* : **EPIC BKY-I18N-JS 3/4 US** — reste JS-4 (propagation
`maquette/js/` → `jbake/assets/js/` byte-identique + garde parse↔write +
failover zéro HTTP direct), clôture EPIC. Débloque talaria `T-I18N-BAKERY`
US-2 (wiring) + US-3 (retrait harness node).

== Session 218 — 2026-09-17 — BKY-I18N-JS-2 : `I18nJsDictionary` (parser/writer JS pur)

[cols="2,3"]
|===
| Tâche | Statut

| **BKY-I18N-JS-2** — `bakery.i18n.js.I18nJsDictionary`, object pur (`parse(vararg sources)` fusion chrome+patch déterministe, `languagesOf`, `insertTranslations` delta clés manquantes). Miroir du harness `tools/translate-i18n.mjs` (S-044). Loi Économie d'Encre : no-op **byte-identique** si corpus complet, idempotent. 2 invariants supérieurs au harness : scanner de braces **string-aware** (`{`/`}` dans un littéral double-quote ignorés — valeur `"{0}"` ne tronque plus un bloc) + regex de bloc ancrée `^\s{4}[a-z]{2}: \{` (entrées hors bloc ignorées). Domaine pur, zéro I/O, zéro Gradle, zéro LLM.
| ✅ TERMINÉ

| **Bug TDD attrapé** — `String.replace(Regex, "\\n")` en Kotlin passe par `Matcher.replaceAll` où `\n` est un échappement de groupe : le backslash était perdu (`nligne2` au lieu de `\nligne2`). Fix : lambda de remplacement `{ "\\n" }`. Attrapé par `inserted values escape quotes and newlines`.
| ✅ TERMINÉ

| **Tests** — 16 UT `I18nJsDictionaryTest` + 5 intégration `I18nJsDictionaryTalariaIntegrationTest` (dogfooding réel lecture seule, skip `assumeTrue` CI : `i18n.js` 11 langues + plancher `fr` 205 clés hors `.html`, `i18n-extra-langs.js` 11 langues + plancher identique, `i18n-content.js` 22 langues, round-trip byte-identique chrome + patch toutes langues). Baby-step TDD strict RED (compileTestKotlin FAILED `Unresolved reference 'I18nJsDictionary'` ×13) → GREEN → REFACTOR.
| ✅ TERMINÉ

| **Gate** — `./gradlew test` **1707 tests, 2 FAIL préexistants, 3 skipped** (baseline S-217 1686/2 → +21 net exactement ; FAIL `FixtureAlignmentAuditorTest` + `RealSiteI18nMigrationIntegrationTest` = drift `contact.thyme` cheroliv.com, reproduits isolément). `publishToMavenLocal` VERT (règle INDEX 0). Gate grep métier src/ = 0. 3 nouveaux fichiers, zéro existant modifié. Zéro secret, zéro bump BOM, zéro publication Central.
| ✅ TERMINÉ

| Clôture gouvernance — rapport `.agents/sessions/218-BKY-I18N-JS-2-js-dictionary.adoc`, PROMPT_REPRISE S-219, SESSIONS_HISTORY, INDEX (session 218 + EPIC BKY-I18N-JS), BACKLOG (BKY-I18N-JS-2 ✅), TODO.adoc, TEST_COVERAGE_ANALYSIS.
| ✅ TERMINÉ
|===

*Clôture* : **BKY-I18N-JS-2 TERMINÉ** — brique non-LLM de l'EPIC BKY-I18N-JS,
bloquant talaria `T-I18N-BAKERY` US-2/US-3.

== Session 214 — 2026-09-09 — EPIC BKY-TEST-FLAKY clôturé (stabilisation des 3 FAIL i18n récurrents)

[cols="2,3"]
|===
| Tâche | Statut

| Diagnostic RED baseline — `./gradlew test` = 1670 tests / 3 FAIL identifiés via XML JUnit (`build/test-results/test/`) : FLAKY-2 `FixtureAlignmentAuditorTest` (11 templates fixture cheroliv-com désynchronisés), FLAKY-1 `RealSiteI18nMigrationIntegrationTest` (golden master sitemap.thyme périmé), FLAKY-3 `I18nDeltaWireCucumberTest` (3 scénarios, expected 3 but was 4).
| ✅ TERMINÉ

| FLAKY-2 — resync fixtures : copie des 11 templates du site réel (`office/sites/cheroliv.com/jbake/templates/`) vers `src/test/resources/i18n-fixtures/cheroliv-com/jbake/templates/`. Site réel évolué post-S150 : SRI integrity hashes, JSON-LD @graph Article/Breadcrumb, `lang="fr"`, `head(null)` paramétré, footer-bottom-links supprimé, magic-stick URL cheroliv.com → cccp.education. Auditor 7/7 PASS.
| ✅ TERMINÉ

| FLAKY-1 — régénération golden master : `PostMigrationSnapshotGenerator` re-activé (retrait @Disabled, pattern REAL-4 S170), 3/3 PASS, re-@Disabled mention S-214. Nouvelles clés : header.5 (og:site_name/author), header.6 (robots noindex, follow). Clés disparues : footer.15-18 (Privacy policy/Terms/Legal/Site map). `translations_en.properties` étendu (header.1-6 réalignés, footer.15-18 supprimés). 0 clé EN vide. Test 2/2 PASS.
| ✅ TERMINÉ

| FLAKY-3 — feature 57 : la fixture `cheroliv-com-i18n-deploy` a 4 articles depuis S-190 (`diagrammes-mixtes.adoc` ajouté pour feature 55) mais feature 57 attendait 3. Feature + `I18nDeltaWireSteps` Given mis à jour 3→4 (fresh 4/0, idempotence 0/4, delta 1/3). 3/3 scénarios PASS.
| ✅ TERMINÉ

| Gate finale — `./gradlew test` = **1670 tests, 0 FAIL** (baseline 1670/3). Runners dédiés features 55/56/60/62/57 = 5/5 PASS. `PostMigrationGoldenMasterTest` 3/3 PASS. `publishToMavenLocal` VERT. `compileKotlin` VERT. Gate D16 = 0 usage.
| ✅ TERMINÉ

| Clôture gouvernance — rapport `.agents/sessions/214-flaky-123-stabilisation-i18n.adoc`, PROMPT_REPRISE S-215, SESSIONS_HISTORY, INDEX (portefeuille 215), BACKLOG (EPIC ✅ + Version 24.9), TODO.adoc (3 US cochées).
| ✅ TERMINÉ
|===

*Clôture* : **EPIC BKY-TEST-FLAKY TERMINÉ 3/3 US**. Les gates cross-borough
MEMPHIS n'ont plus besoin de la mention "baseline exacte 3 FAIL".
Zéro code production (33 fichiers = fixtures/golden master/feature/steps/
générateur doc), zéro bump BOM, zéro publication Central.

== Session 216 — 2026-09-10 — Transfert BKY-FPA-SITE → TRN-11 (training) + arbitrage licence FPA

[cols="2,3"]
|===
| Tâche | Statut

| Arbitrage pilote licence FPA (2026-09-10) : corpus FPA = *document personnel* (zéro droit de tiers) → corpus brut (scan → AsciiDoc) privé dans `office/metiers/FPA/`, jamais publié. Produits formation dérivés (SPG, SPDs, slides, PDFs, quiz, site) = travaux originaux = Apache 2.0 = publiables. Le site navigable héberge les produits, pas le texte intégral brut.
| ✅ TERMINÉ

| BKY-FPA-SITE ✅ TRANSFÉRÉ (S-216 → TRN-11 training, ex GBL-003.5). Pattern TRN-9 (ex BKY-TRN-9) / TRN-TEMP (ex BKY-JB-1) : le portail formation est un livrable métier training (anneau A4), bakery = mécanisme JBake uniquement.
| ✅ TERMINÉ

| bakery BACKLOG.adoc : EPIC BKY-FPA-SITE marqué ✅ TRANSFÉRÉ, en-tête « Dernière mise à jour » → Session 216, table statut final cohérente, ligne "Reste en backlog" purgée.
| ✅ TERMINÉ

| bakery `.agents/CADRAGE_EPIC_BKY_FPA_SITE.adoc` : section « Statut final (2026-09-10, S-216) » ajoutée (TRANSFÉRÉ → TRN-11).
| ✅ TERMINÉ

| bakery `.agents/INDEX.adoc` + `PROMPT_REPRISE.adoc` : `:last_update:` → S-216 ; mission S-217 (à la demande) avec BKY-FPA-SITE retiré, portail évoqué via TRN-9/TRN-11.
| ✅ TERMINÉ

| training BACKLOG.adoc : EPIC TRN-11 ajouté (2 pts, ex BKY-FPA-SITE, GBL-003.5, dépend GBL-003.3/003.4 + document S-082 + bakery). training INDEX.adoc : TRN-11 🟡 CADRÉ (statut + roadmap l.62).
| ✅ TERMINÉ

| racine `BACKLOG.adoc` (GBL-003.5 → training TRN-11, GBL-003.1) + `AGENTS.adoc` (table Bushwick S-216, anneau A4, historique 2026-09-10).
| ✅ TERMINÉ

| Vérité Étape 7 : `grep` marqueurs périmés — seuls MASS-6 (reporté) + BKY-I18N-CI (cadré) légitimes. Zéro code source, zéro test lancé, zéro secret.
| ✅ TERMINÉ
|===

== Session 215 — 2026-09-09 — Bug chemin dogfooding clôturé (P3, item S-215)

[cols="2,3"]
|===
| Tâche | Statut

| Diagnostic cause racine : `SiteConfiguration.resolvePaths` (BakeryPlugin.kt:245-251) rend `contentRoot` absolu (projectDir + srcPath) → `--contentI18nSource=content/blog/2026` déjà préfixé par `srcPath: jbake` doublait `content/` (« répertoire source n'existe pas »). Workaround S-207 (`blog/2026`).
| ✅ TERMINÉ

| DDD : nouveau domaine `bakery.i18n.path` — `ContentSourcePathResolver` objet pur `internal` segment-aware (match par segments, `jb`≠`jbake`), 3 règles (absolu tel quel / préfixé `srcPath` → `projectDir.resolve(sourceDir)` sans doublement / sinon `projectDir.resolve(srcPath).resolve(sourceDir)` — comportement historique). 8 UT.
| ✅ TERMINÉ

| Wiring TDD : `MigrateContentI18nTask.contentSrcPath: String?` + `resolveSourceDir()` câblé au resolver ; `ContentTaskRegistrar` dérive `contentSrcPath` relatif au projectDir (le registrar connaît la vérité issue de `resolvePaths`). +2 UT (9 `ExecuteContentMigration`).
| ✅ TERMINÉ

| FT TestKit : +2 `MigrateContentI18nDeploySmokeFunctionalTest` (formes préfixée `jbake/content/blog` et relative `content/blog`, assertion chemin `i18n-en/en/2020/0016_simple_post.adoc` + `doesNotContain("répertoire source n'existe pas")`) — 9/9.
| ✅ TERMINÉ

| BDD : feature `63_i18n_source_path_resolution.feature` (5 scénarios `@source-path`) + `I18nSourcePathSteps.kt` + runner dédié `I18nSourcePathCucumberTest` — 5/5. Découverte : `CucumberTestRunner` standard filtre `@pipeline-contracts and not @wip` → feature 63 par runner dédié.
| ✅ TERMINÉ

| Gate finale — `test` **1682/0 FAIL** (+11 vs 1671), `functionalTest` BUILD SUCCESSFUL 5m9s 119/0, `cucumberTest` 581 completed 2 FAIL préexistants environnementaux (0 nouveau), `publishToMavenLocal` VERT. Rétro-compatibilité totale (workaround S-207 inutile).
| ✅ TERMINÉ

| Clôture gouvernance — rapport `.agents/sessions/215-contenti18nsource-double-chemin-resolu.adoc`, PROMPT_REPRISE S-216, SESSIONS_HISTORY, INDEX (session 215), BACKLOG (note S-207 corrigée), TEST_COVERAGE_ANALYSIS (baseline 1682).
| ✅ TERMINÉ
|===

*Clôture* : **item S-215 « Bug chemin dogfooding (P3) » TERMINÉ**. 9 artefacts
(2 fichiers domaine + resolver + 5 fichiers test + 1 feature + 1 steps),
zéro bump BOM, zéro publication Central, zéro secret.
== Session 217 — 2026-09-15 — EPIC BKY-LANG-22 (parité talaria.school) — 7/10 US

[cols="2,3"]
|===
| Tâche | Statut

| **LANG-22-1** — N0 `contracts.i18n.LanguageCatalog` 10 → 22 langues (12 ajoutées : `it nl de el tr vi th id ko ja sr` + `fa` RTL). DDD/TDD baby-step strict RED → GREEN. `LanguageCatalogTest` : 22 langues, 3 RTL (`ar ur fa`), parité exacte set talaria, Persian/Italian/Japanese/Korean by code. 52 tests 0 FAIL.
| ✅ TERMINÉ

| **LANG-22-2** — Publication Central `education.cccp:i18n-contracts:0.0.4` — bundle `c1a9de57` (AUTOMATIC), repo1 HTTP 200 confirmé.
| ✅ TERMINÉ

| **LANG-22-3** — MEMPHIS `workspace-bom`/`workspace-catalog:0.0.43` publiés Central (constraint `i18n-contracts:0.0.4`) — bundle `eb310d04`, repo1 200.
| ✅ TERMINÉ

| **LANG-22-4** — bakery : `BakeryConstants.SUPPORTED_LANGS = LanguageCatalog.supportedCodes()` (dérivation du contrat N0 — split-brain tué). Bump dep 0.0.4 + platform/catalog 0.0.43. Tests intention/scaffold/i18n alignés. `test` 1686/2 FAIL préexistants (drift `contact.thyme`, stash-prouvé).
| ✅ TERMINÉ

| **LANG-22-5** — capsule : `VoiceMapping` +12 modèles Piper validés rhasspy/piper-voices `voices.json` + 12 espeak codes (espeak-ng couvre 22). BDD `multi_language_capsule.feature` catalog-driven + runner dédié `CapsuleMultiLangCucumberRunner` + task `cucumberTestMultiLang`. **22/22 languages rendered** (11m18s).
| ✅ TERMINÉ

| **LANG-22-6** — slider : `RevealUiMessageCatalog` +12 libellés natifs (nav + controls), `fa` RTL. Features 5/6/13 + steps catalog-driven. `TranslatedDemoDeckTest` pinné sur corpus S-030 (27 fichiers gelés). `test` 947/3 FAIL Playwright préexistants (stash-prouvé).
| ✅ TERMINÉ

| **LANG-22-7** — document : dep `i18n-contracts:0.0.4` + BOM/catalog 0.0.43. `SiteTranslationPlan` déjà N0-driven. compileKotlin/compileTestKotlin VERT.
| ✅ TERMINÉ

| **LANG-22-8** — Dogfooding slider corpus 22 langues (régénérer 3 decks → 12 nouvelles langues, 27 → 63 fichiers, LLM metered — Loi Économie d'Encre). Reporté S-218 (accord pilote donné).
| 🟡 EN COURS

| **LANG-22-9** — Dogfooding capsule vidéo 22 langues (Piper/espeak réels). Reporté S-218.
| 🟡 EN COURS

| **LANG-22-10** — Publication Central consommateurs (bakery/capsule/slider sur N0 0.0.4 + BOM 0.0.43). Reporté S-218.
| 🟡 EN COURS

| Gate finale — N0 test 52/0, bakery test 1686/2 préexistants, capsule cucumberTestMultiLang 22/22, slider test 947/3 Playwright préexistants, document compile VERT. Zéro secret commité, zéro publication plugin.
| ✅ TERMINÉ

| Clôture gouvernance — rapport `.agents/sessions/217-BKY-LANG-22-languagecatalog-22.adoc`, PROMPT_REPRISE S-218, SESSIONS_HISTORY, INDEX (session 217), BACKLOG (EPIC BKY-LANG-22 🟡 EN COURS), TEST_COVERAGE_ANALYSIS, AGENTS racine (table capsule/slider/document/bakery/MEMPHIS).
| ✅ TERMINÉ
|===

*Clôture* : **EPIC BKY-LANG-22 🟡 EN COURS — 7/10 US TERMINÉES**. Cycle release
cross-borough complet (N0 → BOM → 4 consommateurs → Central). Reste dogfooding
LLM (slider/capsule) + publication Central consommateurs. 5 repos modifiés.


---

== Session 221 — Cross-borough T-I18N-BAKERY US-2/US-3 (`IaConfigResolver` + harness audit-only)

=== Fait
* **US-2 (wiring)** — le pool LLM d'un consommateur est résolu depuis `site.yml → ollama:`.
  * Audit : `-Pia.enabled` documenté mais jamais lu, `site.yml → ollama:` parsé mais ignoré,
    seul le DSL `bakery { ia {} }` marchait — or talaria n'applique pas bakery (runner N3).
  * Nouveau domaine pur `bakery.llm.IaConfigResolver` (précédence DSL activé > YAML > inactif).
  * Câblage local `ContentTaskRegistrar.registerTranslateI18nClientTask` (périmètre strict).
  * 7 UT `IaConfigResolverTest` + 2 FT `TranslateI18nClientOllamaConfigFunctionalTest`
    (RED prouvé par stash).
* **US-3 (harness)** — côté talaria : `tools/translate-i18n.mjs` réduit à un audit read-only,
  garde d'architecture `i18n-harness-audit-only.test.mjs` (4 tests).
* **Bugs** — fixture YAML `trimIndent` multi-ligne ; collision de session concurrente
  graphify (`LensSteps.kt` compat `GraphModel(nodes=…)`, fichiers graphify non committés).

=== Prochaine session
* BKY-LANG-22-8/9 (dogfooding LLM 22 langues) OU 10 (publication Central) OU MASS-6
  — **accord pilote requis** (coût LLM metered / publication).
* Publishing `bakery-plugin` ≥ 0.0.13 Central débloque le wiring talaria en CI.

---

== Session 222 — FLAKY-4 : drift fixture `contact.thyme` résolu (baseline 1777/0)

=== Fait
* **Diagnostic** — les « 2 FAIL préexistants » (S-218→S-221) sont un **drift réel**, pas un flake.
  * Site réel `office/sites/cheroliv.com` : champ `subject` du contact retiré
    (commit `d96dcec`, 2026-09-14, US-35). Fixture bakery non resynchronisée.
  * `FixtureAlignmentAuditorTest` (fixture ↔ réel) + `RealSiteI18nMigrationIntegrationTest`
    (copie fraîche du réel vs golden master figé avec `subject`) tombent.
* **Fix (5 fichiers, +6/-23)** — chirurgical.
  * Resync `i18n-fixtures/cheroliv-com/jbake/templates/contact.thyme` (bloc `subject` retiré).
  * Recâblage clés messages : `contact.6=Subject` disparaît, `Votre message` `contact.7`→`contact.6`
    (`translations_en.properties` + golden master `messages_fr`/`messages_en`).
  * Golden master cheroliv-com régénéré via la procédure one-shot `PostMigrationSnapshotGenerator`
    (retrait `@Disabled` → run → re-`@Disabled` mention S-222). Churn timestamp réverté
    (magic-stick/cccp ne différaient que par la ligne de date `Properties.store`).
* **Périmètre** — seul `contact.thyme` drift réellement ; `footer.thyme` = secrets seuls
  (déjà neutralisés) ; aucun drift magic-stick/cccp-education.

=== Gate
* `test` **1777 tests, 0 FAIL, 3 skipped** (baseline S-221 1775/2 → les 2 FAIL disparaissent).
* `FixtureAlignmentAuditorTest` 8/0, `RealSiteI18nMigrationIntegrationTest` 2/2.
* `publishToMavenLocal` VERT, grep métier src/ = 0, zéro secret, zéro BOM, zéro LLM.
* Les gates cross-borough MEMPHIS n'ont plus besoin de la mention « baseline 2 FAIL ».

=== Prochaine session
* BKY-LANG-22-8/9 (dogfooding LLM 22 langues) OU 10 (publication Central) OU MASS-6
  — **accord pilote requis** (coût LLM metered / publication).

---

== Session 223 — BKY-I18N-JS-5 : catalogue structurel (ownership + couverture)

=== Fait
* **Diagnostic** — le cadrage talaria S-054 déclare **3** dictionnaires i18n JS.
  * Le 3e, `maquette/js/i18n-content.js` (`TALARIA.I18N.CATALOG`), est **imbriqué**
    (langues → formations `fpa`/`cda` → scalaires + listes) et n'était **jamais traité** :
    `I18nJsDictionary` est plat (clés quotées) → **0 clé** extraite.
  * **Bug latent** : le catalogue réutilise l'indentation flat → `languageOwners` le
    désignait **propriétaire des 22 langues** (tri alphabétique `i18n-content.js` en tête).
    Une clé chrome `fr` ajoutée aurait été insérée dans le catalogue (corruption).
  * La tâche ne partitionnait pas ; les tests ne voyaient pas le catalogue (fixture chrome+patch).
* **Fix (5 créés, 8 modifiés)** — baby-step DDD/TDD/BDD strict.
  * `I18nJsFormat` (enum pur `FLAT`/`CATALOG` par marqueur, 6 UT).
  * `I18nCatalog` + `I18nCatalogPlan` (couverture langue/formation/champ, 11 UT).
  * `I18nClientDelta.languageOwners` saute les sources CATALOG (RED prouvé, +2 UT).
  * `TranslateI18nClientTask` : partition plat/catalogue + report `warn` + seam
    `catalogueCoverage` (4 UT).
  * REFACTOR `I18nJsBlockScanner` (scanner balanced-brace string-aware partagé, -37 lignes).
  * Dogfooding réel talaria (4 UT : 22 langues × 2 formations, 0 lacune).
  * BDD feature 64 **10 → 14 scénarios** + 4 steps + générateur de catalogue.

=== Gate
* `test` **1804 tests, 0 FAIL, 0 erreur, 3 skipped** (baseline 1777/0 → **+27 net**).
* Feature 64 **14/14** ; `functionalTest --rerun-tasks` **130/130** (4m28s).
* `publishToMavenLocal` VERT, grep métier src/ = 0, zéro secret, zéro BOM.
* Incident infra : 2× `:test` `EOFException` (load 21-27 + swap plein) — relance verte.

=== Prochaine session
* BKY-LANG-22-8/9 (dogfooding LLM 22 langues) OU 10 (publication Central) OU MASS-6
  OU traduction du contenu catalogue imbriqué — **accord pilote requis**.

'''

== Session 224 — BKY-I18N-JS-6 : traduction du contenu catalogue imbriqué

=== Fait
* **Diagnostic** — le catalogue imbriqué (`i18n-content.js`, 98881 octets, 22 langues →
  formations → scalaires + listes `objectives[]` + objets inline `modules[]`) n'était
  **jamais traduit** : le parser plat y voit 0 clé, `I18nCatalog` ne faisait que compter
  les lacunes. Le writer plat (`insertTranslations`) corromprait l'objet à clés non quotées.
* **Fix (6 créés prod, 3 créés test)** — baby-step DDD/TDD/BDD strict.
  * `I18nJsObjectParser` (internal) : parseur récursif descendant objet/tableau/chaîne JS,
    chaîne-aware, skippe commentaires `//`/`/* */`, conserve les byte-ranges des littéraux.
  * `I18nCatalogDocument` : aplatissement pathé (`fpa.title`, `fpa.objectives[0]`,
    `fpa.modules[0].desc`) + `missingLiterals`. 7 UT.
  * `I18nCatalogWriter` : `translate` substitution in-place par byte-ranges ;
    `complete` contrat S-054 (langue absente → clone de la structure de référence,
    langue incomplète → complétée, valeur existante > traduction > référence). 11 UT.
  * `I18nCatalogDelta` (`I18nCatalogFilePlan`) : plan de delta imbriqué. 5 UT.
  * `I18nCatalogFormatGuard` : `unbalanced-catalogue-block`, `language-structure-drift`,
    `round-trip-drift`, rewriter injectable. 5 UT.
  * REFACTOR `I18nJsStringCodec` (escape/unescape partagé plat+imbriqué).
  * `TranslateI18nClientTask` : partition plat/catalogue étendue au **delta**, complétion,
    création de langue, garde en production, échec total = zéro écriture. +5 UT task.
  * Dogfooding réel talaria (5 UT `NestedCatalogueDogfood` : 22 langues, no-op strict,
    garde verte, translation par ses propres valeurs = no-op).
  * FT Oven 9 → 11 ; BDD feature 64 **14 → 17 scénarios** + 2 steps.
* **Bugs attrapés par TDD** — (1) raw string Kotlin ≠ saut de ligne (test d'échappement) ;
  (2) le writer initial ne faisait que substituer — le contrat S-054 exige la complétion.

=== Gate
* `test` **1842 tests, 0 FAIL, 0 erreur, 3 skipped** (baseline 1804/0 → **+38 net**).
* Feature 64 **17/17** ; `functionalTest --rerun-tasks` **132/132**.
* `publishToMavenLocal` VERT, grep métier src/ = 0, zéro secret, zéro BOM.

=== Prochaine session
* BKY-LANG-22-8/9 (dogfooding LLM 22 langues) OU 10 (publication Central) OU MASS-6 —
  **accord pilote requis**. Option locale sans coût : dogfooding dry catalogue complet
  avec `TranslationService` factice.

== Session 226 — EPIC BKY-LANG-22 TERMINÉ 10/10 US (2026-09-17)

Cross-borough slider (LANG-22-8) + capsule (LANG-22-9) + MEMPHIS (LANG-22-10).

- **8 — dogfooding slider 22 langues** : 3 sources × 12 nouvelles langues = 36
  paires via `translateDeck` (`gemma4:31b-cloud` — `gpt-oss:120b-cloud` à sa
  limite mensuelle, ports 11439-11444). Corpus 63 adoc + 63 context. Pin
  `TranslatedDemoDeckTest` levé (dérivé de `LanguageCatalog.supportedCodes()`).
  Drift préexistant `7_rtl_layout` (`fa`, S-217) corrigé catalog-driven.
  Feature 14 : 9→21 adoc/source, totaux 27→63. Gates : 261 UT + 270 cucumber.
- **9 — dogfooding capsule 22 langues** : **bug P0 réel** — la chaîne réelle
  `extractSpeakerNotes` produit `<slug>_<lang>-deck-script.txt` (suffixe `-deck`
  survit) mais `CapsuleVideoPlanner` exigeait `<deckName>_<lang>-script.txt` →
  fail-fast `no matching deck + script pair` (S-082 contournait par `mv` manuel).
  Fix baby-step DDD/TDD/BDD : `CapsuleVideoPlanner.resolveScript` tolère le
  suffixe `-deck` (miroir S-082 côté sortie WebM). RED prouvé. BDD 14/14.
  Dogfooding réel consumer composite (rendu HTML via Ruby système — blocage gems
  JRuby Gradle préexistant) → **21/22 rendered + 1 skipped = 22/22 vidéos**
  espeak+Playwright, 26-81s. Dégradation propre th/ja/ar. Gate capsule 1168/0.
- **10 — publication Central** : ordre DAG réel = capsule-first (gardes MEMPHIS
  D8 anti-ghost). capsule 0.0.1 (infra nmcp créée + fix POM marker
  name/description) `54a58526` → MEMPHIS 0.0.48 `9d43c5b7` → bakery 0.0.13
  `3d53b77e` → slider 0.0.19 `03289d70`. Propagation repo1 HTTP 200 vérifiée.
- **EPIC BKY-LANG-22 clos 10/10 US.** Rapport
  `.agents/sessions/226-bky-lang-22-8-9-10-cloture-epic.adoc`.
- Dette notée : rendu `asciidoctorRevealJs` Gradle cassé (gems JRuby, S-023/S-034).

== Session 225 — Fix `ArticleGenerator` 22 langues (2026-09-17)

- Bug : `buildPrompt(intention)` avait 2 branches (`en` / else→français) pour
  22 langues validées N0 → 20 langues produisaient du français.
- Fix TDD strict : `languageInstruction(code)` via `LanguageCatalog` (nom +
  nom natif + code ISO), RED 3 tests → GREEN → REFACTOR.
- Suite complète 1 845 tests / 0 échec / 0 erreur. `publishToMavenLocal` VERT.
- Commit `f6cc35b`. Issu de la revue globale S-217 (GBL-006 § 7).
