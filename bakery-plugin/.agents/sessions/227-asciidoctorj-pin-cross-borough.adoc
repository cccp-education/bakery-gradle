= Session 227 — Dette `asciidoctorRevealJs` soldée (cross-borough slider)
:session: 227
:date: 2026-09-17
:type: cross-borough + DDD/TDD/BDD (implémentation côté slider) + diagnostics
:borough: Bushwick (bakery-gradle) — pilotage cross-borough MIAMI (slider-gradle)
:score: 9/10

== Contexte

Reprise bakery à la demande pilote (hook d'ouverture standard). Le
PROMPT_REPRISE 227 listait deux missions bloquées par accord pilote
(GBL-006 COPY MULTI-MARCHÉ ~31 pts, MASS-6 ~675h LLM) et une **dette locale
actionnable sans coût externe** : le rendu `asciidoctorRevealJs` Gradle cassé
(gems JRuby `jar-dependencies 0.5.4 vs 0.4.1`, préexistant S-023/S-034).

Arbitrage pilote : mission **B** = traiter cette dette.

== Constat de périmètre — la dette n'est pas dans bakery

Audit : `asciidoctorRevealJs` est un task fourni par asciidoctor-gradle,
appliqué et configuré **côté slider-gradle** (`RevealJsTaskRegistrar`). Bakery
n'utilise AsciidoctorJ que via JBake + diagram, sans `revealjs`. La dette
listée dans la gouvernance bakery était donc une dette **slider**.

== Livrable effectif

L'implémentation complète (diagnostic + correctif DDD/TDD/BDD + bump 0.0.20)
est documentée dans le borough propriétaire :
`.agents/sessions/072-asciidoctorj-pin-revealjs-repare.adoc` (slider-plugin).

Résumé :

. *Cause racine* : double JRuby sur le worker OUT_OF_PROCESS — défaut
   asciidoctor-gradle `asciidoctorj:3.0.0` (`jruby-complete:9.4.8.0`,
   `jar-dependencies 0.4.1`) vs classpath plugin `asciidoctorj:3.0.1`
   (`jruby-stdlib:9.4.14.0`, `0.5.4`) → `Gem::LoadError`, jar de gems vide,
   `LoadError: no such file to load -- asciidoctor`.
. *Correctif* : `slider.extension.AsciidoctorJPin` value object +
   `SliderExtensionRegistrar.pinAsciidoctorJ`.
. *Preuve* : dogfooding from-scratch → `Successfully installed
   asciidoctor-revealjs-5.2.0` + 80 decks HTML.
. *Bonus* : 3 tests Playwright préexistants S-217 passent.
. *Bump* : slider 0.0.19 → 0.0.20.

== Zéro impact bakery

Aucun code bakery modifié. Gate bakery non relancée (règle Economie d'Encre :
sources bakery inchangées depuis la dernière gate S-226 1845/0). Seule la
gouvernance bakery est mise à jour (PROMPT_REPRISE : dette soldée,
session 228).

== Dette résiduelle

- *GBL-006 COPY MULTI-MARCHÉ* (~31 pts) — accord pilote requis (US-2 revue
  profils). Les US-1 (`ContentSeed`/`PlatformFormat`/`CopyPiece`) et US-3
  (`MarketCopyPromptBuilder`) sont des domaines purs zéro coût LLM.
- *MASS-6* (~675h LLM) — accord pilote requis.
- Slider : `slider-plugin/gradle/libs.versions.toml` `slider = "0.0.14"` résidu
  stale (version inerte) — nettoyage candidat côté slider.
- Slider : EPIC PUB 0.0.20 candidat (publication Central du correctif).
