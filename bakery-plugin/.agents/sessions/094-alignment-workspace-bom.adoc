= Session 094 — Alignement workspace-bom (MEMPHIS)
:date: 2026-06-05
:type: refactor + governance
:score: 9/10

== Contexte

Bakery gérait ses dépendances en silo via son propre `libs.versions.toml`, en
dérive avec les versions homogénéisées du `workspace-bom` (MEMPHIS, N0). Objectif :
importer la plateforme BOM comme source de vérité des versions, retirer les
doublons, aligner les versions déviantes.

== Modifications

=== build.gradle.kts

- Ajout `implementation(platform("education.cccp:workspace-bom:0.1.0"))` sur 3
  configurations (implementation, testImplementation, functionalTest, e2eTest)
- `"org.slf4j:slf4j-api:2.0.17"` → `libs.slf4j.api`
- `"ch.qos.logback:logback-classic:1.5.26"` → `libs.logback.classic` (version
  1.5.32 via BOM)

=== gradle/libs.versions.toml

- Retiré 6 versions BOM-managées : `jackson`, `langchain4j`, `arrow`,
  `playwright`, `cucumber`, `junit_platform`
- `version.ref` supprimé sur les libs correspondantes
- `kotlin 2.2.20` → `2.3.20` (aligné BOM)
- `plugin-publish 1.3.1` → `2.1.0` (aligné BOM)
- Ajout `slf4j-api` + `logback-classic` au catalogue

=== src/test/scenarios/.../AugmentedContextN3Steps.kt

- Fix Kotlin 2.3.20 : `createTempDir` déprécié →
  `Files.createTempDirectory(...).toFile()`

== Résultats

- Compilation : BUILD SUCCESSFUL, zéro erreur
- Tests : 757 UT PASS, zéro régression
- Diff : 3 fichiers, +31/−30 lignes

== Prochaine session (095)

Switch borough — Bakery 100% avec BOM aligné. Prêt pour le prochain borough.
