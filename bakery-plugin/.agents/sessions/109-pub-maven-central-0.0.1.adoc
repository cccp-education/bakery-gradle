= Session 109 — Publication Maven Central 0.0.1
:date: 2026-06-11
:status: ✅ TERMINÉE

== Contexte

Publication du plugin bakery-gradle sur Maven Central via nmcp.
VAGUE 4.5 — Bushwick.

== Changements réalisés

* [x] Version `0.1.4` → `0.0.1` hardcodée dans `build.gradle.kts`
* [x] Dépendance `codebase-contracts` → `0.0.1`
* [x] Dépendance `workspace-bom` → `0.0.1`
* [x] POM sans guard — `withType<MavenPublication>` (pas seulement `pluginMaven`)
  - nom, description, licence Apache 2.0, developer cccp-education, SCM
* [x] Migration Sonatype staging → nmcp : `repositories { mavenCentral() }`
* [x] `settings.gradle.kts` : plugin `com.gradleup.nmcp.settings` 1.5.0 +
  `nmcpSettings { centralPortal { username/password depuis ~/.gradle/gradle.properties, publishingType = "AUTOMATIC" } }`
  avec `mavenCentral()` en premier dans `repositories`
* [x] `signing { useGpgCmd() }`
* [x] Build vérifié : `./gradlew build -x test`

== Publication

`./gradlew publishAggregationToCentralPortal --no-daemon` — ✅ succès

Artifact : `education.cccp:bakery-gradle:0.0.1` sur Maven Central

== Fichiers modifiés

* `build.gradle.kts` — version 0.0.1, codebase-contracts 0.0.1, workspace-bom 0.0.1, POM complet, nmcp, signing
* `settings.gradle.kts` — nmcp plugin + nmcpSettings + mavenCentral first

== Score

9/10

== Prochaine session (110)

À définir — BKY-COV-2 microfissures coverage, ou switch borough.