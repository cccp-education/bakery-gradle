= Session 157 — Parser AsciiDoc→YAML Pivot (DDD/TDD/BDD) + READMEs badges
:session: 157
:date: 2026-06-24

== Mission

Demande utilisateur : « Reprenons bakery avec son prompt de reprise et son backlog.
Surtout oublie pas ma methodologie de baby step DDD/TDD/BDD de unit/fonctionnal/cucumber
tests, en agile software craftmanship. »

Puis : « tu mettra a jour le readme consommateur et plugin et qu'ils supportent
@10 langues les plus parlés.csv avec les indicateur de build couverture de test »

Puis : « cloture la sessions, une fois cela fais tu commit et push ! »

== Réalisations

=== 1. Alignement YAML pivots (contrat homogène)

* `ab-partition.yaml` : 2 tables migrées de `text:` simple vers `inline[]`
  (header + 6 rows × 3-4 cellules = 16 cellules converties).
* `quick-start.yaml` : aucune table, rien à aligner.
* Le contrat pivot YAML est homogène sur les 3 fichiers (quick-start, ab-partition, scripts).

=== 2. Parser AsciiDoc→YAML — Baby steps DDD/TDD

3 classes de domaine pur + 3 classes de test (30 UT PASS au total) :

* **PivotModel.kt** (sealed classes) — 8 types blocs (Heading, Paragraph, ListBlock,
  Table, Admonition, Source, Hr, +future frontmatter) + 4 types inline (Text, Bold,
  Code, Link). `PivotModelTest` : 4 UT.
* **PivotYamlRenderer.kt** — renderer déterministe PivotArticle→YAML.
  `PivotYamlRendererTest` : 9 UT (heading, paragraph+inline, source, hr, list,
  table cols/null, admonition, link).
* **AsciiDocParser.kt** — parser AsciiDoc→PivotArticle (frontmatter, headings,
  paragraphs, inline bold/code/link, lists ordered/unordered, source, admonition,
  hr, tables avec/sans cols, cellules inline code+text).
  `AsciiDocParserTest` : 17 UT.

* **PivotGoldenMasterTest.kt** — 3 tests golden master (quick-start, ab-partition,
  scripts) créés. BUT : problème de découverte Gradle — le test compile (bytecode
  présent) mais n'est pas exécuté par `--tests "bakery.pivot.PivotGoldenMasterTest"`.
  Le filtre `filter { excludeTestsMatching("bakery.scenarios.*") }` du build.gradle.kts
  n'exclut pas `bakery.pivot`, mais le test n'est pas découvert. À debugger en session 158.
  Les ressources golden master (6 fichiers : 3 .adoc + 3 .yaml) sont dans
  `src/test/resources/pivot-golden-master/`.

=== 3. READMEs mis à jour (badges + 10 langues du CSV)

4 fichiers README modifiés :

* `README.adoc` (consommateur EN, 0.0.2) :
  - 8 badges shields.io (version, build, coverage 85.88%, i18n 10 langues, Gradle, Kotlin, Java, license)
  - Tableau i18n enrichi avec nombre de locuteurs du CSV (~5.4 milliards total)
  - Section coverage mise à jour (85.88%, threshold 85%)
* `README_truth.adoc` (consommateur EN ancien 0.0.9) :
  - 3 badges (version, build, license)
  - Section i18n 10 langues + section coverage
* `README_fr.adoc` (consommateur FR) :
  - 3 badges + section i18n FR + section coverage FR
* `README_truth_fr.adoc` (consommateur FR ancien) :
  - 3 badges + section i18n FR + section coverage FR

Les 10 langues du CSV `10 langues les plus parlés.csv` sont documentées avec leur
nombre de locuteurs : en (1.53B), zh (1.14B), hi (630M), es (560M), fr (417M),
ar (272M), bn (273M), pt (265M), ru (255M), ur (234M).

== Méthodologie

* Baby steps DDD : domaine pur d'abord (sealed classes sans logique de parsing),
  puis renderer, puis parser. Chaque étape testée isolément.
* TDD : test d'abord (RED), implémentation minimale (GREEN), refactor.
* Le golden master test pilote le parser sans toucher au code production.
* Bac à sable `/tmp/opencode/bakery-i18n-lab/` pour les YAML pivots (non versionné).

== Tests

* 30 UT PASS (4 modèle + 9 renderer + 17 parser).
* PivotGoldenMasterTest (3 tests) non exécuté par Gradle (bug découverte à investiguer).
* Aucun test lancé en fin de session sans permission (règle AGENT.adoc respectée
  pour `check`/`cucumberTest`).

== Code modifié

* `bakery-plugin/src/main/kotlin/bakery/pivot/PivotModel.kt` (nouveau, 84 lignes)
* `bakery-plugin/src/main/kotlin/bakery/pivot/PivotYamlRenderer.kt` (nouveau, 134 lignes)
* `bakery-plugin/src/main/kotlin/bakery/pivot/AsciiDocParser.kt` (nouveau, 226 lignes)
* `bakery-plugin/src/test/kotlin/bakery/pivot/PivotModelTest.kt` (nouveau, 60 lignes)
* `bakery-plugin/src/test/kotlin/bakery/pivot/PivotYamlRendererTest.kt` (nouveau, 160 lignes)
* `bakery-plugin/src/test/kotlin/bakery/pivot/AsciiDocParserTest.kt` (nouveau, 414 lignes)
* `bakery-plugin/src/test/kotlin/bakery/pivot/PivotGoldenMasterTest.kt` (nouveau, 56 lignes)
* `bakery-plugin/src/test/resources/pivot-golden-master/*` (6 fichiers ressources)
* `README.adoc` (badges + i18n enrichi + coverage)
* `README_truth.adoc` (badges + i18n + coverage)
* `README_fr.adoc` (badges + i18n FR + coverage FR)
* `README_truth_fr.adoc` (badges + i18n FR + coverage FR)
* `/tmp/opencode/bakery-i18n-lab/magic-stick/content/ab-partition.yaml` (non versionné, aligné)

== Décision pour session 158

* Débugger le problème de découverte Gradle de `PivotGoldenMasterTest`.
* Si le golden master passe, finaliser le parser sur les edge cases restants.
* Câbler le parser dans une tâche Gradle `generatePivotYaml` (N2 → YAML pivot).

== Score session

8/10 — Parser complet (30 UT), READMEs à jour. Golden master bloqué par un
problème Gradle non résolu (découverte de test). Méthodologie DDD/TDD respectée.

== Notes techniques

* Ports Ollama : 11437-11465. Modèles : `gpt-oss:120b-cloud`, `gemma4:31b-cloud`.
* `publishToMavenLocal` non requis (aucun changement code production consommateur).
* Bac à sable `/tmp/opencode/bakery-i18n-lab/` = volatile.
* Loi de l'Économie d'Encre : encodée dans 10 boroughs.