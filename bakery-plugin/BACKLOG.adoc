= Backlog — Bakery Gradle Plugin
:description: Backlog nettoyé — EPICs historiques archivés, synthèse uniquement
:toc: left
:toclevels: 3
:icons: font
:sectnums:

_Dernière mise à jour : 2026-07-18 — Session 178. EPIC BKY-LANG-SWITCH CLOS (26/26 pts, 6/6 US). EPIC BKY-I18N-DEPLOY cadré (35 pts, 7 US). LANG-SWITCH-3+5+6 livrés S178 (Renderer + Task + BDD). Backlog actif : BKY-I18N-DEPLOY. Plugin 0.0.4 mavenLocal._
_Backlog précédent archivé dans_ : `.agents/backup/Y2026-sessions-145-154/BACKLOG_avant_nettoyage_sess154.adoc`
_Code review source_ : `.agents/CODE_REVIEW.adoc` (V5, Session 117) + `.agents/CODE_REVIEW_V4.adoc` (Session 096)

[CAUTION]
====
**EPIC BKY-LANG-SWITCH CLOS — 26/26 pts (100%).** 6/6 US livrées en 2 sessions
(S177 domaine + fixture, S178 renderer + task + BDD). Bug sélecteur de langue
cheroliv.com/en/ résolu : `injectLangSwitch` génère les URLs inter-langues
correctes (FR root→EN subdir, EN subdir→FR root, no self-loop).
**Backlog actif : EPIC BKY-I18N-DEPLOY (35 pts, 7 US).** Dogfooding
i18n sur sites réels via copies — généralisation du socle `ContentTranslationService` +
`MigrateContentI18nTask` (S134) aux 8 sites et 10 langues, avec classification
PlantUML (TRANSLATE/PRESERVE/BORROW) et injection RTL. Constate que les 108
articles `office/sites/cheroliv.com/content-i18n/en/blog/` sont restés en FR
(copie brute sans `migrateContentI18n`), en-têtes JBake cassés. Voir section EPIC
ci-dessous. Candidats parallèles pour S179+ :
. Déploiement cheroliv.com : appliquer `injectLangSwitch` sur site réel
. BKY-OUTPUT-5 : `ThemeResolver` → `TreeBakeService` (5 pts, 44/49 livrés)
. SEC-1 audit bakery (deepseek + ports 11434-11436)
. Publication Maven Central 0.0.4 (nmcp)

**Bakery est production-ready (S170). EPIC BKY-I18N-REAL TERMINÉ — 24/24 pts
(100%). S170 a livré REAL-1→6 en 1 session (projection 2).
Golden masters post-migration persistants figent le résultat attendu
pour les 3 sites (magic-stick 12, cccp 11, cheroliv 24 templates).
Idempotence (Ink Economy Law) validée sur données réelles.**
*Publication* : 0.0.3 sur Maven Central (LIVE, bundle 49be80ef) + Gradle Plugin Portal.
0.0.4 en mavenLocal (S178, non publié Central).
*Coverage* : 93,09% (Kover, S169), seuil `koverThresholdCheck` PASS. +27 UT snapshot (S170).
*Code smells* : 24/24 V2-V4 + 60 V5 + 3 V6 résolus (24/24 V5 via US-1→8, 8/8 V6).
*Loi de l'Économie d'Encre* : encodée dans bakery + 9 boroughs transverses (S154).
  Implémentée dans bakery (I18nMigrationService delta S153 + generatePivotYaml
  load-before-write S163 + SubtreeI18nPlanner delta par feuille S167 +
  PostMigrationIdempotenceTest 3 sites S170).
  9 autres boroughs = gouvernance only (AGENT.adoc).
*Parser pivot* : 41 UT + 4/4 golden masters GREEN + tâche `generatePivotYaml` (6 functional + 4 BDD).
*Arbre de site* : 99 UT tree package + 7 features Cucumber (186 scénarios/steps).
*Snapshot post-migration* : 27 UT snapshot + 1 feature Cucumber (6 scénarios @i18n-real).
*LangSwitch* : 40 UT (S177 22 + S178 18) + 7 FT + 5 BDD (feature 51).
====

'''

== Synthèse — EPICs terminés (archive compacte)

_Tous les EPICs ci-dessous sont clos. Le détail (US, baby steps, commits) est archivé dans
le backup sess154 et les rapports de sessions `.agents/sessions/NNN-*.adoc`._

[cols="1,3,2,2"]
|===
| EPIC | Description | Pts | Clôture

| EPIC 1 | Intégration gouvernance agent | — | S001
| EPIC 2 | Migration Supabase → Firebase | — | S002
| EPIC 3 | Amélioration build.gradle.kts | — | S003
| EPIC 4 | Formulaire de contact Firebase | — | S012
| EPIC 5 | Correction timeout Cucumber | — | Implémenté (5min)
| EPIC 6 | Intégration deployProfile | 13→3 | S072
| EPIC 7 | Tolérance parsing YAML | — | S029
| EPIC 8 | Bumper version 0.1.4 | — | S016
| EPIC 9 | Orchestration LLM | — | SUPERSEDÉ par BKY-IA-0
| EPIC 10 | Scaffolding TDD | — | S029
| EPIC 11 | Optimisation durée tests | — | S064 (-77%)
| EPIC 12 | Articles connexes KG | — | SUPERSEDÉ par BKY-LENS
| EPIC 13 | Couverture tests 73→77.25% | 5→3 | S077
| EPIC 14 | Roadmap IA (9 EPICs) | 73 | S022→069
| EPIC 15 | PluginApplicationTest NPE | 3 | S028
| EPIC 16 | Cucumber Scaffolding steps | 2 | S028
| EPIC 17 | generateArticle graceful fail | 5 | S053

| BKY-JB-0 | Scaffolding site JBake | 5 | S026
| BKY-JB-1 | Templates métier formation | — | Transféré training-gradle
| BKY-JB-2 | Composants Thymeleaf | 5 | S029
| BKY-JB-3 | Google Forms embed | 5 | S030
| BKY-JB-4 | Firebase Auth + Comments | 5 | S032
| BKY-JB-5 | Analytics + Newsletter | 3 | S033
| BKY-JB-6 | Système de thème | 5 | S034
| BKY-JB-7 | Layouts | 3 | S039
| BKY-JB-8 | Article IA | 13 | S040
| BKY-JB-9 | E2E Tests Playwright | 8 | S038

| BKY-CONV-1 | ConfigResolver 4 couches | 5 | S049
| BKY-IA-0 | Service LLM partagé | 5 | S023
| BKY-IA-1 | Scaffolding assisté IA | 5 | S065-066
| BKY-IA-2 | Thème IA paramétrique | 8 | S068-069
| BKY-IA-3 | Validation Firebase IA | 3 | S070
| BKY-IA-4 | Génération article IA | 8 | Remplacé par BKY-JB-8
| BKY-IA-5 | Google Suite (clasp) | — | Transféré KINGSTON
| BKY-N3-7 | Contrat N3 metadata.json | 5 | S024
| BKY-BKG | Articles connexes KG | 16 | SUPERSEDÉ par BKY-LENS

| BKY-LENS-1 | SubgraphExtractor | 5 | S055
| BKY-LENS-2 | AugmentedArticlesService | 4 | S057
| BKY-LENS-3 | Budget + Injection JBake | 3 | S058
| BKY-LENS-4 | Cucumber Features 17-19 | 3 | S059
| BKY-LENS-5 | Connect N3 Composite Context | 3 | S061
| BKY-LENS-6 | Templates Augmentés JBake | 3 | S062

| BKY-CR-V5 | Code smells V5 (60 smells) | 24 | S124
| BKY-CR-V6 | Duplications structurelles résiduelles | 8 | S145
| BKY-KOV-1 | Kover Coverage 78% → 85.88% | 16 | S104
| BKY-COV-2 | Microfissures coverage | 12 | S110

| BKY-PUB-1 | Publication Maven Central 0.0.1 | 8 | S109
| BKY-PUB-2 | Publication Maven Central + Portal 0.0.2 | 3 | S143
| BKY-I18N | i18n 10 langues | 23 | S116
| BKY-I18N-MIG | Tâche migrateToI18n | 16 | S134
| BKY-I18N-MIG-4 | Service traduction transverse (codebase N1) | 3 | S134
| BKY-I18N-PROD-VAL | Validation dry-run (magic-stick) | 3 | S130
| BKY-I18N-PROD-MIG | Migration magic-stick + traduction FR→EN | 5 | S131
| BKY-I18N-PROD-GEN | Généralisation cccp.education + cheroliv.com | 8 | S132
| BKY-I18N-PROD-AUDIT | Alignement fixtures ↔ sites réels | 5 | S150
| BKY-A11Y-1 | Accessibilité WCAG/RGAA | 13 | S152
| BKY-I18N-DELTA | Loi de l'Économie d'Encre (delta traduction) | 3 | S153
| BKY-I18N-REAL | Real i18n migration of 3 sites on copies (golden masters) | 24 | S170
| BKY-FIX-1 | Corriger deployProfile sans site.yml | — | S147
| ARROW-1 | Arrow-kt v1.1.0 roadmap | 5 | S093
|===

_Total points livrés_ : ~274 pts sur 170 sessions.
_Total smells résolus_ : 24 (V2-V4) + 60 (V5) + 3 (V6) = 87.

'''

== EPICs transférés (hors périmètre bakery)

[cols="1,3,3,2"]
|===
| EPIC | Destination | Raison | Statut

| BKY-IA-5
| **KINGSTON** `foundry/private/gsuite-gradle`
| Expert Google Suite (clasp, Forms, Sheets, Drive)
| 🔄 À créer

| BKY-TRN-9
| **training-gradle** TRN-9
| Domaine métier formation (portail)
| 🔄 Dans Newark

| BKY-JB-1
| **training-gradle** TRN-TEMP
| Templates métier formation
| 🔄 Transféré
|===

'''

== Code smells — Récapitulatif résolution

_Tous résolus. Le détail par smell est dans le backup sess154 et `.agents/CODE_REVIEW.adoc`._

[cols="1,3,2,2]
|===
| Vague | Contenu | Smells | Clôture

| V2-V3 (CS-FIN-1→10)
| configPath guard, JSON Jackson, nettoyage DRY, ConfigResolver cascade,
  GenerateSiteService extraction, GitService validation, ConfigPrompts Reader monad,
  FileSystemManager Either, BfsProximityCache, masquage secrets
| 10
| S080→S092

| V4 (CS-V4-P1/P2/P3/ARCH)
| Throwable fantôme, Path CWD, imbrication run, DeployProfileTask props,
  getByName sécurisé, rename resolveOrPrompt, catch muets, doc idempotence,
  YAML Either.catch, parallélisme documenté, side-effect traçable
| 12
| S097

| V5 (BKY-CR-V5)
| JsonEscape ordre, slugify dedup, parser JSON maison, first{} unsafe,
  println→logger, secrets LLM, ObjectMapper cache, ResolvedConfigs refactor,
  SUPPORTED_LANGS dedup, updateProperty dedup, pattern résolution,
  pattern injection DSL, code mort, resolveAll data-driven, magic numbers,
  YAML→Jackson, article.lang, catch muets, divers P3
| 60
| S124

| V6 (BKY-CR-V6)
| ResolveIntention<T> helper (×4), DomainConfigInjectors data-driven (9),
  ConfigResolver.resolveAll() data-driven (10 blocs)
| 3 structurels
| S145
|===

'''

== Pistes ouvertes

_Bakery était production-ready (S164). EPIC BKY-TREE (S165) rouvre le backlog
pour introduire le concept unifiant `SiteTree` (Composite Site→Section→Article
→AST). Détail dans la section EPIC BKY-TREE ci-dessous._

[cols="1,4,2"]
|===
| # | Piste | Methode

| 1-4
| **TOUTES RESOLUES (S159->S163)** — Debug PivotGoldenMasterTest, CLI @Option natif --input/--output + Loi Economie d Encre generatePivotYaml (load-before-write), 4e golden master nested-plantuml, bug cucumber features 9/16.
| —

| 5
| **BKY-TREE — Arbre de Site** (S166→S168). TREE-1→4 ✅ livrés (18 pts).
  TREE-5+6 ✅ livrés S167 (13 pts). TREE-7 ✅ livré S168 (5 pts).
  **EPIC CLOS 36/36 pts (100%).** Backlog vide.
| DDD/TDD/BDD

| 6
| **BKY-I18N-REAL — Real i18n migration** (S170). REAL-1→6 ✅ livrés (24 pts).
  Golden masters post-migration persistants pour 3 sites. Idempotence
  Ink Economy Law validée. **EPIC CLOS 24/24 pts (100%).** Backlog vide.
| DDD/TDD/BDD
|===

'''

== EPIC BKY-TREE — Arbre de Site : Composite Site→Section→Article→AST

_Cadré Session 165 (2026-06-25). Réouvre le backlog bakery après confirmation
production-ready (S164). Concept unifiant manquant qui relie PivotModel (AST
AsciiDoc, S157-163), SiteConfiguration (config plate), ScaffoldOutput (liste
plate de templates), et la Loi de l'Économie d'Encre (delta global → delta par
sous-arbre)._

[CAUTION]
====
**Motivation** : `SiteConfiguration` est plate (infra only : push, firebase,
theme). `ScaffoldOutput.templates: List<String>` est plate. La migration i18n
walk `templates/` à plat. Aucun concept ne modélise l'arborescence éditoriale
du site (section → article → contenu). Conséquence : la Loi de l'Économie
d'Encre ne peut pas calculer un delta ciblé par sous-arbre — elle opère
globalement, re-traduisant plus que nécessaire.

**Vision** : introduire un agregat `SiteTree` (Composite pattern) à 4 niveaux —
`Site` (root) → `Section` → `Article` (feuille) → `PivotModel` (AST déjà
construit S157-163). Chaque niveau manipulable indépendamment : theme hérité
ou surchargé, i18n par sous-arbre, accessibilité par section, Économie d'Encre
par article (delta sur l'AST, pas sur tout le site).

**Pré-requis respectés** :
. *Ne pas réinventer PivotModel* — il est fait, testé, golden-master. `Content`
  l'enveloppe comme feuille, point.
. *Backward compat* — `ScaffoldOutput.templates: List<String>` reste accepté
  via adapter flatten. Le nouveau `tree:` est additif dans `site.yml`.
. *Scope* — ~36 pts sur 7 US, à slicer sur 2-3 sessions minimum.
. *Méthodo* — baby steps DDD (sealed interface pur, testable sans Gradle) →
  TDD (UT rouge avant code) → BDD (feature Cucumber pour bout en bout). Aucune
  I/O dans les classes de domaine.
====

=== Arbre à 4 niveaux

----
Site (root)                    ← jbake.properties, theme global, i18n config
└── Section                    ← template de section, theme override (optionnel)
    └── Article                ← frontmatter + AsciiDoc source
        └── PivotModel (AST)  ← déjà construit (S157-163)
            ├── Heading, Paragraph, List, Source, Table, Admonition...
            └── inline: Bold, Code, Link, Text
----

=== User Stories — baby steps DDD/TDD/BDD

[cols="1,4,2,2,2"]
|===
| US | Description (domaine pur, no I/O) | Tests | Pts | Dépend de

| BKY-TREE-1
| *`SiteNode` sealed interface + implémentations* — `Site`, `Section`, `Article`
  comme Composite (children) / Leaf (no children). Identity par chemin relatif
  (`path: String`, ex `formations/ab-partition`). Data classes immutable.
  Égalité structurelle. Pas de logique, pas d'I/O.
| UT : construction, égalité, immutabilité, `isLeaf`/`isSection`, `path` canonical
| 5
| —

| BKY-TREE-2
| *`SiteTree` aggregate + visiteurs* — racine `Site`, méthodes `walk()`,
  `filter(predicate)`, `findSubtree(path)`, `findByPath(path)`, `leaves()`,
  `sections()`. Visiteur générique `fun <T> visit(transform: (SiteNode) -> T)`.
  Pas de mutation in-place (retourne nouveaux arbres ou listes).
| UT : traversal pré/post-order, subtree selection par path, collecte feuilles,
  filtrage par type, arbre vide, arbre à 1 niveau, arbre à 3 niveaux
| 5
| TREE-1

| BKY-TREE-3
| *`Content` wrapper sur `PivotModel`* — l'article comme feuille AST. `Article`
 porte un `Content` qui expose `pivot: PivotModel`, `blocs(): List<PivotBlock>`,
`inlineTexts(): List<PivotInline>`, `translatableSegments(): List<PivotInline>`
(délègue à `TextTranslatableClassifier` S158). Lien explicite Article↔AST.
| UT : article expose son AST, walk blocs/inline, translatable segments,
  article sans contenu (vide), article avec table imbriquée
| 3
| TREE-1

| BKY-TREE-4
| *YAML serialization — `tree:` section dans `site.yml`* — Jackson polymorphic
  (`@JsonTypeInfo` + `@JsonSubTypes` sur `SiteNode`). Round-trip parse/serialize
  déterministe. Additif — `site.yml` sans `tree:` reste valide (fallback
  existing flat behavior). Section `tree:` optionnelle.
| UT + FT : round-trip parse/serialize byte-identique, arbre vide, arbre 3
  niveaux, absence de `tree:` (compat), erreur YAML explicite
| 5
| TREE-1, TREE-2

| BKY-TREE-5
| *Theme inheritance — site → section → article* — `effectiveTheme(node)` :
  résout le thème effectif par nœud (héritage du parent si non surchargé,
  override explicite sinon). `ThemeConfig` déjà existant (S068). Pas de
  mutation — retourne `ResolvedTheme`. Détection de conflits (override
  partiel interdit : si override, toutes les valeurs doivent être explicites).
| UT : résolution thème effectif par nœud, héritage simple, override section,
  override article, conflit override partiel détecté, racine sans theme
| 5
| TREE-2

| BKY-TREE-6
| *i18n migration par sous-arbre* — remplace le walk flat `templates/` actuel
  par `tree.walk().filter { it is Article }`. Delta ciblé : un AsciiDoc modifié
  → re-traduire seulement ses clés. Intégrité hors-sous-arbre préservée
  (checksums des autres articles inchangés). Implémente la Loi de l'Économie
  d'Encre par article (S153 global → S166 par feuille).
| UT + BDD : delta ciblé article modifié, intégrité hors-sous-arbre préservée,
  delta section renommée, idempotence re-exécution (Loi Économie d'Encre),
  backward compat (walk flat si pas de `tree:`)
| 8
| TREE-2, TREE-3, TREE-4

| BKY-TREE-7
| *Scaffold IA retourne un arbre* — `ScaffoldOutput.tree: SiteNode` au lieu de
  `templates: List<String>`. Adapter flatten pour backward compat (si
  `tree:` absent, `templates:` est accepté). LLM prompt mis à jour pour
  retourner une structure hiérarchique JSON. Feature Cucumber bout en bout.
| FT + BDD : scaffold IA retourne arbre valide, adapter flatten backward
  compat, scaffold sans tree (legacy), intégrité arbre vs templates liste
| 5
| TREE-4
|===

_Total_ : ~36 pts, 7 US.

=== Dépendances (DAG)

----
TREE-1 (SiteNode) ─┬─→ TREE-2 (SiteTree) ─┬─→ TREE-4 (YAML) ─→ TREE-7 (Scaffold IA)
                   │                        ├─→ TREE-5 (Theme)
                   └─→ TREE-3 (Content) ────┘
                                            └─→ TREE-6 (i18n sous-arbre)
----

*TREE-1* est racine (tout dépend de lui).
*TREE-6* est le point de fusion (consume TREE-2 + TREE-3 + TREE-4) — c'est
aussi le livrable qui concrétise la Loi de l'Économie d'Encre par sous-arbre.

=== Projection sessions (à affiner)

* *S166* — TREE-1 + TREE-2 (10 pts, 2 US, socle domaine pur)
* *S167* — TREE-3 + TREE-4 (8 pts, 2 US, AST + YAML)
* *S168* — TREE-5 + TREE-6 (13 pts, 2 US, theme + i18n, le gros morceau)
* *S169* — TREE-7 (5 pts, 1 US, scaffold IA, clôture EPIC)

Projection conservatrice : 4 sessions, ~36 pts, méthodo baby step
DDD/TDD/BDD préservée à chaque US.

*Réalité* : S166 a livré TREE-1→4 (avance), S167 a livré TREE-5+6.
S168 = TREE-7 (clôture EPIC). 3 sessions au lieu de 4.

=== Risques et mitigations

[cols="3,5,4]
|===
| Risque | Mitigation
| PivotModel non suffisant pour Article | `Content` wrapper ajoute métadonnées
  (frontmatter, slug, date) sans toucher à PivotModel. PivotModel reste
  l'AST pur, Content l'enveloppe.
| `site.yml` casse sites existants | `tree:` est additif et optionnel. Fallback
  behavior flat walk si absent. Testé en UT (TREE-4).
| Scaffold IA casse LLM prompt | Adapter flatten garantit que `templates:
  List<String>` reste accepté. Migration prompt progressive.
| Scope trop gros pour 1 session | Slicing 4 sessions, chaque US est baby
  step indépendant. TREE-1 seul = domaine pur testable sans Gradle.
|===

'''

== EPIC BKY-I18N-REAL — Migration i18n réelle des 3 sites sur copies (golden masters post-migration)

_Cadré Session 170 (2026-06-29). La tuyauterie `I18nMigrationService` +
`MigrateToI18nTask` existe et fonctionne (tests d'intégration GREEN sur
`@TempDir`). Les 3 fixtures pré-migration sont alignées (S150), les
`translations_en.properties` sont prêtes. **Le gap** : aucun snapshot
post-migration persistant n'existe — la migration n'a jamais été figée
comme golden master. Aucun `messages_*.properties` réel n'existe dans
les fixtures (uniquement `translations_en.properties` séparé)._

[CAUTION]
====
**Motivation** : les 3 tests d'intégration (`MagicStickI18nMigrationIntegrationTest`,
`CccpEducationI18nMigrationIntegrationTest`, `CherolivComI18nMigrationIntegrationTest`)
valident que la migration *fonctionne* sur copies temporaires, mais le
résultat n'est jamais persisté. Conséquence : aucune régression sur le
rendu post-migration ne peut être détectée, et l'idempotence (Loi de
l'Économie d'Encre) n'est pas testée sur données réelles.

**Vision** : introduire un concept `PostMigrationSnapshot` (DDD pur,
testable sans Gradle) qui capture l'état d'un site après migration
(templates migrés + `messages_fr.properties` + `messages_en.properties`
rempli). Des golden masters persistants (un par site) figent le résultat
attendu. Un test TDD rouge assert byte-identical. Une feature BDD Cucumber
valide bout en bout. L'idempotence est testée : re-execute migrate sur
copie déjà migrée = zéro changement.

**Pré-requis respectés** :
. *Ne pas toucher à la production* — tout sur copies (`@TempDir` ou
  `src/test/resources/i18n-fixtures/{site}/post-migration/`).
. *Backward compat* — les fixtures pré-migration restent intactes (nouveaux
  snapshots sont additifs dans `post-migration/`).
. *Méthodo* — baby steps DDD (concept pur, testable sans Gradle) → TDD
  (UT rouge avant code) → BDD (feature Cucumber bout en bout). Aucune I/O
  dans les classes de domaine.
====

=== User Stories — baby steps DDD/TDD/BDD

[cols="1,4,2,2,2"]
|===
| US | Description (domaine pur, no I/O) | Tests | Pts | Dépend de

| BKY-I18N-REAL-1
| *`PostMigrationSnapshot` concept pur* — data class immutable représentant
  l'état d'un site après migration : `templatesMigrated: Map<String, String>`
  (nom template → contenu migré), `messagesFr: Map<String, String>`,
  `messagesEn: Map<String, String>`. Méthodes `hasAllKeysTranslated()`,
  `templateKeys()`, `isComplete()`. Pas de logique, pas d'I/O. Comparable
  par égalité structurelle.
| UT : construction, égalité, immutabilité, `hasAllKeysTranslated` (toutes
  clés FR ont une traduction EN non vide), `templateKeys` (clés extraite
  des templates migrés), `isComplete` (tous templates migrés + messages
  non vides)
| 3
| —

| BKY-I18N-REAL-2
| *`PostMigrationSnapshotLoader`* — charge un snapshot depuis un répertoire
  `post-migration/` (templates + `messages_fr.properties` +
  `messages_en.properties`). Retourne `PostMigrationSnapshot`. I/O isolée
  dans ce loader, pas dans le domaine.
| UT : charge snapshot valide, snapshot incomplet (messages_en manquant),
  snapshot vide, round-trip (load → assert contenu)
| 3
| REAL-1

| BKY-I18N-REAL-3
| *Golden master test TDD rouge* — `PostMigrationGoldenMasterTest` : migrate
  fixture pré-migration → compare résultat au snapshot attendu
  (`post-migration/`). Assert byte-identical sur templates migrés +
  `messages_fr.properties` + `messages_en.properties` (après application
  des `translations_en.properties`). Test échoue tant que le snapshot
  n'existe pas (TDD rouge).
| UT : 3 sites × golden master (magic-stick, cccp, cheroliv). Assert
  byte-identical templates + messages_fr + messages_en.
| 5
| REAL-1, REAL-2

| BKY-I18N-REAL-4
| *Produire les 3 snapshots post-migration* — exécuter migrateToI18n sur
  copie de chaque fixture, appliquer `translations_en.properties`, persister
  le résultat dans `src/test/resources/i18n-fixtures/{site}/post-migration/`.
  3 snapshots : `templates/` migrés + `messages_fr.properties` +
  `messages_en.properties` rempli. Golden master test passe au vert.
| — (production d'artefacts, validé par REAL-3)
| 5
| REAL-3

| BKY-I18N-REAL-5
| *BDD Cucumber bout en bout* — feature `46_i18n_real_migration.feature` :
  `migrateToI18n` sur copie site réel → assert artefacts migrés conformes
  au golden master. Scénarios : dry-run safe, real migration génère
  messages_fr/en, templates contiennent `th:text`, idempotence
  (re-execute = zéro changement).
| BDD : 3-4 scénarios par site (dry-run, real, idempotence, conformité
  golden master)
| 5
| REAL-4

| BKY-I18N-REAL-6
| *Idempotence Loi Économie d'Encre* — re-execute migrate sur copie déjà
  migrée = zéro changement (templates inchangés, messages_fr/en inchangés).
  Test UT + scénario BDD. Valide que `I18nMigrationService` détecte les
  templates déjà migrés (`th:text` déjà présent) et ne re-extrait pas.
| UT : idempotence sur 3 sites, delta vide, messages inchangés
| 3
| REAL-4
|===

_Total_ : ~24 pts, 6 US.

=== Dépendances (DAG)

----
REAL-1 (Snapshot) ─→ REAL-2 (Loader) ─→ REAL-3 (Golden master TDD rouge)
                                              └─→ REAL-4 (Produire snapshots) ─→ REAL-5 (BDD)
                                                                                └─→ REAL-6 (Idempotence)
----

*REAL-1* est racine (tout dépend de lui).
*REAL-4* est le point de bascule (TDD rouge → vert).
*REAL-6* clôture l'EPIC (valide la Loi de l'Économie d'Encre sur données réelles).

=== Projection sessions

* *S170* — REAL-1 + REAL-2 + REAL-3 (11 pts, 3 US, socle domaine + golden master rouge)
* *S171* — REAL-4 + REAL-5 + REAL-6 (13 pts, 3 US, snapshots + BDD + idempotence, clôture EPIC)

Projection conservatrice : 2 sessions, ~24 pts, méthodo baby step
DDD/TDD/BDD préservée à chaque US.

=== Risques et mitigations

[cols="3,5,4]
|===
| Risque | Mitigation
| Snapshots non déterministes (ordre clés properties) | `Properties.store()` ordre non garanti. Utiliser `LinkedHashMap` ou trier les clés avant écriture dans le golden master.
| Templates migrés diffèrent légèrement (whitespace) | Normalisation whitespace avant comparaison, ou assert sur contenu structuré (DOM) plutôt que byte-identical.
| Idempotence cassée par re-extraction | `I18nMigrationService` détecte déjà `th:text` (regex `alreadyI18n`). Test REAL-6 valide ce comportement sur données réelles.
| Scope trop gros pour 1 session | Slicing 2 sessions, chaque US est baby step indépendant. REAL-1 seul = domaine pur testable sans Gradle.
|===

'''

_Investigation S162 — voir `.agents/sessions/BKY-PERF-1-profiling.adoc` pour le détail._

[CAUTION]
====
**EPIC BKY-PERF TERMINÉ — Conclusion : aucune optimisation possible via l'API GradleRunner.**

*Baseline* : `./gradlew functionalTest` = **4 min 10 s** pour 39 tests (3 forks parallèles, `maxParallelForks = 3`).
*Bottleneck fondamental* : `GradleRunner.create().build()` fork un daemon Gradle à **chaque appel** (~20-25 s/test incluant startup daemon + compilation plugin). L'API GradleRunner ne permet PAS de réutiliser le daemon entre tests.
====

[cols="1,4,2,2"]
|===
| US | Description | Pts | Statut

| BKY-PERF-1
| **Profiling — mesurer par classe de test** — 13 classes testées individuellement. Classification SINGLE-SHOT (10 classes, 35 tests) vs MULTI-SHOT (3 classes, 19 tests). Rapport sauvé dans `.agents/sessions/BKY-PERF-1-profiling.adoc`.
| 2
| ✅ TERMINÉ

| BKY-PERF-2
| **`withDebug(true)` sur tests readonly** — Testé sur `IaFunctionalTests` (pilote). *Résultat* : `withDebug(true)` active le mode JDWP (debugger), PAS l'exécution in-process. 1m35s vs 27s → **2x plus lent**. ABANDONNÉ.
| 5
| ❌ ABANDONNÉ

| BKY-PERF-3
| **Projet partagé `@TestInstance(PER_CLASS)` + @BeforeAll** — Testé sur `IaFunctionalTests` (pilote). *Résultat* : `@TempDir` partagé ne change rien — `GradleRunner.create().build()` fork un daemon frais à chaque `.build()`, même dans le même projet. Regroupement de 4 tests en 1 seul test → 1m42s, pas d'amélioration. ABANDONNÉ.
| 5
| ❌ ABANDONNÉ

| BKY-PERF-4
| **Configuration cache GradleTestKit** — Testé sur `IaFunctionalTests` (pilote) avec `--configuration-cache`. *Résultat* : 1m35s, pas d'amélioration. Le cache n'est pas réutilisable car chaque `@TempDir` est un projet différent. ABANDONNÉ.
| 3
| ❌ ABANDONNÉ

| BKY-PERF-5
| **`maxParallelForks` fixé à 3** — Passé de `Runtime.getRuntime().availableProcessors()` (8) à `3` (build.gradle.kts:158). Seule optimisation appliquée et conservée.
| 2
| ✅ TERMINÉ
|===

*Conclusion* : L'API `GradleRunner` est le bottleneck. Chaque `.build()` fork un daemon Gradle (JVM séparée, ~20-25s par test). Aucune option de l'API (`withDebug`, `--no-daemon`, `--configuration-cache`, `PER_CLASS`, `@BeforeAll`, regroupement de tests) ne permet de réutiliser le daemon entre tests. Les 4 min 10 s sont le coût incompressible de 39 daemons Gradle forkés (3 par 3).

*Pistes non explorées ( hors API GradleRunner)* :
* **Migration vers `@GradleProject` test fixtures** (Gradle 9.x) — API plus récente permettant de partager un projet Gradle entre tests. Investiguer si compatible avec plugin testing.
* **Réduction du nombre de tests fonctionnels** — Fusionner les tests DSL triviaux (ex. `IaFunctionalTests`, `ScaffoldIntentionFunctionalTests`, `ArticleIntentionFunctionalTests`, `ThemeIntentionFunctionalTests` — tous lancent `tasks` et assert `BUILD SUCCESSFUL`) en un seul test paramétré qui ne fork qu'un seul daemon.
* **Tests unitaires purs** pour la validation DSL — Remplacer les tests fonctionnels triviaux (qui ne font que vérifier que le DSL compile) par des tests unitaires qui chargent le plugin classpath sans fork de daemon.

*Note* : `--no-daemon` a été testé mais n'est pas supporté par `GradleRunner` (`CommandLineArgumentException`).

*Notes techniques* :
* Ports Ollama autorisés : 11437-11465 uniquement. Modèles : `gpt-oss:120b-cloud`, `gemma4:31b-cloud`.
* `./gradlew test` = 1m 1s SUCCESS (tous tests unitaires, S159 fix découverte).
* `./gradlew test --tests "bakery.pivot.AsciiDocParserTest" --tests "bakery.pivot.PivotModelTest" --tests "bakery.pivot.PivotYamlRendererTest" --tests "bakery.pivot.TextTranslatableClassifierTest"` = 41/41 PASS.
* `--tests "bakery.pivot.PivotGoldenMasterTest.golden master scripts adoc to yaml is byte identical"` = workaround noms backtick (bug Gradle/JUnit5).
* `./gradlew functionalTest --tests "bakery.GeneratePivotYamlFunctionalTest"` = 3/3 PASS.
* `./gradlew cucumberTest -Dcucumber.features="src/test/features/38_generate_pivot_yaml.feature"` = 2/2 scénarios PASS.
* Timeout cucumberTest 5min (EPIC 5) — ne pas lancer `check` complet sans permission.
* `publishToMavenLocal` fait (jar 0.0.2).
* *Loi de l'Économie d'Encre* : encodée dans 10 boroughs. Toute tâche Gradle LLM DOIT load-before-write + calcul delta + fusion existing+new.

'''

== EPIC BKY-LANG-SWITCH — Language Switcher URL Resolver (TDD/BDD on public fixture) — ✅ CLOS

_Cadré Session 177 (2026-07-16). Clos Session 178 (2026-07-18). 26/26 pts (100%).
Bug constaté sur cheroliv.com/en/ : le sélecteur de langue génère des URLs
incorrectes. Sur `/en/`, le lien FR pointe vers
`index.html` (résolu en `/en/index.html` = page EN courante) au lieu de
`../index.html` (résolu en `/index.html` = FR). Les liens inter-langues doivent
toujours repartir de la racine du site, pas du sous-répertoire courant._

[CAUTION]
====
**Motivation** : le sélecteur de langue est codé en dur dans `menu.thyme` avec
`th:href="${content.rootpath + 'en/index.html'}"`. Quand `rootpath` vaut `../`
(sous-répertoire `/en/`), `rootpath + 'index.html'` = `../index.html` → `/index.html`
(FR correct), MAIS `rootpath + 'en/index.html'` = `../en/index.html` → `/en/index.html`
(self-loop, la page EN pointe vers elle-même). Le problème est que les URLs
inter-langues doivent être calculées relativement à la racine du site, pas au
sous-répertoire courant.

**Vision** : introduire un concept DDD `LangSwitchUrl` (domaine pur, testable
sans Gradle) qui calcule l'URL correcte vers chaque langue depuis n'importe
quelle page. Une tâche Gradle `injectLangSwitch` génère ou met à jour le
fragment Thymeleaf du sélecteur de langue dans `menu.thyme` avec les bonnes
URLs. Un fixture public `cheroliv-com-lang-switch` (copie minimaliste : 2 langues
FR/EN, 1 index.html par langue) permet de tester sans exposer le site privé.

**Pré-requis respectés** :
. *Ne pas toucher au site privé* — tout sur fixture public dans `src/test/resources/`.
. *Backward compat* — si pas de `i18n` config, aucune injection (no-op).
. *Méthodo* — baby steps DDD (concept pur, testable sans Gradle) → TDD
  (UT rouge avant code) → BDD (feature Cucumber bout en bout). Aucune I/O
  dans les classes de domaine.
====

=== Le bug en détail

----
Scénario : utilisateur sur https://cheroliv.com/en/index.html

Menu FR (menu.thyme ligne 117):
  th:href="${content.rootpath + 'index.html'}"
  rootpath = "../" (JBake relatif depuis /en/)
  Résultat : ../index.html → /index.html ✅ FR correct

Menu EN (menu.thyme ligne 120):
  th:href="${content.rootpath + 'en/index.html'}"
  rootpath = "../" (JBake relatif depuis /en/)
  Résultat : ../en/index.html → /en/index.html ❌ self-loop (page EN courante)

Le vrai problème :
  - Depuis /en/, pour aller vers EN on devrait avoir juste "index.html" (déjà dessus)
    ou pas de lien du tout (option active = current page)
  - Depuis /en/, pour aller vers FR on devrait avoir "../index.html"
  - Depuis /en/, pour aller vers AR on devrait avoir "../ar/index.html"
  - Depuis / (FR), pour aller vers EN on devrait avoir "en/index.html"
  - Depuis / (FR), pour aller vers FR on devrait avoir "index.html" (ou no link, active)
  - Depuis / (FR), pour aller vers AR on devrait avoir "ar/index.html"
----

=== User Stories — baby steps DDD/TDD/BDD

[cols="1,4,2,2,2"]
|===
| US | Description (domaine pur, no I/O) | Tests | Pts | Dépend de

| BKY-LANG-SWITCH-1
| *`LangSwitchUrl` concept pur* — data class immutable représentant une URL
  de sélecteur de langue. `targetLanguage: String`, `currentPath: String`
  (chemin relatif de la page courante, ex `""` pour root FR, `en/` pour EN),
  `siteRootPath: String` (racine absolue du site, ex `/`). Méthode
  `resolve(): String` calcule l'URL relative correcte :
  - si currentLang == targetLang → `index.html` (self, page courante)
  - si targetLang == defaultLang → `rootpath + 'index.html'` (remonter à racine)
  - si targetLang != defaultLang → `rootpath + '{lang}/index.html'` (descendre)
  où `rootpath` = `currentPath` remonté à la racine (ex `en/` → `../`).
  Pas de logique I/O. Comparable par égalité structurelle.
| UT : resolve self-link (same lang), resolve FR from EN, resolve EN from FR,
  resolve AR from EN, resolve FR from FR (root), resolve EN from root,
  rootpath computation for nested paths
| 5
| —

| BKY-LANG-SWITCH-2
| *`LangSwitchMenu` aggregate* — génère la liste des `LangSwitchUrl` pour toutes
  les langues supportées. `supportedLanguages: List<String>`,
  `defaultLanguage: String`, `currentLanguage: String`, `currentPath: String`.
  Méthode `generateLinks(): List<LangSwitchUrl>`. Détecte la langue active
  (marquée `active` dans le rendu). Pas de logique I/O, pas de HTML.
| UT : generateLinks pour 2 langues (FR default + EN), 10 langues,
  active detection, default lang link, non-default lang link,
  empty supported languages (error)
| 3
| LANG-SWITCH-1

| BKY-LANG-SWITCH-3
| *`LangSwitchThymeleafRenderer`* — transforme `List<LangSwitchUrl>` en
  fragment HTML Thymeleaf (le bloc `<ul class="dropdown-menu">` avec les
  `<li><a class="dropdown-item lang-option">`). I/O isolée dans ce renderer
  uniquement (génération de String). Respecte la structure existante de
  `menu.thyme` (classes CSS, icônes Bootstrap). Le renderer produit le
  fragment Thymeleaf avec `th:href` correctement calculé.
| UT : render 2 langues, render 10 langues, active class sur langue courante,
  render empty (error), render avec icônes, output contient th:href correct
| 5
| LANG-SWITCH-2

| BKY-LANG-SWITCH-4
| *Fixture public `cheroliv-com-lang-switch`* — copie minimaliste du site
  cheroliv.com dans `src/test/resources/fixtures/cheroliv-com-lang-switch/` :
  2 langues (FR default + EN), `jbake/templates/menu.thyme` (sans sélecteur
  de langue), `jbake/content/index.html` (FR), `i18n/en/templates/menu.thyme`
  (sans sélecteur), `i18n/en/content/index.html` (EN), `site.yml` minimal.
  Aucun contenu privé — uniquement la structure pour tester le sélecteur.
| — (production d'artefacts, validé par LANG-SWITCH-5)
| 3
| —

| BKY-LANG-SWITCH-5
| *`InjectLangSwitchTask` + intégration bake* — tâche Gradle qui lit la config
  i18n (`I18nConfig`), génère le fragment Thymeleaf via `LangSwitchMenu` +
  `LangSwitchThymeleafRenderer`, et l'injecte dans `menu.thyme` (remplace le
  bloc `lang-switcher-container` existant ou l'ajoute si absent). Exécutée
  avant `bake` dans le pipeline. Intégration dans `ContentTaskRegistrar`.
| FT : inject dans fixture 2 langues, inject dans fixture 10 langues,
  inject idempotent (re-run = même résultat), inject avec sélecteur existant
  (remplace), inject sans sélecteur existant (ajoute)
| 5
| LANG-SWITCH-3, LANG-SWITCH-4

| BKY-LANG-SWITCH-6
| *BDD Cucumber bout en bout* — feature `51_lang_switch_url_resolver.feature` :
  bake fixture FR+EN → assert sélecteur de langue dans index.html FR contient
  lien `en/index.html` pour EN, assert sélecteur dans `en/index.html` EN
  contient lien `../index.html` pour FR, assert langue active marquée,
  assert pas de self-loop (lien EN depuis /en/ ne pointe pas vers /en/).
  Scénarios : FR root → EN link, EN subdir → FR link, EN subdir → AR link,
  active class correct, no self-loop.
| BDD : 5 scénarios (FR→EN, EN→FR, EN→AR, active class, no self-loop)
| 5
| LANG-SWITCH-5
|===

_Total_ : ~26 pts, 6 US.

=== Dépendances (DAG)

----
LANG-SWITCH-1 (LangSwitchUrl) ─→ LANG-SWITCH-2 (Menu) ─→ LANG-SWITCH-3 (Renderer)
                                                          ├─→ LANG-SWITCH-5 (Task + bake integration)
LANG-SWITCH-4 (Fixture) ─────────────────────────────────┘   └─→ LANG-SWITCH-6 (BDD)
----

*LANG-SWITCH-1* est racine (tout dépend de lui).
*LANG-SWITCH-4* (fixture) est indépendant, peut être créé en parallèle.
*LANG-SWITCH-5* est le point de fusion (consume renderer + fixture).
*LANG-SWITCH-6* clôture l'EPIC (valide bout en bout sur fixture public).

=== Projection sessions

* *S177* — LANG-SWITCH-1 + LANG-SWITCH-2 (8 pts, 2 US, socle domaine pur)
* *S177* — LANG-SWITCH-4 (3 pts, 1 US, fixture public, parallélisable)
* *S178* — LANG-SWITCH-3 + LANG-SWITCH-5 + LANG-SWITCH-6 (15 pts, 3 US, renderer + task + BDD)

Projection conservatrice : 2 sessions, ~26 pts, méthodo baby step
DDD/TDD/BDD préservée à chaque US.

=== Risques et mitigations

[cols="3,5,4]
|===
| Risque | Mitigation
| JBake rootpath non déterministe | Ne pas dépendre de `content.rootpath` dans le
  calcul. `LangSwitchUrl` calcule depuis `currentPath` (chemin relatif connu),
  pas depuis une variable Thymeleaf runtime.
| menu.thyme structure varie entre sites | Renderer paramétrable : selector CSS
  configurable pour trouver le bloc à remplacer. Fixture couvre 2 cas (avec
  et sans sélecteur existant).
| Fixture expose du contenu privé | Fixture minimaliste : `index.html` avec
  "Hello FR" / "Hello EN" uniquement. Zéro contenu éditorial, zéro image,
  zéro secret.
| Self-loop non détecté | BDD scénario explicite : assert href EN depuis /en/
  ne contient pas `en/index.html` (self-loop interdit).
|===

'''

== EPIC BKY-I18N-DEPLOY — Dogfooding i18n sur sites réels via copies (10 langues, 8 sites)

_Cadré Session 178 (2026-07-18). Constat : la tuyauterie `ContentTranslationService`
(S153) + `MigrateContentI18nTask` `migrateContentI18n` (S134) +
`LlmServiceTranslationAdapter` (S134) + `LanguageCatalog.supportedCodes()` (S116)
existe, est testée (41 UT parser + 99 UT tree + 27 UT snapshot + 7 features
Cucumber = 186 scénarios) et production-ready (S170, EPIC BKY-I18N-REAL 24/24 pts).
Golden masters post-migration figent le résultat pour 3 sites (magic-stick 12,
cccp 11, cheroliv 24 templates)._

_Pourtant les 108 articles `office/sites/cheroliv.com/content-i18n/en/blog/` sont
**encore en français**, en-têtes JBake cassés (`title=/date=/~~~~~~` aplatis). Le
pipeline bakery n'a jamais été lancé sur le vrai cheroliv.com — seulement sur
les fixtures de test. Les 108 fichiers sont le résultat d'un `cp` brut sans
`migrateContentI18n`, d'où le casse-tête. La traduction réelle des articles
n'est pas testée._

[CAUTION]
====
**Motivation** : `ContentTranslationService.translate` parse un `.adoc` →
`PivotArticle` → traduit bloc par bloc (Heading/Paragraph/List/Table/Admonition),
skipe Source/Code/non-translatable via `TextTranslatableClassifier`, préserve les
headers JBake via `JbakeNativeRenderer`. C'est exactement ce qu'il faut pour
traduire les 108 articles cheroliv.com en 10 langues. Mais :
. Aucun test ne valide `migrateContentI18n` sur un site réel (hors fixtures).
. Aucun test ne couvre la classification PlantUML (TRANSLATE/PRESERVE/BORROW) —
  `PivotBlock.Source` est `translatable = false` systématiquement, ce qui
  préserve les blocs `[plantuml]` sans traduction. Insuffisant pour les diagrammes
  à labels traduisibles.
. Aucun test ne couvre l'injection `:jbake-lang:` + `lang/dir` RTL post-traduction
  (ar/ur), alors que `LanguageCatalog` porte déjà le champ `rtl`.
. Aucun test ne couvre l'extension `targetLanguages` aux 10 langues sur un site
  réel (seulement FR→EN en fixtures).
. Aucun dogfooding n'a jamais été lancé sur les 8 sites (cheroliv, magic-stick,
  talaria, edster, cccp.education + 3 autres).

**Vision** : bakery reste une library/plugin pur. Les sites restent des données
consommatrices. Chaque site a sa copie de travail (déjà le cas :
`office/sites/cheroliv.com/`), bakery y pointe via `--contentI18nSource` /
`--contentI18nOutput` + `contentRootDir`. Tests FT via projet consommateur
includeBuild (pattern déjà utilisé pour `injectLangSwitch` FT S178). On dogfood
en vraie grandeur, en suivant la même méthodo baby step DDD/TDD/BDD que les
EPICs précédents — aucune exception.

**Pré-requis respectés** :
. *Boundary bakery* — bakery traduit le contenu, ne touche pas au thème ni au
  sélecteur de langue (déjà couvert par BKY-LANG-SWITCH S177-178). Boundary
  document-gradle (WRITE+PUBLISH) ↔ codex (READ+RAG) préservé.
. *Ne pas réinventer* — `ContentTranslationService`, `MigrateContentI18nTask`,
  `LlmServiceTranslationAdapter`, `LanguageCatalog`, `PivotModel` existent et
  sont testés. On les étend, on ne les remplace pas.
. *Ne pas toucher au contenu original* — tout sur copies de travail
  (`content-i18n/{lang}/`), dry-run par défaut (`contentI18nDryRun=true`).
. *Méthodo* — baby steps DDD (concept pur, testable sans Gradle) → TDD
  (UT rouge avant code) → BDD (feature Cucumber bout en bout). Aucune I/O
  dans les classes de domaine.
. *Ports Ollama* : 11437-11465 uniquement, rotation. Modèles autorisés :
  `gpt-oss:120b-cloud`, `gemma4:31b-cloud`.
. *Réutilisations cross-boroughs* :
  ** `LanguageCatalog.supportedCodes()` (MEMPHIS i18n-contracts, S116) — top 10 langues
  ** `PlantUmlClassifier` TRANSLATE/PRESERVE/BORROW (HTOWN EPIC PLT-BOUNDARY, S163-166)
  ** `DeckTranslator.ensureRtlDirection` (MIAMI EPIC SLD-5, S029-030)
  ** `LanguageModelAdapter` port + `OllamaLanguageModelAdapter` (MIAMI SLD-5)
  ** `LlmServiceTranslationAdapter` (bakery S134) déjà câblé
====

=== Pipeline cible

----
content/blog/ (FR source)  ─┬─→ MigrateContentI18nTask ─→ content-i18n/{lang}/blog/
                             │   (copie + traduit .adoc)
                             │
                             ├─→ PlantUmlClassifier ─→ TRANSLATE | PRESERVE | BORROW
                             │   (par bloc [plantuml], stratégie décidée)
                             │
                             └─→ RtlDirectionInjector ─→ :jbake-lang: + :lang: rtl
                                 (post-traduction, ar/ur uniquement)

content-i18n/{lang}/  ─→ injectLangSwitch (BKY-LANG-SWITCH) ─→ bake JBake ─→ CI deploy
----

=== User Stories — baby steps DDD/TDD/BDD

[cols="1,4,2,2,2]
|===
| US | Description (domaine pur, no I/O) | Tests | Pts | Dépend de

| BKY-I18N-DEPLOY-0
| *Cadrage boundary + copies de travail* — documenter le boundary bakery
  (traduit contenu, ne touche pas thème/sélecteur). Mettre en place les
  copies de travail des 8 sites si pas déjà (`content-i18n/{lang}/`). Vérifier
  que `migrateContentI18n --contentI18nDryRun=true` parse les 108 articles
  cheroliv.com sans écrire (smoke test). Pas de code production, uniquement
  configuration + documentation + smoke FT.
| FT : dry-run `migrateContentI18n` sur cheroliv.com (108 articles), dry-run sur
  magic-stick, dry-run sur cccp.education. Assert : zéro fichier modifié, logs
  listent les 108 fichiers, aucun crash parsing.
| 3
| —

| BKY-I18N-DEPLOY-1
| *`PlantUmlClassifier` port DDD pur* — sealed interface `PlantUmlStrategy`
  avec 3 variantes `TranslateLabels`, `PreserveTechnical`, `BorrowVocabulary`.
  Data class `PlantUmlBlock(val raw: String, val labels: List<String>)`. Port
  `PlantUmlClassifier { fun classify(block: PlantUmlBlock): PlantUmlStrategy }`.
  Méthode `labels()` extrait les labels traduisibles (entre guillemets, après
  `|`, labels d'acteurs). Pas de logique I/O, pas d'appel LLM.
  Réutilise l'ontologie de HTOWN EPIC PLT-BOUNDARY (S163-166).
| UT : classify labels purs → TranslateLabels, classify technical terms (class
  diagram, sequence, ER) → PreserveTechnical, classify REAC/AFNOR vocabulary →
  BorrowVocabulary, extract labels from class diagram, extract from sequence,
  extract from usecase, no labels → PreserveTechnical, mixed → TranslateLabels
| 5
| DEPLOY-0

| BKY-I18N-DEPLOY-2
| *`PlantUmlTranslationAdapter`* — applique `PlantUmlStrategy` à un bloc
  `[plantuml]` pendant la traduction. `TranslateLabels` → traduit les labels
  via `TranslationService`, `PreserveTechnical` → retourne le bloc inchangé,
  `BorrowVocabulary` → traduit + préserve le vocabulaire métier (REAC/AFNOR)
  dans une registry. Intégré dans `ContentTranslationService.translateBlock`
  via dispatch sur `PivotBlock.Source` quand `language == "plantuml"`. I/O
  isolée dans l'adapter (appel LLM), pas dans le domaine.
| UT : TranslateLabels traduit labels uniquement, PreserveTechnical no-op,
  BorrowVocabulary préserve "REAC" / "AFNOR" / "DC" / "TS", round-trip
  block inchangé quand PreserveTechnical, labels contenant du code skippés
| 5
| DEPLOY-1

| BKY-I18N-DEPLOY-3
| *`RtlDirectionInjector` concept pur* — data class `RtlDirective(val lang:
  String, val rtl: Boolean)`. Méthode `inject(frontmatter: PivotFrontmatter):
  PivotFrontmatter` — ajoute `:jbake-lang: {lang}` et `:lang: rtl` (si rtl)
  dans `asciidocAttributes`. Délègue à `LanguageCatalog.findByCode(lang).rtl`
  (i18n-contracts). Pas de logique I/O, pas d'écriture fichier. Réutilise
  le pattern `DeckTranslator.ensureRtlDirection` (MIAMI S030).
| UT : inject ar → `:jbake-lang: ar` + `:lang: rtl`, inject ur → idem, inject
  fr → `:jbake-lang: fr` + pas de `:lang: rtl`, inject en → idem fr, inject
  zh → idem fr, lang inconnue → no-op (ou erreur selon config), idempotence
  (re-inject = même résultat)
| 5
| DEPLOY-0

| BKY-I18N-DEPLOY-4
| *`RtlDirectionInjectionTask` + FT* — tâche Gradle qui applique
  `RtlDirectionInjector` à tous les `.adoc` d'un répertoire cible
  (`content-i18n/{lang}/`) post-`migrateContentI18n`. Exécutée après
  traduction, avant `bake`. Intégration dans `ContentTaskRegistrar` (après
  `registerMigrateContentI18nTask`). FT sur fixture 10 langues (cf. DEPLOY-6
  fixture) via includeBuild.
| FT : inject sur `content-i18n/ar/` → tous `.adoc` ont `:jbake-lang: ar` +
  `:lang: rtl`, inject sur `content-i18n/fr/` → pas de `:lang: rtl`, idempotence
  (re-run = même résultat), skip langues LTR
| 5
| DEPLOY-3

| BKY-I18N-DEPLOY-5
| *LLM réel dogfooding cheroliv.com FR→EN* — apply `migrateContentI18n`
  `--contentI18nSourceLang=fr --contentI18nTargetLangs=en --contentI18nDryRun=false`
  sur `office/sites/cheroliv.com/` (108 articles). LLM réel
  `gpt-oss:120b-cloud` port 11437 (rotation). Assertions qualité via
  heuristique ratio FR/EN (mots FR résiduels < 5%) + LLM-judge (prompt
  d'évaluation qualité de traduction). Snapshot du résultat dans
  `content-i18n/en/blog/` (remplace les 108 fichiers cassés actuels).
| FT : 108 articles traduits, zéro en-tête cassé (`title=/date=/~~~~~~`
  disparu), `:jbake-title:` présent et traduit, ratio FR résiduel < 5%,
  LLM-judge score > 3/5, blocks `[source]` inchangés, blocks `[plantuml]`
  selon DEPLOY-2
| 5
| DEPLOY-2

| BKY-I18N-DEPLOY-6
| *Fixture public 10 langues + BDD Cucumber end-to-end* — copie minimaliste
  d'un site (cheroliv-com-lang-switch déjà existant S177-178, à étendre aux 10
  langues) dans `src/test/resources/fixtures/cheroliv-com-i18n-deploy/` :
  `content/blog/` (3 articles FR), `i18n/{ar,bn,en,es,hi,pt,ru,ur,zh}/` vides.
  Feature `52_i18n_deploy_end_to_end.feature` : `migrateContentI18n` FR→10
  langues + `injectRtlDirection` + `injectLangSwitch` → assert 10 variantes
  localisées conformes : headers préservés, PlantUML classés (TRANSLATE/
  PRESERVE/BORROW), RTL pour ar/ur, lang switcher présent, zéro FR résiduel
  (sauf BorrowVocabulary REAC/AFNOR). Scénarios à la `US-BND-7` HTOWN S166.
| BDD : 5-7 scénarios (FR→EN, FR→AR RTL, FR→ZH, PlantUML TRANSLATE, PlantUML
  PRESERVE, PlantUml BORROW, end-to-end 10 langues)
| 5
| DEPLOY-4, DEPLOY-5

| BKY-I18N-DEPLOY-7
| *Généralisation 8 sites + 10 langues* — dogfooding étendu aux 8 sites
  (cheroliv.com, magic-stick, talaria.school, edster.cloud, cccp.education +
  3 autres). Pour chaque site : dry-run → apply FR→10 langues → vérifications
  qualité (ratio FR résiduel, headers préservés, RTL, PlantUML). Snapshot des
  résultats. CI : 10 bakes séquentiels (1 checkout/lang, `configPath` override
  par `i18n/{lang}/site-{lang}.yml`) — pattern S031 cheroliv.com déjà éprouvé
  pour EN, à étendre aux 10 langues. Documenter la procédure reproductible.
| FT : 8 sites × 10 langues = 80 variantes générées, ratio FR résiduel < 5%
  partout, headers préservés, RTL pour ar/ur, CI 10 bakes séquentiels GREEN
| 7
| DEPLOY-6
|===

_Total_ : ~35 pts, 7 US.

=== Dépendances (DAG)

----
DEPLOY-0 (Cadrage + copies) ─┬─→ DEPLOY-1 (PlantUmlClassifier port)
                             │
                             ├─→ DEPLOY-3 (RtlDirectionInjector) ─→ DEPLOY-4 (Task + FT)
                             │
                             └─→ DEPLOY-2 (PlantUml adapter) ─→ DEPLOY-5 (LLM réel dogfooding)
                                                                    │
                                                                    └─→ DEPLOY-6 (Fixture 10 langues + BDD)
                                                                          │
                                                                          └─→ DEPLOY-7 (8 sites + CI)
----

*DEPLOY-0* est racine (tout dépend de lui). Doit être fait en premier.
*DEPLOY-1* et *DEPLOY-3* sont indépendants (peuvent être menés en parallèle).
*DEPLOY-6* est le point de fusion (consume DEPLOY-4 + DEPLOY-5).
*DEPLOY-7* clôture l'EPIC (généralisation + CI 10 langues).

=== Projection sessions

* *S179* — DEPLOY-0 + DEPLOY-1 (8 pts, 2 US, cadrage + PlantUmlClassifier port)
* *S180* — DEPLOY-2 + DEPLOY-3 (10 pts, 2 US, PlantUml adapter + Rtl injector)
* *S181* — DEPLOY-4 + DEPLOY-5 (10 pts, 2 US, Rtl task + LLM réel dogfooding)
* *S182* — DEPLOY-6 (5 pts, 1 US, fixture 10 langues + BDD end-to-end)
* *S183* — DEPLOY-7 (7 pts, 1 US, 8 sites + CI, clôture EPIC)

Projection conservatrice : 5 sessions, ~35 pts, méthodo baby step
DDD/TDD/BDD préservée à chaque US.

=== Réutilisations cross-boroughs

[cols="2,3,3]
|===
| Source | Élément réutilisé | Rôle dans BKY-I18N-DEPLOY

| MEMPHIS `i18n-contracts` (S116, MEM-3)
| `LanguageCatalog.supportedCodes()` + `SupportedLanguage.rtl`
| Top 10 langues cibles + détection RTL (ar/ur)

| HTOWN EPIC PLT-BOUNDARY (S163-166)
| `PlantUmlClassifier` TRANSLATE / PRESERVE / BORROW
| Classification des blocs `[plantuml]` pendant traduction

| MIAMI EPIC SLD-5 (S029-030)
| `DeckTranslator.ensureRtlDirection` + `LanguageModelAdapter` port
| Injection RTL post-traduction + port LLM abstrait

| Bakery S134 (BKY-I18N-MIG)
| `ContentTranslationService` + `MigrateContentI18nTask` +
  `LlmServiceTranslationAdapter` + `PivotModel` + `TextTranslatableClassifier`
| Socle traduction .adoc existant — étendu, pas réinventé

| Bakery S177-178 (BKY-LANG-SWITCH)
| `injectLangSwitch` task + `LangSwitchThymeleafRenderer`
| Sélecteur de langue déployé sur sites traduits (post-bake)
|===

=== Risques et mitigations

[cols="3,5,4]
|===
| Risque | Mitigation

| LLM non-déterministe — ratio FR résiduel variable | Snapshot golden master
  après 1er run réussi (cf. BKY-I18N-REAL S170). Re-runs comparent au snapshot.
  LLM-judge avec seuil > 3/5 tolère la variabilité.

| PlantUML labels mélangés (technique + traduisible) | `PlantUmlClassifier`
  par label, pas par bloc : chaque label classifié indépendamment. Un bloc
  peut avoir 3 labels TranslateLabels + 1 PreserveTechnical. Testé en UT
  (DEPLOY-1).

| En-têtes JBake cassés dans copies existantes | DEPLOY-0 smoke test détecte.
  Corriger via re-run `migrateContentI18n` (écrase proprement) avant traduction.
  Les 108 articles `content-i18n/en/blog/` actuels sont écrasés en DEPLOY-5.

| 108 articles × 10 langues = 1080 appels LLM (latence + coût) | `parallelism`
  paramètre de `ContentTranslationService` (S153) — paralléliser à 5-10.
  Rotation ports 11437-11465 + 2 modèles pour distribuer la charge. Dry-run
  d'abord pour valider le parsing avant de payer le LLM.

| CI 10 bakes séquentiels trop long | Pattern S031 éprouvé (double checkout
  FR+EN). Étendre à 10 : matrix GitHub Actions sur 10 jobs parallèles (1
  checkout/lang, `OLLAMA_TEST_PORT` + `OLLAMA_TEST_MODEL` injectés). Pas
  séquentiel si la CI le permet.

| Boundary bakery ↔ sites brouillé | DEPLOY-0 documente le boundary :
  bakery traduit contenu uniquement. Thème, sélecteur, CI deploy = sites.
  Aucune logique site-spécifique dans bakery (extensions via DSL uniquement).
|===

'''

[cols="2,3]
|===
| Document | `BACKLOG.adoc`
 | Version | 20.0 — Session 178 (EPIC BKY-LANG-SWITCH CLOS + EPIC BKY-I18N-DEPLOY cadré)
 | Historisé dans | `.agents/backup/Y2026-sessions-145-154/BACKLOG_avant_nettoyage_sess154.adoc`
 | Basé sur | Backup sess154 (1269 lignes) + rapports sessions 001-178 + EPIC LANG-SWITCH + EPIC I18N-DEPLOY
 | Prochaine màj | Session 179 — DEPLOY-0 cadrage + DEPLOY-1 PlantUmlClassifier
|===